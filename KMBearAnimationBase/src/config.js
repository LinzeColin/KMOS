// config.js: project settings.
//   duration: the video's length in seconds.
//   bpm:      the rhythm that bounces, dances and pulse() follow; with music, set it to the song's tempo and offset to
//             the time in seconds of its first downbeat.
//   flat:     true paints watercolour fills as flat washes (fast on machines without a GPU; render.mjs --flat does the same).
const PROJECT = { duration: 6, bpm: 120, offset: 0 };
