# KM_BidScout（public-safe 脚手架草案）

**状态：** `SKILL_CONTRACT_READY / PRODUCT_NOT_IMPLEMENTED`。

本目录已提供可直接调用的 9 个只读 Skill 契约、Schema、规则和私有 runtime 边界；完整搜标产品、真实来源适配器、企业 DWS 和生产性能仍未实现。Owner 已确认 GitHub `LinzeColin/KMOS`、本机只读主树 `/Users/linzezhang/Documents/Codex/GithubProject/KMOS`、KMIDS 实际目录 `KM_IDSystem/`。任何后续产品实施仍必须完成 G0：重新读当前六条铁律、KMOS main、父级 AGENTS/HANDOFF，并在独立 worktree 中执行。

V1 默认外部只读和本地产出；真实公司/DWS/来源快照放仓库外受控根。每 run 先检查输入充分性，最终报告本地输出绝对路径。跨项目数据走 KMDatabase schema/API，不直接引用其他项目私有目录。
