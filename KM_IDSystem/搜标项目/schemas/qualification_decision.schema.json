{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "qualification_decision.schema.json",
  "title": "QualificationDecision",
  "type": "object",
  "additionalProperties": false,
  "required": ["contract_version", "lot_id", "legal_entity_id", "eligibility", "commercial_worth", "capacity_state", "evidence_quality", "readiness", "requirements", "gaps", "next_action", "legal_vehicle_review"],
  "properties": {
    "contract_version": {"const": "0.4.1"},
    "lot_id": {"type": "string", "minLength": 1},
    "legal_entity_id": {"type": "string", "minLength": 1},
    "eligibility": {"enum": ["met", "not_met", "unknown"]},
    "commercial_worth": {"enum": ["worth", "not_worth", "unknown"]},
    "capacity_state": {"enum": ["available", "conflict", "unknown"]},
    "evidence_quality": {"enum": ["complete", "partial", "insufficient"]},
    "readiness": {"enum": ["P0_DIRECT", "P1_RESOLVABLE", "P2_EVIDENCE", "X_EXCLUDE", "DISAGREEMENT_REVIEW"]},
    "requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["requirement_id", "kind", "state", "as_of", "valid_until", "evidence_refs"],
        "properties": {
          "requirement_id": {"type": "string"},
          "kind": {"enum": ["hard", "scored", "commercial", "capacity"]},
          "state": {"enum": ["met", "not_met", "unknown"]},
          "as_of": {"type": ["string", "null"], "format": "date-time"},
          "valid_until": {"type": ["string", "null"], "format": "date-time"},
          "evidence_refs": {"type": "array", "items": {"type": "string"}}
        }
      }
    },
    "gaps": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["requirement_id", "reason_code", "resolvable_before_deadline"],
        "properties": {
          "requirement_id": {"type": "string"},
          "reason_code": {"type": "string"},
          "resolvable_before_deadline": {"type": ["boolean", "null"]}
        }
      }
    },
    "next_action": {"type": "string", "minLength": 1},
    "legal_vehicle_review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "owner_approved", "evidence_refs"],
      "properties": {
        "mode": {"enum": ["single_entity", "manual_consortium_review", "manual_prime_sub_review"]},
        "owner_approved": {"type": "boolean"},
        "evidence_refs": {"type": "array", "items": {"type": "string"}}
      }
    }
  }
}
