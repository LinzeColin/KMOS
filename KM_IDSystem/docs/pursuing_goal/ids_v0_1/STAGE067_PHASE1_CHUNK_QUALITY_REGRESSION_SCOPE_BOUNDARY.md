# STAGE-067 Phase 1：切块质量回归范围、输入输出与边界确认

## 当前结论

本步骤只定义 `IDS-V0_1-STAGE067-P1` 的静态切块质量回归合同。唯一合同上下文是冻结
Stage067 任务包、Stage066 已完成本地整阶段机械复审工件和 Batch061-070 上传锁。合同只登记未来
切块质量回归所需的受控引用、工程步骤/验收条款/参数表保护面、追溯、重复 embedding/index 写入边界、
失败关闭、中文反馈和回滚边界；没有读取、打开、解析、切分、计算、检测、分类、生成、写入或删除任何
业务资料、原始元数据、文档、页面、章节、表格、parser 输出、来源片段、chunk、身份、哈希、版本、覆盖率、
质量结果、索引或业务结论。

`document_ref`、`page_ref`、`section_ref`、`parser_output_ref`、`table_context_ref`、
`source_fragment_ref` 与 `duplicate_chunk_control_ref` 均为不透明受控引用，当前不得填入正文、物理路径、
URL、页码值、章节名称、表格内容、来源片段、实际重复项或任何实际计数。质量回归状态、保护面状态、
重复写入边界状态、人工复核状态和质量降级交接状态仅是未来 schema 字段；本步骤没有实际质量分数、
阈值、失败、重复检测、去重、embedding/index 抑制或质量降级结论。

## 职责交界

| 事项 | 唯一职责阶段 | 本步骤处置 |
| --- | --- | --- |
| 章节感知切块边界 | Stage063 | 只复用已复审引用边界，不重新检测或切分 |
| chunk 身份、哈希与版本 | Stage064 | 只引用已复审身份与版本字段，不生成或计算 |
| 工程语义资产目录 | Stage065 | 只引用已复审目录边界，不重新分类 |
| Chunk 覆盖率指标 | Stage066 | 只引用已复审覆盖率合同，不计算覆盖率 |
| 切块质量回归 | Stage067 | 只定义未来质量回归合同，不运行验证 |
| 质量降级与人工复核 | Stage068 | 只声明未来交接入口，不生成降级或业务结论 |

## 质量、保护面与追溯边界

未来质量回归必须先取得章节感知切块、chunk 身份与版本、工程语义资产目录、Chunk 覆盖率、文档、页面、
章节、parser output、表格上下文、来源片段和重复写入边界的受控引用。工程步骤、验收条款和参数表是三类
保护语义面：不得被任意切断、合并、覆盖或由质量状态替代。未来记录必须保留 document、page、section、
parser output、表格上下文和来源片段六维引用，当前追溯绑定数为零。

长文档、跨页参数表、工程步骤、验收条款、参数表、引用页码、重复 chunk、语义边界或来源追溯无法确认时，
未来流程必须关闭并转业务线白箱人工处理。重复边界只声明未来不得重复 embedding 或索引；当前没有检测
真实重复项、计算真实身份/哈希/版本，或形成真实去重和写入抑制结论。

来源文档与经授权的业务线人工复核始终保持权威；质量字段、控制标签、模型输出或本合同不得替代来源形成
事实、决策结论或第二权威事实源。

## 失败、中文反馈与回滚

缺少任何上游受控引用、遇到保护语义面或跨页参数表、无法确认追溯或重复写入边界，或未获授权执行质量
回归时，未来流程必须关闭并转人工处理；当前没有任何实际失败、输入、质量测量、重复检测、来源定位或
处置记录。不得自动写入事实库、证据账本、数据库、索引、报告、生产状态或业务线结论。

回滚只允许移除本步骤的范围说明、静态合同、聚焦用例、machine run、事件、机器事实投影、治理路线和
生成中文视图，恢复到 `STAGE066_REVIEWED_LOCAL_CHUNK_COVERAGE_METRICS_RUNTIME_DISABLED`。真实资料、
manifest、evidence ledger、audit log、已交付报告、事实库、数据库、索引、GitHub、OVH 与应用状态不在
回滚范围内。

一旦需要真实资料、授权 fixture、实际 parser 输出、实际切块、实际身份/版本、实际覆盖率、实际质量回归、
实际重复检测或去重、embedding/index、质量降级、Agent、模型、OVH、生产、Phase2、整阶段复审、批次复审、
上传或推送，立即停止本步骤。

## 后续门

本步骤通过后的唯一后续门为 `IDS-STAGE067-P2-GATE`，且必须由新的独立 run 进入。
