# Stage074 整阶段机械复审

## 目标

只在内存中机械复审冻结 Stage074 的 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 控制回退说明，确认本地 Embedding 兜底路线仍是 reference-only 合同：默认 denied、两跳自动继承、document 只能收紧、五条固定控制请求、五条三十五字段场景、十八字段审计投影、四条业务线白箱人工处理与 metadata-only 交付保持一致。

## 复审范围与固定形状

- P1：3/2/12/10/7/16/8/6/18/12 静态合同；
- P2：五条 20/10/14/10/7/16/6/8/18 纯内存控制投影；
- P3：五条三十五字段场景、十八字段审计、九十次字段检查、三个未来调用候选、四条人工处理与十一类场景失败关闭；
- P4：五条策略样例、五条审计投影、五条零值成本、五条失败处理、五条未外发引用、七键查询、四条中文反馈、十二类失败关闭及仅回到 P3 的控制回退。

## 不可突破边界

- 只读取本仓冻结任务包和既有控制工件；不读取、打开、复制、写入、查询或回退真实资料、来源正文、摘要、文本块、物理路径、真实 URI 或业务事实。
- 不选择或下载本地 provider/模型，不执行本地 Embedding、队列、缓存、失败重试、成本、预算、审计、外部 API、模型 Token、数据库、Agent、OVH、生产、上传或推送。
- 来源文档与业务线白箱人工复核仍是唯一权威；复审输出不得替代来源文档、成为业务事实或自动作出业务决策。

## 通过与失败关闭

复审仅在 P1--P4 合同和控制报告全部通过、固定形状完全一致、单一权威与运行时关闭边界全部成立时通过。任何合同、报告、计数、审计前置、白箱处理、回退或零运行时标志不一致，都留在 IDS-STAGE074-REVIEW-GATE，不打开后续阶段。

通过后只开放 IDS-STAGE075-P1-GATE，但不启动 Stage075；Stage075 仍须在新的独立 run 中按其冻结任务包进入。Review 回滚仅撤回本说明、复审模块、聚焦测试、machine run、事件、机器事实、路线图、中文投影和交接，返回 PASS_PHASE4_LOCAL_EMBEDDING_FALLBACK_DELIVERY_RUNTIME_DISABLED，保留 P1--P4、冻结任务包、真实资料、fixture、事实库、数据库、GitHub、OVH 与应用状态。
