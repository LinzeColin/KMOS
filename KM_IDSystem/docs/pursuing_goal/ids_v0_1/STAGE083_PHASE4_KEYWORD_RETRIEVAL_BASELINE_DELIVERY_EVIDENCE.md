# Stage083 Phase 4 · 关键词检索基线交付证据、回滚与中文反馈

## 目标与停止条件

本阶段仅从 Stage083 P3 已验证的八个纯内存受控场景派生交付证据：检索样例、trace 日志、过滤结果、有效性测试报告、检索不足或证据缺口记录，以及检索参数回滚说明。所有引用必须继续使用 Stage083 P2 的固定 `:control:stage083-p2:` 标签；不得写入真实检索样例、trace、过滤结果、报告、证据账本或参数。

完成条件是：P4 合同、纯内存交付模块、聚焦测试和机器事实一致地证明 P3 的 5 条控制请求、250 次字段检查、8 个场景、26 个场景字段和 208 次场景字段检查仍完整，P4 交付形状全部为 metadata-only，所有实际运行计数为零，所有运行闭锁字段为 `false`。满足后路线只能推进到 `IDS-STAGE083-REVIEW-GATE`，本 run 不进入整阶段复审。

## 唯一事实边界

冻结 Stage083 任务包、Stage083 P1/P2/P3 合同与 Stage082 已审核复审工件是本阶段唯一合同上下文。来源文档、真实检索资料、原始元数据、manifest、证据账本、审计日志和业务线白箱人工复核仍保持各自权威；P4 的控制交付证据不能成为业务事实、实际检索结果、真实 trace 或参数配置的权威副本。

不得输入或读取业务正文、真实材料牌号、设备型号、标准号、文件路径、URL、数据库连接、OVH 运行参数、模型或 Agent 参数。P4 不执行 PostgreSQL schema 或连接、FTS/BM25/pgvector、关键词或向量检索、语义计算、过滤、排序、Top-K、trace／证据账本读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送。

## 交付形状

| 交付类别 | 数量 | 失败关闭约束 |
| --- | ---: | --- |
| 检索样例控制记录 | 8 | 只复用 query、candidate、selected 与活动索引版本控制引用；不写样例。 |
| trace 日志控制记录 | 8 | 只复用 trace、活动索引版本与证据引用；不写日志。 |
| 过滤结果控制记录 | 8 | 只记录 P3 已声明的过滤引用形状；不执行过滤。 |
| 有效性测试报告控制记录 | 8 | 只记录 Top-K、评分解释、选择与有效性状态；不测试或写报告。 |
| 检索不足／证据缺口控制记录 | 8 | 每个场景显式保留白箱处理，自动补全和自动采纳关闭。 |
| 检索参数回滚说明 | 3 | 仅描述未来授权变更的回退前置；不存在可回滚的实时参数。 |
| 中文反馈 | 4 | 说明控制边界，不作能力夸大或自动化承诺。 |

## 失败关闭与恢复边界

P3 报告不是 `PASS_KEYWORD_RETRIEVAL_BASELINE_CONTROLLED_SCENARIOS_RUNTIME_DISABLED`、P3 的精确字段／计数／路线边界偏离、任一引用失去控制标签、任一交付记录形状不完整，或任一 P3/P4 运行闭锁字段为 `true`，均不得产生交付记录，P4 必须失败关闭并保留 P3 为最近可恢复点。

回滚只能撤回 P4 的范围说明、合同、纯内存交付模块、聚焦测试、机器事实、治理路线、回执、生成中文视图与本交接，恢复到 P3 的通过状态。不得修改 P1/P2/P3、Stage082 Review、冻结任务包、来源文档、真实检索参数、证据账本、审计记录、数据库、索引、GitHub、OVH 或应用状态。

## 验证范围

本阶段只运行 P4 聚焦单元测试、Stage060--083 白箱回归、Stage005 治理回归、既有批次复核、机器事实生成的中文文档门禁以及 Git worktree 审计。它们不构成真实 PostgreSQL、FTS、BM25、pgvector、检索、过滤、Top-K、trace、参数回滚、业务线审批、OVH 部署、生产运行或发布验收。
