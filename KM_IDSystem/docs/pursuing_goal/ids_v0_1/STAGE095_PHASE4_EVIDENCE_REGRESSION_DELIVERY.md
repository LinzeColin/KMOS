# Stage095 P4 · 证据回归交付证据

## 目标与范围

本阶段只从 Stage095 P3 已验收的 7 个固定、非业务、`reference-only` 异常场景派生进程内控制记录。它不读取真实资料、原始元数据、证据账本、审计日志或业务报告，不创建第二权威事实源。

## 交付形状

- evidence ledger 样例：7 条，每条 14 字段。
- 证据等级报告：7 条，每条 13 字段。
- 撤回影响清单：7 条，每条 13 字段；只声明影响，不更新报告状态。
- 回归测试记录：7 条，每条 14 字段。
- 不可作为结论依据的证据类型：7 条，每条 11 字段。
- 证据降级说明：4 条，每条 10 字段。
- 撤回和恢复说明：2 条，每条 11 字段。

合计 517 个固定交付字段检查点。所有引用都是 `:control:stage095-p2:` 或 `:control:stage095-p4:` 控制引用；来源文档和真实证据账本保持唯一业务事实权威。

## 白箱处置与回滚

无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料及低等级伪装高可信结论均不能自动作为结论依据。证据降级、撤回、恢复和报告状态处置由业务线白箱人工复核前置。

回滚只撤回本 P4 的范围说明、纯内存交付模块、合同、聚焦用例、machine run、事件、机器事实、治理路线、生成中文视图和本交接，恢复到 `PASS_EVIDENCE_REGRESSION_CONTROLLED_SCENARIOS_RUNTIME_DISABLED`。Stage095 P1--P3、Stage094 Review、冻结任务包、真实资料、证据账本、GitHub main/release、OVH 与应用状态保持原状。

