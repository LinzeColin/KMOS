# STAGE-053 Phase 4：按页 OCR 输出交付证据、回滚与中文反馈

## 本轮结论

`IDS-V0_1-STAGE053-P4` 只从 P3 的五类固定非业务按页 OCR 质量 control 报告派生交付证据：五个 metadata-only 按情形样例、置信度汇总、一条显式失败清单、两条未排队复核路由证明、质量限制说明、三条中文人工确认提示和缓存重跑说明。

这些样例不含 OCR 文本、业务正文、文件路径、页面图像、表格单元或真实来源内容。它们不是真实 OCR 输出、准确率报告、表格提取结果、实际人工复核记录或生产服务验收；实际复核路由与缓存保留仍分别由 `STAGE-054` 与 `STAGE-056` 独立负责。

## 交付证据

| 交付项 | 数量 | 白箱含义 | 不作出的声明 |
| --- | ---: | --- | --- |
| metadata-only 按页输出样例 | 5 | 保留 P3 场景号、控制页引用、语言、置信度、状态和处置 | 不含 OCR 文本，非真实 OCR 输出 |
| 置信度汇总 | 1 | HIGH=2、MEDIUM=1、LOW=1、UNKNOWN=1 | 不代表识别准确率或质量门 |
| 失败清单 | 1 | 低质量 control 维持显式失败且不能提升证据 | 不代表真实失败日志 |
| 复核路由证明 | 2 | 模糊图片与中英文混合 control 声明 `Stage054` 后续路由 | 未创建复核任务或队列 |

五个场景保持明确处置，静默丢弃为零。候选、降级与失败状态都不能直接进入高可信证据层。

## 质量限制与人工确认

业务线阅读本交付时，应看到以下克制中文提示：

1. 本页仅为按页 OCR control 交付元数据，不可替代真实 OCR 结果或高可信证据。
2. 低置信和中英文混合 control 仅保留降级提示，实际复核仍由 `Stage054` 独立处理。
3. 当前没有缓存路径或临时产物；不得以本说明触发目录扫描或删除。

上述提示只记录待人工确认的边界，没有自动确认、质量通过、证据提升或运行时操作。

## 缓存清理与重跑

P3/P4 的缓存策略是 `IN_MEMORY_REBUILDABLE_NOT_PERSISTED`，临时产物数为 `0`，清理结论为 `NO_TEMPORARY_ARTIFACT_CREATED`。因此本轮不执行目录扫描、删除、移动或清理。

重跑仅允许在内存中重放 P3 的五类 control，并重新核对五个 metadata-only 样例、置信度汇总、失败清单和两条未排队复核路由证明。实际缓存保留期、容量控制、清理执行和真实重跑能力仍由 `STAGE-056` 独立承担。

## 明确未执行

- 未打开、读取、扫描、渲染、保留或提交真实 PDF、图片、页面、表格、来源正文、原始元数据或 Owner fixture；
- 未选择、配置或调用 OCR 引擎，未执行文件检测、真实路由、PDF 栅格化、图片处理、表格提取或识别准确率评估；
- 未创建持久队列、持久按页输出、缓存、清理任务、实际复核记录、质量门、证据提升、数据库、审计、证据账本、报告或运行时日志；
- 未启动 Agent、模型调用或模型 Token 消耗，未启动本地服务、OVH、生产运行、上传或推送；
- 未进入 Stage053 整阶段复审、批次复审或应用重装。

## 回滚与下一门

回滚只撤销本 P4 说明、合同、纯内存交付模块、聚焦测试、machine run、事件、事实投影、治理状态和生成中文视图，恢复到 `PHASE3_PER_PAGE_OCR_CONTROLLED_QUALITY_SCENARIOS_ENGINE_DISABLED`。不得改变真实资料、原始元数据、manifest、evidence ledger、audit log、已交付报告、持久运行状态、GitHub、OVH 或应用状态。

下一步只能在新的独立 run 进入 `IDS-STAGE053-REVIEW-GATE`。
