# Stage092 · 证据风险评分 · 整阶段机械复审

## 复审目标

本复审以冻结 `STAGE-092_证据风险评分.md` 与 Stage092 P1--P4 控制合同、控制报告为唯一工程上下文，机械确认来源、OCR 置信度、版本、复核状态和冲突状态的未来风险输入形状、关键结论 evidence_id/evidence_gap 约束、A/B/C/D/E、异常处置、交付控制证据和回退路径保持一致。来源文档、真实证据账本与业务线白箱人工复核继续承担业务事实权威。

复审模块只读取本地控制工件并在进程内构造控制报告。控制标签不承载真实资料、业务事实、证据内容或业务结论，不建立第二权威事实源。

## 固定复审形状

| 层级 | 固定形状 | 复审重点 |
| --- | --- | --- |
| P1 | `20/10/5`，`14` 类失败关闭 | 风险字段、关联、五类未来风险输入、A/B/C/D/E、公式 owner 前置 |
| P2 | `6×27` 输入、`11` 组投影、每条 `97`、合计 `582` | 固定 reference-only 输入、风险关联、五类风险输入、撤回／投毒／报告状态引用 |
| P3 | `7×32=224`，`15` 类失败关闭 | 无内部证据、低 OCR、旧版本、冲突、撤回、恶意、低等级伪装高可信结论 |
| P4 | `7/7/7/7/7/4/2`、`517`、`4` 条中文反馈、`18` 类失败关闭 | 交付控制记录、不可作为结论依据类型、降级／撤回／恢复与 P4→P3 回退 |

风险权重、阈值和业务判定公式继续保持业务线白箱 owner 前置；本 Review 不生成风险分值、可信等级变更、自动结论或自动处置。无内部证据保持 evidence gap；低 OCR、旧版本、冲突和撤回资料保持降级；疑似恶意资料保持隔离；低等级伪装高可信结论保持拒绝；撤回影响只声明而不应用。

## 失败关闭与运行边界

任一 P1--P4 合同、控制报告、固定形状、控制引用、单一权威、风险公式 owner 前置、失败关闭、P4→P3 回退、业务线白箱人工处理或零运行时边界漂移，复审结果保持 `FAIL_REVIEWED_EVIDENCE_RISK_SCORING_RUNTIME_DISABLED`，下一门禁保持 `IDS-STAGE092-REVIEW-GATE`。

本复审不读取、复制、写入、查询或解析真实资料、原始元数据、manifest、检索结果、证据账本、审计日志、回答、报告、数据库或物理索引；不执行来源、OCR、版本、复核或冲突评估、检索、证据捕获、风险计算、可信等级变更、撤回、恢复、投毒防护、报告状态更新、模型调用、模型 Token、Agent、OVH、生产或正式上传。

## 验证与回滚

聚焦验证命令：

```bash
python3 -B -m unittest -q KM_IDSystem.docs.pursuing_goal.ids_v0_1.tests.test_stage092_evidence_risk_scoring_stage_review
```

复审通过只开放 `IDS-STAGE093-P1-GATE`；Stage093 保持未启动。本阶段回滚只撤回本说明、Review 合同、纯内存模块、聚焦用例、local receipt、治理投影与生成中文视图，恢复到 `PASS_EVIDENCE_RISK_SCORING_DELIVERY_EVIDENCE_RUNTIME_DISABLED`。Stage092 P1--P4、Stage091 Review、冻结任务包、受保护资料、真实证据账本、GitHub、OVH 与应用状态保持原状。
