# Stage082 整阶段机械复审

## 目标

只在内存中机械复审冻结 Stage082 的 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 回退边界，确认候选与活动索引隔离、失败关闭、上一活动版本保留、未测量空间影响、重建／暂停／恢复说明和业务线白箱人工处理保持一致。

## 复审范围与固定形状

- P1：7/5 索引版本／活动指针字段、5/6/5 构建与冒烟字段、10/6 保留策略／清理资格字段，以及 14 类失败关闭；
- P2：5 条固定控制请求、9 组各 5 条控制投影，以及 305 次字段检查；
- P3：6 条构建未完成、冒烟失败、切换失败、回滚窗口未设值、并发检索隔离和版本可见性场景，31 字段、186 次字段检查、5 条 Operations 及 5 条报告快照控制视图；
- P4：5 条索引清单、6 条冒烟测试日志、5 条切换记录、5 条回滚证明、1 条旧索引保留／未测量空间投影、3 条重建／暂停／恢复说明、4 条中文反馈和 15 类失败关闭。

## 不可突破边界

- 只读取本仓冻结任务包和既有控制工件；不读取、打开、复制、写入、查询、删除、构建、切换、回滚或测量真实资料、来源正文、原始元数据、物理索引、实际清单、实际日志、证据账本、审计日志或业务事实。
- 不执行批量导入、数据库、检索、并发检索、provider／模型、模型 Token、Agent、OVH、生产、上传或推送。
- 来源文档与业务线白箱人工复核仍是唯一权威；复审输出不得替代来源文档、成为业务事实或自动作出业务决策。

## 通过与失败关闭

复审仅在 P1--P4 合同和控制报告全部通过、固定形状完全一致、失败停止与 P4→P3 回退链有效、最低上一活动版本保留与人工处理边界成立、所有运行时标志关闭时通过。任一合同、报告、记录形状、版本保留、操作说明、回退路径、单一权威或零运行时标志不一致，都留在 `IDS-STAGE082-REVIEW-GATE`，不打开后续阶段。

通过后只开放 `IDS-STAGE083-P1-GATE`，但不启动 Stage083；Stage083 仍须在新的独立 run 中按其冻结任务包进入。Review 回滚仅撤回本说明、复审模块、聚焦测试、machine run、事件、机器事实、路线图、中文投影和交接，返回 `PASS_OLD_INDEX_RETENTION_DELIVERY_EVIDENCE_RUNTIME_DISABLED`，保留 P1--P4、冻结任务包、真实资料、fixture、manifest、evidence ledger、audit log、数据库、索引、GitHub、OVH 与应用状态。
