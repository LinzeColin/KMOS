# Stage096 · 知识库投毒防护 · 整阶段机械复审

## 复审目标

本复审以冻结 `STAGE-096_知识库投毒防护.md`、Stage096 P1--P4 控制合同与控制报告、以及已复审的 Stage095 控制工件为唯一工程上下文。它在内存中机械确认知识库投毒防护的固定形状、失败关闭、单一权威、业务线白箱人工处理和 P4→P3 回退保持一致。

来源文档、真实证据账本和业务线白箱人工复核继续承担业务事实权威。复审结果是控制报告，不承载真实资料、业务事实、证据内容或业务结论。

## 固定复审形状

| 层级 | 固定形状 | 复审重点 |
| --- | --- | --- |
| P1 | `8/6/5/14` | 八个未来控制引用、六个控制定义、A/B/C/D/E 标签与 14 类失败关闭 |
| P2 | `6×21` 输入、`6` 组投影、每条 `58`、合计 `348` | reference-only 输入、证据关联、检索证据捕获、风险与可信等级、撤回与投毒、关键结论与报告影响引用 |
| P3 | `7×32=224`，`15` 类失败关闭 | 无内部证据、低 OCR、旧版本、冲突、撤回资料、恶意资料和低等级伪装高可信结论 |
| P4 | `7/7/7/7/7/4/2`、`517`、`4` 条中文反馈、`18` 类失败关闭 | 交付控制记录、不可作为结论依据类型、降级／撤回／恢复、回归与 P4→P3 回退 |

无内部证据保持 evidence gap；低 OCR、旧版本、冲突和撤回保持降级控制；恶意资料保持隔离控制；低等级不支撑高可信结论；撤回影响只声明未来报告复核。投毒判定、隔离、高可信准入、风险、可信等级、撤回、降级、恢复、报告状态和业务判定继续保持业务线白箱 owner 前置。

## 失败关闭与运行边界

任一 P1--P4 合同、控制报告、固定形状、控制引用、单一权威、业务线白箱边界、失败关闭、P4→P3 回退或零运行时边界发生漂移，复审结果保持 `FAIL_REVIEWED_KNOWLEDGE_BASE_POISONING_DEFENSE_RUNTIME_DISABLED`，下一门禁保持 `IDS-STAGE096-REVIEW-GATE`。

本复审只处理本地控制工件。真实资料、原始元数据、manifest、检索结果、证据账本、审计日志、回答、报告、数据库、物理索引、实际来源／OCR／版本／复核／冲突评估、投毒检测、隔离、准入、风险计算、可信等级变更、撤回、降级、恢复、报告状态更新、模型、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权边界。

## 验证与回滚

聚焦验证命令：

```bash
python3 -B -m unittest -q KM_IDSystem.docs.pursuing_goal.ids_v0_1.tests.test_stage096_knowledge_base_poisoning_defense_stage_review
```

复审通过只开放 `IDS-STAGE097-P1-GATE`。本阶段回滚只撤回本说明、Review 合同、纯内存模块、聚焦用例、local receipt、治理投影与生成中文视图，恢复到 `PASS_KNOWLEDGE_BASE_POISONING_DEFENSE_DELIVERY_EVIDENCE_RUNTIME_DISABLED`。Stage096 P1--P4、Stage095 Review、冻结任务包、受保护资料、真实证据账本、GitHub `main`／release、OVH 与应用状态保持原状。
