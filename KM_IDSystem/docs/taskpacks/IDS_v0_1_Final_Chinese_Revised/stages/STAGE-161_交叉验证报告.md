# STAGE-161 · 交叉验证报告

- 本地编号：D25-S010
- 当前版本：v0.1
- 能力域：D25 · 测试、压测与发布验证
- 入口：IDS 系统运营入口
- Acceptance ID：ACC-STAGE-161
- 建议并行度：是
- 并行说明：可作为横切质量工作与相关实现 Stage 并行推进；至少在 STAGE-168 前完成。
- 预计实际执行开发时间：4-8 小时

## 追求目标（Pursuing Goal）

记录两轮六视角交叉验证结论，防止结构性遗漏。

## Codex 执行范围

### 允许

- 只处理本 Stage 明确相关文件。
- 可以新增本 Stage 所需的代码、schema、测试、配置、文档或 UI 切片。
- 必须输出修改文件列表、真实测试结果、验收证据和回滚说明。

### 禁止

- 不得提交真实原始资料。
- 不得提交 secrets、API key、数据库密码或云端凭证。
- 不得移动、删除、覆盖 `00_ORIGINAL_RAW_DATA`。
- 不得把旧产品名用于新 UI、报告、文档显示名。
- 不得声称测试通过但没有真实测试输出。
- 不得扩大到其他 Stage 的范围。

## Phase / Task

### Phase 1：范围、输入输出与边界确认
1. 定义黄金测试集、场景测试、结果有效性、禁止行为、竞态、端到端、压测和长稳测试。
2. 确认测试先用 Tier 0/1/2，再进入 150–500GB Tier 3。
3. 定义不能只验证有输出，还要验证输出有效。

### Phase 2：实现、接入与最小可运行切片
1. 实现测试样例、测试框架、场景脚本或发布验证切片。
2. 把移动硬盘断开、压缩包攻击、索引失败、证据不足等场景加入测试。
3. 把 UI/UX 走查和交叉验证结果写入发布门禁。

### Phase 3：交叉验证报告 专项验证与异常场景
1. 运行正向流程、负向流程、边界流程、并发流程和长稳流程。
2. 验证系统不会修改原始文件、不会绕过外部 API 策略、不会无证据伪装。
3. 验证数据处理结果、报告引用和前端状态一致。

### Phase 4：交叉验证报告 交付证据、回滚与中文反馈
1. 交付测试报告、失败清单、修复建议和发布阻断项。
2. 记录哪些测试是 v0.1 必跑，哪些留到 v0.3 性能硬化。
3. 提供 release pack 和回滚包要求。

## 验收标准

- 追求目标对应能力已经可运行，或已形成可执行、可测试、可回滚的工程合同。
- 本 Stage 的失败状态、停止条件、审计记录、回滚路径明确。
- 本 Stage 不破坏原始资料、manifest、证据账本、审计日志和已交付报告。
- 相关测试、场景验证或文档证据真实存在。
- 中文交互反馈清楚、克制、面向企业用户，不使用夸大或 AI 化承诺。

## 停止条件

- 需要真实资料但没有 fixture 或 owner 授权样本。
- 可能删除、移动、覆盖原始文件。
- schema migration 无法回滚。
- 测试失败且原因不明。
- 修改范围超出本 Stage。
- 并行开发导致共享文件冲突。

## 回滚方式

回滚本 Stage 的代码、schema、配置或 UI 变更；不得影响原始资料、manifest、evidence ledger、audit log 和已交付报告。若本 Stage 产生派生产物，应只清理明确允许清理的临时文件或可重建缓存。
