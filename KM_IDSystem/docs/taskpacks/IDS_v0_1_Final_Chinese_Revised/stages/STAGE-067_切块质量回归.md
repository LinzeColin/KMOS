# STAGE-067 · 切块质量回归

- 本地编号：D11-S005
- 当前版本：v0.1
- 能力域：D11 · Chunking 与语义资产
- 入口：IDS 系统运营入口
- Acceptance ID：ACC-STAGE-067
- 建议并行度：是
- 并行说明：可与 STAGE-119~123 的质量门禁设计并行；至少在 STAGE-083 前完成。
- 预计实际执行开发时间：4-12 小时

## 追求目标（Pursuing Goal）

验证工程步骤、验收条款、参数表不会被错误切断。

## Codex 执行范围

### 允许

- 只处理本 Stage 明确相关文件。
- 可以新增本 Stage 所需的代码、schema、测试、配置、文档或 UI 切片。
- 必须输出修改文件列表、真实测试结果、验收证据和回滚说明。

### 禁止

- 不得提交真实原始资料。
- 不得提交 secrets、API key、数据库密码或云端凭证。
- 不得移动、删除、覆盖 `00_ORIGINAL_RAW_DATA`。
- 不得把旧产品名用于新 UI、报告、文档显示名。
- 不得声称测试通过但没有真实测试输出。
- 不得扩大到其他 Stage 的范围。

## Phase / Task

### Phase 1：范围、输入输出与边界确认
1. 定义章节感知切块、chunk 身份、版本、覆盖率和工程语义资产类型。
2. 确认工程步骤、验收条款、参数表不能被随意切断。
3. 定义 chunk 与 document、page、section、parser output 的追溯关系。

### Phase 2：实现、接入与最小可运行切片
1. 实现切块、chunk_hash、语义资产分类、覆盖率指标或质量降级机制。
2. 保留页码、章节路径、表格上下文和来源片段。
3. 把低质量 chunk 标记为需复核或低可信。

### Phase 3：切块质量回归 专项验证与异常场景
1. 验证长文档、跨页表格、施工步骤、参数表和引用页码的切块质量。
2. 验证重复 chunk 不重复 embedding，不重复索引。
3. 验证 chunk 可以从报告反查到原文位置。

### Phase 4：切块质量回归 交付证据、回滚与中文反馈
1. 交付 chunk JSONL 样例、覆盖率报告、低质量 chunk 清单和回归测试结果。
2. 记录切块策略适用边界。
3. 提供 chunk 重新生成和版本回滚说明。

## 验收标准

- 追求目标对应能力已经可运行，或已形成可执行、可测试、可回滚的工程合同。
- 本 Stage 的失败状态、停止条件、审计记录、回滚路径明确。
- 本 Stage 不破坏原始资料、manifest、证据账本、审计日志和已交付报告。
- 相关测试、场景验证或文档证据真实存在。
- 中文交互反馈清楚、克制、面向企业用户，不使用夸大或 AI 化承诺。

## 停止条件

- 需要真实资料但没有 fixture 或 owner 授权样本。
- 可能删除、移动、覆盖原始文件。
- schema migration 无法回滚。
- 测试失败且原因不明。
- 修改范围超出本 Stage。
- 并行开发导致共享文件冲突。

## 回滚方式

回滚本 Stage 的代码、schema、配置或 UI 变更；不得影响原始资料、manifest、evidence ledger、audit log 和已交付报告。若本 Stage 产生派生产物，应只清理明确允许清理的临时文件或可重建缓存。
