<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage052-p1-local | 2026-08-13 | 完成 Stage052 P1 中英文 OCR 静态合同：在 Stage051 已复审队列基线上声明中文简体、英文和中英文混合页面的 reference-only 输入、按页输出引用、置信度隔离、缓存边界和 Stage054 后续复核路由。未读取真实资料，未执行语言检测或 OCR 引擎、队列、缓存、复核、质量门、持久化、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P2。 |
| v0.1-stage051-review-local | 2026-08-13 | 完成 Stage051 OCR 队列基线整阶段本地白箱复审：P1--P4 合同、四页控制切片、五类显式质量处置、5 个 metadata-only 交付样例、中文确认、缓存边界和 P4→P3→P2→P1→BATCH041_050 回滚链一致。未读取真实资料，未调用 OCR 引擎、创建队列/缓存/复核记录或生产运行，未执行智能体、模型 Token、OVH、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P1。 |
| v0.1-stage051-p4-local | 2026-08-13 | 完成 Stage051 P4 OCR 队列交付证据：从 P3 五类固定非业务标量 control 派生 5 个 metadata-only 交付样例、置信度汇总、1 条显式失败、2 条未排队复核路由、中文人工确认提示与零临时产物重跑说明。未读取真实资料，未选择或调用 OCR 引擎，未创建队列、缓存、复核记录或生产运行，未执行智能体、模型、OVH、上传或推送。 |
| v0.1-stage051-p3-local | 2026-08-13 | 完成 Stage051 P3 OCR 质量受控场景：重放 P2 的四页固定非业务 control 队列，以扫描 PDF、模糊图片、表格图片、中英文混合和低质量五类标量类别验证候选、降级与失败处置，静默丢弃为零。P2 缓存继续仅内存可重建且没有临时产物；未打开文件或图片，未调用 OCR 引擎、图像处理、表格提取、准确率评估、实际人工复核、持久化、智能体、模型、OVH、生产、上传或推送。 |
| v0.1-stage051-p2-local | 2026-08-12 | 完成 Stage051 P2 OCR 队列最小可运行切片：四个固定非业务控制页在 P1 七字段引用输入上形成纯内存队列记录、八字段逐页结构、来源页引用、低置信/失败/中英混合可解释状态与仅内存可重建缓存策略。未读取真实资料，未选择或调用 OCR 引擎，未写入持久队列、缓存、复核记录、质量门、证据、智能体、模型、OVH、生产、上传或推送。 |
| v0.1-stage051-p1-local | 2026-08-12 | 完成 Stage051 P1 OCR 队列白箱合同：固定七字段引用输入、三类输入提示、八字段按页输出、默认中文简体与英文、低置信度隔离、未来可重建缓存、中文反馈与回滚范围。未读取真实资料，未选择或调用 OCR 引擎，未创建队列、按页输出、缓存、复核记录、质量门、持久化、Agent、模型调用、OVH、生产、上传或推送。 |
| `v0.1-batch041-050-review-local` | 2026-08-12 | 完成 Stage041-050 本地白箱批次复审：十个既有 Stage 复审工件、接口责任链、批次治理路线和中文事实投影一致；不读取真实资料，不执行文件检测、路由、parser、fallback、质量门、持久化、Agent、模型调用、OVH、生产、Stage051 或上传，所有上传路径保持关闭直到最终 ACC-STAGE-168。 |
| `v0.1-stage050-review-local` | 2026-08-12 | 完成 Stage050 P1-P4 本地白箱复审：单一事实源、P2 evidence-only 标记、11 个明确场景、8 个仅结构样例、11 条非运行时记录、五类失败关闭、指令文本边界、空运行时格式集合与完整回滚链一致；不读取真实资料，不执行真实检测、路线、parser、fallback、运行时标记、质量门、持久化、Agent、模型调用、OVH、批次复审、上传或生产动作。 |
| `v0.1-stage050-p4-local` | 2026-08-12 | 完成 Stage050 P4 提示注入标记收口证据：从 P3 的 11 个格式标签化非业务 control 派生 8 个仅结构 parser 输出样例、11 条非运行时 fallback 处置记录、质量指标、五类失败分类、格式边界与配置回滚说明。聚焦用例通过 13/13，Stage050 P1-P3 前序兼容用例通过 26/26，Stage049 P1-P4 及复审前序兼容用例通过 47/47，Stage048 P1-P4 及复审前序兼容用例通过 48/48。未读取真实资料，未执行真实检测、路线、parser、fallback、运行时标记、质量门、持久化、Agent、模型调用、OVH、生产、整阶段复审、上传或推送。 |
| `v0.1-stage050-p3-local` | 2026-08-12 | 完成 Stage050 P3 格式标签化提示注入标记受控场景：11 个非业务 control 覆盖 PDF、DOCX、XLSX、CSV、TXT、PNG、JPEG、TIFF、未知、坏输入和指令样文本；全部返回明确 evidence-only、未排队复核或拒绝处置，静默丢弃为零。聚焦用例通过 9/9，Stage050 P1/P2 前序兼容用例通过 17/17，Stage049 P1-P4 及复审前序兼容用例通过 47/47，Stage048 P1-P4 及复审前序兼容用例通过 48/48。未读取真实资料，未执行真实检测、路线、parser、fallback、运行时标记、质量门、持久化、Agent、模型调用、OVH、生产、上传或推送。 |
