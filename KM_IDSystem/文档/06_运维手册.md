<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage086-p1-local | 2026-08-22 | 完成 Stage086 混合排序 P1 静态工程合同：只固定 query 的 11 字段、文档类型／年份／项目／设备／资料状态／证据等级六类 metadata filter 与 filter state 的 7 字段、candidate 的 14 字段、selected 的 10 字段、active_index_version 的 7 字段、关键词／向量／过滤匹配／资料质量／新鲜度／业务模块／混合 score 与排序策略的 10 字段及 retrieval trace 的 11 字段；关键词与向量基线均为必需，模型／版本／维度／相似度度量、活动索引版本、元数据过滤合同、排序解释与证据账本引用均须声明，vector-only 与任一混合排序控制引用缺失失败关闭。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、评分、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE086-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage085-review-local | 2026-08-22 | 完成 Stage085 元数据过滤整阶段纯内存机械复审：只重放冻结 P1--P4 合同、P2/P3/P4 控制报告、固定形状、失败关闭、业务线白箱人工处理与 P4→P3 控制回退。Review 聚焦 11/11、P1--Review 聚焦 44/44、Stage060--085 白箱 1190/1190；通过后只开放 IDS-STAGE086-P1-GATE，Stage086 未启动。未建立第二权威事实源，未读取真实资料，未执行数据库、索引、embedding、查询、过滤、排序、Top-K、检索样例／trace／过滤／有效性报告／证据缺口读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；继续使用既有唯一开发 worktree。 |
| v0.1-stage085-p4-local | 2026-08-22 | 完成 Stage085 元数据过滤 P4 纯内存 metadata-only 交付证据：只从 P3 的八个固定、非业务、reference-only 场景派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口记录、4 条参数回滚说明和 4 条中文反馈，共 572 个交付字段检查点。P1/P2/P3/P4 聚焦 33/33、Stage060--085 白箱 1179/1179、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、混合排序、Top-K、检索样例／trace／过滤／有效性报告／证据缺口读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE085-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage085-p3-local | 2026-08-22 | 完成 Stage085 元数据过滤 P3 纯内存受控场景：只重放 P2 六条固定、非业务、reference-only 控制投影为关键词基线、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引服务版本轨迹八类场景；领域项只作类别，模型、版本、维度、相似度度量和全部引用保持不透明，P2 控制投影字段检查 366 次，P3 八场景各 31 字段共 248 次。P1/P2/P3 聚焦 22/22、Stage060--085 白箱 1168/1168、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE085-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage085-p2-local | 2026-08-22 | 完成 Stage085 元数据过滤 P2 纯内存控制切片：六条固定、非业务、reference-only 请求各含 20 个不透明输入字段，覆盖文档类型、年份、项目、设备、资料状态和证据等级，并各投影 query 11、filter 8、candidate 11、hybrid score 7、selected 8、retrieval trace 10 与 future integration 6 个字段，共 366 个控制检查点；关键词与向量基线必须同时声明，vector-only、非固定输入或任一关键引用缺失均失败关闭。P1/P2 聚焦 15/15、Stage060--085 白箱 1161/1161、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、资料状态读取、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE085-P3-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage085-p1-local | 2026-08-22 | 完成 Stage085 元数据过滤 P1 静态工程合同：只固定 query 的 11 字段、文档类型／年份／项目／设备／资料状态／证据等级六类 metadata filter 与 filter state 的 7 字段、candidate 的 11 字段、selected 的 8 字段、keyword/vector/hybrid score 的 7 字段与 retrieval trace 的 10 字段；关键词与向量基线均为必需，模型／版本／维度／相似度度量、活动索引版本、元数据过滤合同、排序解释与证据账本引用均须声明，vector-only 与任一六类过滤控制引用缺失失败关闭。P1 聚焦 7/7、Stage060--085 白箱 1153/1153、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、资料状态读取、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE085-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage084-review-local | 2026-08-22 | 完成 Stage084 向量检索基线整阶段纯内存机械复审：只复审冻结 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 控制回退，固定核验 P1 11/6/10/7/7/10 字段与 17 类失败关闭、P2 五条控制请求／七组投影／290 次字段检查、P3 八类 31 字段场景／248 次字段检查／八条人工处理，以及 P4 8/8/8/8/8/4/4 交付形状、572 次字段检查与 17 类失败关闭。Review 聚焦 11/11、P1--Review 聚焦 44/44、Stage060--084 白箱 1146/1146、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、混合排序、Top-K、检索样例／trace／过滤／有效性报告／证据账本读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE085-P1-GATE，继续使用既有唯一开发工作树。 |
| v0.1-stage084-p4-local | 2026-08-22 | 完成 Stage084 向量检索基线 P4 纯内存 metadata-only 交付证据：只从 P3 的八个固定、非业务、reference-only 控制场景派生八条检索样例、八条 trace 日志、八条过滤结果、八条有效性测试报告、八条检索不足／证据缺口记录、四条参数回滚说明和四条中文反馈。模型／版本／维度／相似度度量、活动索引版本与全部检索引用均是不透明控制标签；自动缺口处理、自动业务建议和自动参数回滚均关闭。P1/P2/P3/P4 聚焦 33/33、Stage060--084 白箱 1135/1135、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、混合排序、Top-K、trace、证据账本读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE084-REVIEW-GATE，继续使用既有唯一开发工作树。 |
| v0.1-stage084-p3-local | 2026-08-22 | 完成 Stage084 向量检索基线 P3 纯内存受控场景：只重放 P2 的五条固定、非业务、reference-only 控制投影为关键词、材料牌号、设备型号、标准号、语义相似、五维过滤组合、Top-K／排序解释／结果有效性和旧索引版本轨迹八类场景；材料牌号、设备型号、标准号和语义相似只作类别标签，模型／版本／维度／相似度度量及全部检索引用保持不透明，vector-only、关键引用缺失、控制输出偏离或任一运行信号均失败关闭。P2 控制记录字段检查 290 次，P3 八场景各 31 字段共 248 次，P1/P2/P3 聚焦 22/22、Stage060--084 白箱 1124/1124、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE084-P4-GATE，继续使用既有唯一开发工作树。 |
| v0.1-stage084-p2-local | 2026-08-22 | 完成 Stage084 向量检索基线 P2 纯内存控制切片：五条固定、非业务、reference-only 控制请求各含 19 个输入字段，并各投影 query 11、filter 7、candidate 10、hybrid score 7、selected 7、retrieval trace 10 与 future integration 6 个字段，共 290 项投影字段检查；关键词与向量基线、模型／版本／维度／相似度度量、五类过滤、活动索引版本、排序解释与证据账本引用均为不透明控制标签，vector-only 与任一关键引用缺失失败关闭。P1/P2 聚焦 15/15、Stage060--084 白箱 1117/1117、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE084-P3-GATE，继续使用既有唯一开发 worktree。 |
