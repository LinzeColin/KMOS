<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage096-p3-local | 2026-08-24 | 完成 Stage096 知识库投毒防护 P3 纯内存异常场景验证：严格重放 P2 的 6 条非业务、reference-only 控制投影，覆盖无内部证据、低 OCR、旧版本、冲突、撤回资料报告状态影响、恶意资料隔离和低等级伪装高可信结论拒绝 7 个场景，每场景 32 个字段、共 224 个检查点、15 类失败关闭与 4 条中文反馈；无内部证据保持 evidence_gap，低 OCR/旧版本/冲突/撤回保持降级候选，恶意资料保持隔离候选，撤回报告影响只声明，低等级不得支撑高可信结论，业务线白箱人工处理保持前置。P3 聚焦 9/9、P2/P1 聚焦 18/18、Stage088--Stage096 完整关联链 436/436，Stage005 valid=true，Batch041-050 与 Batch051-060 均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查均通过。真实资料、证据账本、检索、实际投毒检测或隔离、风险计算、可信等级变更、撤回、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE096-P4-GATE。 |
| v0.1-stage096-p2-local | 2026-08-24 | 完成 Stage096 知识库投毒防护 P2 纯内存受控最小切片：固定 6 条非业务、reference-only 控制请求、21 个输入字段、6 组投影、每条 58 个字段、共 348 个控制检查点、20 类失败关闭与 4 条中文反馈；保持 evidence 与 document、chunk、fact、query、answer、report 的未来关联、关键结论 evidence_id/evidence_gap 约束、低 OCR/旧版本/冲突/撤回降级候选、疑似恶意资料隔离候选与业务线白箱人工复核。P2 聚焦 9/9，含 P1 的历史关联链 418/418、含 P2 的完整关联链 427/427，Stage005 valid=true，Batch041-050 与 Batch051-060 均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查均通过。真实资料、证据账本、检索、实际投毒检测或隔离、风险计算、可信等级变更、撤回、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE096-P3-GATE。 |
| v0.1-stage096-p1-local | 2026-08-24 | 完成 Stage096 知识库投毒防护 P1 静态控制合同：固定 Evidence Ledger、证据缺口、风险评分、可信等级、撤回和投毒防护的 8 个未来控制引用、6 个控制定义、5 个已复审 A/B/C/D/E 等级标签、14 类失败关闭与 4 条中文反馈。关键结论只以 evidence_id 或 evidence_gap 关联，投毒判定、隔离、高可信准入、风险公式、等级分配、阈值、撤回条件和业务判定保持业务线白箱 owner 后续前置。P1 聚焦 9/9、Stage088--Stage095 历史关联链 409/409、含 P1 的完整关联链 418/418，Stage005 valid=true，Batch041-050 与 Batch051-060 均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查均通过。真实资料、证据账本、检索、实际投毒检测或隔离、风险计算、可信等级变更、撤回、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE096-P2-GATE。 |
| v0.1-stage095-review-local | 2026-08-24 | 完成 Stage095 证据回归测试 P1--P4 的纯内存机械复审：固定复核 P1 的 14/14/6/5 与 14 类失败关闭、P2 的 6 条 21 字段控制请求／6 组投影／每条 58 字段／348 个检查／20 类失败关闭、P3 的 7 个 32 字段场景／224 个检查／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／517 个检查／4 条中文反馈／18 类失败关闭。Review 聚焦 14/14，Stage088--Stage095 精确关联链 409/409，Stage005 valid=true，两个批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器事实渲染 7 个中文文件且文档预算、无登记阻塞与单项目双平面检查均通过。真实资料、检索、证据账本、实际回归、风险、等级、撤回、投毒、报告、数据库、模型 Token、Agent、OVH、生产、上传和推送均未执行；下一步仅可在新的独立 run 进入 IDS-STAGE096-P1-GATE。 |
| v0.1-stage095-p4-local | 2026-08-24 | 完成 Stage095 证据回归测试 P4 纯内存交付：从 P3 七个固定、非业务、reference-only 场景派生 evidence ledger 样例、证据等级报告、撤回影响、回归和不可作为结论依据类型各 7 条，降级说明 4 条、撤回恢复说明 2 条；字段形状为 14/13/13/14/11/10/11，共 517 个交付字段检查点、18 类失败关闭与 4 条中文反馈。P4 聚焦 11/11，Stage088--Stage095 关联链 395/395，Stage005 valid=true，两个批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，批次投影门禁 7/7 通过。实际资料、检索、证据账本写入、回归、风险、等级、撤回、投毒、报告、数据库、模型 Token、Agent、OVH、生产、上传和推送均未执行；下一步仅可在新的独立 run 进入 IDS-STAGE095-REVIEW-GATE。 |
| v0.1-stage095-p3-local | 2026-08-24 | 完成 Stage095 证据回归测试 P3 纯内存异常场景：重放 P2 的 6 条 reference-only 请求、21 个输入字段、6 组投影、每条 58 个字段与 348 个源控制检查点，形成无内部证据、低 OCR、旧版本、冲突、撤回资料报告影响、恶意资料隔离和低等级伪装高可信结论拒绝 7 个场景，每场景 32 个字段、共 224 个场景检查点、15 类失败关闭与 4 条中文反馈。P3 聚焦 9/9、P2/P1 聚焦 18/18、Stage088--Stage095 关联链 384/384，Stage005 valid=true，两个批次检查器均通过，机器事实渲染 7 个中文文件且批次投影门禁 13/13 通过。所有实际运行计数为 0，真实资料、检索、证据账本、实际回归、风险、等级、撤回、投毒、报告、模型 Token、Agent、OVH、生产与正式上传保持后续授权范围。 |
| v0.1-stage095-p2-local | 2026-08-24 | 完成 Stage095 证据回归测试 P2 纯内存控制切片：6 条 reference-only 请求、21 个输入字段、6 组投影、每条 58 个字段、348 个控制检查点、20 类失败关闭与 4 条中文反馈。关键结论继续关联 evidence_id 或 evidence_gap；低 OCR、旧版本、冲突与撤回保持降级候选，疑似恶意资料保持隔离候选。P2/P1 聚焦各 9/9，Stage088--Stage095 P1/P2 关联链 375/375，Stage005 valid=true，两个批次检查器通过，批次投影门禁 13/13 通过。所有实际运行计数为 0，真实资料、检索、证据账本、风险、等级、撤回、投毒、报告、模型 Token、Agent、OVH、生产与正式上传保持后续授权范围。 |
| v0.1-stage095-p1-local | 2026-08-24 | 完成 Stage095 证据回归测试 P1 静态控制合同：固定 Evidence Ledger、证据缺口、风险评分、可信等级、撤回和知识库投毒防护的 14 个未来字段、14 个关联字段、6 个控制定义、5 个继承自 Stage094 Review 的 A/B/C/D/E 等级标签、14 类失败关闭与 4 条中文反馈。关键结论只以 evidence_id 或 evidence_gap 关联，回归规则、风险公式、等级分配、阈值、撤回条件、投毒处置和业务判定保持业务线白箱 owner 后续前置。P1 聚焦 9/9、Stage088--Stage094 历史关联链 357/357、含 Stage095 P1 的完整关联链 366/366，Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查均通过。真实资料、证据账本、检索、实际回归、风险计算、可信等级变更、撤回、投毒、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE095-P2-GATE。 |
| v0.1-stage094-review-local | 2026-08-24 | 完成 Stage094 证据撤回 P1--P4 纯内存机械复审：固定复核 P1 的 18/14/6/5 与 16 类失败关闭、P2 的 6 条 29 字段控制请求／11 组投影／每条 105 字段／630 个检查／30 类失败关闭、P3 的 7 个 32 字段场景／224 个检查／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／517 个检查／4 条中文反馈／18 类失败关闭。复审保持单一权威、撤回规则 owner 前置、业务线白箱人工处理、撤回影响声明与 P4→P3 回退；真实资料、证据账本、风险计算、可信等级变更、撤回、降级、恢复、投毒处置、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE095-P1-GATE。 |
| v0.1-stage094-p4-local | 2026-08-24 | 完成 Stage094 证据撤回 P4 纯内存交付控制验证：从 P3 七个固定、非业务、reference-only 控制场景派生 7 条 evidence ledger 样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条降级说明与 2 条撤回／恢复说明，字段形状为 14/13/13/14/11/10/11，共 517 个交付字段检查点、18 类失败关闭与 4 条中文反馈。P4 聚焦 11/11、精确历史后继链 164/164，Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED。真实资料、证据账本、风险计算、可信等级变更、撤回、降级、恢复、投毒处置、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE094-REVIEW-GATE。 |
