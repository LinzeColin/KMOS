<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage081-p3-local | 2026-08-22 | 完成 Stage081 影子索引 P3 纯内存受控场景：只重放 P2 五条固定、非业务、reference-only 控制投影，以六条显式场景核验构建未完成时旧活动版本继续服务、影子冒烟失败阻断切换、切换失败保持活动版本、回滚目标保留上一活动版本、后台构建期间检索隔离，以及 Operations／报告快照的控制版本可见性。P2 225 次字段检查与 P3 168 次字段检查均通过；五条 Operations 与五条报告快照仅为未写入的控制视图，业务线白箱人工处理仍为未来前置。P3 聚焦 10/10、P1/P2/P3 聚焦 24/24、Stage060--081 白箱 994/994、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器平面渲染 7 个中文文件且文档预算、无登记阻塞、单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、后台构建、物理索引、实际冒烟、活动指针读写、切换、检索、并发检索、回滚、Operations 或报告写入、模型 Token、Agent、OVH、生产、上传或推送；P4 与 Review 未进入，下一步仅可在新的独立 run 进入 Stage081 P4，继续使用既有唯一开发 worktree。 |
| v0.1-stage081-p2-local | 2026-08-22 | 完成 Stage081 影子索引 P2 纯内存控制切片：只以五条固定、非业务、reference-only 控制请求投影索引版本、候选构建与影子、活动指针、冒烟输入输出、未应用切换和未应用回滚资格，所有 chunk_count 为 0，候选、活动、上一活动与影子引用保持隔离。构建未完成、冒烟未执行或失败、计划切换失败时活动版本保持不变；回滚候选只指向保留且仍在窗口内的上一活动版本，业务线白箱人工处理只作为未来控制标签。P2 聚焦 8/8、P1/P2 聚焦 14/14、Stage060--081 白箱 984/984、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器平面 7 个中文文件及文档预算、无登记阻塞、单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage081 P3。 |
| v0.1-stage081-p1-local | 2026-08-22 | 完成 Stage081 影子索引合同 P1 静态工程合同：固定未来索引版本、活动指针、构建中版本、影子索引、冒烟测试、失败保持活动版本、旧活动版本连续服务与回滚窗口；未来批量导入后候选与影子必须隔离，未通过、缺失或未记录冒烟测试不得切换，回滚只指向保留且仍在窗口内的上一活动版本。P1 聚焦 6/6、Stage060--081 白箱 976/976、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器平面 7 个中文文件及文档预算、无登记阻塞、单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage081 P2。 |
| v0.1-stage080-review-local | 2026-08-22 | 完成 Stage080 索引回滚整阶段纯内存机械复审：只重放 P1--P4 合同与 P2/P3/P4 控制报告，核验 P1 7/5/5/6/5/8/8/5/9、P2 五条控制请求与七组投影／225 次字段检查、P3 六条场景／28 字段／168 次检查／五条控制视图／六条人工处理、P4 5/6/5/5/1/3/4/13 交付形状、失败关闭、旧活动版本连续服务、回滚目标、未测量空间影响、业务线白箱人工处理、单一权威、零运行时与 P4→P3 回退链；下一步仅可在新的独立 run 进入 Stage081 P1。 |
| v0.1-stage080-p4-local | 2026-08-22 | 完成 Stage080 索引回滚 P4 纯内存交付证据：只从 P2 五条固定、非业务、reference-only 控制投影与 P3 六条专项场景派生五条索引清单、六条冒烟测试日志、五条切换记录、五条回滚证明、一条旧活动版本保留／未测量空间影响投影、三条重建／暂停／恢复说明和四条中文反馈。P4 聚焦 8/8、P1/P2/P3/P4 聚焦 33/33、Stage060--080 白箱 960/960、Stage005 直接治理、两个既有批次检查和双平面验证均已本地通过；未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage080 Review。 |
| v0.1-stage080-p3-local | 2026-08-22 | 完成 Stage080 索引回滚 P3 纯内存专项场景：只重放 P2 五条固定、非业务、reference-only 控制投影，以六条显式场景覆盖构建未完成失败关闭、冒烟失败、切换失败、回滚资格、旧活动版本连续服务、后台构建期间并发检索控制隔离及 Operations／报告快照版本控制可见性。P3 聚焦 11/11、P1/P2/P3 聚焦 25/25、Stage060--080 白箱 952/952 已本地通过；未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、Operations 或报告写入、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage080 P4。 |
| v0.1-stage080-p2-local | 2026-08-22 | 完成 Stage080 索引回滚 P2 纯内存控制切片：只接收五条固定、非业务、reference-only 控制请求，记录索引版本、文档范围引用、零切块计数和嵌入模型引用，投影候选构建、影子隔离、冒烟输入输出、活动指针、未应用切换与未应用回滚资格。构建未完成或冒烟失败关闭切换，计划切换失败保持活动版本，回滚只投影保留且仍在窗口内的上一活动版本；业务线白箱审批只作控制标签而非实际审批。P2 聚焦 8/8、P1/P2 聚焦 14/14、Stage060--080 白箱 941/941、Stage005 直接治理、两个既有批次检查和双平面验证均已本地通过。未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage080 P3。 |
| v0.1-stage080-p1-local | 2026-08-22 | 完成 Stage080 索引回滚 P1 静态合同：固定未来索引版本、活动指针、构建中版本、影子索引、冒烟测试、失败保持活动版本、旧活动版本连续服务与回滚窗口；未来批量导入后候选必须隔离，未通过、缺失或未记录冒烟测试不得切换，回滚只指向保留且仍在窗口内的上一活动版本。Stage080 P1 聚焦 6/6、Stage060--080 白箱 933/933、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未读取真实资料，未执行批量导入、数据库、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage080 P2。 |
| v0.1-stage079-review-local | 2026-08-22 | 完成 Stage079 索引原子切换整阶段机械复审：只在内存中复审 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 回退边界，复核 P1 的 7/5/5/6/5/8/8/5/9、P2 的五条固定控制请求及六组投影／205 次字段检查、P3 的六条场景／26 字段／156 次字段检查、P4 的 5/6/5/5/1/3/4/13 交付形状、旧活动版本保留／未测量空间影响、重建／暂停／恢复说明和业务线白箱人工处理。Stage079 聚焦 43/43、Stage060--079 白箱 927/927 已通过；真实资料、批量导入、数据库、索引、实际清单／日志／切换／回退、模型 Token、Agent、OVH、生产、上传与推送保持关闭；下一步仅可在新的独立 run 进入 Stage080 P1。 |
| v0.1-stage079-p4-local | 2026-08-21 | 完成 Stage079 P4 索引原子切换纯内存交付证据：从 P2 五条固定、非业务、reference-only 控制投影与 P3 六条专项场景派生五条索引清单、六条冒烟测试日志、五条切换记录、五条回退证明、一条旧活动版本保留／未测量空间影响投影、三条重建／暂停／恢复说明和四条中文反馈；不建立第二权威事实源。Stage079 P4 聚焦 8/8、P1/P2/P3/P4 聚焦 33/33、Stage060--079 白箱 917/917、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面生成 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未读取真实资料，未执行批量导入、数据库、后台构建、物理索引、实际清单／日志／切换／回退写入、空间测量、索引重建、暂停、恢复、检索、Operations 或报告写入、模型、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage079 Review。 |
