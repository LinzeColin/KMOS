<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage065-p3-local | 2026-08-14 | 完成 Stage065 P3 工程语义资产分类受控专项场景：重放 P2 七条固定、非业务、reference-only 十六字段低可信控制记录，覆盖长文档、跨页参数表、施工步骤、参数表、引用页码与重复 chunk embedding/index 写入边界六类显式人工处置，静默丢弃为零并保留六维控制追溯。重复场景仅确认控制模块不发起 embedding 或索引写入，不检测真实重复项或形成真实去重结论；未读取、打开、检测、解析、切分、计算、分类、去重或创建真实资料、分类记录、来源绑定、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P4。 |
| v0.1-stage065-p2-local | 2026-08-14 | 完成 Stage065 P2 工程语义资产分类纯内存控制切片：七条固定、非业务、reference-only 十二字段请求投影七条十六字段低可信待人工复核控制记录，覆盖 procedure/risk/acceptance/material/equipment/case/bid_response、工程步骤/验收条款/参数表三类受保护语义面和六维控制追溯。控制标签不代表真实 chunk、hash、分类、质量或来源验证；未读取、打开、解析、切分、计算或创建真实资料、分类记录、来源绑定、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P3。 |
| v0.1-stage065-p1-local | 2026-08-14 | 完成 Stage065 P1 工程语义资产分类静态合同：定义 12 个仅引用输入、16 个未来分类字段、procedure/risk/acceptance/material/equipment/case/bid_response 七类资产标签、工程步骤/验收条款/参数表三类保护语义面、document/page/section/parser output 六维追溯、10 类失败关闭、中文反馈和回滚范围。聚焦用例 7/7、受影响阶段链路 212/212、两个批次检查器和 Stage005 治理回归均已本地通过，中文事实投影已重渲染 7 个文件。未读取、打开、解析、分类或创建真实资料、chunk、分类记录、来源绑定、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P2。 |
| v0.1-stage064-review-local | 2026-08-14 | 完成 Stage064 P1--P4 纯内存 Chunk 身份与版本控制证据机械复审；下一门为 Stage065 P1，运行时与上传继续关闭 |
| v0.1-stage064-p4-local | 2026-08-14 | 完成 Stage064 P4 Chunk 身份与版本 metadata-only 交付证据：从 P3 六类固定、非业务、reference-only 控制场景派生 6 条 JSONL 样例、控制覆盖率报告、6 条低质量待人工清单、控制回归结果、策略适用边界、回到 P3 的重生成/版本回退说明和 3 条中文确认。控制覆盖率、低质量清单和回归结果均不代表真实文档覆盖率、真实质量或真实版本管理；未读取、打开、检测、解析、切分、写入、重生成或回退真实资料或 fixture，未执行真实 parser、章节检测、chunk 身份/版本、重复检测或去重、分类、覆盖率、质量、来源追溯、索引、数据库、Agent、模型 Token、OVH、生产、Stage064 Review、上传或推送；下一步仅可在新的独立 run 进入 Stage064 Review。 |
| v0.1-stage064-p3-local | 2026-08-14 | 完成 Stage064 P3 Chunk 身份与版本受控专项场景：重放 P2 三条固定、非业务、reference-only 十四字段控制记录，覆盖长文档、跨页参数表、施工步骤、参数表、引用页码与重复 chunk 写入边界六类显式人工处置，静默丢弃为零，并保留 document/page/section/parser output/表格上下文/来源片段六维控制引用形状。重复场景仅确认控制模块没有发起 embedding 或索引写入，不检测真实重复项或形成真实去重结论。Stage064 P3 聚焦用例通过 8/8，含 Stage060、Stage061--063、Stage064 P1--P3 与 Batch051-060 的受影响阶段链路通过 191/191，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或切分真实资料或 fixture，未执行真实 parser、章节检测、chunk 身份/版本/Hash、重复检测或去重、分类、覆盖率、质量、来源追溯、索引、数据库、Agent、模型 Token、OVH、生产、P4、上传或推送；下一步仅可在新的独立 run 进入 Stage064 P4。 |
| v0.1-stage064-p2-local | 2026-08-14 | 完成 Stage064 P2 Chunk 身份与版本纯内存控制切片：三条固定、非业务、reference-only 十字段请求投影三条十四字段待人工复核控制记录，保留 chunk_id/chunk_hash/document_id/page/section/version 控制标签、工程步骤/验收条款/参数表三类保护语义面、六维追溯、十类失败关闭、中文反馈和回滚范围。Stage064 P2 聚焦用例通过 8/8，受影响阶段链路回归通过 183/183，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件。未读取、打开、解析、切分、计算或创建真实资料、chunk、身份、哈希、document 绑定、版本、分类、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage064 P3。 |
| v0.1-stage064-p1-local | 2026-08-14 | 完成 Stage064 P1 Chunk 身份与版本静态合同：定义 10 个仅引用输入、14 个未来身份/版本字段、chunk_id/chunk_hash/document_id/page/section/version 字段标签、工程步骤/验收条款/参数表三类保护语义面、document/page/section/parser output 六维追溯、9 类失败关闭、中文反馈和回滚范围。Stage064 P1 聚焦用例通过 7/7，受影响阶段链路回归通过 181/181，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件。未读取、打开、解析、切分、计算或创建真实资料、chunk、身份、哈希、document 绑定、版本、分类、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage064 P2。 |
| v0.1-stage063-review-local | 2026-08-14 | 完成 Stage063 章节感知切块整阶段本地机械复审：核验 P1--P4 的 8/14/3/6/8 静态形状、3 条固定 control 请求、3 条控制候选、6 类显式人工处置、6 条 metadata-only JSONL 样例、6 条低质量待人工记录、3 条中文确认和 P4→P3 控制回退链，发现数为零。Stage063 Review 聚焦用例通过 8/8，受影响阶段链路回归通过 174/174，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件。复审只读取冻结合同与纯内存报告，未建立第二权威事实源，未读取、打开、检测、解析、切分、写入、重生成或回退真实资料或 fixture，未执行真实身份/版本、重复检测或去重、分类、覆盖率、质量、来源追溯、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage064 Phase 1。 |
| v0.1-stage063-p4-local | 2026-08-14 | 完成 Stage063 P4 章节感知切块 metadata-only 交付证据：从 P3 六类固定、非业务、reference-only 控制场景派生 6 条 JSONL 样例、控制覆盖率报告、6 条低质量待人工清单、控制回归结果、策略适用边界、回到 P3 的重生成/版本回退说明和 3 条中文确认。控制覆盖率、低质量清单和回归结果均不代表真实文档覆盖率、真实质量或真实版本管理；未读取、打开、检测、解析、切分、写入、重生成或回退真实资料或 fixture，未执行真实 parser、章节检测、chunk 身份/版本、重复检测或去重、分类、覆盖率、质量、来源追溯、索引、数据库、Agent、模型 Token、OVH、生产、Stage063 Review、上传或推送；下一步仅可在新的独立 run 进入 Stage063 Review。 |
