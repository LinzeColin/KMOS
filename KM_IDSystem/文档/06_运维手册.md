<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage077-p2-local | 2026-08-21 | 完成 Stage077 P2 后台索引构建纯内存控制切片：五条固定、非业务、reference-only 控制请求投影索引版本、候选后台构建、影子隔离、冒烟门、活动指针、未来原子切换候选和上一活动版本回退候选；不建立第二权威事实源。Stage077 P1/P2 聚焦 14/14、Stage060--077 白箱 812/812、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面生成 7 个中文文件，文档三道门、无登记阻塞与单项目双平面检查通过。未读取真实资料、未执行批量导入、数据库、后台构建、物理索引、冒烟、切换、检索、回退、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 P3。 |
| v0.1-stage077-p1-local | 2026-08-21 | 完成 Stage077 P1 后台索引构建静态合同：固定未来批量导入触发、六字段控制输入与输出、候选版本与影子索引隔离、旧活动索引持续服务、冒烟测试门、失败禁止切换、原子切换条件与上一活动版本回退保留；不建立第二权威事实源。Stage077 P1 聚焦 6/6、Stage060--077 白箱 804/804、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面生成 7 个中文文件且文档三道门、无登记阻塞与单项目双平面检查通过。未读取真实资料、未执行后台构建、批量导入、数据库、索引、冒烟、切换、检索、回退、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 P2。 |
| v0.1-stage076-review-local | 2026-08-21 | 完成 Stage076 Review：只在内存中机械复审冻结 P1--P4 合同、P2/P3/P4 控制报告及 P4→P3 回退边界，确认固定形状、失败禁止切换、旧活动版本连续服务、业务线白箱人工处理、单一权威与零运行时；发现数为零。Stage076 定向 44/44、Stage060--076 白箱 798/798、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面生成 7 个中文文件且文档三道门、无登记阻塞与单项目双平面检查通过。未读取真实资料、未执行索引、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 P1。 |
| v0.1-stage076-p4-local | 2026-08-21 | 完成 Stage076 P4 索引版本 Schema 控制版交付证据：只在内存中从 P2 五条固定、非业务、reference-only 控制投影与 P3 六条专项场景派生五条索引清单、六条冒烟记录、五条切换记录、五条回退证明、一条旧活动版本保留／空间影响投影、三条重建／暂停／恢复说明和四条中文反馈。所有样例均不写入实际清单、日志、索引、报告或持久状态，空间影响未测量，操作均要求业务线白箱人工处理。P4 8/8、Stage060--069 473/473、Stage070--076 315/315、Stage005 直接治理 valid=true，两个批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器平面 7 个中文文件及文档三道门通过；下一步仅可在新的独立 run 进入 Stage076 Review。 |
| v0.1-stage076-p3-local | 2026-08-21 | 完成 Stage076 P3 索引版本 Schema 受控场景：只在内存中重放 P2 的五条固定、非业务、reference-only 控制投影，以六条场景覆盖构建失败、冒烟验证失败、切换失败、回退、旧活动版本持续服务、后台构建期间检索隔离，以及 Operations／报告快照版本可见性；未读取真实资料，未执行批量导入、数据库 schema、索引构建、切换、检索、回退、Operations 或报告写入、模型 Token、Agent、OVH、生产、上传或推送。P3 11/11、Stage060--069 473/473、Stage070--076 307/307、Stage005 直接治理 valid=true，两个批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；下一步仅可在新的独立 run 进入 Stage076 P4。 |
| v0.1-stage076-p2-local | 2026-08-21 | 完成 Stage076 P2 索引版本 Schema 纯内存控制切片：五条固定、非业务、reference-only 控制请求投影三类索引版本、八字段版本记录、五字段构建中版本、五字段活动指针、影子候选、六项切换前验证、原子切换候选、切换失败保持活动版本和回退候选；未读取真实资料，未执行批量导入、数据库 schema、索引构建、切换、检索、回退、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P3。 |
| v0.1-stage076-p1-local | 2026-08-21 | 完成 Stage076 P1 索引版本 Schema 静态合同：固定三类未来索引版本、八字段版本记录、五字段活动指针、五字段构建中版本、影子候选隔离、每次未来批量导入建立新版本、旧活动索引持续服务、切换前验证失败关闭和上一活动版本保留；未读取真实资料，未执行批量导入、数据库 schema、索引构建、切换、检索、回退、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P2。 |
| v0.1-stage075-review-local | 2026-08-21 | 完成 Stage075 外部 API 覆盖授权审计整阶段机械复审：只在内存中重放 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 控制回退，核验默认 denied、两跳继承、document 收紧、预算暂停、五条控制请求、十九字段审计、九十五次字段检查、owner 四字段前置、五条 metadata-only 交付样例、八键查询、十四/十/十二/十三类失败关闭、单一权威和业务线白箱人工复核；发现数为零。未读取真实资料，未创建持久审计记录，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P1。 |
| v0.1-stage075-p4-local | 2026-08-21 | 完成 Stage075 P4 外部 API 覆盖授权审计 metadata-only 交付证据：只从 P3 五条固定、非业务、reference-only 控制场景和 P2 控制投影派生五条策略样例、五条十九字段审计投影、九十五次字段检查、五条零值成本、五条失败处理、五条未外发原因、八键进程内查询、一条 owner 强制允许外发 actor/reason/old_value/new_value 四字段前置样例、P4→P3 回滚和四条中文反馈；未读取真实资料，未创建持久审计记录，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 Review。 |
| v0.1-stage075-p3-local | 2026-08-21 | 完成 Stage075 P3 外部 API 覆盖授权审计专项场景：只重放 P2 五条固定、非业务、:control: 投影，验证 denied 阻断外发、summary_only 仅摘要引用、document 收紧不得升级、full_text_allowed 仅未来文本块引用、预算不足暂停、每条 19 字段审计控制投影、95 次字段检查、3 个未来调用候选与 owner 强制允许外发前 actor/reason/old_value/new_value 四字段前置；未读取真实资料，未创建持久审计记录，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 P4。 |
