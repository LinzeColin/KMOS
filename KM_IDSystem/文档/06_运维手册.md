<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage062-review-local | 2026-08-14 | 完成 Stage062 表格证据绑定整阶段本地机械复审：核验 P1--P4 的 19/17 绑定形状、2/2 control 请求与候选、六类显式人工处置、6 个 metadata-only 交付样例、6 个字段引用标签、6 条质量结果、6 条人工建议、3 条中文确认和 P4→P3 回滚链，发现数为零。Stage062 Review 聚焦用例通过 10/10，阶段链路回归通过 129/129，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实/质量验证、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage063 Phase 1。 |
| v0.1-stage062-p4-local | 2026-08-14 | 完成 Stage062 P4 表格证据绑定 metadata-only 交付证据：从 P3 六类固定、非业务、reference-only 场景派生 6 个表格事实交付样例、6 个字段引用标签、6 条控制质量结果、6 条人工处理建议、3 条中文确认和回到 P3 control 状态的表格重解析/事实回滚说明。P4 聚焦用例通过 13/13，阶段链路回归通过 119/119，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录、事实、证据或 fixture，未执行真实 schema/字段/事实、统计、数据库、Agent、模型 Token、OVH、生产、整阶段复审、上传或推送；下一步仅可在新的独立 run 进入 Stage062 Review。 |
| v0.1-stage062-p3-local | 2026-08-14 | 完成 Stage062 P3 表格证据绑定受控异常场景：重放 P2 两条固定、非业务、reference-only 未绑定候选，覆盖空表、合并单元格、单位混乱、日期格式不一、异常值和重复行六类显式人工处置；静默丢弃为零，保留 evidence_id/document_id/sheet/row/column/source_uri 六维控制引用形状，未验证数值阻断统计和模型确定性结论。Stage062 P3 聚焦用例通过 11/11，阶段链路回归通过 106/106，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录、事实、证据或 fixture，未执行真实 schema/字段/事实、统计、数据库、Agent、模型 Token、OVH、生产、P4、上传或推送；下一步仅可在新的独立 run 进入 Stage062 P4。 |
| v0.1-stage062-p2-local | 2026-08-14 | 完成 Stage062 P2 表格证据绑定纯内存控制切片：两条固定、非业务、reference-only 十九字段请求投影两条十七字段未绑定候选，覆盖 XLSX/CSV、生产/质检类别、evidence_id/document_id/sheet/row/column/source_uri 六维控制引用、人工确认和数值关闭。Stage062 P2 聚焦用例通过 8/8，阶段链路回归通过 95/95，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录、事实、证据或 fixture，未执行真实 schema/字段/事实、统计、数据库、Agent、模型 Token、OVH、生产、P3、上传或推送；下一步仅可在新的独立 run 进入 Stage062 P3。 |
| v0.1-stage062-p1-local | 2026-08-14 | 完成 Stage062 P1 表格证据绑定静态合同：定义十九字段引用式绑定输入、十七字段未来绑定输出、evidence_id/document_id/sheet/row/column/source_uri 六维绑定、八类字段语义、数值不得由模型猜测、结构化事实与 RAG 摘要分离、十三类失败关闭、中文反馈和回滚范围。Stage062 P1 聚焦用例通过 8/8，阶段链路回归通过 87/87，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取或绑定真实表格、生产记录、质检记录、事实、证据或 fixture，下一步仅可在新的独立 run 进入 Stage062 P2。 |
| v0.1-stage061-review-local | 2026-08-14 | 完成 Stage061 结构化数据质量整阶段本地白箱复审：机械核验 P1--P4 的 16/18/5/8/6/11 静态形状、两条固定非业务 control、十条未评估候选、六类显式人工处置、6 份 metadata-only 交付、3 条中文确认、单一权威、数值关闭与 P4→P3 重解析/事实回滚链。Stage061 Review 聚焦用例通过 11/11，阶段链路回归通过 79/79，两个批次检查器均返回 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true，中文事实投影已重渲染 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实/质量验证、统计、数据库、Agent、模型 Token、OVH、生产、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage062 Phase 1。 |
| v0.1-stage061-p4-local | 2026-08-14 | 完成 Stage061 P4 结构化数据质量 metadata-only 交付证据：从 P3 六类固定非业务 control 场景派生 6 个质量交付样例、6 个字段引用标签、6 条控制质量结果、6 条人工处理建议、3 条中文确认和回到 P3 control 状态的表格重解析/事实回滚说明。本地验证为 P4 聚焦用例 13/13、P4/P3/P2/P1/两批次/Stage060 Review 阶段链路回归 68/68、两个批次检查器 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED、Stage005 治理回归 valid=true 与中文事实投影 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实/质量验证、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage061 Review。 |
| v0.1-stage061-p3-local | 2026-08-14 | 完成 Stage061 P3 结构化数据质量受控异常场景：重放 P2 两条固定、非业务、reference-only 十六字段控制输入与十条十八字段未评估质量候选，覆盖空表、合并单元格、单位混乱、日期格式不一、异常值和重复行六类显式人工处置，静默丢弃为零，控制来源位置引用形状保持可追溯，未验证数值阻断统计和模型确定性结论。本地验证为 P3 聚焦用例 13/13、P2/P1/两批次/Stage060 Review 聚焦兼容用例 55/55、两个批次检查器 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED、Stage005 治理回归 valid=true 与中文事实投影 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实质量验证、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage061 P4。 |
| v0.1-stage061-p2-local | 2026-08-14 | 完成 Stage061 P2 结构化数据质量纯内存控制切片：两条固定、非业务、reference-only 十六字段输入投影十条十八字段未评估质量候选，覆盖字段完整性、单位一致性、日期合法性、主键重复和异常值五类维度，并保持六类来源位置控制引用、人工复核和数值关闭。本地验证为 P2 聚焦用例 10/10、P1/两批次/Stage060 Review 聚焦兼容用例 42/42、两个批次检查器 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED、Stage005 治理回归 valid=true 与中文事实投影 7 个文件；未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实质量验证、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage061 P3。 |
| v0.1-stage061-p1-local | 2026-08-14 | 完成 Stage061 P1 结构化数据质量测试静态合同：定义十六字段引用输入、十八字段未来质量结果、字段完整性/单位一致性/日期合法性/主键重复/异常值五类维度、八类字段语义、数值不得由模型猜测、事实与 RAG 摘要分离、十一类失败关闭、中文反馈和回滚范围。未读取或验证真实表格、生产记录、质检记录或 fixture，未执行字段完整性、单位一致性、日期合法性、主键重复、异常值、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage061 P2。 |
