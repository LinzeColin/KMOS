<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage070-p4-local | 2026-08-15 | 完成 Stage070 P4 Embedding 队列与缓存 metadata-only 交付：从 P3 五条固定、非业务、reference-only 控制场景派生五条策略样例、五条十八字段审计投影样例、五条零成本估算、五条失败处理、五条未外发控制引用记录、六键查询、三条中文确认、十二类失败关闭和 P4→P3 回滚说明。所有样例、字段、引用和计数只验证冻结控制合同，不代表真实资料、摘要、文本、外发、队列、缓存、重试、审计、成本、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage070 Review。 |
| v0.1-stage070-p3-local | 2026-08-15 | 完成 Stage070 P3 Embedding 队列、缓存与失败关闭专项场景：重放 P2 五条固定、非业务、reference-only 控制记录，输出五条二十九字段场景结果、五条十八字段审计投影、九十次审计字段检查和三个未来外部调用候选，覆盖 denied 无外发、两个 summary_only 范围、full_text_allowed 文本块控制范围与预算不足暂停。所有结果、字段、引用和计数只验证冻结控制合同，不代表真实资料、摘要、文本、外发、队列、缓存、重试、审计、成本、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P4。 |
| v0.1-stage070-p2-local | 2026-08-15 | 完成 Stage070 P2 Embedding 队列、缓存与审计纯内存控制切片：五条固定、非业务、reference-only 十七字段请求机械投影五条策略继承、五条十二字段未来队列、五条十字段未来缓存、五条七字段失败重试、五条八字段零成本/模型版本和五条十八字段审计记录，覆盖默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停。所有字段、引用和计数只验证冻结控制合同，不代表真实资料、摘要、文本、外发、队列、缓存、重试、审计、成本、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P3。 |
| v0.1-stage070-p1-local | 2026-08-15 | 完成 Stage070 P1 Embedding 队列与缓存静态合同：复用默认 denied、三档外部 API 策略和 data source/document→chunk 自动继承，定义 17 个仅引用输入、12 个未来队列字段、10 个未来缓存字段、7 个未来失败重试字段、8 个成本/模型版本字段、18 个审计字段、12 类失败关闭、三档策略流程、业务线白箱人工处理和回滚边界。所有字段只表示未来 schema，不代表真实资料、摘要、文本、外发、队列、缓存、重试、审计、成本、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P2。 |
| v0.1-stage069-review-local | 2026-08-15 | 完成 Stage069 外部 API 策略继承整阶段本地机械复审：只读重放 P1--P4 合同与 P2/P3/P4 纯内存控制报告，核验 15/23/12/8/18/13 静态形状、五条策略解析/队列意图/成本投影/审计投影、五条专项场景、四条业务线白箱人工处理、九十次审计字段检查、五条 metadata-only 交付样例、四键查询、三条中文确认、十二类失败关闭、单一权威、零运行时和 P4→P3 控制回退链。未读取、打开、解析、生成、外发、写入、查询或回退真实资料，未执行 provider/模型选择、外部 API、模型 Token、OVH、生产、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P1。 |
| v0.1-stage069-p4-local | 2026-08-15 | 完成 Stage069 P4 外部 API 策略继承 metadata-only 交付证据：从 P3 五条固定、非业务、reference-only 控制场景派生 5 条策略样例、5 条十八字段审计日志投影样例、5 条零 Token/零成本估算、5 条失败处理结果、5 条未外发控制引用记录、查询说明、回到 P3 的回滚说明和 3 条中文确认。所有交付工件只表示固定控制形状，不代表真实资料、摘要、文本、外发、审计日志、成本、队列、缓存、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入、查询或回退真实资料、审计、成本或 fixture，未执行真实外部 API、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage069 Review。 |
| v0.1-stage069-p3-local | 2026-08-15 | 完成 Stage069 P3 外部 API 策略继承纯内存专项场景：重放 P2 五条固定、非业务、reference-only 控制记录，验证 denied 无载荷、summary_only 仅摘要引用、document 收紧、full_text_allowed 仅文本块引用、预算不足暂停，以及每条控制场景的审计投影与三个未来调用候选的先审计不变量，固定控制形状为 5/23/18/90/1/2/1/1/3。所有结果只验证冻结控制合同，不读取、打开、解析、生成或外发真实资料、摘要正文或文本块，不创建队列、缓存、审计、索引或数据库，不选择 provider/模型、调用外部 API、消耗模型 Token、部署 OVH、启用生产、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P4。 |
| v0.1-stage069-p2-local | 2026-08-14 | 完成 Stage069 P2 外部 API 策略继承纯内存控制切片：五条固定、非业务、reference-only 十五字段请求投影五条二十三字段策略解析、五条十二字段 Embedding 队列意图、五条八字段零成本/模型版本字段和五条十八字段审计投影，覆盖默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停。所有字段、引用、队列意图、缓存状态、成本和审计投影只验证冻结控制合同，未读取、打开、解析、总结或外发真实资料，未创建队列、缓存、审计、索引或数据库，未选择 provider/模型、调用外部 API、消耗模型 Token、部署 OVH、启用生产、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P3。 |
| v0.1-stage069-p1-local | 2026-08-14 | 完成 Stage069 P1 外部 API 策略继承静态合同：定义 data source/document→chunk 自动继承、默认 denied、denied/summary_only/full_text_allowed 三档、15 个仅引用输入、23 个未来策略输出、12 个未来 Embedding 队列字段、8 个成本/模型版本字段、18 个未来审计字段、13 类失败关闭、中文反馈和回滚范围。本地验证只覆盖静态合同、治理与零运行时边界；未读取、打开、解析、总结或外发真实资料，未创建队列、审计、缓存、索引或数据库，未选择 provider/模型、调用外部 API、消耗模型 Token、部署 OVH、启用生产、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P2。 |
| v0.1-stage068-review-local | 2026-08-14 | 完成 Stage068 质量降级整阶段机械复审：只读重放 P1--P4 合同与 P3/P4 纯内存控制报告，核验 13/19/3/6/17、4/4/19/24、六类显式人工处置、36 次控制追溯检查、6 条 metadata-only JSONL 样例、4 条唯一质量降级控制记录、6 条低质量待人工项、3 条中文确认、12 类失败关闭和 P4→P3 控制回退链，发现数为零。所有结果只说明冻结控制形状，未读取、打开、检测、解析、切分、计算、写入、重生成或回退真实资料、chunk、质量、质量降级、低可信证据、重复检测、去重、来源绑定、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；Stage069 尚未开始，下一步仅可在新的独立 run 进入 Stage069 P1。 |
