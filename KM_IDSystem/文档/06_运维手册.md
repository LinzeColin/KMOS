<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| `v0.1-stage044-p3-local` | 2026-07-19 | 验证十四项隔离 reference-only 半成品清理异常与竞态场景；重复请求、隔离 worker 自退出、drive/disk/API 压力、活动或未知 writer、陈旧身份、同源锁冲突和十四类受保护资料均失败关闭，删除、恢复、持久化、上传与生产动作保持关闭。 |
| `v0.1-stage044-p2-local` | 2026-07-19 | 注册五个未生产校准的提议时序边界并实现 reference-only 半成品候选决策；仅两类可重建半成品在身份、保留、持有、引用、资源、命名空间锁和写入者静止证据完整时进入人工复核，正向结果不授权删除，真实路径访问、扫描、探测、遍历、移动、覆盖、删除、审计写入、上传与生产动作保持关闭。 |
| `v0.1-stage044-p1-local` | 2026-07-19 | 绑定批准来源、Stage043 复审祖先与 Stage029/037-043 清理控制链，交付尝试、批准根目录、相对路径、不可变文件身份候选规则，受保护资料、资源暂停、命名空间锁、写入者静止、目录描述符无跟随删除约束、竞态替换防护、幂等和审计静态合同；步骤二、扫描、遍历、删除、上传与生产动作保持关闭。 |
| `v0.1-stage043-review-local` | 2026-07-19 | 整阶段复审修复一项严重和五项重要问题；恢复身份、崩溃时间、资源信号、Stage039 错误分类、Phase1 结构化失败关闭和当前治理路由均机器验证，Stage044、上传与生产动作继续关闭。 |
| `v0.1-stage043-p4-local` | 2026-07-19 | 收口 Worker 崩溃恢复状态、重试、压力、十三项场景、同源锁、清理白名单、条件处理、安全关闭、恢复与回滚证据；当前自动恢复资格与成功均为空，整阶段复审、上传与生产动作保持关闭。 |
| `v0.1-stage043-p3-local` | 2026-07-18 | 验证十三项隔离 Worker 崩溃恢复异常场景；重复与陈旧请求、控制子进程自行退出、资源暂停、四类同源锁冲突和受保护清理均失败关闭，真实恢复、状态写入、删除、上传与合并保持关闭。 |
| `v0.1-stage043-p2-local` | 2026-07-18 | 注册五个提议时间边界并实现 reference-only Worker 崩溃恢复候选决策；规范身份、时间证据、终态、栅栏、幂等冲突和受保护输出均失败关闭，真实进程恢复、状态写入、上传与合并保持关闭。 |
| `v0.1-stage043-p1-local` | 2026-07-18 | 绑定批准来源、Stage042 复审祖先与 Stage037-042 控制链，交付崩溃证据、幂等、栅栏、资源暂停、检查点续作候选和局部输出隔离静态合同；最终全量回归 878/878 与双平面通过，步骤二、运行态、上传与合并保持关闭。 |
| `v0.1-stage042-review-local` | 2026-07-18 | 整阶段复审修复一项严重和四项重要问题；规范请求身份、严格正版本与动作原因、恢复稳定时间证据、暂停态清理和当前治理路由均失败关闭，Stage043 与上传继续关闭。 |
| `v0.1-stage042-p4-local` | 2026-07-18 | 交付状态图、失败重试、七类反压、清理白名单、三类受控重新排队候选、人工处理、安全关闭、恢复与回滚闭环，并通过 856/856 全量回归与双平面门；观察到的自动恢复成功为空，整阶段复审、实际生命周期、上传与合并保持关闭。 |
