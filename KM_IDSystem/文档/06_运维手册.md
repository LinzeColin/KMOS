<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage088-p1-local | 2026-08-23 | 完成 Stage088 检索结果有效性门禁 P1 静态合同：固定关键词检索、向量检索、六类元数据过滤、混合排序、检索轨迹与有效性门禁之间的控制关系，以及 query、filter、candidate、selected、score、active_index_version、retrieval trace 与结果有效性门禁的控制记录。P1 聚焦 7/7、与 Stage087 Review 关联聚焦 18/18，Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。关键词与向量基线均必需，vector-only 或仅凭系统输出均失败关闭，业务使用前必须经业务线白箱人工复核。未建立第二权威事实源，未读取真实资料，未执行数据库、索引、embedding、查询、过滤、排序、Top-K、trace、结果有效性判定、证据账本、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE088-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage087-review-local | 2026-08-23 | 完成 Stage087 检索轨迹整阶段纯内存机械复审：只重放冻结 P1--P4 合同、P2/P3/P4 控制报告、固定形状、失败关闭、业务线白箱人工处理、单一权威与 P4→P3 控制回退。Review 聚焦 11/11、Stage087 P1--Review 聚焦 43/43；通过后只开放 IDS-STAGE088-P1-GATE，Stage088 未启动。未建立第二权威事实源，未读取真实资料，未执行数据库、索引、embedding、查询、过滤、排序、Top-K、检索样例／trace／过滤／有效性报告／证据缺口读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；继续使用既有唯一开发 worktree。 |
| v0.1-stage087-p4-local | 2026-08-23 | 完成 Stage087 检索轨迹 P4 纯内存 metadata-only 交付证据：只从 P3 的八个固定、非业务、reference-only 受控场景派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口记录、4 条参数回滚说明和 4 条中文反馈，共 572 个交付字段检查点；所有 query、filter、candidate、selected、score、活动索引版本、trace 与证据引用均保持不透明控制标签，交付记录未持久化。P1/P2/P3/P4 聚焦 32/32、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。验证范围仅为 Stage087 固定合同、纯内存控制投影与治理，不宣称仓库级全量测试全绿。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、排序、Top-K、trace、证据账本读写、参数回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE087-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage087-p3-local | 2026-08-23 | 完成 Stage087 检索轨迹 P3 纯内存受控场景：只重放 P2 六条固定、非业务、reference-only 控制投影及其 426 个源字段检查点，覆盖关键词、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引服务版本轨迹八类场景；每场景 31 个字段，共 248 个检查点与八条业务线白箱人工处理要求。P1/P2/P3 聚焦 21/21、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过，不宣称仓库级全量测试全绿。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE087-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage087-p2-local | 2026-08-23 | 完成 Stage087 检索轨迹 P2 纯内存受控切片：六条固定、非业务、reference-only 控制请求各含 query、关键词与向量基线、六类 metadata filter、candidate/selected chunks、score、active_index_version、trace 和 future integration 的 18 个输入字段，并各投影 query 9、filter 8、active index 7、candidate chunk 10、score 7、selected chunk 10、trace 14 与 future integration 6 个字段，共 426 个控制检查点及 27 类失败关闭；vector-only 与任何非固定输入均失败关闭。P1/P2 聚焦 14/14、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；不宣称仓库级全量测试全绿。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE087-P3-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage087-p1-local | 2026-08-23 | 完成 Stage087 检索轨迹 P1 静态工程合同：固定 query 9、六类 metadata filter 与 filter state 7、candidate chunks 10、selected chunks 10、score 7、active_index_version 7、retrieval trace 14 个不透明控制字段及 21 类失败关闭；关键词与向量基线均必需，vector-only 被拒绝。P1 聚焦 7/7、Stage060--087 受控白箱 1241/1241、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE087-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage086-review-local | 2026-08-22 | 完成 Stage086 混合排序整阶段纯内存机械复审：固定复核 P1 11/7/14/10/10/11 字段与 25 类失败关闭、P2 6 条固定控制请求／8 组投影／480 次字段检查／30 类失败关闭、P3 8 类 31 字段场景／248 次字段检查／8 条人工处理，以及 P4 8/8/8/8/8/4/4 交付形状／572 次字段检查／20 类失败关闭；P4→P3 回退、单一权威、业务线白箱人工处理与零运行时边界保持。Review 11/11、P1--Review 44/44、Stage060--086 白箱 1234/1234 已实测；下一步仅可在新的独立 run 进入 IDS-STAGE087-P1-GATE，继续使用既有唯一开发 worktree。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、评分、排序、Top-K、trace、证据账本读写、参数回滚、模型 Token、Agent、OVH、生产、Stage087、上传或推送。 |
| v0.1-stage086-p4-local | 2026-08-22 | 完成 Stage086 混合排序 P4 纯内存 metadata-only 交付证据：只从 P3 的八个固定、非业务、reference-only 场景派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口记录、4 条参数回滚说明和 4 条中文反馈，共 572 个交付字段检查点。P1/P2/P3/P4 聚焦 33/33、Stage060--086 白箱 1223/1223、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、评分、排序、Top-K、trace、证据账本读写、参数回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE086-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage086-p3-local | 2026-08-22 | 完成 Stage086 混合排序 P3 纯内存受控场景：只重放 P2 六条固定、非业务、reference-only 控制投影为关键词基线、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引服务版本轨迹八类场景；领域项只作类别，资料质量、新鲜度、业务模块、模型、版本、维度、相似度度量和全部引用保持不透明。P2 控制投影字段检查 480 次，P3 八场景各 31 字段共 248 次；独立活动索引版本、candidate、selected 和 trace 同链，P2 输出偏离或任一运行信号失败关闭。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、资料质量／新鲜度／业务模块评分、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE086-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage086-p2-local | 2026-08-22 | 完成 Stage086 混合排序 P2 纯内存受控切片：只以六条固定、非业务、reference-only 控制请求各投影 query 11、filter 8、active_index_version 7、candidate 14、hybrid score 10、selected 10、retrieval trace 11 与 future integration 9 个字段，共 480 个控制检查点。关键词与向量基线均为必需，文档类型／年份／项目／设备／资料状态／证据等级六类过滤均覆盖，candidate、selected、trace 的活动索引版本、元数据过滤、模型版本、相似度度量、排序策略、评分解释与证据账本引用链保持一致，vector-only 与任何非固定输入失败关闭。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、评分、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE086-P3-GATE，继续使用既有唯一开发 worktree。 |
