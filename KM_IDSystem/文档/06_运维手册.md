<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage057-p2-local | 2026-08-13 | 完成 Stage057 P2 XLSX/CSV 接入纯内存控制切片：两条固定非业务 reference-only 控制记录严格使用 P1 12 字段，投影 2 个 schema profile、10 个字段/19 字段空值事实候选和 2 个 metadata-only RAG 摘要候选，并保留来源定位和数值边界。未读取、检测或解析真实表格或 fixture，未执行真实 schema 推断、真实事实抽取、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P3。 |
| v0.1-stage057-p1-local | 2026-08-13 | 完成 Stage057 P1 XLSX/CSV 接入静态合同：定义 12 字段引用输入、19 字段未来事实输出、七类字段语义、五类来源定位、数值不得由模型猜测、事实与 RAG 摘要分离、六类失败关闭、中文反馈和回滚范围。未读取或解析真实表格、生产记录、质检记录或 fixture，未执行 schema 推断、事实抽取、摘要、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P2。 |
| v0.1-stage056-review-local | 2026-08-13 | 完成 Stage056 OCR 缓存保留策略整阶段本地白箱复审：P1--P4 静态合同、四条固定非业务 control、五类显式质量处置、5 个 metadata-only 交付样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1、1 条显式失败、3 条候选复核路由、中文确认、零物理缓存和回滚链一致。未读取真实资料、调用 OCR、创建或清理缓存、执行人工复核、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P1。 |
| v0.1-stage056-p4-local | 2026-08-13 | 完成 Stage056 P4 OCR 缓存保留策略交付证据：从 P3 五类固定非业务 control 场景派生 5 个 metadata-only 样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1 控制置信度汇总、1 条显式失败、3 条候选复核路由证明、3 条中文人工确认提示与零物理缓存重跑说明。未打开真实样本、未保留 OCR 文本、真实缓存或磁盘信息，未执行 OCR、复核、质量门、缓存清理、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 整阶段复审。 |
| v0.1-stage056-p3-local | 2026-08-13 | 完成 Stage056 P3 OCR 缓存保留策略受控专项场景：重放四条固定非业务 reference-only 缓存策略候选，覆盖扫描 PDF、模糊图片、表格图片、中英文混合和低质量五类控制处置、低置信/混合降级、失败禁止自动清理与零静默丢弃。未读取真实资料、样本、缓存或磁盘信息，未执行 OCR、缓存创建/写入/清理、磁盘扫描、容量评估、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P4。 |
| v0.1-stage056-p2-local | 2026-08-13 | 完成 Stage056 P2 OCR 缓存保留策略纯内存 control 切片：四条固定非业务 reference-only 记录形成 10 字段策略候选、来源页引用，以及低置信、中英文混合和失败产物的可解释状态。未读取真实资料或缓存，未扫描磁盘、评估容量、创建/写入/清理缓存，未执行 OCR、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P3。 |
| v0.1-stage056-p1-local | 2026-08-13 | 完成 Stage056 P1 OCR 缓存保留策略静态合同：定义临时图片、中间文本和失败产物的引用式类别、未来输入输出字段、保留/清理资格、双语与低置信边界、容量前置条件、中文反馈和回滚范围。未读取真实资料或缓存，未扫描磁盘、评估容量、创建/写入/清理缓存，未执行 OCR、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P2。 |
| v0.1-stage055-review-local | 2026-08-13 | 完成 Stage055 OCR 回归语料整阶段本地白箱复审：P1--P4 的十字段引用输入、十一字段未来逐页输出、五类固定非业务 control、显式处置、5 个 metadata-only 样例、HIGH=1/MEDIUM=2/LOW=1/UNKNOWN=1、1 条显式失败、3 条候选复核路由、中文确认、缓存边界和回滚链一致。未读取真实资料、调用 OCR、创建缓存或人工复核任务，未执行 Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P1。 |
| v0.1-stage055-p4-local | 2026-08-13 | 完成 Stage055 P4 OCR 回归语料交付证据：从 P3 五类固定非业务 control 报告派生 5 个 metadata-only 样例、HIGH=1/MEDIUM=2/LOW=1/UNKNOWN=1 控制置信度汇总、1 条显式失败、3 条候选复核路由证明、3 条中文人工确认提示与零临时产物重跑说明。未打开真实样本、未保留 OCR 文本或来源路径，未执行 OCR、复核、质量门、缓存清理、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage055 整阶段复审。 |
| v0.1-stage055-p3-local | 2026-08-13 | 完成 Stage055 P3 OCR 回归语料受控专项场景：重放五条固定非业务 control，覆盖扫描 PDF、模糊图片、表格图片、中英文混合和低质量的显式候选、降级或失败处置与零静默丢弃。未读取样本、调用 OCR 引擎、执行真实回归、创建缓存/人工复核/持久状态，未执行 Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage055 P4。 |
