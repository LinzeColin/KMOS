<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage068-p3-local | 2026-08-14 | 完成 Stage068 P3 质量降级纯内存专项场景：重放 P2 四条固定、非业务、reference-only 十九字段低可信控制记录，覆盖长文档、跨页参数表、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界六类显式人工处置，固定 4/19/6/6/36/4/0/6 形状，保留三类受保护语义面、六维控制追溯、低质量不等于自动完全失败与重复写入禁令。控制标签和人工处置只验证冻结合同，不构成真实资料、质量、重复检测、去重、来源追溯或业务结论；未读取、打开、解析、切分、检测、计算、创建或写入真实资料、页面、chunk、质量、低可信证据、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P4。 |
| v0.1-stage068-p2-local | 2026-08-14 | 完成 Stage068 P2 质量降级纯内存控制切片：四条固定、非业务、reference-only 十三字段请求投影四条十九字段低可信待人工复核控制记录，保留工程步骤/验收条款/参数表三类保护语义面、document/page/section/parser output/表格上下文/来源片段六维控制追溯、一个重复 embedding/index 写入边界、三条业务线白箱人工复核和一条低可信证据人工复核；低质量不等于自动完全失败。所有字段、引用、计数和处理结果只验证冻结控制合同，未读取、打开、解析、切分、检测、计算或创建真实资料、页面、chunk、hash、质量结果、降级结果、低可信证据、重复项、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P3。 |
| v0.1-stage068-p1-local | 2026-08-14 | 完成 Stage068 P1 质量降级静态合同：定义 13 个仅引用输入、19 个未来质量降级字段、需业务线白箱人工复核/低可信证据需人工复核两个未来分流、工程步骤/验收条款/参数表三类保护语义面、document/page/section/parser output 六维追溯、重复 embedding/index 写入边界、17 类失败关闭、中文反馈和回滚范围。本地验证通过：聚焦用例 7/7、Stage060--068 链路 391/391、两个既有批次检查器、Stage005 治理回归 valid=true、中文投影 7 个文件及文档门。所有字段只说明未来合同形状，未读取、打开、解析、切分、检测、计算或创建真实资料、页面、chunk、质量结果、降级结果、低可信证据、重复项、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P2。 |
| v0.1-stage067-review-local | 2026-08-14 | 完成 Stage067 切块质量回归整阶段机械复审：只读重放 P1--P4 合同与 P3/P4 纯内存控制报告，核验 12/17/3/6/15、4/4/24、六类显式人工处置、36 次控制追溯检查、6 条 metadata-only JSONL 样例、4 条唯一控制质量回归记录、6 条低质量待人工项、3 条人工确认、11 类失败关闭和 P4→P3 控制回退链，发现数为零。所有结果只说明冻结控制形状，未读取、打开、检测、解析、切分、计算、写入、重生成或回退真实资料、chunk、质量、重复检测、去重、来源绑定、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；Stage068 尚未开始，下一步仅可在新的独立 run 进入 Stage068 P1。 |
| v0.1-stage067-p4-local | 2026-08-14 | 完成 Stage067 P4 切块质量回归 metadata-only 交付证据：从 P3 六类固定、非业务、reference-only 控制场景派生 6 条内存 JSONL 样例、控制交付覆盖报告、6 条低质量待人工清单、控制回归结果、切块策略适用边界、回到 P3 的重生成/版本回滚说明和 3 条中文确认。所有交付工件只表示固定控制形状，不代表真实文档、真实 chunk、真实质量、真实重复检测、去重、来源验证、重生成或版本回退；未读取、打开、检测、解析、切分、写入、重生成或回退真实资料或 fixture，未执行真实 parser、章节检测、chunk 身份/版本、重复检测或去重、质量、来源追溯、embedding、索引、数据库、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage067 Review。 |
| v0.1-stage067-p3-local | 2026-08-14 | 完成 Stage067 P3 纯内存切块质量回归专项场景：重放 P2 四条固定、非业务、reference-only 十七字段低可信控制记录，覆盖长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界六类显式人工处置，保留六维控制追溯、零静默丢弃和四条唯一控制记录。控制场景不代表真实文档、页码、chunk、hash、质量、重复检测、去重、来源验证或质量降级；未读取、打开、解析、切分、计算或创建真实资料、页面、chunk、质量、重复项、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P4。 |
| v0.1-stage067-p2-local | 2026-08-14 | 完成 Stage067 P2 纯内存切块质量回归控制切片：四条固定、非业务、reference-only 十二字段请求投影四条十七字段低可信待人工复核控制记录，覆盖工程步骤/验收条款/参数表三类保护语义面、六维控制追溯和一个重复 embedding/index 写入边界。控制标签不代表真实 chunk、hash、页码、质量、重复检测、去重、来源验证或质量降级；未读取、打开、解析、切分、计算或创建真实资料、页面、chunk、质量、重复项、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P3。 |
| v0.1-stage067-p1-local | 2026-08-14 | 完成 Stage067 P1 切块质量回归静态合同：定义 12 个仅引用输入、17 个未来质量回归字段、工程步骤/验收条款/参数表三类保护语义面、重复 embedding/index 写入边界、document/page/section/parser output 六维追溯、15 类失败关闭、中文反馈和回滚范围。所有字段只说明未来合同形状，未读取、打开、解析、切分、检测、计算或创建真实资料、页面、chunk、质量结果、重复项、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P2。 |
| v0.1-stage066-review-local | 2026-08-14 | 完成 Stage066 Chunk 覆盖率指标整阶段机械复审：只读重放 P1--P4 合同与 P3/P4 纯内存控制报告，核验 12/17/3/6/14、4/4/24/1/4、六类显式人工场景、六条 metadata-only JSONL 样例、四条唯一控制记录、六条低质量待人工记录、三条人工确认、十一类失败关闭与 P4→P3 控制回退链，发现数为零。所有结果只说明冻结控制形状，未读取、打开、检测、解析、切分、计算、写入、重生成或回退真实资料、chunk、来源绑定、覆盖率、质量、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送；Stage067 尚未开始，下一步仅可在新的独立 run 进入 Stage067 P1。 |
| v0.1-stage066-p4-local | 2026-08-14 | 完成 Stage066 P4 Chunk 覆盖率指标 metadata-only 交付证据：从 P3 六类固定、非业务、reference-only 控制场景派生 6 条内存 JSONL 样例、控制覆盖率报告、6 条低质量待人工清单、控制回归结果、切块策略适用边界、回到 P3 的重生成/版本回退说明和 3 条中文确认。控制覆盖率、低质量清单和回归结果均不代表真实文档、真实 chunk、真实覆盖率或真实质量；未读取、打开、检测、解析、切分、写入、重生成或回退真实资料或 fixture，未执行真实 parser、章节检测、chunk 身份/版本、重复检测或去重、覆盖率、质量、来源追溯、embedding、索引、数据库、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage066 Review。 |
