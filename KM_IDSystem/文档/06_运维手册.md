<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| `v0.1-stage048-p2-local` | 2026-08-12 | 实现纯内存解析器降级处置切片：对七字段仅引用控制结果返回候选、复核、显式失败、受阻或不支持、无效输入的中文处置，并记录 control-fixture parser 版本与置信度；不读取真实资料，不执行文件识别、真实路线、解析器、真实回退、人工复核队列、质量门、持久化、Agent、模型调用、OVH、上传或生产动作。 |
| `v0.1-stage048-p1-local` | 2026-08-12 | 定义主解析器失败后的仅引用降级边界：七字段输入、五种显式处置、克制中文反馈、质量门与不可信文本职责均固定；不读取真实资料，不执行解析器、回退、人工复核队列、质量门、持久化、Agent、模型调用、OVH、上传或生产动作。 |
| `v0.1-stage047-review-local` | 2026-07-24 | 独立整阶段复审实时重验批准来源、不可变 Phase4 快照与 Phase1-4，修复两项严重和四项重要问题：完整请求/结果/来源链、非法 Unicode 结构化拒绝、lower-ASCII 规范引用、互惠内部引用图、有界精确状态/错误及单调请求生产时间；真实业务源、解析器、回退、质量门、持久化、Stage048、上传与生产动作保持关闭。 |
| `v0.1-stage047-p4-local` | 2026-07-23 | 重放已提交的 16 个步骤三场景，交付八个不保留正文的结构投影、16 条零尝试的非运行时回退记录、精确质量指标、七类失败关闭、支持边界与版本回滚；真实源、解析器、回退、质量门、持久化、整阶段复审、Stage048、上传与生产动作保持关闭。 |
| `v0.1-stage047-p3-local` | 2026-07-23 | 交付 16 个格式标签化、预解析、非业务场景：覆盖八种支持格式及未知/损坏格式、低质量、显式失败、指令文本和篡改；11 个控制输出被规范化、3 个失败关闭、2 个路线明确无输出，静默丢弃为零。真实源、解析器、回退、扫描器、公式、质量门、持久化、步骤四、上传与生产动作保持关闭。 |
| `v0.1-stage047-p2-local` | 2026-07-23 | 交付纯内存解析器输出归一化切片：三个非业务控制覆盖 `candidate/partial/failed`，精确验证 `routing lineage`、18 字段 envelope、版本/置信度、安全错误、嵌套引用与证据文本边界；fixture adapter 不是真实解析器，业务源、解析器、回退、质量门、持久化、步骤三、上传与生产动作保持关闭。 |
| `v0.1-stage047-p1-local` | 2026-07-22 | 绑定批准来源与不可变 Stage046 复审快照，交付 reference-only route input、18 字段 parser output envelope、text/tables/pages/sections/confidence/errors 六字段精确形状、嵌套 schema、lineage、safe error 和质量/证据失败关闭合同；业务源、解析器、输出、回退、差异评估、提示注入扫描、持久化、步骤二、上传与生产动作保持关闭。 |
| `v0.1-stage046-review-local` | 2026-07-22 | 独立整阶段复审实时重验批准来源、Phase4 不可变提交与 Phase1-4，修复两项严重、三项重要和一项次要问题；结果级完整性身份、invalid 失败结果、规范引用、事实等级、场景 PASS 不变量和阶段边界均失败关闭，Stage047、解析器、回退、持久化、上传与生产动作保持关闭。 |
| `v0.1-stage046-p4-local` | 2026-07-22 | 从十四项已提交解析器路由场景派生六个解析输出结构样例、十四项 fallback 控制日志、质量指标、五类失败处置、支持边界与配置回滚；样例和日志均不是实际运行结果，解析器、回退、持久化、整阶段复审、Stage047、上传与生产动作保持关闭。 |
| `v0.1-stage046-p3-local` | 2026-07-22 | 复用已提交的仅元数据路由器，以十四个确定性场景覆盖八种治理格式、未知、损坏、冲突、低置信、不支持与指令标记；十四项全部显式处置且无静默丢弃，解析器、回退、输出、持久化、步骤四、整阶段复审、上传与生产动作保持关闭。 |
