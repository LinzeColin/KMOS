<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage053-p4-local | 2026-08-13 | 完成 Stage053 P4 按页 OCR 输出交付证据：从 P3 五类固定非业务 control 质量报告派生 5 个 metadata-only 样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1 控制置信度汇总、1 条显式失败、2 条未排队复核路由、3 条中文人工确认提示与零临时产物重跑说明。未打开真实样本、未保留 OCR 文本或来源路径，未执行 OCR、复核、质量门、缓存清理、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage053 整阶段复审。 |
| v0.1-stage053-p3-local | 2026-08-13 | 完成 Stage053 P3 按页 OCR 输出质量受控场景：重放 P2 四页纯内存输出，以五个固定非业务质量类别验证候选、降级复核提示、表格未评估、显式失败、零静默丢弃和零临时产物边界。未读取真实资料，未执行语言检测或 OCR 引擎，未创建持久队列、输出、缓存、复核、审计、质量门、证据、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage053 P4。 |
| v0.1-stage053-p2-local | 2026-08-13 | 完成 Stage053 P2 按页 OCR 输出受控切片：四个固定非业务控制页在 P1 七字段引用输入上形成纯内存十一字段逐页结构、符号化 OCR 文本、符号化图片引用、受控失败分类、置信度和低置信/混合/失败可解释状态；缓存仅内存可重建且不落盘。未读取真实资料，未执行语言检测或 OCR 引擎，未创建持久队列、输出、缓存、复核、审计、质量门、证据、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage053 P3。 |
| v0.1-stage053-p1-local | 2026-08-13 | 完成 Stage053 P1 按页 OCR 输出静态合同：定义未来 11 字段按页文本输出、置信度、图片引用、失败原因、默认中文简体与英文、低置信度/混合/失败页隔离、缓存与审计引用边界、中文反馈和回滚范围。未读取真实资料，未创建 OCR 文本、图片引用、失败记录、缓存或审计，未执行 OCR、复核、质量门、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage053 P2。 |
| v0.1-stage052-review-local | 2026-08-13 | 完成 Stage052 中英文 OCR 合同整阶段本地白箱复审：P1--P4 合同、双语默认声明、四页控制切片、五类显式质量处置、5 个 metadata-only 交付样例、中文确认、缓存边界和 P4 到 P3 到 P2 到 P1 到 Stage051 review 回滚链一致。未读取真实资料，未调用 OCR 引擎、创建队列/缓存/复核记录或生产运行，未执行智能体、模型 Token、OVH、上传或推送；下一步仅可在新的独立 run 进入 Stage053 P1。 |
| v0.1-stage052-p4-local | 2026-08-13 | 完成 Stage052 P4 中英文 OCR 交付证据：从 P3 五类固定非业务 control 质量报告派生 5 个 metadata-only 样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1 控制置信度汇总、1 条显式失败、2 条未排队复核路由、3 条中文人工确认提示与零临时产物重跑说明。未打开真实样本、未保留 OCR 文本或来源路径，未执行 OCR、复核、质量门、缓存清理、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage052 整阶段复审。 |
| v0.1-stage052-p3-local | 2026-08-13 | 完成 Stage052 P3 中英文 OCR 质量受控场景：重放 P2 的纯内存四页 control 队列，以扫描 PDF、模糊图片、表格图片、中英文混合和低质量五类固定非业务类别验证候选、降级复核提示、表格未评估和显式失败处置，静默丢弃为零。未打开真实样本、未保留符号化输出或 OCR 文本，缓存仅内存可重建且无临时产物；未执行 OCR、复核、质量门、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P4。 |
| v0.1-stage052-p2-local | 2026-08-13 | 完成 Stage052 P2 中英文 OCR 控制队列切片：四个固定非业务控制页在 P1 七字段引用输入上形成纯内存队列记录、符号化八字段逐页结构、来源页引用、置信度和低置信/失败/中英混合可解释状态；缓存仅内存可重建且不落盘。未读取真实资料，未执行语言检测或 OCR 引擎，未创建持久队列、缓存、复核、质量门、证据、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P3。 |
| v0.1-stage052-p1-local | 2026-08-13 | 完成 Stage052 P1 中英文 OCR 静态合同：在 Stage051 已复审队列基线上声明中文简体、英文和中英文混合页面的 reference-only 输入、按页输出引用、置信度隔离、缓存边界和 Stage054 后续复核路由。未读取真实资料，未执行语言检测或 OCR 引擎、队列、缓存、复核、质量门、持久化、智能体、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P2。 |
| v0.1-stage051-review-local | 2026-08-13 | 完成 Stage051 OCR 队列基线整阶段本地白箱复审：P1--P4 合同、四页控制切片、五类显式质量处置、5 个 metadata-only 交付样例、中文确认、缓存边界和 P4→P3→P2→P1→BATCH041_050 回滚链一致。未读取真实资料，未调用 OCR 引擎、创建队列/缓存/复核记录或生产运行，未执行智能体、模型 Token、OVH、上传或推送；下一步仅可在新的独立 run 进入 Stage052 P1。 |
