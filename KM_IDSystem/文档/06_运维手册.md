<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage091-p3-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P3 纯内存异常场景验证：重放 P2 六条固定、非业务、reference-only 控制投影，形成无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料及低等级伪装高可信结论七个场景，每场景 32 个字段、共 224 个场景字段检查点，并保持 P2 的 468 个源控制字段检查点与 15 类失败关闭。撤回只声明未来报告状态影响引用，D 等级不得进入高可信结论，全部场景保持业务线白箱人工处理。真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage091-p2-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P2 纯内存受控最小切片：只接收 6 条固定、非业务、reference-only 的 27 字段控制请求，绑定 Stage091 P1 evidence gap 合同与已复审 Stage090 检索证据捕获控制链，并投影 evidence gap schema binding 6、evidence gap relation 7、检索证据捕获 binding 10、风险 9、撤回 8、投毒防护 8、关键结论 binding 7、降级 10、报告状态影响 6 与 future integration 7 个字段，共 468 个控制检查点与 25 类失败关闭。资料不足场景只以 evidence_gap 绑定关键结论，低可信、冲突、过期和撤回保持降级候选，疑似投毒保持隔离候选，全部业务使用保持白箱人工复核。真实资料、检索、证据账本、回答、报告、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-P3-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage091-p1-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P1 静态工程合同：固定内部资料不足时以 evidence_gap 表达的 12 字段未来引用形状、evidence gap 与 document/chunk/fact/query/answer/report 的 7 字段关联、A/B/C/D/E 可信等级、关键结论 evidence_id/evidence_gap 约束与 12 类失败关闭。P1 聚焦 8/8，与 Stage090 Review/P4/P3/P2/P1、Stage089 Review、Stage088 Review 的精确关联聚焦 85/85，Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED。evidence gap 不能伪装成内部经验；真实资料、检索、证据账本、回答、报告、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage090-review-local | 2026-08-24 | 完成 Stage090 从检索捕获证据整阶段纯内存机械复审：复核 P1 的 10/9/7/5 静态形状与 12 类失败关闭、P2 的 6 条 26 字段控制请求／10 组投影／462 个字段检查／25 类失败关闭、P3 的 7 个 32 字段场景／224 个字段检查／7 条人工处理／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／4 条中文反馈／517 个字段检查／18 类失败关闭。Review 聚焦 14/14，与 Stage090 P4/P3/P2/P1、Stage089 Review、Stage088 Review 的精确关联聚焦 77/77；业务事实继续由来源文档、真实证据账本与业务线白箱人工复核裁定，撤回影响只声明不应用。真实资料、检索、证据账本、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与上传保持在后续授权范围；下一步在新的独立 run 进入 IDS-STAGE091-P1-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage090-p4-local | 2026-08-24 | 完成 Stage090 从检索捕获证据 P4 纯内存交付控制验证：只从 P3 七条固定、非业务、reference-only 控制场景派生 7 条 evidence ledger 样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条降级和 2 条撤回／恢复说明，共 517 个交付字段检查点，并保持 P2 的 462 个源字段检查点与 P3 的 224 个场景字段检查点。撤回只声明未来报告状态影响引用；所有证据类型保持不可自动作为结论依据，降级、撤回与恢复保持业务线白箱人工处理前置。P4 聚焦 11/11；真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与上传保持在后续授权范围；下一步在新的独立 run 进入 IDS-STAGE090-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage090-p3-local | 2026-08-24 | 完成 Stage090 从检索捕获证据 P3 纯内存异常场景验证：重放 P2 六条固定、非业务、reference-only 控制投影，形成无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料及低等级伪装高可信结论七个场景，每场景 32 个字段、共 224 个场景字段检查点，并保持 P2 的 462 个源字段检查点。撤回输出未来报告状态影响引用；低可信、冲突、旧版本和撤回保持降级候选，疑似投毒保持隔离候选，D 等级进入高可信结论拒绝状态。P3 聚焦 11/11；真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与上传保持在后续授权范围；下一步在新的独立 run 进入 IDS-STAGE090-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage090-p2-local | 2026-08-23 | 完成 Stage090 从检索捕获证据 P2 纯内存受控最小切片：只接收 6 条固定、非业务、reference-only 的 26 字段控制请求，绑定 Stage089 前序 evidence schema，并投影 schema binding 6、检索捕获 10、账本捕获 9、关联 7、风险 8、撤回 7、投毒防护 8、关键结论绑定 7、降级 8 与 future integration 7 个字段，共 462 个控制检查点与 25 类失败关闭。低可信、冲突、过期和撤回证据均为降级候选，疑似投毒为隔离候选，A 等级仍待业务线白箱人工复核。P2 聚焦 9/9；未建立第二权威事实源，未读取真实资料，未执行真实检索、证据账本、回答、报告、数据库、风险、可信等级变更、撤回、投毒防护、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE090-P3-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage090-p1-local | 2026-08-23 | 完成 Stage090 从检索捕获证据 P1 静态合同：只固定检索证据捕获请求 10 字段、未来证据账本捕获 9 字段、evidence 与 document/chunk/fact/query/answer/report 关联 7 字段、A/B/C/D/E 可信等级与 12 类失败关闭。关键结论未来必须关联 evidence_id 或 evidence_gap；低可信、冲突、过期、撤回或疑似投毒证据不得自动升格或采纳。P1 聚焦 8/8、与 Stage089 Review/Stage088 Review 关联聚焦 32/32，Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED。未建立第二权威事实源，未读取真实资料，未执行真实检索、证据账本、回答、报告、数据库、风险、可信等级变更、撤回、投毒防护、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE090-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage089-review-local | 2026-08-23 | 完成 Stage089 证据账本 Schema 整阶段纯内存机械复审：复审 P1 的 10/8/5/8/7/8/7 字段与 23 类失败关闭、P2 的 6 条固定控制请求／24 个输入字段／10 组投影／444 个字段检查／23 类失败关闭、P3 的 7 个 32 字段场景／224 个字段检查／7 条人工处理／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／4 条中文反馈／517 个字段检查／18 类失败关闭。撤回只声明不应用，所有控制记录仍待业务线白箱人工处理。未建立第二权威事实源，未读取真实资料，未执行真实证据账本、报告、数据库、OCR 或版本评估、冲突裁决、风险计算、可信等级变更、撤回、恢复、投毒防护、模型 Token、Agent、OVH、生产、Stage090、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE090-P1-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage089-p4-local | 2026-08-23 | 完成 Stage089 证据账本 Schema P4 纯内存交付控制证据：只从 P3 七个固定、非业务、reference-only 异常场景派生 7 条证据账本样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条证据降级和 2 条撤回／恢复说明，共 517 个交付字段检查点。撤回只声明报告影响且不更新报告；无内部证据、低 OCR、旧版本、冲突、撤回、恶意与低等级伪装不得自动作为结论依据。P4 聚焦 11/11；未建立第二权威事实源，未读取真实资料，未执行真实证据账本、数据库、OCR 或版本评估、冲突裁决、风险计算、可信等级变更、撤回、恢复、投毒防护、报告状态更新、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE089-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
