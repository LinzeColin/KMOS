<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage084-p1-local | 2026-08-22 | 完成 Stage084 向量检索基线 P1 静态工程合同：只固定 query 的 11 字段、五类 metadata filter 的 6 字段、candidate 的 10 字段、selected 的 7 字段、keyword/vector/hybrid score 的 7 字段与 retrieval trace 的 10 字段；关键词基线与向量基线均为必需，模型／版本／维度／相似度度量、活动索引版本、排序解释与证据账本引用均须声明，vector-only 失败关闭。P1 聚焦 7/7、Stage060--084 白箱 1109/1109、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、embedding、关键词或向量检索、过滤、混合排序、Top-K、trace、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 IDS-STAGE084-P2-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage083-review-local | 2026-08-22 | 完成 Stage083 关键词检索基线整阶段纯内存机械复审：只复审冻结 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 回退，固定核对 P1 7/6/8/7/7/8 字段与 13 类失败关闭、P2 五条控制请求／七组投影／250 次字段检查、P3 八类场景／26 字段／208 次字段检查／八条人工处理，以及 P4 8/8/8/8/8/3/4 交付形状、432 次字段检查与 16 类失败关闭。Review 聚焦 11/11、P1--Review 聚焦 47/47、Stage060--083 白箱 1102/1102；Stage005 直接治理与两个既有批次检查器均通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、混合排序、Top-K、trace、证据账本读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；仅开放 IDS-STAGE084-P1-GATE，Stage084 未启动，继续使用既有唯一开发 worktree。 |
| v0.1-stage083-p4-local | 2026-08-22 | 完成 Stage083 关键词检索基线 P4 纯内存 metadata-only 交付证据：只从 P3 的八类固定、非业务、reference-only 控制场景派生八条检索样例、八条 trace 日志、八条过滤结果、八条有效性测试报告、八条检索不足或证据缺口记录、三条参数回滚说明和四条中文反馈。P3 场景与 P2 控制标签保持不透明且未持久化；自动补全、自动业务推荐和自动参数回滚均关闭，未来参数变更必须有版本化参数、业务线白箱批准和可验证回退目标。P4 聚焦 11/11、Stage060--083 白箱 1091/1091、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、混合排序、Top-K、trace、证据账本读写、参数写入或回滚、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage083 Review，继续使用既有唯一开发 worktree。 |
| v0.1-stage083-p3-local | 2026-08-22 | 完成 Stage083 关键词检索基线 P3 纯内存受控场景：只重放 P2 的五条固定、非业务、reference-only 控制投影为关键词、材料牌号、设备型号、标准号、语义相似、五维过滤组合、Top-K／排序解释／结果有效性和旧索引版本轨迹八类场景；材料牌号、设备型号、标准号和语义相似均只作类别标签。P2 控制记录字段检查 250 次，P3 八个场景各 26 字段共 208 次，P3 聚焦 10/10、Stage060--083 白箱 1080/1080、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、过滤、混合排序、Top-K、轨迹、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage083 P4，继续使用既有唯一开发 worktree。 |
| v0.1-stage083-p2-local | 2026-08-22 | 完成 Stage083 关键词检索基线 P2 纯内存控制切片：只以五条固定、非业务、reference-only 请求投影关键词基线、文档类型／年份／项目／设备／证据等级过滤、candidate、keyword/vector/hybrid score、selected、retrieval trace、活动索引版本与证据账本引用；vector-only 和任何输入偏离均失败关闭，future PostgreSQL FTS／BM25、pgvector、过滤、混合排序与轨迹路由只声明未执行。P2 聚焦 8/8、Stage060--083 白箱 1070/1070、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、关键词或向量检索、过滤、混合排序、Top-K、轨迹、证据账本读写、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage083 P3，继续使用既有唯一开发 worktree。 |
| v0.1-stage083-p1-local | 2026-08-22 | 完成 Stage083 关键词检索基线 P1 静态工程合同：固定 query 的 7 字段、五类 metadata filter 的 6 字段、candidate 的 8 字段、selected 的 7 字段、keyword/vector/hybrid score 的 7 字段与 retrieval trace 的 8 字段，要求关键词检索基线存在且不得只依赖向量相似度，候选、选择和轨迹关联 active_index_version、排序解释与证据账本引用。P1 聚焦 7/7、Stage060--083 白箱 1062/1062、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED。未建立第二权威事实源，未读取真实资料，未执行 PostgreSQL、FTS、BM25、pgvector、数据库、索引、查询、过滤、混合排序、Top-K、轨迹、证据账本、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage083 P2，继续使用既有唯一开发 worktree。 |
| v0.1-stage082-review-local | 2026-08-22 | 完成 Stage082 旧索引保留策略整阶段纯内存机械复审：只复审冻结 P1--P4 合同、P2/P3/P4 控制报告与 P4→P3 回退，固定核对 P1 7/5/5/6/5/10/6 字段与 14 类失败关闭、P2 五条控制请求／九组投影／305 次字段检查、P3 六条场景／31 字段／186 次字段检查／五条 Operations 与五条报告快照控制视图／六条人工处理，以及 P4 5/6/5/5/1/3/4 交付形状与 15 类失败关闭。Review 聚焦 11/11、P1--Review 聚焦 43/43、Stage060--082 白箱 1055/1055、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行任何索引、清单、日志、指针、切换、检索、回滚、旧索引清理、空间测量、模型 Token、Agent、OVH、生产、上传或推送；仅开放 IDS-STAGE083-P1-GATE，Stage083 未启动，继续使用既有唯一开发 worktree。 |
| v0.1-stage082-p4-local | 2026-08-22 | 完成 Stage082 旧索引保留策略 P4 metadata-only 交付证据：只从 P2 五条固定、非业务、reference-only 控制投影与 P3 六条受控场景派生五条索引清单、六条冒烟测试日志、五条切换记录、五条回滚证明、一条旧索引保留／未测量空间影响投影、三条重建／暂停／恢复说明和四条中文反馈。最低保留一个上一活动版本；额外保留数量、回滚窗口、清理时点或业务线白箱批准未设值时，回滚与旧索引清理保持失败关闭。P4 聚焦 8/8、P1/P2/P3/P4 聚焦 32/32、Stage060--082 白箱 1044/1044、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行实际 manifest／日志／切换／回滚证明写入、空间测量、旧索引删除、重建、暂停、恢复、批量导入、数据库、后台构建、物理索引、实际冒烟、活动指针读写、检索、回滚、模型 Token、Agent、OVH、生产、上传或推送；Review 未进入，下一步仅可在新的独立 run 进入 Stage082 Review，继续使用既有唯一开发 worktree。 |
| v0.1-stage082-p3-local | 2026-08-22 | 完成 Stage082 旧索引保留策略 P3 纯内存受控场景：只重放 P2 五条固定、非业务、reference-only 控制引用为六条构建未完成、影子冒烟失败、计划切换失败、回滚窗口未设值、旧活动版本连续服务、后台构建检索隔离及 Operations／报告快照版本可见性场景；所有引用不透明且未写入，候选、活动、上一活动与影子保持隔离。最低保留一个上一活动版本；额外保留数量、回滚窗口、清理时点或业务线白箱批准未设值时，回滚与旧索引清理保持失败关闭。P3 聚焦 10/10、P1/P2/P3 聚焦 24/24、Stage060--082 白箱 1036/1036、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、后台构建、物理索引、实际冒烟、活动指针读写、切换、检索、并发检索、回滚、旧索引清理、Operations 或报告写入、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage082 P4，继续使用既有唯一开发 worktree。 |
| v0.1-stage082-p2-local | 2026-08-22 | 完成 Stage082 旧索引保留策略 P2 纯内存控制切片：以五条固定、非业务、reference-only 控制请求投影版本、构建、影子、冒烟、候选切换、回滚和保留／清理资格；全部 chunk_count 为 0，候选、活动、上一活动与影子引用隔离。最低保留一个上一活动版本；额外保留数量、回滚窗口、清理时点或业务线白箱批准未设值时，回滚与旧索引清理保持失败关闭。P2 聚焦 8/8、P1/P2 聚焦 14/14、Stage060--082 白箱 1026/1026、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED；机器平面重渲染 7 个中文文件，文档预算、无登记阻塞与单项目双平面检查通过。根级精简治理入口在当前稀疏工作树中不存在，未陈述为已执行。未建立第二权威事实源，未读取真实资料，未执行批量导入、数据库、后台构建、物理索引、实际冒烟、活动指针读写、切换、检索、回滚、旧索引清理、空间测量、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage082 P3，继续使用既有唯一开发 worktree。 |
