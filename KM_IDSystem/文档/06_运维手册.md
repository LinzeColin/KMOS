<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage074-p1-local | 2026-08-21 | 完成 Stage074 P1 本地 Embedding 兜底静态合同：固定未来本地路线、默认 denied、三档策略、data source/document 到 chunk 自动继承、owner 不逐条标记 chunk、12/10/7 队列/缓存/失败重试、16/8 成本、6 字段模型版本、18 字段审计和 12 类失败关闭；未选择或下载模型，未执行本地/外部 Embedding、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage074 P2。 |
| v0.1-stage073-review-local | 2026-08-20 | 完成 Stage073 Embedding 审计测试整阶段本地机械复审：只读重放 P1--P4 合同与 P2/P3/P4 纯内存控制报告，核验 3/2/12/8/6/18/7 静态形状、五条策略/队列/缓存/重试/模型版本/成本/审计投影、五条三十五字段场景、四条业务线白箱人工处理、九十次审计字段检查、五条 metadata-only 交付样例、七键查询、四条中文反馈、十二类失败关闭、单一权威、零运行时和 P4→P3 控制回退链；未读取、打开、解析、生成、外发、写入、查询或回退真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage074 P1。 |
| v0.1-stage073-p4-local | 2026-08-20 | 完成 Stage073 P4 Embedding 审计测试 metadata-only 交付证据：只从 P3 五条固定、非业务、reference-only 控制场景及 P2 纯内存投影派生五条策略样例、五条十八字段审计投影、九十次审计字段检查、五条零值成本、五条失败处理、五条未外发原因、七键查询、回到 P3 的控制回退和四条中文反馈。所有字段、标签、引用和零值只表示控制形状，不代表真实资料、摘要、文本、外发、队列、缓存、重试、模型版本、成本、审计、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage073 Review。 |
| v0.1-stage073-p3-local | 2026-08-20 | 完成 Stage073 P3 Embedding 审计测试专项验证：只重放 P2 五条固定、非业务、reference-only 控制记录，机械核验 denied 阻断外发、summary_only 仅摘要引用、document 收紧不得升级、full_text_allowed 仅文本块引用候选、预算不足暂停，以及五条十八字段审计投影、九十次审计字段检查和三个未来调用审计前置。所有字段、标签、引用和零值只表示控制形状，不代表真实资料、摘要、文本、外发、队列、缓存、重试、模型版本、成本、审计、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage073 P4。 |
| v0.1-stage073-p2-local | 2026-08-20 | 完成 Stage073 P2 Embedding 审计测试纯内存控制切片：五条固定、非业务、:control: 二十字段请求机械投影策略继承、12/10/7 队列/缓存/失败重试、八字段零值成本、六字段模型版本和十八字段审计，覆盖默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停。控制审计投影保留 provider、model、token_count=0、不透明 chunk_id 与 policy reason；未读取真实资料、未创建持久审计记录、未调用外部 API、未消耗模型 Token、未执行 Agent、OVH、生产、上传或推送，下一步仅可在新的独立 run 进入 Stage073 P3。 |
| v0.1-stage073-p1-local | 2026-08-20 | 完成 Stage073 P1 Embedding 审计测试静态合同：固定默认 denied、三档策略、data source/document→chunk 自动继承、owner 不逐条标记 chunk、12 字段未来队列、8 字段成本/模型控制、6 字段模型版本、18 字段审计形状与 7 类失败关闭；只扩展历史测试和两项批次检查器以承认 Stage073 P1 的合法后继状态。Stage073 P1 8/8、Stage072 49/49、Stage071 53/53、Stage070 47/47、Stage060--069 473/473 本地通过，两个批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，Stage005 治理回归 valid=true；未读取真实资料、未创建审计记录、未调用外部 API、未消耗模型 Token、未执行 Agent、OVH、生产、上传或推送，下一步仅可在新的独立 run 进入 Stage073 P2。 |
| v0.1-stage072-review-local | 2026-08-20 | 完成 Stage072 Embedding 模型版本整阶段本地机械复审：只读重放 P1--P4 合同与 P2/P3/P4 纯内存控制报告，核验 6/2/12/10/7/8/18/9 静态形状、五条策略/队列/缓存/重试/模型版本/成本/审计投影、五条三十五字段场景、四条业务线白箱人工处理、九十次审计字段检查、五条 metadata-only 交付样例、七键查询、四条中文反馈、十二类失败关闭、单一权威、零运行时和 P4→P3 控制回退链；未读取、打开、解析、生成、外发、写入、查询或回退真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage073 P1。 |
| v0.1-stage072-p4-local | 2026-08-20 | 完成 Stage072 P4 Embedding 模型版本 metadata-only 交付证据：只从五条固定、非业务、reference-only P3 控制场景派生五条策略样例、五条十八字段审计投影、九十次审计字段检查、五条零值成本、五条失败处理、五条未外发记录、七键查询、P4 到 P3 控制回退和四条中文反馈。所有字段、标签、引用和零值只表示控制形状，不代表真实资料、摘要、文本、外发、队列、缓存、重试、模型版本、成本、审计、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage072 Review。 |
| v0.1-stage072-p3-local | 2026-08-20 | 完成 Stage072 P3 Embedding 模型版本专项验证：只重放五条固定、非业务、reference-only P2 控制记录，机械核验 denied 阻断外发、summary_only 只保留摘要引用、document 收紧不得升级、full_text_allowed 只保留文本块引用、预算不足暂停，以及五条十八字段审计投影、九十次审计字段检查和三个未来调用审计前置。所有字段、标签、引用和零值只表示控制形状，不代表真实资料、摘要、文本、外发、队列、缓存、重试、模型版本、成本、审计、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage072 P4。 |
| v0.1-stage072-p2-local | 2026-08-20 | 完成 Stage072 P2 Embedding 模型版本纯内存控制切片：五条固定、非业务、reference-only 二十字段请求机械投影策略继承、12/10/7 队列/缓存/失败重试、六字段模型版本、八字段零值成本和十八字段审计，覆盖默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停。所有字段、引用和计数只验证冻结控制合同，不代表真实资料、摘要、文本、外发、队列、缓存、重试、模型版本、成本、provider、模型或业务结论；未读取、打开、解析、生成、外发、写入或查询真实资料，未执行外部 API、模型 Token、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage072 P3。 |
