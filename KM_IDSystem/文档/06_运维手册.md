<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage093-p1-local | 2026-08-24 | 完成 Stage093 证据可信等级 A/B/C/D/E P1 静态合同：固定 14 个未来可信等级字段、10 个未来关联字段、A/B/C/D/E、关键结论 evidence_id/evidence_gap 关联与 14 类失败关闭。P1 聚焦 9/9、精确历史关联聚焦 191/191、Stage005 直接治理 valid=true，两个既有批次检查器均为 PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED，机器事实重渲染 7 个中文文件，文档预算、无登记阻塞与双平面检查均通过。等级分配规则、阈值和业务判定保持业务线白箱 owner 前置；真实资料、真实证据等级、风险、撤回、投毒、报告状态、数据库、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步仅可在新的独立 run 进入 IDS-STAGE093-P2-GATE。 |
| v0.1-stage092-review-local | 2026-08-24 | 完成 Stage092 证据风险评分整阶段纯内存机械复审：复核 P1 的 20/10/5 静态形状与 14 类失败关闭、P2 的 6 条固定控制请求／27 个输入字段／11 组投影／每条 97 字段／582 个字段检查／28 类失败关闭、P3 的 7 个 32 字段场景／224 个字段检查／7 条人工处理／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／4 条中文反馈／517 个字段检查／18 类失败关闭。无内部证据保持 evidence_gap，撤回影响只声明不应用，低等级伪装高可信结论保持拒绝，风险权重、阈值和业务判定公式保持 owner 前置，业务线白箱人工处理、单一权威与 P4→P3 回退保持；下一步仅开放新的独立 run 的 IDS-STAGE093-P1-GATE。 |
| v0.1-stage092-p4-local | 2026-08-24 | 完成 Stage092 证据风险评分 P4 纯内存交付控制验证：仅从 P3 七条固定、非业务、reference-only 异常场景派生 7 条 evidence ledger 样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条降级说明及 2 条撤回／恢复说明，共 517 个交付字段检查点与 18 类失败关闭；P2 的 6 条／582 个源控制字段检查点和 P3 的 7 个场景／224 个字段检查点保持一致。撤回影响只声明且不应用，低等级伪装高可信结论保持拒绝，全部处置保持业务线白箱人工审批。真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传均未执行；下一步只能在新的独立 run 进入 IDS-STAGE092-REVIEW-GATE。 |
| v0.1-stage092-p3-local | 2026-08-24 | 完成 Stage092 证据风险评分 P3 纯内存异常场景验证：重放 P2 六条固定、非业务、reference-only 控制投影，形成无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料及低等级伪装高可信结论七个场景，每场景 32 个字段、共 224 个场景字段检查点，并保持 P2 的 582 个源控制字段检查点与 15 类失败关闭。撤回只声明未来报告状态影响引用，D 等级不得进入高可信结论，全部场景保持业务线白箱人工处理。风险权重、阈值和业务判定公式继续保持 owner 后续前置；真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE092-P4-GATE。 |
| v0.1-stage092-p2-local | 2026-08-24 | 完成 Stage092 证据风险评分 P2 纯内存受控最小切片：六条固定、非业务、reference-only 的 27 字段控制请求分别投影风险 schema 绑定、10 字段风险关联、检索证据捕获、五类风险输入、风险评分路由、撤回、投毒防护、关键结论绑定、降级、报告状态影响与 future integration 11 组、每条 97 字段，共 582 个控制检查点与 28 类失败关闭。风险权重、阈值和业务判定公式保持业务线白箱 owner 前置；真实资料、风险计算、证据账本、可信等级变更、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE092-P3-GATE。 |
| v0.1-stage092-p1-local | 2026-08-24 | 完成 Stage092 证据风险评分 P1 静态合同：固定来源、OCR 置信度、版本、复核状态和冲突状态五类未来风险输入、20 字段风险引用、10 字段关联、A/B/C/D/E、关键结论 evidence_id/evidence_gap 约束、14 类失败关闭与业务线白箱 owner 风险公式前置。真实资料、风险计算、证据账本、可信等级变更、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE092-P2-GATE。 |
| v0.1-stage091-review-local | 2026-08-24 | 完成 Stage091 证据缺口处理整阶段纯内存机械复审：复核 P1 的 12/7/5 静态形状与 12 类失败关闭、P2 的 6 条固定控制请求／27 个输入字段／10 组投影／468 个字段检查／25 类失败关闭、P3 的 7 个 32 字段场景／224 个字段检查／7 条人工处理／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／4 条中文反馈／517 个字段检查／18 类失败关闭。资料不足保持 evidence_gap，撤回影响仅声明不应用，低等级伪装高可信结论保持拒绝，业务事实继续由来源文档、真实证据账本与业务线白箱人工复核裁定；下一步仅开放新的独立 run 的 IDS-STAGE092-P1-GATE。 |
| v0.1-stage091-p4-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P4 纯内存交付控制验证：只从 P3 七条固定、非业务、reference-only 异常场景派生 7 条 evidence ledger 样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条降级和 2 条撤回／恢复说明，共 517 个交付字段检查点，并保持 P2 的 468 个源字段检查点与 P3 的 224 个场景字段检查点。撤回只声明未来报告状态影响引用；无内部证据、低 OCR、旧版本、冲突、撤回、恶意与低等级伪装均不能自动作为结论依据。P4 聚焦 11/11；真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-REVIEW-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage091-p3-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P3 纯内存异常场景验证：重放 P2 六条固定、非业务、reference-only 控制投影，形成无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料及低等级伪装高可信结论七个场景，每场景 32 个字段、共 224 个场景字段检查点，并保持 P2 的 468 个源控制字段检查点与 15 类失败关闭。撤回只声明未来报告状态影响引用，D 等级不得进入高可信结论，全部场景保持业务线白箱人工处理。真实资料、检索、证据账本、OCR、版本、数据库、风险、撤回、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-P4-GATE，继续使用既有唯一开发 worktree。 |
| v0.1-stage091-p2-local | 2026-08-24 | 完成 Stage091 证据缺口处理 P2 纯内存受控最小切片：只接收 6 条固定、非业务、reference-only 的 27 字段控制请求，绑定 Stage091 P1 evidence gap 合同与已复审 Stage090 检索证据捕获控制链，并投影 evidence gap schema binding 6、evidence gap relation 7、检索证据捕获 binding 10、风险 9、撤回 8、投毒防护 8、关键结论 binding 7、降级 10、报告状态影响 6 与 future integration 7 个字段，共 468 个控制检查点与 25 类失败关闭。资料不足场景只以 evidence_gap 绑定关键结论，低可信、冲突、过期和撤回保持降级候选，疑似投毒保持隔离候选，全部业务使用保持白箱人工复核。真实资料、检索、证据账本、回答、报告、数据库、风险、撤回、恢复、投毒、报告状态、模型 Token、Agent、OVH、生产与正式全局上传保持后续授权范围；下一步只能在新的独立 run 进入 IDS-STAGE091-P3-GATE，继续使用既有唯一开发 worktree。 |
