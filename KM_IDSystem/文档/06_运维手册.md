<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/config.yaml、machine/facts/ops.json、changelog.json -->
<!-- 上限 200 行 -->

# 运维手册

怎么把它跑起来、参数在哪调、报错了怎么办、都改过些什么。

## 一、怎么跑

改完机器平面，按顺序跑这三条，让人类平面刷新并过关：

```bash
python3 machine/tools/render_human.py            # 先渲染人类平面
python3 machine/tools/check_doc_budget.py        # 体积、中文、纯净三道门
python3 machine/tools/check_blocker_stop.py      # 阻塞重审门
```

## 二、参数在哪调

改一个参数要动哪个文件，都在这。为什么定这个值在 `02_系统架构.md`。

| 参数 | 当前值 | 改哪里 |
|---|---|---|
| 租约持续时间 | 30 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 续租间隔 | 10 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 过期宽限 | 5 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 获取超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 最大等待 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 重试抖动 | 0 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |
| 死锁超时 | 1 秒（PROPOSED，非生产校准） | `docs/pursuing_goal/ids_v0_1/lock_registry/stage041_lock_registry_runtime_contract.json` |

## 三、报错了怎么办

> 还没有登记常见故障。踩过坑、解决了，就往 machine/facts/ops.json 里记一条，这里会自动列出，下次少走弯路。

## 四、都改过什么（最近 10 条）

| 版本 | 时间 | 改了什么 |
|---|---|---|
| v0.1-stage104-p4-local | 2026-08-26 | 完成 Stage104 RAG 负向测试 P4 纯内存交付证据：从 P3 的 5 条、每条 34 字段、共 170 个固定 reference-only 场景检查点派生回答样例、负向测试结果、prompt/version 记录、可复现日志和模型输出权限边界各 5 条，以及 prompt 回滚和模型配置回退说明 2 条；字段形状为 17/12/14/17/12/12，共 384 个交付字段检查点、16 类失败关闭与 4 条中文反馈。已通过 P4 聚焦 8/8、Stage104 P1--P4 显式兼容 32/32、Stage088--Stage104 精确白箱链 805/805、Stage005 直接治理校验 valid=true、Batch041-050、Batch051-060、7 个机器生成中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；用户明确授权的 07f3a0970 远端恢复检查点只保全在制工件，本 P4 完成态没有新增远端写入，正式全局上传、main、release、OVH 与生产继续由 Stage104 Review 及后续门禁承接。 |
| v0.1-stage104-p2-local | 2026-08-26 | 完成 Stage104 RAG 负向测试 P2 纯内存受控最小切片：固定 5 条、每条 29 字段的非业务 reference-only 控制请求，形成 4 组、每条 57 字段、共 285 个控制检查点；五个负向标签保持控制标签，文档 evidence 与文档内潜在指令保持不可信、不可执行参考，IDS 规则优先，evidence_gap 保持独立来源类型，外部增强展示保留 external_public_reference 与 model_reasoning 的底层来源类型。已通过 P2 聚焦 8/8、Stage088--Stage104 精确白箱链 789/789、Stage005 直接治理校验 valid=true、Batch041-050、Batch051-060、7 个机器生成中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；用户授权的 Stage104 P1 与当前 P2 已验收提交同步到既有开发分支，作为恢复检查点。本 P2 没有正式 GitHub 上传，main、release、OVH 与生产继续由后续门禁承接，下一步仅可在新的独立 run 进入 IDS-STAGE104-P3-GATE。 |
| v0.1-stage104-p1-local | 2026-08-26 | 完成 Stage104 RAG 负向测试 P1 纯内存静态合同：固定 13 个未来控制引用、5 个未来回答结构区段、4 类底层来源类型、5 类输出权限、5 个未来负向测试标签、19 类失败关闭与 4 条中文反馈。检索文档保持 evidence 身份，文档内潜在指令保持不可信、不可执行参考，IDS 规则、输出权限与业务线白箱人工确认保持优先。已通过 P1 聚焦 8/8、Stage103 P1--Review 与 Stage104 P1 显式兼容 51/51、Stage088--Stage104 精确白箱链 781/781、Stage005 直接治理校验 valid=true、Batch041-050、Batch051-060、7 个机器生成中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；此前用户授权的隔离恢复检查点只保全恢复能力，正式全局上传、main、release、OVH 与生产继续由后续门禁承接，下一步仅可在新的独立 run 进入 IDS-STAGE104-P2-GATE。 |
| v0.1-stage103-review-local | 2026-08-26 | 完成 Stage103 模型输出权限门禁 P1--P4 的纯内存机械复审：固定复核 P1 的 13/5/4/5/24/4、P2 的 5×26/4/46/230、P3 的 5×34=170/5/5/28 与 P4 的 5/5/5/5/5/2、17/12/14/17/12/12、384/4/16；八元控制引用、文档 evidence 边界、IDS 规则优先、来源类型、输出权限、业务线白箱人工处理、失败关闭与 P4→P3 回退保持一致。已通过 Review 聚焦 10/10、P1--Review 兼容 75/75、Stage088--Stage103 精确白箱链 773/773、Stage005 治理回归 178/178 与直接校验 valid=true、Batch041-050、Batch051-060、7 个机器生成中文视图、文档预算、无登记阻塞和双平面检查。回执记录运行计数均为 0、运行标志均为 false；正式全局上传、main、release、OVH 与生产继续保持后续门禁，下一步仅可在新的独立 run 进入 IDS-STAGE104-P1-GATE。 |
| v0.1-stage103-p4-local | 2026-08-25 | 完成 Stage103 模型输出权限门禁 P4 纯内存交付证据：从 P3 的 5 条、每条 34 字段、共 170 个固定 reference-only 场景检查点派生回答样例、负向测试结果、prompt/version 记录、可复现日志和模型输出权限边界各 5 条，以及 prompt 回滚和模型配置回退说明 2 条；字段形状为 17/12/14/17/12/12，共 384 个交付字段检查点、16 类失败关闭与 4 条中文反馈。已通过 P4 与显式前序聚焦 65/65、Stage088--Stage103 精确白箱链 763/763、Stage005 治理回归 178/178 与直接校验 valid=true、Batch041-050 与 Batch051-060 本地审阅。运行计数均为 0、运行标志均为 false；业务线白箱人工复核、正式全局上传、main、release、OVH 与生产继续保持后续门禁。 |
| v0.1-stage103-p3-local | 2026-08-25 | 完成 Stage103 模型输出权限门禁 P3 纯内存专项异常场景验证：严格重放 P2 的 5 条、每条 26 字段、4 组、每条 46 字段、共 230 个前序控制检查点，形成 5 条、每条 34 字段、共 170 个场景检查点、5 个控制视图、5 条业务线白箱处理记录、28 类失败关闭与 4 条中文反馈。文档 evidence 与文档内潜在指令保持不可信、不可执行参考，IDS 规则保持优先；evidence_gap 保持独立来源类型，不能伪装为内部经验；高风险工程建议、合同承诺和生产写回保持业务线白箱人工确认，最终结论保持未发布。已通过 P3 与显式前序聚焦 57/57、Stage088--Stage103 精确白箱链 755/755、Stage005 治理回归 178/178 与直接校验 valid=true、Batch041-050 与 Batch051-060 本地审阅、7 个中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；用户授权的隔离恢复检查点已同步，正式全局上传与推送权限保持锁定。 |
| v0.1-stage103-p2-local | 2026-08-25 | 完成 Stage103 模型输出权限门禁 P2 纯内存受控最小切片：固定 5 条、每条 26 字段的非业务 reference-only 控制请求，记录 query、index_version、prompt_version、model_version 与 selected evidence 的未来可复现形状，形成 4 组、每条 46 字段、共 230 个控制检查点、33 类失败关闭与 4 条中文反馈。文档 evidence 与文档内潜在指令保持不可信、不可执行参考，IDS 规则、输出权限与业务线白箱人工确认保持优先；external_augmentation_opinion 保留外部公开参考和模型推理的底层来源类型，evidence_gap 保持独立来源类型。已通过 P2/前序聚焦 49/49、Stage088--Stage103 精确白箱链 747/747、Stage005 治理回归 178/178 与直接校验 valid=true、Batch041-050 与 Batch051-060 本地审阅、7 个中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；Stage103 P3 保持未启动，正式全局上传与推送权限保持锁定。 |
| v0.1-stage103-p1-local | 2026-08-25 | 完成 Stage103 模型输出权限门禁 P1 纯内存静态合同：固定 13 个未来控制引用、5 个未来回答结构区段、4 类底层来源类型、5 类输出权限、24 类失败关闭和 4 条中文反馈。文档 evidence 与文档内潜在指令保持不可信、不可执行参考，IDS 规则保持优先级；evidence_gap 保持独立来源类型，外部增强保持外部增强身份；高风险工程建议、合同承诺和生产写回保持业务线白箱人工确认，最终结论保持未发布。已通过 P1 聚焦 8/8、P1 合同与 Stage102 P1--Review 前序用例 42/42、Stage088--Stage103 精确白箱链 738/738、Stage005 治理回归 178/178 与直接校验 valid=true、Batch041-050 与 Batch051-060 本地审阅、7 个中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；Stage103 P2 保持未启动，正式全局上传与推送权限保持锁定。 |
| v0.1-stage102-review-local | 2026-08-25 | 完成 Stage102 文档内提示注入防护 P1--P4 的纯内存机械复审：固定复核 P1 的 17/7/4/5/25/4、P2 的 7×28/4/50/350、P3 的 7×34=238/5/7/27 与 P4 的 7/7/7/7/7/2、17/12/14/17/12/12、528/4/16 控制形状，保留八元控制引用、文档内提示注入拒绝、IDS 规则优先、evidence_gap、来源类型分离、三类高风险业务线白箱人工处理、最终结论未发布和 P4→P3 回退。已通过 Review 聚焦 10/10、P1--Review 聚焦 43/43、Stage088--Stage102 精确白箱链 730/730、Stage005 治理回归 178/178、Batch041-050、Batch051-060、7 个中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；正式全局上传与推送权限保持锁定，下一步仅可在新的独立 run 进入 IDS-STAGE103-P1-GATE。 |
| v0.1-stage102-p4-local | 2026-08-25 | 完成 Stage102 文档内提示注入防护 P4 的纯内存交付证据：从 P3 的 7 条、每条 34 字段、共 238 个固定 reference-only 场景检查点派生回答样例、负向测试结果、prompt/version 记录、可复现日志和模型输出权限边界各 7 条，以及 prompt 回滚和模型配置回退说明 2 条；字段形状为 17/12/14/17/12/12，共 528 个交付字段检查点、16 类失败关闭和 4 条中文反馈。已通过 P4 聚焦 8/8、P1--P4 显式前序聚焦 33/33、Stage088--Stage102 精确白箱链 720/720、Stage005 治理回归 178/178、Batch041-050、Batch051-060、7 个中文视图、文档预算、无登记阻塞与双平面检查。运行计数均为 0、运行标志均为 false；既有隔离分支 P3 恢复检查点保持，P4 保持本地恢复提交，下一步仅可在新的独立 run 进入 IDS-STAGE102-REVIEW-GATE。 |
