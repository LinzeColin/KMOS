<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/status.json、blockers.json、roadmap.json -->
<!-- 上限 120 行 -->

# 我在哪

一眼看清这个项目现在是什么状态、卡在哪、下一步该做什么。

**更新于** 2026-08-14

## 一、当前状态

|  |  |
|---|---|
| 版本 | `1.0.0` |
| 进行到哪 | `IDS-STAGE066` · `IDS-V0_1-STAGE066-P2` · `IDS-V0_1-STAGE066-P2` |
| 进度 | Stage066 P2 已完成固定纯内存 Chunk 覆盖率控制切片：4 条仅引用请求投影 4 条十七字段待人工复核控制记录，保留解析覆盖率、Chunk 覆盖率和未覆盖页的控制标签、3 类保护语义面、6 维追溯、1 条未知分母关闭与 4 条低可信人工处理标记；所有真实输入、真实覆盖率、真实未覆盖页、追溯绑定和运行时计数仍为零，下一步仅可在新的独立 run 进入 Stage066 P3，上传路径继续关闭 |
| 报告可信度 | 冻结 Stage066 任务包、P1 静态合同、Stage065 本地复审工件、Batch061-070 上传锁、P2 固定控制切片、治理路线与中文事实投影一致；本步骤未产生实际覆盖率、未覆盖页或质量结论 |
| 业务结论 | 本步骤只证明固定控制引用、覆盖率字段标签、未知分母关闭和业务线白箱人工边界已本地接线；不证明真实文档解析、真实页面集合、真实 Chunk 覆盖率、未覆盖页面、质量、OVH、生产服务或上传能力已启用 |
| 卡住的事 | 无 |

## 二、卡住的事

> 目前没有卡住的事。一旦出现只有你能拍板的阻塞，会自动列在这里并提醒你。

## 三、路线图

| 阶段 | 名称 | 过关标准 | 状态 |
|---|---|---|---|
| IDS-STAGE066 | Chunk 覆盖率指标 | P1 静态合同与 P2 固定控制切片已本地通过；下一步须在新的独立 run 进入 Stage066 Phase 3 | P2 已固定 4 条仅引用请求与 4 条十七字段待人工复核控制记录，保留解析覆盖率、Chunk 覆盖率和未覆盖页的控制标签、3 类保护语义面、6 维追溯、1 条未知分母关闭和 4 条低可信人工处理；真实文档、页面、chunk、hash、覆盖率、未覆盖页、质量、索引、OVH 与生产仍禁用 |
| IDS-STAGE065 | 工程语义资产分类 | P1--P4 合同、P3/P4 纯内存控制报告和整阶段机械复审已本地通过；下一步须独立进入 Stage066 Phase 1 | Stage065 整阶段本地复审完成，发现数为零：12/16 静态形状、7 类工程语义资产、7/7 控制请求与记录、3 类受保护语义面、6 维追溯、6 类显式人工处置、6 条 metadata-only JSONL 样例、4 条唯一控制记录、6 条低质量待人工记录、3 条人工确认、11 类失败关闭和 P4→P3 控制回退链保持一致；真实分类、chunk/hash、重复检测或去重、真实覆盖率、质量、索引、OVH 与生产仍禁用 |
| IDS-STAGE064 | Chunk 身份与版本 | P1--P4 合同、P3/P4 纯内存控制报告和整阶段机械复审已本地通过；下一步须独立进入 Stage065 Phase 1 | Stage064 整阶段本地复审完成，发现数为零：10/14 静态形状、3/3 控制请求与记录、3 类受保护工程语义面、6 维追溯、6 类显式人工处置、6 条 metadata-only JSONL 样例、6 条低质量待人工记录、3 条中文确认和 P4→P3 控制回退链保持一致；真实 chunk、ID、Hash、版本、分类、覆盖率、质量、索引、OVH 与生产仍禁用 |
| `IDS-STAGE040` | 反压策略 | 十阶段批次已合并 | 完成 |
| `IDS-STAGE041` | 锁注册与竞态控制 | 四个步骤与整阶段复审本地通过 | 本地复审完成 |
| `IDS-STAGE042` | 自动运行、暂停、恢复与关闭 | 四个步骤与五项复审修复本地通过 | 本地复审完成 |
| `IDS-STAGE043` | Worker 崩溃恢复 | 四个步骤与六项复审修复本地通过 | 本地复审完成 |
| `IDS-STAGE044` | 半成品输出清理 | 四个步骤与六项复审修复本地通过；下一步须独立进入 Stage045 | 本地复审完成 |
| `IDS-STAGE045` | 文件类型检测 | 四个步骤与七项整阶段复审发现本地通过；下一步须独立进入 Stage046 | 本地复审完成 |
| `IDS-STAGE046` | 解析器路由合同 | 四个步骤与六项整阶段复审发现本地通过；下一步须独立进入 Stage047 | 本地复审完成，解析器与回退仍禁用 |
| `IDS-STAGE047` | 解析器输出合同 | 四个步骤与六项整阶段复审发现本地通过；下一步须独立进入 Stage048 | 本地复审完成；真实解析器、业务输出、回退、质量门与持久化仍禁用 |
| `IDS-STAGE048` | 解析器失败降级链 | 四个步骤与整阶段本地复审已通过；下一步须独立进入 Stage049 Phase 1 | 本地复审完成；真实文件路由、真实解析器、真实回退、人工复核队列、质量门、持久化、OVH 与生产仍禁用 |
| `IDS-STAGE049` | 差异化解析器评估 | 四个步骤与整阶段本地复审已通过；下一步须独立进入 Stage050 Phase 1 | 本地复审完成；真实文件路由、解析正文比较、真实 parser、真实 fallback、质量门、持久化、OVH 与生产仍禁用 |
| `IDS-STAGE050` | 解析阶段提示注入标记 | Stage041-050 十个本地复审已通过独立批次复审；Stage051 P1/P2/P3 已独立完成 | 批次复审完成且全局上传锁保持关闭；真实文件检测、真实路由、真实 parser、OCR、运行时标记、质量门、持久化、OVH 与生产仍禁用 |
| IDS-STAGE051 | OCR 队列基线 | P1--P4 与整阶段本地白箱复审已通过；Stage052 整阶段复审已独立完成 | P1--P4 合同、四页控制切片、五类显式质量处置、5 个 metadata-only 交付样例、中文确认、缓存边界和回滚链已本地复审；OCR 引擎、持久队列、缓存、复核、OVH 和生产运行仍禁用，上传锁保持关闭 |
| IDS-STAGE052 | 中英文 OCR 合同 | P1--P4 与整阶段本地白箱复审已通过；Stage053 整阶段复审已独立完成 | 已复审双语 reference-only 输入与边界、四页纯内存控制队列、五类质量处置、metadata-only 交付、中文确认、缓存边界和回滚链；真实资料、OCR 引擎、持久队列/缓存/复核、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE053 | 按页 OCR 输出 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage054 Phase 1 | 已复审十一字段按页结构、五类显式质量处置、五个 metadata-only 样例、控制置信度汇总、显式失败、未排队复核路由、中文确认、缓存边界和回滚链；真实资料、OCR 引擎、实际输出/图片引用/失败记录、持久缓存/审计/复核、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE054 | 低置信度复核路由 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage055 Phase 1 | 已复审九字段复核输入、十字段候选请求、五类显式质量处置、五个 metadata-only 样例、控制置信度汇总、显式失败、三条候选复核路由、中文确认、缓存边界和回滚链；真实资料、OCR、人工复核任务/结果、持久队列/缓存/审计、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE055 | OCR 回归语料 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage056 Phase 1 | 已复审十字段引用输入、十一字段未来逐页输出、五类固定非业务 control、metadata-only 交付、HIGH=1/MEDIUM=2/LOW=1/UNKNOWN=1 控制置信度汇总、显式失败、三条候选复核路由、中文确认、缓存边界和回滚链；真实样本、真实 OCR、识别准确率、人工复核、真实缓存容量/清理、持久队列/缓存/审计、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE056 | OCR 缓存保留策略 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage057 Phase 1 | 已复审 11 字段缓存引用输入、10 字段策略候选、四条固定非业务 control、五类显式质量处置、5 个 metadata-only 交付样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1 控制置信度汇总、显式失败、三条候选复核路由、中文确认、零物理缓存和回滚链；真实资料、OCR、真实缓存容量/清理、人工复核、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE057 | XLSX/CSV 接入合同 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage058 Phase 1 | 已复审 12/19/7/5/6 静态形状、两条固定非业务控制记录、六类显式质量处置、6 个 metadata-only 交付样例、中文人工处理和重解析/事实回滚链；真实 XLSX/CSV、质量验证、数值统计、事实库、数据库、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE058 | 表格 Schema 推断 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage059 Phase 1 | 已复审十字段引用输入、十八字段 Schema profile、九类字段候选、数值/RAG 权威边界、两条固定非业务 control、六类显式处置、6 个 metadata-only 交付样例、中文人工处理和重解析/事实回滚链；真实 XLSX/CSV、真实质量验证、真实 schema、事实库、数据库、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE059 | 事实抽取基线 | P1--P4 与整阶段本地白箱复审已通过；下一步须在新的独立 run 进入 Stage060 Phase 1 | 已复审十二字段引用输入、二十五字段 future typed fact、三类事实、七类 typed 语义、两条固定非业务 control、三条控制候选、六类显式质量处置、6 个 metadata-only 交付样例、中文人工处理和重解析/事实回滚链；真实 XLSX/CSV、真实 production/quality/inspection facts、真实字段推断、真实异常验证、来源/证据绑定、数值统计、RAG、数据库、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE060 | 表格到 RAG 摘要 | Stage051--060 独立批次复审已通过；Stage061 P1 已独立完成 | 已完成十个既有整阶段证据的独立批次复审、051→060 接口责任链、单一权威、可恢复范围和全局上传锁；真实 OCR、真实 XLSX/CSV、真实字段推断、真实事实、真实摘要正文、数值统计、数据库、OVH 和生产仍禁用，上传锁保持关闭 |
| IDS-STAGE061 | 结构化数据质量测试 | P1--P4 静态合同、P3/P4 受控内存报告、六类显式人工处置、metadata-only 交付、中文确认和 P4→P3 重解析/事实回滚说明已完成本地白箱复审；下一步须独立进入 Stage062 Phase 1 | 整阶段本地复审完成，发现数为零；未读取或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实字段完整性、单位一致性、日期合法性、主键重复、异常值、数值统计、数据库、OVH 或生产运行，上传锁保持关闭 |
| IDS-STAGE062 | 表格证据绑定 | P1--P4 静态合同、P3/P4 control 报告、metadata-only 交付、中文确认和 P4→P3 重解析/事实回滚说明已完成整阶段机械复审；下一步须在新的独立 run 进入 Stage063 Phase 1 | 整阶段本地复审完成，发现数为零：19/17 绑定形状、2/2 control 请求与候选、六类显式人工处置、6 个 metadata-only 交付样例、字段引用、质量结果、人工建议和中文确认保持一致。真实表格、真实来源位置验证、真实证据绑定、数值统计、数据库、OVH 与生产仍禁用，上传锁保持关闭 |
| IDS-STAGE063 | 章节感知切块 | P1--P4 静态合同、P3/P4 纯内存控制报告、metadata-only 交付、中文确认和 P4→P3 控制回退链已完成整阶段机械复审；下一步须在新的独立 run 进入 Stage064 Phase 1 | 整阶段本地复审完成，发现数为零：8/14 静态形状、3/3 control 请求与候选、六类显式人工处置、6 条 metadata-only JSONL 样例、控制覆盖率、6 条低质量待人工记录、控制回归结果和中文确认保持一致。真实章节检测、真实切块、身份/版本、重复检测或去重、分类、覆盖率、质量、索引、OVH 与生产仍禁用，上传锁保持关闭 |

## 四、这套文档怎么读

想了解什么，就翻对应的一份。七份全部由机器平面自动生成，你只需复审：

| 想知道 | 翻哪份 |
|---|---|
| 这东西为谁做、要解决什么、不碰什么 | `01_产品需求.md` |
| 有哪些功能、数据怎么流、参数为什么这么设 | `02_系统架构.md` |
| 每个数字到底怎么算、外部数据得长什么样 | `03_口径字典.md` |
| 业务上一步步怎么走 | `04_操作流程.md` |
| 这一轮在做什么、怎么算做完、做到哪了 | `05_执行与验收.md` |
| 怎么跑起来、参数改哪、报错了怎么办 | `06_运维手册.md` |
