<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/glossary.json -->
<!-- 全项目唯一裁定"一个数字是什么"的地方。有争议以本文件为准。 -->
<!-- 中文门：正文出现的英文术语必须在第五节术语表登记，否则渲染 FAIL。 -->

# 口径字典

## 一、关键数字口径

| 项 | 裁定 | 状态 |
|---|---|---|
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |

## 二、外部数据形态

| 来源 | 必须长什么样 | 状态 |
|---|---|---|
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |

## 三、恒定为真的规则

| 规则 | 说明 |
|---|---|
| Stage075 P4 不执行外部 API、模型 Token、审计写入、OVH 或生产运行 | P4 只派生五条固定 metadata-only 交付样例、八键进程内查询说明和回到 P3 的控制回退；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，Stage075 Review 必须由新的独立 run 处理。 |
| Stage075 P3 不执行外部 API、模型 Token、审计写入、OVH 或生产运行 | P3 只重放 P2 的五条固定、非业务、reference-only 控制投影，验证 denied 阻断外发、summary_only 摘要引用、document 收紧、full_text_allowed 文本块引用、预算暂停、十九字段审计前置与 owner 四字段前置；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P4 必须由新的独立 run 处理。 |
| Stage072 P2 不执行真实模型版本、队列、缓存、外部 API、OVH 或生产运行 | P2 只在五条固定、非业务、reference-only 控制记录上投影默认 denied、data source/document→chunk 自动继承、队列、缓存、失败重试、六字段模型版本、零值成本和十八字段审计；没有读取真实资料、生成摘要或文本载荷、创建或运行持久队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P3 必须由新的独立 run 处理。 |
| Stage071 Review 不执行成本估算、审计查询、队列、外部 API、OVH 或生产运行 | Review 只读机械复审 P1--P4 合同与 P2/P3/P4 纯内存控制报告，核验默认 denied、策略继承、三类预算暂停、成本治理/队列/缓存/重试闭合、审计前置、白箱人工处理、metadata-only 交付和 P4→P3 控制回退链；所有真实资料访问、成本或预算计算、外发、队列、缓存、重试、审计、索引、数据库、Agent、模型 Token、OVH、生产、上传和推送保持关闭，Stage072 必须由新的独立 run 处理。 |
| Stage071 P4 不执行成本估算、审计查询、队列、外部 API、OVH 或生产运行 | P4 只从七条固定、非业务、reference-only P3 控制场景和 P2 控制投影派生策略、审计、零成本、失败、未外发、查询、回滚和中文反馈；没有读取真实资料、计算成本或预算、创建或运行持久队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、查询真实审计、执行真实回滚、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，Stage071 Review 必须由新的独立 run 处理。 |
| Stage071 P3 不执行成本估算、预算查找、队列、外部 API、OVH 或生产运行 | P3 只重放七条固定、非业务、reference-only P2 控制记录，验证 denied、summary_only、document 收紧、full_text_allowed、三类预算暂停和审计前置；没有读取真实资料、计算成本或预算、创建或运行持久队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P4 必须由新的独立 run 处理。 |
| Stage071 P2 不执行成本估算、预算查找、队列、外部 API、OVH 或生产运行 | P2 只在七条固定、非业务、reference-only 控制记录上投影默认 denied、data source/document→chunk 自动继承、三重预算暂停、未来队列/缓存/失败重试、成本/模型版本和审计字段；没有读取真实资料、计算成本或预算、创建或运行持久队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P3 必须由新的独立 run 处理。 |
| Stage071 P1 不执行成本估算、预算查找、队列、外部 API、OVH 或生产运行 | P1 只固定本批次、自然月和单任务三重预算的静态失败关闭、策略继承、模型版本和审计字段；没有读取真实资料、写入金额或 Token、计算成本或预算、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P2 必须由新的独立 run 处理。 |
| Stage070 Review 不执行队列、缓存、失败重试、外部 API、OVH 或生产运行 | Review 只复审 P1--P4 的冻结合同和 P2/P3/P4 纯内存控制报告；Stage071 尚未开始，所有真实资料访问、摘要或文本载荷创建、外发、队列、缓存、失败重试、审计、成本、索引、数据库、Agent、模型 Token、OVH、生产、上传和推送保持关闭。 |
| Stage070 P4 交付证据不执行队列、缓存、失败重试、外部 API、OVH 或生产运行 | P4 只从五条固定 P3 控制场景派生五条策略、审计、成本、失败和未外发样例，以及查询和回滚说明；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、查询真实审计、执行真实回滚、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，Stage070 Review 必须由新的独立 run 处理。 |
| Stage070 P3 专项场景不执行队列、缓存、失败重试、外部 API、OVH 或生产运行 | P3 只在内存中重放五条固定 P2 控制记录，验证 denied、两个 summary_only、full_text_allowed、预算暂停、五条审计投影、九十次审计字段检查和三个未来候选；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P4 必须由新的独立 run 处理。 |
| Stage070 P2 不执行队列、缓存、失败重试、外部 API、OVH 或生产运行 | P2 只固定五条非业务控制输入并机械投影策略继承、队列、缓存、失败重试、成本/模型版本和审计字段；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P3 必须由新的独立 run 处理。 |
| Stage070 P1 不执行队列、缓存、失败重试、外部 API、OVH 或生产运行 | P1 只固定未来 schema、三档策略流程、失败关闭和回滚边界；没有读取真实资料、生成摘要或文本载荷、创建或运行队列/缓存/重试/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P2 必须由新的独立 run 处理。 |
| Stage069 Review 不执行外部 API、OVH 或生产运行 | Review 只复审 P1--P4 的冻结合同和 P2/P3/P4 纯内存控制报告；Stage070 尚未开始，所有真实资料访问、摘要或文本载荷创建、外发、队列、缓存、审计、成本、索引、数据库、Agent、模型 Token、OVH、生产、上传和推送保持关闭。 |
| Stage069 P4 交付证据不执行外发、审计查询或回滚运行时 | P4 只投影五条策略样例、审计日志投影样例、零成本估算、失败处理、未外发原因、查询与回滚说明；没有读取真实资料、生成摘要或文本载荷、创建队列/缓存/审计、选择 provider/模型、调用外部 API、消耗 Token、查询真实审计、执行真实回滚、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，Review 必须由新的独立 run 处理。 |
| Stage069 P3 外部 API 策略场景不执行运行时 | P3 只在内存中验证 denied 无载荷、summary_only 摘要引用、full_text_allowed 文本块引用、预算暂停和先审计不变量；没有读取真实资料、生成摘要或文本载荷、创建队列/缓存/审计、选择 provider/模型、调用外部 API、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P4 必须由新的独立 run 处理。 |
| Stage069 P1 不执行外部 API 或生产运行 | P1 只固定默认 denied、source/document→chunk 自动继承、未来队列/成本/模型版本/审计和失败关闭；没有读取真实资料、生成摘要、外发文本、创建队列/审计、选择 provider/模型、消耗 Token、写入索引/数据库、运行 Agent、部署 OVH、启用生产、上传或推送，P2 必须由新的独立 run 处理。 |
| Stage068 Review 不启动外部 API、OVH 或生产运行 | Review 只复审 P1--P4 的冻结合同和纯内存控制报告；Stage069 尚未开始，所有真实资料访问、质量测量/回归/降级、低可信证据、重复检测/去重、索引、数据库、Agent、模型 Token、OVH、生产、上传和推送保持关闭。 |
| Stage068 P3 专项场景不替代真实质量、降级、去重或来源验证 | P3 的长文档、跨页表格、施工步骤、参数表、引用页码和重复 chunk 只表示固定控制类别；六类均须业务线白箱人工处理，低质量不等于自动完全失败，控制模块不检测真实重复项、不创建低可信证据、不形成真实质量降级、去重、质量或来源反查结论，并仅作为 P4 metadata-only 交付的固定前序。 |
| Stage068 P2 不执行真实质量降级或生产运行 | P2 只在内存中投影四条固定控制记录及人工复核分流，保留受保护语义面、追溯和重复写入边界；没有读取真实资料，未执行质量测量、质量回归、质量降级、低可信证据创建、重复检测/去重、索引、数据库、Agent、模型 Token、OVH、生产、上传或推送，P3 必须由新的独立 run 处理。 |
| Stage067 Review 不启动质量降级或生产运行 | Review 只复审 P1--P4 的冻结合同和纯内存控制报告；Stage068 尚未开始，所有真实资料访问、质量测量/回归/降级、重复检测/去重、索引、数据库、Agent、模型 Token、OVH、生产、上传和推送保持关闭。 |
| Stage067 P4 交付证据不替代真实质量、去重或来源验证 | P4 的六条 JSONL 样例、控制交付覆盖报告、低质量待人工清单、控制回归结果、策略边界和 P4→P3 回退说明只检验固定控制形状；长文档、跨页表格、工程步骤、参数表、引用页码和重复 chunk 仍须业务线白箱人工复核，不能形成真实质量、去重或业务结论。 |
| Stage067 P3 专项场景不替代真实质量、去重或来源验证 | P3 的长文档、跨页表格、施工步骤、参数表、引用页码和重复 chunk 只表示控制类别；六类均须业务线白箱人工处理，控制模块不检测真实重复项、不形成真实去重、质量或来源反查结论，并仅作为 P4 metadata-only 交付的固定前序。 |
| Stage067 P1 不执行切块质量回归 | P1 只固定未来质量回归、保护语义面、重复 embedding/index 写入边界和人工处理字段；工程步骤、验收条款、参数表、跨页参数表、重复 chunk 或来源追溯无法确认时必须关闭并交由业务线白箱复核，P2 必须由新的独立 run 处理。 |
| Stage066 P4 交付证据不等于真实覆盖率或质量回归 | P4 的六条 JSONL 样例、控制覆盖率、低质量待人工清单和回归结果只检验固定控制形状；长文档、跨页表格、工程步骤、参数表、引用页码和重复 chunk 仍须业务线白箱人工复核，不能形成真实覆盖率、质量或业务结论。 |
| Stage066 P3 专项场景不替代真实质量、去重或来源验证 | P3 的长文档、跨页表格、施工步骤、参数表、引用页码和重复 chunk 只表示控制类别；六类均须业务线白箱人工处理，控制模块不检测真实重复项、不形成真实去重、覆盖率、质量或来源反查结论。 |
| Stage066 P2 控制标签不等于真实覆盖率 | P2 只验证四条固定控制请求的十七字段接线、未知分母关闭、保护语义面和人工复核边界；解析覆盖率、Chunk 覆盖率和未覆盖页均保持 :control: 标签，不能形成真实质量、索引、决策或生产状态，P3 必须由新的独立 run 处理。 |
| Stage066 P1 不执行覆盖率计算 | P1 只固定未来解析覆盖率、Chunk 覆盖率和未覆盖页的字段与公式标签；分母未知、页面集合或来源追溯无法确认时必须保持关闭并交由业务线白箱处理，P2 必须由新的独立 run 处理。 |
| Stage065 复审不启动 Stage066 | Stage065 Review 只读核验 P1--P4 控制工件并将下一门禁指向 Stage066 P1；它不计算真实覆盖率、不创建运行时或生产状态，Stage066 必须由新的独立 run 处理。 |
| P4 交付证据不等于真实覆盖率或质量回归 | Stage065 P4 的六条 JSONL 样例、控制覆盖率、低质量待人工清单和回归结果只检验固定控制形状；长文档、跨页参数表、工程步骤、参数表、引用页码和重复 chunk 仍须业务线白箱人工复核，不能形成真实分类或质量结论。 |
| 专项场景不替代真实质量或去重验证 | Stage065 P3 的长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk 只表示控制类别；六类均须业务线白箱人工处理，控制模块不检测真实重复项、不形成真实去重或来源反查结论。 |
| 低可信控制标记不是质量结论 | Stage065 P2 的七条固定控制记录全部要求业务线白箱人工复核；该标记不检测真实低质量，不计算覆盖率或质量回归，也不替代来源文档。 |
| 保护语义面与来源权威 | 工程步骤、验收条款和参数表不得被任意切断或由分类规则覆盖；来源文档和业务线人工复核始终优先于分类标签、模型输出或本合同。 |

## 四、证据等级定义

| 等级 | 含义 |
|---|---|
| `已提取` | 从代码或配置提取并核对 |
| `已声明` | 只有文档说法，未核对 |

## 五、术语对照

正文出现的英文术语在此登记。

| 英文 | 中文 | 说明 |
|---|---|---|
| Owner | 负责人 | 项目/产品的决策与复审责任人 |
| Stage | 阶段 | 路线图一级单元 |
| Phase | 步骤 | 阶段下的二级单元 |
| Task | 任务 | 步骤下的三级单元 |
| Roadmap | 路线图 | 阶段→步骤→任务的完整计划 |
| evidence_id | 证据标识 | Stage089 P1 中仅作为未来证据账本的不可持久化控制引用；当前不读取、生成、绑定或写入真实证据。 |
| document_id | 文档标识 | Stage089 P1 中仅作为未来证据与文档关联的控制引用；当前不定位、读取或登记真实文档。 |
| chunk_id | 文本切块标识 | Stage089 P1 中仅作为未来证据与文本切块关联的控制引用；当前不创建、读取或持久化真实切块。 |
| fact_id | 事实标识 | Stage089 P1 中仅作为未来证据与事实关联的控制引用；当前不形成、确认或写入业务事实。 |
| report_id | 报告标识 | Stage089 P1 中仅作为未来证据与报告关联的控制引用；当前不创建、更新或采纳真实报告。 |
| source_type | 来源类型 | Stage089 P1 中仅定义未来来源分类的控制字段；当前不读取来源正文、不判断真实来源类型。 |
| evidence_grade | 证据可信等级 | Stage089 P1 中仅定义 A/B/C/D/E 的未来等级控制引用；当前不评价、升降级或采纳真实证据。 |
| evidence_gap | 证据缺口 | Stage089 P1 中仅标示关键结论缺少证据时的未来控制引用；当前不写入真实缺口记录或业务结论。 |
| risk_score | 风险评分引用 | Stage089 P1 中仅保留未来风险计算的控制字段；当前不计算真实风险分数。 |
| poisoning_defense | 知识库投毒防护 | Stage089 P1 中仅定义未来来源溯源、冲突、异常、隔离和人工复核的控制引用；当前不检测或处置真实资料。 |
| critical_conclusion | 关键结论 | Stage089 P1 中仅定义未来结论必须关联证据标识或证据缺口的控制边界；当前不产生业务结论。 |
| document | 文档 | 仅作为未来证据关联对象的术语；当前不读取、打开或解析真实文档。 |
| chunk | 文本切块 | 仅作为未来证据关联对象的术语；当前不生成、查询或持久化真实文本切块。 |
| fact | 事实 | 仅作为未来证据关联对象的术语；当前不形成、确认或写入业务事实。 |
| answer | 回答 | 仅作为未来证据关联对象的术语；当前不生成、评价或采纳业务回答。 |
| relation | 关联 | 仅描述未来证据对象之间的控制关联形状；当前不建立真实关系记录。 |
| type | 类型 | 仅描述未来控制字段的类别；当前不推断或写入真实业务类型。 |
| grade | 可信等级 | 仅描述未来 A/B/C/D/E 控制等级；当前不对真实证据升降级。 |
| gap | 缺口 | 仅描述未来证据不足的控制状态；当前不形成实际缺口结论。 |
| risk | 风险 | 仅描述未来风险控制引用；当前不计算或判定真实风险。 |
| capture | 证据捕获 | Stage089 P2 中仅描述未来检索证据捕获的控制引用；当前不读取、提取、写入或持久化真实证据。 |
| degradation | 证据降级 | Stage089 P2 中仅描述低可信、冲突、过期、撤回或疑似投毒证据的控制性降级或隔离候选；当前不改变真实证据等级或业务结论。 |
| revocation | 撤回 | 仅描述未来证据撤回及受影响对象的控制引用；当前不撤回真实证据或报告。 |
| poisoning | 投毒 | 仅描述未来知识库投毒风险控制；当前不检测、隔离或处置真实资料。 |
| defense | 防护 | 仅描述未来投毒防护控制状态；当前不执行防护运行时动作。 |
| critical | 关键 | 仅用于标示未来关键结论控制边界；当前不形成业务结论。 |
| conclusion | 结论 | 仅描述未来需经证据链和业务线白箱复核的结论对象；当前不产生业务结论。 |
| chunks | 文本切块集合 | Stage087 P1 中 candidate chunks 与 selected chunks 仅表示未来检索链上的不透明控制引用集合；当前不读取、生成、查询、排序、持久化或解释真实切块内容。 |
| explanation | 评分解释 | Stage087 P1 中仅作为 score explanation 的未来静态控制字段；当前不计算、生成或解释真实排序评分。 |
| kind | 类别 | Stage087 P1 中仅作为 query 或控制记录的未来类别字段；当前不识别、推断或写入真实业务类别。 |
| future-only | 仅未来接入 | Stage085 P2 用于标示 PostgreSQL FTS／BM25、pgvector、元数据过滤、混合排序和检索轨迹的未来实现路由；本阶段不执行、不连接、不写入，也不表示已有运行时能力。 |
| CONTROL_BUILD_INCOMPLETE_REFERENCE_ONLY | 候选构建未完成仅引用控制状态 | Stage080 P3 只用该控制标签表达既有 P2 候选构建未完成时的失败关闭；候选不得替换活动版本，旧活动版本继续服务。它不表示真实索引构建失败、物理索引或运行时状态。 |
| CONTROL_CANDIDATE_BUILD_NOT_COMPLETE | 候选构建未完成控制关闭状态 | Stage078 P3 只用该控制标签表达候选构建未完成时的失败关闭；候选不得替换活动版本，旧活动版本继续服务。它不表示真实索引构建失败、物理索引或运行时状态。 |
| CONTROL_BACKGROUND_BUILD_NOT_STARTED | 后台索引构建未启动的控制关闭状态 | Stage077 P3 只从 P2 固定、非业务、reference-only 控制投影观察该未完成状态；它阻断未来切换并保持旧活动版本继续服务，不表示真实后台构建失败、实际索引或运行时状态。 |
| Stage075 Phase4 external API coverage audit delivery evidence | Stage075 P4 外部 API 覆盖授权审计交付证据 | 只从 P3 的五条固定、非业务、reference-only 控制场景和 P2 控制投影派生策略、审计、零值成本、失败、未外发、进程内查询、回滚与中文反馈；不读取真实正文、不创建外发载荷、持久审计或业务事实。 |
| Stage075 Phase3 external API coverage audit controlled scenarios | Stage075 P3 外部 API 覆盖授权审计专项场景 | 只重放 P2 的五条固定、非业务、:control: 投影，验证 denied、summary_only、document 收紧、full_text_allowed、预算暂停和审计前置；不读取真实正文、不创建外发载荷、持久审计或业务事实。 |
| actor | 操作主体 | 仅作为 owner 强制允许外发前的静态审计字段名，记录应由业务线白箱人工复核；P1 不创建实际审计记录或策略变更。 |
| old_value | 原值 | 仅作为 owner 强制允许外发前的静态审计字段名，表示拟变更前的受控值；P1 不读取或写入真实策略。 |
| new_value | 新值 | 仅作为 owner 强制允许外发前的静态审计字段名，表示拟变更后的候选受控值；P1 不应用任何实际策略变更。 |
| Stage072 Phase2 embedding model version control slice | Stage072 P2 Embedding 模型版本控制切片 | 只在五条固定、非业务、reference-only 控制引用上纯内存投影策略继承、队列、缓存、失败重试、模型版本、零值成本和审计字段；不创建运行时记录，也不能成为第二权威事实源。 |
| Embedding model version | Embedding 模型版本 | Stage072 P1/P2 的模型版本合同能力域；P1 只定义未来字段，P2 只投影固定控制标签；不选择真实模型、不记录实际版本，也不形成业务事实。 |
| provider_ref | 供应方引用字段 | 未来模型版本记录的受控引用字段；Stage072 P1 不读取凭据、不选择供应方，也不写入真实引用。 |
| model_ref | 模型引用字段 | 未来模型版本记录的受控引用字段；Stage072 P1 不选择模型，也不写入真实引用。 |
| dimension | 向量维度字段 | 未来模型版本记录的 schema 字段；Stage072 P1 不写入真实维度值。 |
| created_at | 创建时间字段 | 未来模型版本记录的 schema 字段；Stage072 P1 不写入真实时间。 |
| sent_to_external_api | 外部 API 外发状态字段 | 未来模型版本审计扩展字段；Stage072 P1 不创建外发、不记录真实外发状态。 |
| model | 模型 | 未来外部处理的模型引用字段；Stage071 P2 只保留固定 :control: 标签，不选择模型、不初始化客户端、不调用模型且不消耗 Token。 |
| model_version | 模型版本 | 未来模型版本的引用字段；Stage071 P2 只保留固定 :control: 标签，不记录实际版本或形成业务事实。 |
| policy | 策略 | data source/document→chunk 外部 API 授权继承的受控规则；默认 denied，未知、无效或放宽均保持关闭，不能替代业务线白箱人工复核。 |
| reason | 原因 | 控制投影中策略继承、预算关闭或失败处置的说明标签；只解释固定控制状态，不表示真实资料、预算、成本或业务决定。 |
| policy_inheritance_reason | 策略继承原因字段 | 记录 data source/document 到 chunk 的固定控制继承理由；Stage072 P2 只投影非业务引用，不解析来源正文、不放宽策略，也不形成业务结论。 |
| token_count | Token 计数字段 | 审计控制投影中的零值计数字段；Stage072 P2 固定为 0，不代表真实模型调用或 Token 消耗。 |
| Stage070 local mechanical review | Stage070 本地机械复审 | 只读复审 Stage070 P1--P4 合同与 P2/P3/P4 纯内存控制报告的治理步骤；通过只表示固定策略继承、队列/缓存/重试闭合、审计先决条件、白箱人工处理、metadata-only 交付与回退链一致，不替代来源文档或业务事实。 |
| Stage070 Phase4 embedding queue/cache metadata-only delivery | Stage070 P4 Embedding 队列与缓存 metadata-only 交付 | 只从 P3 五条固定、非业务、reference-only 控制场景派生策略、审计、成本、失败、未外发、查询和回到 P3 的回滚说明的内存报告，不创建任何运行时记录。 |
| Stage070 Phase3 embedding queue/cache controlled scenarios | Stage070 P3 Embedding 队列与缓存专项场景 | 只重放 P2 五条固定、非业务、reference-only 控制记录并在内存中验证 denied、summary_only、full_text_allowed、预算暂停和审计前置关系的场景报告，不创建任何运行时记录。 |
| Stage070 Phase2 embedding queue/cache control slice | Stage070 P2 Embedding 队列与缓存控制切片 | 只在固定、非业务、reference-only 控制引用上投影策略继承、未来队列、缓存、失败重试、成本、模型版本和审计字段的纯内存切片，不创建任何运行时记录。 |
| Stage070 Phase1 embedding queue/cache contract | Stage070 P1 Embedding 队列与缓存合同 | 只定义未来队列、缓存、失败重试、成本、模型版本和审计字段及边界的静态合同，不创建任何运行时记录。 |
| Embedding queue | Embedding 队列 | 未来用于受控外部处理的队列字段集合；本 P1 未创建、未执行且不能代表真实外发。 |
| cache | 缓存 | 未来受策略、授权、预算和审计约束的引用式缓存边界；本 P1 不读写缓存。 |
| failed retry | 失败重试 | 未来失败处理字段集合；本 P1 不启动调度器、不创建重试记录。 |
| Stage069 local mechanical review | Stage069 本地机械复审 | 只读复审 Stage069 P1--P4 合同与 P2/P3/P4 纯内存控制报告的治理步骤；通过只表示固定策略继承、审计先决条件、白箱人工处理、metadata-only 交付与回退链一致，不替代来源文档或业务事实。 |
| Stage068 local mechanical review | Stage068 本地机械复审 | 只读复审 Stage068 P1--P4 合同与 P3/P4 纯内存控制报告的治理步骤；通过只表示固定控制形状、白箱人工处理、metadata-only 交付与回退链一致，不替代来源文档或业务事实。 |
| denied | 禁止外部调用 | 外部 API 策略默认值；不允许发送摘要或文本块，不创建队列或调用记录，必须保持失败关闭并交由业务线白箱复核。 |
| summary_only | 仅允许受控摘要 | 未来策略档位；仅在逐项授权和后续阶段门均满足时才可处理授权摘要，不允许外发原文或文本块；本阶段未创建队列、未选择模型且未调用外部 API。 |
| full_text_allowed | 允许受控全文 | 未来策略档位；只有逐项授权、后续实施门和审计要求同时满足时才可能使用，不表示本阶段已经外发全文、选择模型、消耗 Token 或启用生产运行时。 |
| Stage067 local mechanical review | Stage067 本地机械复审 | 只读复审 Stage067 P1--P4 合同与 P3/P4 纯内存控制报告的治理步骤；通过只表示固定控制形状、白箱人工处理、metadata-only 交付与回退链一致，不替代来源文档或业务事实。 |
| metadata-only chunk quality regression JSONL delivery sample | 仅元数据切块质量回归 JSONL 交付样例 | Stage067 P4 从 P3 六类固定控制场景派生的内存 JSONL 行；它只保留 :control: 引用和人工复核状态，不包含真实 chunk、质量、身份、Hash、版本、路径、页码内容或业务事实。 |
| controlled chunk quality regression delivery coverage report | 受控切块质量回归交付覆盖报告 | Stage067 P4 只报告六类固定场景、四条唯一控制质量回归记录和三十六次控制引用检查是否完整；它不验证真实文档、真实页码、真实 chunk 或真实质量，不能用作质量结论。 |
| chunk quality regression regeneration and rollback control | 切块质量回归重生成及回滚控制 | Stage067 P4 的纯内存说明只允许撤回到 P3 控制场景，不读取真实资料、不重生成真实 chunk、不回退真实版本、索引或数据库状态。 |
| controlled chunk quality regression scenario | 受控切块质量回归专项场景 | Stage067 P3 只重放四条固定 :control: 切块质量回归记录，并对六类专项表面输出显式人工处置；它不读取来源、不验证真实切块质量、不检测真实重复项或来源位置，也不创建业务事实或替代来源文档。 |
| reference-only chunk quality regression contract | 仅引用切块质量回归合同 | Stage067 P1 对未来切块质量回归的静态 schema、保护语义面、重复 embedding/index 写入边界、追溯、失败关闭和回滚范围定义；它不读取来源、不检测真实质量或重复项、不创建业务事实，也不替代来源文档。 |
| chunk quality regression human handling | 切块质量回归人工处理 | 当工程步骤、验收条款、参数表、跨页表格、重复写入边界或来源追溯无法确认时必须保持关闭并交由业务线白箱复核；不能自动形成质量结论、事实、索引、决策或生产状态。 |
| metadata-only chunk coverage metrics JSONL delivery sample | 仅元数据 Chunk 覆盖率指标 JSONL 交付样例 | Stage066 P4 从 P3 六类固定控制场景派生的内存 JSONL 行；它只保留 :control: 引用和人工复核状态，不包含真实 chunk、覆盖率、身份、Hash、版本、路径、页码内容或业务事实。 |
| controlled chunk coverage metrics coverage report | 受控 Chunk 覆盖率指标覆盖率报告 | Stage066 P4 只报告六类固定场景、四条唯一控制指标记录和三十六次控制引用检查是否完整；它不计算真实文档、真实页码、真实 chunk 或真实覆盖率，不能用作质量结论。 |
| chunk coverage metrics regeneration and rollback control | Chunk 覆盖率指标重生成及回滚控制 | Stage066 P4 的纯内存说明只允许撤回到 P3 控制场景，不读取真实资料、不重生成真实 chunk、不回退真实版本、索引或数据库状态。 |
| controlled chunk coverage metrics scenario | 受控 Chunk 覆盖率指标专项场景 | Stage066 P3 只重放四条固定 :control: 覆盖率指标记录，并对六类专项表面输出显式人工处置；它不读取来源、不计算真实覆盖率、不检测真实重复项或未覆盖页、不创建业务事实，也不替代来源文档。 |
| chunk coverage metrics control slice | Chunk 覆盖率指标控制切片 | Stage066 P2 只在内存中验证四条固定、非业务、reference-only 请求的十七字段控制投影；它不读取来源、不计算真实比率、不识别真实未覆盖页、不创建业务事实，也不替代来源文档。 |
| chunk coverage metrics unknown denominator closure | Chunk 覆盖率指标未知分母关闭 | 当控制记录的页面集合、分母、语义边界或来源追溯无法确认时，覆盖率输出必须保持关闭并交由业务线白箱复核；控制状态和模型文本不能自动形成业务事实、索引、决策或生产状态。 |
| reference-only chunk coverage metrics contract | 仅引用 Chunk 覆盖率指标合同 | Stage066 P1 对未来解析覆盖率、Chunk 覆盖率和未覆盖页面的静态 schema、公式标签、保护语义面、追溯、失败关闭和回滚范围定义；它不读取来源、不计算真实比率或未覆盖页、不创建业务事实，也不替代来源文档。 |
| chunk coverage metrics human handling | Chunk 覆盖率指标人工处理 | 当页面集合、分母、语义边界、未覆盖页或来源追溯无法确认时必须保持关闭并交由业务线白箱复核；不能自动形成事实、索引、决策或生产状态。 |
| Stage065 engineering semantic asset classification review | Stage065 工程语义资产分类整阶段机械复审 | 只读复审冻结 P1--P4 合同与 P3/P4 纯内存控制报告，确认固定接口、白箱人工处置、metadata-only 交付和 P4→P3 回退链；不读取真实资料、不创建第二权威事实源，也不启动 Stage066、OVH 或生产。 |
| metadata-only engineering semantic asset classification JSONL delivery sample | 仅元数据工程语义资产分类 JSONL 交付样例 | Stage065 P4 从 P3 六类固定控制场景派生的内存 JSONL 行；它只保留 :control: 引用和人工复核状态，不包含真实 chunk、分类、身份、Hash、版本、路径、页码内容或业务事实。 |
| controlled engineering semantic asset classification coverage report | 受控工程语义资产分类覆盖率报告 | Stage065 P4 只报告六类固定场景、四条唯一控制分类记录和三十六次控制引用检查是否完整；它不计算真实文档、真实页码、真实 chunk 或真实分类覆盖率，不能用作质量结论。 |
| engineering semantic asset classification regeneration and rollback control | 工程语义资产分类重生成及回滚控制 | Stage065 P4 的纯内存说明只允许撤回到 P3 控制场景，不读取真实资料、不重生成真实 chunk、不回退真实分类、身份、版本、索引或数据库状态。 |
| controlled engineering semantic asset classification scenario | 受控工程语义资产分类专项场景 | Stage065 P3 对 P2 七条固定、非业务、reference-only 低可信控制记录的纯内存重放；长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk 只表示控制类别，始终要求业务线白箱人工复核，不能成为真实切块质量、分类或业务事实。 |
| semantic asset duplicate write control boundary | 语义资产重复写入控制边界 | Stage065 P3 只确认控制模块没有发起 embedding 或索引写入；它不检测真实重复 chunk、不验证真实身份、Hash 或版本、不形成真实去重效果结论。 |
| low-confidence engineering semantic asset classification control record | 低可信工程语义资产分类控制记录 | Stage065 P2 对固定、非业务、仅引用请求的纯内存十六字段投影；标签和 :control: 引用只验证合同接线，全部要求人工复核，不能成为真实分类、业务事实或第二权威事实源。 |
| engineering semantic asset classification control slice | 工程语义资产分类控制切片 | Stage065 P2 只在内存中验证七类固定标签、三类受保护语义面和六维控制追溯的投影形状；真实切块、hash、分类、覆盖率、质量回归和质量降级仍保持关闭并属于对应后续职责。 |
| reference-only engineering semantic asset classification contract | 仅引用工程语义资产分类合同 | Stage065 P1 对 procedure、risk、acceptance、material、equipment、case 和 bid_response 七类未来标签的静态 schema 定义；它不读取来源、不分类真实 chunk、不创建业务事实，也不替代来源文档。 |
| engineering semantic asset type catalog | 工程语义资产类型目录 | 只登记七类未来资产类型标签的受控目录；目录不含真实分类、来源内容、分类依据或业务结论，不能成为第二权威事实源。 |
| semantic asset classification human handling | 语义资产分类人工处理 | 当语义边界、来源追溯、分类依据或跨页参数表关系无法确认时必须保持关闭并交由业务线白箱复核；不能自动形成事实、索引、决策或生产状态。 |
| Stage064 chunk identity and version review | Stage064 Chunk 身份与版本整阶段复审 | 只机械复审 Stage064 P1--P4 冻结合同和纯内存控制报告，不创建真实 chunk、身份、版本或业务事实。 |
| P4-to-P3 chunk identity and version control rollback | P4 到 P3 Chunk 身份与版本控制回退 | 只允许恢复固定、非业务、reference-only P3 控制场景，不执行真实 chunk 重生成或版本回退。 |
| Stage064 whitebox human handling | Stage064 业务线白箱人工处理 | 长文档、跨页参数表、工程步骤、参数表、页码反查和重复 chunk 必须由业务线人工复核，控制模块不能替代来源文档权威。 |
| metadata-only chunk identity and version JSONL delivery sample | 仅元数据 Chunk 身份与版本 JSONL 交付样例 | Stage064 P4 从 P3 六类固定控制场景派生的内存 JSONL 行；它只保留 :control: 引用和人工复核状态，不包含真实 chunk、身份、Hash、版本、路径、页码内容或业务事实。 |
| controlled chunk identity and version coverage report | 受控 Chunk 身份与版本覆盖率报告 | Stage064 P4 只报告六类固定场景、三条唯一控制身份/版本记录和三十六次控制引用检查是否完整；它不计算真实文档、真实页码或真实 chunk 覆盖率，不能用作质量结论。 |
| chunk identity and version regeneration and rollback control | Chunk 身份与版本重生成及回滚控制 | Stage064 P4 的纯内存说明只允许撤回到 P3 控制场景，不读取真实资料、不重生成真实 chunk、不回退真实身份、版本、索引或数据库状态。 |
| controlled chunk identity and version scenario | 受控 Chunk 身份与版本专项场景 | Stage064 P3 对 P2 三条固定、非业务、reference-only 身份/版本控制记录的纯内存重放；长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk 只表示控制类别，始终要求业务线白箱人工复核，不能成为真实切块质量或业务事实。 |
| chunk identity and version duplicate write boundary | Chunk 身份与版本重复写入控制边界 | Stage064 P3 只确认控制模块没有发起 embedding 或索引写入；它不检测真实重复 chunk、不验证真实身份、Hash 或版本、不形成真实去重效果结论，必须留待授权实现和业务线人工确认。 |
| control scenario human disposition | 控制场景人工处置 | 当控制引用不足以确认真实章节边界、跨页表格关系、页码反查或重复依据时，系统必须显式关闭并交由业务线白箱处理；控制处置不等于真实质量验证。 |
| control-only chunk identity and version record | 仅控制 Chunk 身份与版本记录 | Stage064 P2 对固定、非业务、仅引用请求的纯内存十四字段投影；其中 chunk_id、chunk_hash、document_id、page、section 和 version 都是控制标签，不是实际生成、计算、绑定或持久化的业务记录。 |
| identity and version control label | 身份与版本控制标签 | 用于验证固定字段接线的 :control: 不透明标签；它不等于真实 ID、Hash、版本、页码、章节或来源追溯，也不形成业务事实或决策结论。 |
| chunk identity and version control slice | Chunk 身份与版本控制切片 | Stage064 P2 只在内存中验证三条固定控制请求、三类受保护语义面和六维控制引用的投影形状；真实语义分类、覆盖率、质量回归和质量降级仍由后续阶段负责。 |
| reference-only chunk identity and version contract | 仅引用 Chunk 身份与版本合同 | Stage064 P1 对未来 chunk_id、chunk_hash、document_id、page、section 和 version 的静态 schema 定义；它不读取来源、不生成真实 ID、Hash、版本或业务事实，也不替代来源文档。 |
| future chunk identity and version schema field | 未来 Chunk 身份与版本字段 | chunk_id、chunk_hash、document_id、page、section 和 version 仅作为未来字段标签；当前不填入实际值，不执行身份生成、Hash 计算、document 绑定或版本生成。 |
| chunk identity and version human handling | Chunk 身份与版本人工处理 | 当章节边界、来源追溯、身份引用、Hash 或版本依据无法确认时，必须保持关闭并交由业务线白箱复核；不能自动形成事实、索引、决策或生产状态。 |
| Stage063 chapter-aware chunking review | Stage063 章节感知切块整阶段机械复审 | 只机械复审冻结 Stage063 P1--P4 控制合同、纯内存报告和 P4→P3 控制回退链的固定计数、人工处置、单一权威与零运行时边界；它不读取真实文档、不创建真实 chunk、身份或版本，也不启动 Stage064、OVH、生产或上传。 |
| metadata-only chunk JSONL delivery sample | 仅元数据章节切块 JSONL 交付样例 | Stage063 P4 从 P3 六类固定控制场景派生的内存 JSONL 行；它只保留 :control: 引用和人工复核状态，不包含真实 chunk、正文、路径、页码内容或业务事实。 |
| controlled chunk coverage report | 受控章节切块覆盖率报告 | Stage063 P4 只报告六类固定场景和三十六次控制引用检查是否完整；它不计算真实文档、真实页码或真实 chunk 覆盖率，不能用作质量结论。 |
| control chunk regeneration and version rollback | 控制章节切块重生成与版本回退 | Stage063 P4 的纯内存说明只允许撤回到 P3 控制场景，不读取真实资料、不重生成真实 chunk、不回退真实版本、索引或数据库状态。 |
| controlled chapter-aware chunking scenario | 受控章节感知切块专项场景 | Stage063 P3 对 P2 三条固定、非业务、reference-only 候选的纯内存重放；长文档、跨页参数表、施工步骤、参数表、引用页码和重复写入只表示控制类别，始终要求人工复核，不能成为真实切块质量或业务事实。 |
| duplicate chunk write control boundary | 重复切块写入控制边界 | Stage063 P3 仅确认控制模块没有发起 embedding 或索引写入；它不检测真实重复 chunk、不计算身份或哈希、不形成真实去重效果结论，必须留待经授权的后续实现和业务线人工确认。 |
| reference-only chapter-aware chunk candidate | 仅引用章节感知切块候选 | Stage063 P2 对三条固定、非业务控制请求的纯内存投影；它只保留 document、page、section、parser output、表格上下文和来源片段的 :control: 引用形状，始终要求人工复核，不能成为真实 chunk、来源追溯、索引、业务事实或第二权威事实源。 |
| protected semantic surface control candidate | 受保护语义表面控制候选 | Stage063 P2 中工程步骤、验收条款和参数表各自对应一个固定的原子控制候选；控制标签不构成真实章节检测或真实语义分类，真实 chunk 身份/版本/哈希、分类、覆盖率、质量回归和质量降级仍由后续阶段负责。 |
| chapter-aware chunking | 章节感知切块 | 依据未来 document、page、section 和 parser output 引用保留章节语义边界的工程合同；Stage063 P1 只定义接口与关闭条件，不读取文档、不运行 parser 或创建真实 chunk。 |
| protected semantic surface | 保护语义面 | 工程步骤、验收条款和参数表等未来不得按固定字符数任意切断的语义单元；当前只登记边界，不执行识别、分类、切分或质量判断。 |
| reference-only chunk traceability | 仅引用切块追溯 | 未来 chunk 关联 document、page、section、parser output、表格上下文和来源片段的受控引用形状；它不包含正文、物理路径、实际页码或真实来源验证。 |
| chunk quality human handling | 切块质量人工处理 | 当章节边界、来源追溯或跨页参数表关系无法确认时必须保持关闭并交由业务线人工复核的规则；它不产生业务结论。 |
| Stage062 table evidence binding review | Stage062 表格证据绑定整阶段机械复审 | 只机械复审冻结 Stage062 P1--P4 control 合同、报告与 metadata-only 交付的固定计数、人工处理、数值关闭和 P4→P3 回滚说明；它不读取真实表格、不创建真实事实或证据绑定，也不启动 Stage063、OVH、生产或上传。 |
| metadata-only table evidence binding delivery | 仅元数据表格证据绑定交付 | Stage062 P4 从固定控制场景派生的交付样例、字段引用、质量结果、人工建议和回滚说明；它不包含真实表格内容、真实字段映射、真实事实、来源绑定或证据记录，不能成为第二权威事实源。 |
| controlled table evidence binding scenario | 受控表格证据绑定场景 | Stage062 P3 对两条固定、非业务、reference-only 未绑定候选的纯内存异常处置；空表、合并单元格、单位混乱、日期格式不一、异常值和重复行均只表示控制类别，不读取真实表格或形成业务结论。 |
| control-reference traceability shape | 控制引用可追溯形状 | P3 机械保留 evidence_id、document_id、sheet、row、column 与 source_uri 的 :control: 引用字段；它不表示真实文件、真实行列位置、真实来源绑定或真实证据已经验证。 |
| reference-only table evidence binding candidate | 仅引用表格证据绑定候选 | Stage062 P2 对两条固定、非业务控制请求的纯内存投影；它只保留 evidence_id、document_id、sheet、row、column 和 source_uri 的 :control: 引用形状，始终未绑定、必须人工确认且不能成为真实事实、真实证据、来源定位或第二权威事实源。 |
| unbound reference-only binding state | 未绑定仅引用状态 | 在未读取和验证真实来源、行列位置、字段、数值或证据时的 Stage062 P2 关闭状态；它阻断数值统计和确定性结论，并要求人工确认后才可进入后续受控步骤。 |
| table evidence binding | 表格证据绑定 | 未来把结构化表格事实关联到 evidence_id、document_id、sheet、row、column 和 source_uri 的受控接口；Stage062 P1 只定义静态合同，不读取真实表格、不创建事实、证据或来源绑定，也不建立第二权威事实源。 |
| opaque source URI reference | 不透明来源 URI 引用 | Stage062 P1 中 source_uri 的字段语义：只允许受控引用标识，不能填入真实 URL、文件路径、网络位置、来源正文或业务内容。 |
| six-dimension fact traceability | 六维事实可追溯 | 未来数值统计前必须同时具备 evidence_id、document_id、sheet、row、column 和 source_uri 的绑定形状；在真实来源、数值或证据未验证时，统计结论和模型确定性回答保持关闭。 |
| Stage061 structured-data quality review | Stage061 结构化数据质量整阶段复审 | 只机械复审 P1--P4 静态合同、受控内存报告和 metadata-only 交付的字段形状、人工处理与回滚说明；不读取真实表格、不形成真实质量结论，也不启动 Stage062、OVH 或生产运行。 |
| metadata-only structured-data quality delivery | 仅元数据结构化数据质量交付 | Stage061 P4 从固定控制场景派生的交付样例、字段引用、质量结果和人工建议；它不包含真实表格内容、真实质量结果、真实事实、来源绑定或证据记录，不能成为第二权威事实源。 |
| control quality test result | 控制质量测试结果 | 只复用 P3 六类控制场景的显式处置、未评估状态和统计关闭边界的交付证据；它不代表真实字段完整性、单位一致性、日期合法性、主键重复或异常值验证。 |
| control reparse and fact rollback | 控制重解析与事实回滚 | P4 对未来授权输入门的白箱操作说明；当前只允许重放 P3 control 报告，不打开真实文件、不删除或覆写质量结果或事实，撤回范围仅限 P4 派生工件。 |
| controlled structured-data quality scenario | 受控结构化数据质量场景 | Stage061 P3 对固定、非业务、reference-only 质量候选的纯内存异常处置；空表、合并单元格、单位混乱、日期格式不一、异常值和重复行只作为控制类别，不能代表真实表格质量验证。 |
| control traceability shape | 控制可追溯引用形状 | 来源文档、工作簿、工作表、表头行、行列范围和 evidence 的 :control: 引用形状；它帮助人工复核控制合同，但不证明真实文件位置、来源绑定或证据已验证。 |
| reference-only quality control candidate | 仅引用质量控制候选 | Stage061 P2 对固定、非业务引用记录投影的内存候选；它不包含真实表格、业务事实、来源绑定或证据记录，也不能成为第二权威事实源。 |
| unassessed quality state | 未评估质量状态 | 质量候选在未读取和验证真实数据时必须保持的关闭状态；它不表示字段、单位、日期、主键或异常值已通过。 |
| human review required | 必须人工确认 | 真实输入、字段、单位、日期、主键、来源位置或证据无法确认时的唯一处置；不自动写入质量层。 |
| statistical conclusion blocked | 统计结论关闭 | 未验证数值、未绑定来源位置或未绑定证据时的关闭状态；异常值与统计结论不得由模型文本猜测。 |
| structured data quality test | 结构化数据质量测试 | 对未来结构化表格事实的字段完整性、单位一致性、日期合法性、主键重复和异常值进行受控验证的工程边界；Stage061 P1 只定义合同，不读取或验证真实数据。 |
| field completeness | 字段完整性 | 未来判断受控字段引用是否齐备的质量维度；它不代表本地已检查任何真实表头、单元格或业务记录。 |
| unit consistency | 单位一致性 | 未来确认 measurement 与 unit 引用是否满足受控规则的质量维度；Stage061 P1 不执行单位读取、归一化或验证。 |
| primary key duplication | 主键重复 | 未来针对主键引用进行重复状态复核的质量维度；它不代表已读取、去重或删除任何业务记录。 |
| outlier review | 异常值复核 | 未来仅可基于带来源位置与证据绑定的结构化事实进行的数值异常复核；模型文本猜测不能形成统计结论。 |
| OCR | 光学字符识别 | 将图像或扫描页面中的字符转换为机器可处理文本的技术；当前批次未启用、未执行或验证 OCR。 |
| BATCH | 阶段批次治理标识 | 将连续阶段的本地证据、复审与后续门禁归为同一受控批次的机器标识；它不等于上传、生产或运行授权。 |
| IDS-STAGE | 工业数据系统阶段标识前缀 | 冻结任务包中工业数据系统阶段的机器可读前缀；它只标识阶段位置，不代表该阶段的运行能力已启用。 |
| IDS-V | 工业数据系统版本化任务标识前缀 | 冻结任务包中的版本化任务编号前缀；它只标识受控任务，不代表生产运行、部署或上传已获授权。 |
| ACC-STAGE | 阶段验收标识前缀 | 冻结任务包中阶段验收项的机器可读前缀；它只标识验收位置，不等于生产准入、上传许可或运行结果。 |
| ACC-STAGE- | 阶段验收编号前缀 | 冻结任务包中阶段验收条目的机器可读编号前缀；它只标识验收项，不等于生产准入、上传授权或运行结果。 |
| schema-only candidate parse product sample | 仅结构候选解析产物样例 | 由 Stage049 P3 control 候选版本派生的空结构交付证据；只保留受控版本、置信度和处置标签，不保留来源引用、正文、表格、页面、章节或运行时输出 |
| derived control disposition log | 派生控制处置记录 | 从已验证 control 场景派生的非运行时处置证据，明确记录零尝试、未切换 parser、未写人工复核队列和未写运行时日志；不证明真实 fallback 已执行 |
| format-labelled differential scenario | 格式标签化差异评估场景 | 只用格式标签和两个 reference-only control 候选重放 Stage049 P2 资格处置的纯内存报告；格式标签不代表文件检测、真实路线、真实 parser 或解析正文 |
| control differential eligibility slice | 受控差异化资格切片 | 只对两个 reference-only control 候选的版本、置信度和元数据一致性作纯内存资格判断的确定性函数；不读取文件、不比较解析正文、不运行 parser 或质量门 |
| metadata-only comparison | 仅元数据比较 | 只检查候选引用、路线、输出合同、版本、状态和置信度等受控字段的比较方式；不读取或比较 text、tables、pages、sections、confidence、errors 的实际内容 |
| differential parser evaluation | 差异化解析器评估 | 对同一受控候选解析产物的多个 parser 版本进行交叉比较的工程边界；Stage049 步骤一只定义合同，不运行 parser、比较或质量门 |
| candidate parse product | 候选解析产物 | 尚未通过质量门的解析结果概念；只能保持候选事实等级，不能直接成为高可信证据或改写路由与降级处置 |
| comparison eligibility | 比较资格 | 进入后续差异化评估前必须满足的受控条件；P2 只以两个 control-fixture 候选验证版本数量和元数据一致性，不创建持久候选集合或比较解析正文 |
| controlled fallback scenario | 受控降级场景 | 只用格式标签和七字段引用重放降级处置的纯内存验证记录；不代表真实文件、真实路线、解析器或回退已执行 |
| explicit disposition | 明确处置 | 每个候选、复核、失败、受阻或不支持结果都必须返回的受控结论；没有明确处置的静默丢弃被禁止 |
| instruction route invariance | 指令文本路线不变性 | 指令样证据文本与相同格式的普通控制场景必须得到相同处置，且不能改变系统规则、工具权限或策略 |
| in-memory fallback disposition slice | 纯内存降级处置切片 | 只把受控的 reference-only 结果映射为明确中文处置的确定性函数；不读取文件、不运行真实 fallback，也不写队列或持久状态 |
| parser confidence record | 解析置信度记录 | 与 control-fixture parser 版本一起保留在纯内存处置结果中的受控字段；它不等于质量门通过、来源证明或高可信证据 |
| parser output envelope | 解析器输出封套 | 绑定来源、检测、路线、解析器版本、六个核心字段、安全标签和质量门的精确输出结构；步骤四只派生不含业务正文的控制投影 |
| control fixture adapter | 控制夹具适配器 | 仅把预解析、合成、非业务映射送入输出归一化器的测试适配层；不是 Stage046 运行时解析器，不能用于选择、分派、执行或生产 |
| format-labelled preparsed control | 格式标签化预解析控制 | 携带受治理格式与路线元数据的合成内存 payload；用于验证输出合同，不代表读取过文件、检测过类型或执行过 parser |
| sanitized output projection | 脱敏输出投影 | 仅保留格式、路线、版本、身份、状态、计数和安全标记的结构样例；不保留正文、表格单元值、页节文本、公式值、路径、异常或凭据 |
| non-runtime fallback record | 非运行时回退记录 | 由既有控制结果派生的处置证据，明确记录未尝试、零次数、未切换解析器且未静默丢弃；不是运行日志，也不证明回退能力 |
| output identity | 输出身份 | 由解析输出规范投影计算的 SHA-256 完整性标识，不证明外部来源、内容正确或质量通过 |
| lineage | 来源链 | 把解析器输出绑定到检测结果、路由候选、来源引用和解析器版本的受控关联，不单独构成外部来源证明 |
| Git-index | Git 索引 | 提交前暂存区中的受控文件版本；复审要求证据来源与该版本精确一致 |
| binding | 绑定校验 | 把来源、提交身份、工件摘要和治理状态建立可机验的精确关联 |
| Unicode | Unicode 字符编码 | 统一表示字符的编码标准；本阶段要求无法安全编码的文本产生结构化脱敏拒绝 |
| safe error | 安全错误 | 只包含受控错误代码、严重度、可重试标志和消息键，不回显原始异常、堆栈、路径、凭据或业务正文 |
| quality gate | 质量门 | 解析器候选产物进入下游或证据提升前必须通过的独立控制；Stage047 步骤四只重算初始处置指标，不执行质量评估 |
| orphan reference | 悬空引用 | 指向不存在 page、section 或 table 标识的内部引用；合同要求拒绝而不是静默忽略 |
| detection result identity | 检测结果身份 | 由检测结果精确投影生成的完整性摘要；不证明外部来源、生产授权或真实文件身份 |
| projection digest | 投影摘要 | 对受控字段规范序列化后计算的摘要，只用于发现字段漂移或重组 |
| fact level | 事实等级 | 描述路由结果是候选、待复核、不支持、阻断或无效的受控等级 |
| canonical reference | 规范引用 | 满足唯一、有界、lower-ASCII token segment 词法形状且不包含路径、URI、空段、点段、大写或控制字符的证据身份引用 |
| reciprocal reference graph | 互惠引用图 | table 到 page/section 的引用必须被对应对象的 table_refs 精确反向声明；任一单向、悬空或多余引用都会拒绝整个输出 |
| LLM | 待补中文 | 来自功能名，待复审确认释义 |
| route candidate | 路由候选 | 由已治理文件类型映射出的未执行解析器路线，不代表已选择、分派或运行解析器 |
| parser family | 解析器族 | 按文件类型分组的解析能力类别；Stage046 步骤一只声明类别，不提供实现或版本 |
| dispatch | 分派执行 | 将已选解析器实际交给运行时执行的动作；Stage046 步骤一明确禁用 |
| generic parser | 通用解析器 | 未知或不受支持类型的兜底解析路径；失败关闭合同禁止以此绕过复核 |
| text | 文本字段 | 解析器输出合同中的 source-derived 字符串或 null；固定标记为不可信证据文本，不能成为系统或工具指令 |
| tables | 表格集合 | 解析器输出合同中的有序表格数组；每项保留唯一标识、页面/章节引用、二维 cells、置信等级和安全错误 |
| pages | 页面集合 | 解析器输出合同中的有序页面数组；页号必须是唯一递增正整数，步骤一不创建页面输出 |
| sections | 章节集合 | 解析器输出合同中的有序章节数组；章节、页面与表格引用必须全部可解析且层级不得成环 |
| confidence | 置信等级 | 解析结果质量的受控等级字段；没有解析结果时不得伪造高置信结论 |
| errors | 错误代码集合 | 解析器输出合同中的 safe error 数组；失败必须显式且不得回显原始异常、路径、凭据或业务内容 |
| fallback | 回退处理 | 低质量或解析失败后的受控处理路径；Stage046 步骤四只生成非运行时控制日志 |
| parser fallback | 解析器降级处理 | 主解析器失败后的受控处置路径；Stage048 步骤一只定义边界，不执行回退 |
| silent drop | 静默丢弃 | 未给出明确失败、待复核或阻断处置就丢弃结果的行为；Stage048 合同禁止 |
| CAS | 比较并交换 | 用预期栅栏令牌和完整锁版本确认当前状态后再执行原子更新 |
| TDD | 测试驱动开发 | 先固定失败断言，再实现到通过的开发方式 |
| RED | 红灯阶段 | 测试驱动开发中确认新测试按预期失败的阶段 |
| run | 单轮执行 | 一次只处理一个明确步骤并在停止门结束的受控执行 |
| PROPOSED | 提议值 | 已有工程来源与约束但尚未完成生产校准的事实等级 |
| commit | 提交对象 | 固定某一时点仓库内容及父链的 Git 提交身份 |
| tree | 目录树对象 | 固定某次提交内 KM_IDSystem 目录内容的 Git 对象 |
| drive | 外接存储设备 | 资源门检查的外接存储可用性信号，不代表本轮执行物理拔盘 |
| disk | 磁盘空间 | 项目文件系统的只读可用空间观察，不代表本轮分配或填充磁盘 |
| tombstone version | 墓碑版本 | 锁释放后保留的单调版本号，用于拒绝旧持有者凭证；本阶段只存在于进程内存 |
| fail-closed | 失败关闭 | 来源、合同或证据不完整时停止并要求人工复核，不继续执行或放宽安全门 |
| allowlist | 允许清单 | 仅列明允许进入某一受控动作的类别；未列项目默认禁止并要求复核 |
| admission | 准入 | 任务进入队列或执行链前必须通过的控制门 |
| backpressure | 背压 | 资源或容量不足时限制新任务进入并保持失败关闭 |
| cleanup | 清理 | 仅对允许清单内可重建临时产物执行的受控删除动作 |
| crash | 进程崩溃 | 工作进程非正常终止；恢复运行时归 Stage 43 管理 |
| dead-letter | 死信 | 重试预算耗尽后停止自动处理并等待人工复核的任务状态 |
| fencing | 栅栏保护 | 用单调令牌拒绝过期锁持有者继续写入的控制机制 |
| guard | 保护条件 | 状态迁移或动作执行前必须满足的失败关闭条件 |
| lock | 锁 | 用于排除同一受保护资源并发写入的所有权记录 |
| ordered | 有序 | 动作必须按合同声明的固定先后次序执行 |
| ownership | 职责归属 | 明确某项运行时能力由哪个阶段负责实现与复审 |
| predecessor | 前置阶段 | 当前阶段进入前必须已完成并绑定证据的上游阶段 |
| queue | 队列 | 保存已准入控制任务及其调度顺序的逻辑结构 |
| recovery | 恢复 | 异常或暂停后在重新校验资源与所有权后继续处理的受控过程 |
| reference-only | 仅供引用 | 只允许作为合同或证据输入，不代表已激活对应运行时 |
| reopen | 重新打开 | 将已关闭或终态对象恢复为可执行状态；终态对象禁止此动作 |
| retry | 重试 | 满足版本化策略、资源门与预算后再次安排同一受控操作 |
| reviewed | 已复审 | 已完成独立阶段复审并保留本地证据，但不等于生产启用 |
| shutdown | 关闭 | 停止新准入、收敛已接收工作并释放所有权的有序过程 |
| terminal | 终态 | 任务生命周期不可原地重新打开的最终状态 |
| worker | 工作进程 | 领取并执行已准入队列任务的受控执行单元 |
| writer | 写入方 | 可能正在写入候选产物的受控主体；身份或静默证据未知时禁止清理 |
| changed | 已变更 | 同一请求标识下规范载荷与首次请求不一致的冲突状态 |
| extraction | 归档提取 | 从受控归档中提取成员的任务族；与同源处理共享排他域 |
| identity | 身份标识 | 候选对象及其写入方、文件和租约证据的规范身份 |
| index | 索引构建 | 为受控来源构建索引的任务族；与同源处理共享排他域 |
| manifest | 产物清单 | 记录产物身份、归属和完整性的受保护事实来源 |
| processing | 文件处理 | 处理受控来源文件的任务族；与同源其他操作共享排他域 |
| report | 报告生成 | 从受控来源生成报告的任务族；报告快照属于受保护资料 |
| source-pipeline | 同源处理管线 | 对同一来源的处理、提取、索引和报告任务共同使用的排他锁域 |
| worktree | 工作树 | 与主树隔离并绑定独立分支的 Git 开发目录 |
| audit | 审计记录 | 用于追溯候选决策身份、证据与原因的追加式引用 |
| checkpoint | 检查点 | 可验证的持久进度引用；本阶段只判断续作候选，不执行续作 |
| error | 错误引用 | 只记录安全错误代码或引用，不回显原始异常载荷 |
| grace | 宽限期 | 租约到期后仍禁止接管的附加安全等待边界 |
| input | 输入引用 | 控制请求所依据的 Git 跟踪证据引用，不包含原始业务正文 |
| output | 输出引用 | 候选决策指向的结果标识，不代表已写入运行产物 |
| payload | 载荷 | 请求或异常携带的正文内容；安全结果禁止直接回显 |
| quarantine | 隔离引用 | 局部输出的受保护引用状态，不授权读取、清理或删除 |
| request | 请求 | 绑定任务、尝试、工作进程代际、状态版本与崩溃事件的规范控制对象 |
| delete_allowed | 允许删除标志 | 候选决策中的删除授权标志；Stage 44 步骤二固定为 false，不开放删除路径 |
| lstat | 不跟随链接的文件身份检查 | 未来执行前用于读取链接本身身份且不跟随符号链接的系统调用语义；本阶段不调用 |
| checker | 检查器 | 读取批准合同与证据并输出结构化失败关闭结论的只读验证程序 |
| repair | 修复 | 针对已确认复审发现实施并由反例重新验证的有界更正 |
| replay | 重放验证 | 使用既有受控证据重新执行确定性判断，不代表重做真实运行时动作 |
| review | 复审 | 在四个步骤完成后独立核验来源、合同、证据、禁止项与下一停止门 |
| FAILED | 失败终态 | 任务明确失败且不可继续原执行链的终态；仍需满足全部清理门才可成为候选 |
| DEAD_LETTERED | 死信终态 | 重试预算耗尽后进入人工处理的终态；不自动授权清理 |
| CANCELLED | 已取消终态 | 任务经受控取消后形成的终态；不自动授权清理 |
| PAUSED | 已暂停非终态 | 任务因资源或控制条件暂停且仍可能恢复，禁止成为失败任务清理候选 |
| RETRY_WAIT | 等待重试非终态 | 任务等待重试调度且仍可能恢复，禁止成为失败任务清理候选 |
| PASS_REVIEWED_LOCAL_DELETE_DISABLED | 本地复审通过且删除禁用 | Stage 44 复审结果码；只证明本地证据通过，不开放真实清理或删除能力 |
| POSIX | 可移植操作系统接口规范 | 路径词法与未来文件系统调用语义遵循的接口规范；本轮不执行文件系统调用 |
| creator | 创建任务身份 | 创建候选产物的任务身份，必须与受治理任务标识精确一致 |
| evidence | 证据引用 | 支持候选判断且可独立核验的受控事实引用 |
| job | 任务实例 | 受状态图、尝试、所有权和审计约束的单个工作实例 |
| path | 路径 | 候选对象在批准根目录内的规范位置标识 |
| refs | 引用集合 | 请求绑定的批准输入、证据或受保护对象引用集合 |
| resource | 资源条件 | 外接存储、磁盘空间或接口预算等进入受控动作前必须满足的条件 |
| root | 批准根目录 | 治理合同允许候选路径归属的唯一规范根身份 |
| root-relative | 根目录相对路径 | 不含绝对前缀且必须保持唯一规范词法表示的候选路径 |
| durable | 持久可追溯 | 证据跨进程或后续执行仍可定位和独立核验的性质 |
| provenance | 来源链 | 候选对象从任务、输入、批准根、清单到证据的可验证归属关系 |
| HEAD | 当前提交指针 | Git 工作树当前检出的提交位置；祖先校验以该位置为终点 |
| IDS | 工业数据系统 | Industrial Data System 的项目简称 |
| MIME | 媒体类型元数据 | 文件类型检测的辅助信号，可信度低于结构有效的文件签名 |
| OOXML | Office 开放 XML 容器格式 | DOCX 与 XLSX 使用的 ZIP 容器格式；必须验证规范成员标记 |
| PASS_REVIEWED_LOCAL_PARSER_AND_FALLBACK_DISABLED | 本地复审通过且解析器与回退禁用 | Stage 45 最终复审结果码；不代表解析器、回退或生产能力已启用 |
| UTC | 协调世界时 | 请求时间戳必须满足真实日历与严格协调世界时语义 |
| completed_reviewed_local | 本地整阶段复审完成 | 本地来源、阶段、发现与治理证据已通过，但不授权上传或生产激活 |
| instructions | 使用说明来源 | Stage 45 复审精确绑定并重新计算哈希的批准使用说明文件 |
| task-pack | 任务包 | 包含阶段任务定义且由精确归档与成员哈希绑定的批准来源 |
| tests | 测试集合 | 验证合同、反例、治理与兼容性的机器可执行测试文件 |
| snapshot | 不可变快照 | 从指定已提交 Git 对象读取并重新计算哈希的上游证据版本，不引用可变工作树内容 |
| rehash | 重新计算哈希 | 直接从当前批准来源重新计算摘要并与既有绑定比较 |
| NFC | 规范组合形式 | 统一码文本的规范组合形式，用于唯一匹配归档成员名称 |
| PASS | 通过 | 机器验证全部必需条件均为真时使用的通过标记 |
| REVIEW-LOCAL- | 本地复审运行前缀 | 本地整阶段复审机器记录标识的受治理前缀 |
| RUN-IDS-STAGE | 阶段运行标识前缀 | 工业数据系统阶段机器运行记录的受治理标识前缀 |
| ancestry | 提交祖先关系 | 指定提交必须位于当前提交历史链上的可验证关系 |
| echo | 原样回显 | 把请求输入未经净化地返回到结果；无效请求中禁止此行为 |
| invalid | 无效输入 | 未满足受治理形状或枚举约束且必须失败关闭的输入状态 |
| parent | 父提交 | Git 提交直接引用的上一提交，用于不可变阶段绑定 |
| OVH | 远端基础设施提供方 | 受治理的远端部署目标；本阶段未进行部署、生产激活或外部调用 |
| EVIDENCE_ONLY | 仅证据用途 | 受控文本只能作为证据解释，不能成为系统规则、工具授权或策略覆盖 |
| evidence-only | 仅证据用途描述 | 与 EVIDENCE_ONLY 含义一致的连字符写法 |
| JPEG | JPEG 图像格式 | 仅用于控制场景的格式标签，不代表真实文件读取或运行时支持 |
| PNG | PNG 图像格式 | 仅用于控制场景的格式标签，不代表真实文件读取或运行时支持 |
| TIFF | TIFF 图像格式 | 仅用于控制场景的格式标签，不代表真实文件读取或运行时支持 |
| TXT | 纯文本格式 | 仅用于控制场景的格式标签，不代表真实文件读取或运行时支持 |
| UNASSESSED | 未评估 | 候选尚未进入质量评估或证据提升的受控状态 |
| UNTRUSTED_EVIDENCE_TEXT | 不可信证据文本 | 来源文本不得覆盖系统规则、工具授权或策略 |
| REQUIRED_NOT_APPLIED_STAGE | 阶段负责但尚未应用 | 该阶段拥有后续标记职责，但本地合同未执行运行时应用的受控前缀 |
| OWNED | 责任归属 | 受控标记中表示职责归属的后缀 |
| HIGH | 高等级 | 受控枚举中的高等级标签；当前批次不把它解释为真实质量、置信度或生产结论 |
| LOW | 低等级 | 受控枚举中的低等级标签；当前批次不把它解释为真实质量、置信度或生产结论 |
| MEDIUM | 中等级 | 受控枚举中的中等级标签；当前批次不把它解释为真实质量、置信度或生产结论 |
| RAG | 检索增强生成摘要接口 | 后续受控阶段的摘要接口概念；本批次只复审静态合同，不生成真实摘要正文或模型结论 |
| future | 未来接口占位 | 只表示冻结合同为后续受控阶段预留的字段或输出，不代表当前已执行或可运行 |
| inspection | 检验类事实 | 冻结合同中预留的检验事实类别；本批次未读取、创建或验证真实检验记录 |
| production | 生产环境或生产事实 | 指正式运行环境或业务生产记录；本批次未部署、启动或验证生产服务 |
| profile | 结构配置档 | 用于描述受控字段集合或候选形状的静态配置概念，不等于真实文件或真实表格解析结果 |
| typed | 带类型标注 | 冻结合同中对未来字段语义或值类型的标注；当前不包含真实 typed value |
| ACC-BATCH | 批次验收标识前缀 | 连续阶段批次复审的机器可读验收编号前缀；不代表上传、生产或运行授权 |
| BATCH- | 批次编号连接前缀 | 批次治理编号中的连字符写法；仅用于机器标识组合 |
| REVIEW- | 复审编号连接前缀 | 复审任务、验收或运行记录中的连字符写法；只标识本地复审位置 |
| REVIEW-GATE | 复审停止门标识 | 冻结路线图中的复审入口或出口标识；不自动授权后续阶段、上传或部署 |
| PASS_BATCH_REVIEWED_LOCAL_GLOBAL_UPLOAD_LOCKED | 批次本地复审通过且全局上传锁定 | 本批次检查器的通过结果码；只证明本地合同与治理投影一致，上传路径仍关闭 |
| review-local | 本地复审标记 | 机器事实或变更记录中表示本地复审的后缀；不代表远端发布或生产验证 |
| source | 来源引用 | 指受控合同或证据中的来源字段或引用；当前批次未读取真实业务来源正文 |
| valid | 校验有效 | 机器检查在既定合同范围内返回的一致性状态；不扩展为真实运行或生产结论 |
| document_scope_ref | 文档范围控制引用字段 | Stage076 P2 索引版本控制切片中的不透明范围标签；不读取、记录或表示真实文档范围。 |
| chunk_count | 切块数量控制字段 | Stage076 P2 固定为零的控制字段；不统计、生成或表示真实切块数量。 |
| embedding_model_ref | 嵌入模型控制引用字段 | Stage076 P2 的不透明控制标签；不选择、调用或记录真实模型。 |
| Operations | 运维控制展示 | Stage076 P3 中仅用于展示索引版本控制投影的未来运维界面概念；当前不写入、连接或表示实际界面。 |
| value | 字段值 | 字段层面的值概念；本批次未读取、抽取或保存真实业务值 |
| index_version | 索引版本控制字段 | Stage077 P2 纯内存控制切片中的候选索引版本字段；只表示不透明控制标签，不创建、记录或表示真实索引版本。 |
| CONTROL_BACKGROUND_BUILD_INCOMPLETE_REFERENCE_ONLY | 后台构建未完成控制引用 | Stage081 P3 中仅表示候选后台构建尚未完成的纯内存控制状态；不触发真实构建、失败处理或索引切换。 |
| BM | BM25 术语前缀 | 关键词相关性评分术语中的固定前缀；本阶段只登记未来检索基线，不计算真实评分。 |
| FTS | 全文检索 | future PostgreSQL 全文检索路线的缩写；本阶段未建立索引或执行检索。 |
| PostgreSQL | 关系数据库 | future 检索路线可选的关系数据库；本阶段未连接、创建或运行数据库。 |
| Top-K | 前 K 条候选 | 未来检索结果的候选数量边界；本阶段未产生真实候选或排序结果。 |
| active_index_version | 活动索引版本字段 | 未来检索路由使用的受控索引版本引用字段；本阶段未创建或切换真实索引。 |
| filter | 过滤条件 | 未来检索的元数据约束条件；本阶段未对任何业务资料执行过滤。 |
| full-text | 全文文本 | future 全文检索语义的连字符写法；本阶段未读取、保存或检索真实正文。 |
| hybrid | 混合检索 | 关键词与向量结果的未来融合概念；本阶段只声明评分字段，不执行融合。 |
| keyword | 关键词 | Stage083 P1 的唯一检索基线；当前只定义静态合同，不执行关键词查询。 |
| pgvector | 向量扩展 | future PostgreSQL 向量检索路线的可选扩展；本阶段未安装、连接或调用。 |
| query | 查询请求 | 未来检索的受控输入概念；本阶段未接收或执行真实查询。 |
| retrieval | 检索 | 从受控索引中选择候选的未来能力；本阶段仅定义接口字段与失败关闭边界。 |
| score | 相关性评分 | 未来排序解释使用的字段概念；本阶段不计算真实分数。 |
| selected | 已选择结果 | 未来候选排序后保留的结果状态；本阶段不选择或输出真实业务结果。 |
| trace | 检索追溯记录 | 未来检索决策的解释字段集合；本阶段不产生真实运行轨迹。 |
| vector | 向量检索 | future 语义检索路线的概念；P1 禁止 vector-only 路线且不生成向量。 |
| ledger | 证据账本引用 | 仅表示未来检索结果需要保留的受控证据引用字段；Stage083 P2 不读取、写入或创建真实账本。 |
| integration | 集成接口 | 仅表示未来 PostgreSQL FTS／BM25、pgvector、过滤、混合排序与轨迹路由的集成边界；Stage084 P2 不执行任何连接、索引、embedding 或检索。 |
| main | 主分支 | Git 仓库的正式主分支；Stage097 P1 只将其作为后续正式合并目标的术语登记，本地验证继续在既有开发分支完成。 |
| internal_evidence | 内部依据 | Stage097 P2 的底层来源类型；当前仅在 reference-only 控制记录中保留引用形状，不读取、选择或呈现真实内部资料。 |
| external_public_reference | 外部公开参考 | Stage097 P2 的底层来源类型；当前仅在 reference-only 控制记录中保留引用形状，不读取或呈现真实公开资料。 |
| model_reasoning | 模型推理 | Stage097 P2 的底层来源类型；当前仅在 reference-only 控制记录中保留引用形状，不选择模型、不调用模型、不消费模型 Token。 |
| PASS_RETRIEVAL_RESULT_VALIDITY_DELIVERY_EVIDENCE_RUNTIME_DISABLED | 检索结果有效性交付证据已通过且运行时关闭 | Stage088 P4 的纯内存交付控制结果；只表示固定控制工件可回退到该状态，不表示真实检索、结果有效性、参数回滚或生产能力已执行。 |
