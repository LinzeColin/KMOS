<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/glossary.json -->
<!-- 全项目唯一裁定"一个数字是什么"的地方。有争议以本文件为准。 -->
<!-- 中文门：正文出现的英文术语必须在第五节术语表登记，否则渲染 FAIL。 -->

# 口径字典

## 一、关键数字口径

> 暂时还没有数字口径。等在机器平面裁定（`machine/facts/glossary.json` 的 numbers）之后，这里会自动出现内容。现在空着是如实反映——没接上就是没接上。

## 二、外部数据形态

> 暂时还没有外部数据形态。等在机器平面登记（`machine/facts/glossary.json` 的 data_shapes）之后，这里会自动出现内容。现在空着是如实反映——没接上就是没接上。

## 三、恒定为真的规则

> 暂时还没有恒真规则。等在机器平面登记（`machine/facts/glossary.json` 的 invariants）之后，这里会自动出现内容。现在空着是如实反映——没接上就是没接上。

## 四、证据等级定义

| 等级 | 含义 |
|---|---|
| `已提取` | 从代码或配置提取并核对 |
| `已声明` | 只有文档说法，未核对 |

## 五、术语对照

正文出现的英文术语在此登记。

| 英文 | 中文 | 说明 |
|---|---|---|
| Owner | 负责人 | 项目/产品的决策与复审责任人 |
| Stage | 阶段 | 路线图一级单元 |
| Phase | 步骤 | 阶段下的二级单元 |
| Task | 任务 | 步骤下的三级单元 |
| Roadmap | 路线图 | 阶段→步骤→任务的完整计划 |
| parser output envelope | 解析器输出封套 | 绑定来源、检测、路线、解析器版本、六个核心字段、安全标签和质量门的精确输出结构；步骤四只派生不含业务正文的控制投影 |
| control fixture adapter | 控制夹具适配器 | 仅把预解析、合成、非业务映射送入输出归一化器的测试适配层；不是 Stage046 运行时解析器，不能用于选择、分派、执行或生产 |
| format-labelled preparsed control | 格式标签化预解析控制 | 携带受治理格式与路线元数据的合成内存 payload；用于验证输出合同，不代表读取过文件、检测过类型或执行过 parser |
| sanitized output projection | 脱敏输出投影 | 仅保留格式、路线、版本、身份、状态、计数和安全标记的结构样例；不保留正文、表格单元值、页节文本、公式值、路径、异常或凭据 |
| non-runtime fallback record | 非运行时回退记录 | 由既有控制结果派生的处置证据，明确记录未尝试、零次数、未切换解析器且未静默丢弃；不是运行日志，也不证明回退能力 |
| output identity | 输出身份 | 由解析输出规范投影计算的 SHA-256 完整性标识，不证明外部来源、内容正确或质量通过 |
| lineage | 来源链 | 把解析器输出绑定到检测结果、路由候选、来源引用和解析器版本的受控关联，不单独构成外部来源证明 |
| safe error | 安全错误 | 只包含受控错误代码、严重度、可重试标志和消息键，不回显原始异常、堆栈、路径、凭据或业务正文 |
| quality gate | 质量门 | 解析器候选产物进入下游或证据提升前必须通过的独立控制；Stage047 步骤四只重算初始处置指标，不执行质量评估 |
| orphan reference | 悬空引用 | 指向不存在 page、section 或 table 标识的内部引用；合同要求拒绝而不是静默忽略 |
| detection result identity | 检测结果身份 | 由检测结果精确投影生成的完整性摘要；不证明外部来源、生产授权或真实文件身份 |
| projection digest | 投影摘要 | 对受控字段规范序列化后计算的摘要，只用于发现字段漂移或重组 |
| fact level | 事实等级 | 描述路由结果是候选、待复核、不支持、阻断或无效的受控等级 |
| canonical reference | 规范引用 | 满足唯一有界词法形状且不包含路径、URI、空段或点段的证据身份引用 |
| LLM | 待补中文 | 来自功能名，待复审确认释义 |
| route candidate | 路由候选 | 由已治理文件类型映射出的未执行解析器路线，不代表已选择、分派或运行解析器 |
| parser family | 解析器族 | 按文件类型分组的解析能力类别；Stage046 步骤一只声明类别，不提供实现或版本 |
| dispatch | 分派执行 | 将已选解析器实际交给运行时执行的动作；Stage046 步骤一明确禁用 |
| generic parser | 通用解析器 | 未知或不受支持类型的兜底解析路径；失败关闭合同禁止以此绕过复核 |
| text | 文本字段 | 解析器输出合同中的 source-derived 字符串或 null；固定标记为不可信证据文本，不能成为系统或工具指令 |
| tables | 表格集合 | 解析器输出合同中的有序表格数组；每项保留唯一标识、页面/章节引用、二维 cells、置信等级和安全错误 |
| pages | 页面集合 | 解析器输出合同中的有序页面数组；页号必须是唯一递增正整数，步骤一不创建页面输出 |
| sections | 章节集合 | 解析器输出合同中的有序章节数组；章节、页面与表格引用必须全部可解析且层级不得成环 |
| confidence | 置信等级 | 解析结果质量的受控等级字段；没有解析结果时不得伪造高置信结论 |
| errors | 错误代码集合 | 解析器输出合同中的 safe error 数组；失败必须显式且不得回显原始异常、路径、凭据或业务内容 |
| fallback | 回退处理 | 低质量或解析失败后的受控处理路径；Stage046 步骤四只生成非运行时控制日志 |
| CAS | 比较并交换 | 用预期栅栏令牌和完整锁版本确认当前状态后再执行原子更新 |
| TDD | 测试驱动开发 | 先固定失败断言，再实现到通过的开发方式 |
| RED | 红灯阶段 | 测试驱动开发中确认新测试按预期失败的阶段 |
| run | 单轮执行 | 一次只处理一个明确步骤并在停止门结束的受控执行 |
| PROPOSED | 提议值 | 已有工程来源与约束但尚未完成生产校准的事实等级 |
| commit | 提交对象 | 固定某一时点仓库内容及父链的 Git 提交身份 |
| tree | 目录树对象 | 固定某次提交内 KM_IDSystem 目录内容的 Git 对象 |
| drive | 外接存储设备 | 资源门检查的外接存储可用性信号，不代表本轮执行物理拔盘 |
| disk | 磁盘空间 | 项目文件系统的只读可用空间观察，不代表本轮分配或填充磁盘 |
| tombstone version | 墓碑版本 | 锁释放后保留的单调版本号，用于拒绝旧持有者凭证；本阶段只存在于进程内存 |
| fail-closed | 失败关闭 | 来源、合同或证据不完整时停止并要求人工复核，不继续执行或放宽安全门 |
| allowlist | 允许清单 | 仅列明允许进入某一受控动作的类别；未列项目默认禁止并要求复核 |
| admission | 准入 | 任务进入队列或执行链前必须通过的控制门 |
| backpressure | 背压 | 资源或容量不足时限制新任务进入并保持失败关闭 |
| cleanup | 清理 | 仅对允许清单内可重建临时产物执行的受控删除动作 |
| crash | 进程崩溃 | 工作进程非正常终止；恢复运行时归 Stage 43 管理 |
| dead-letter | 死信 | 重试预算耗尽后停止自动处理并等待人工复核的任务状态 |
| fencing | 栅栏保护 | 用单调令牌拒绝过期锁持有者继续写入的控制机制 |
| guard | 保护条件 | 状态迁移或动作执行前必须满足的失败关闭条件 |
| lock | 锁 | 用于排除同一受保护资源并发写入的所有权记录 |
| ordered | 有序 | 动作必须按合同声明的固定先后次序执行 |
| ownership | 职责归属 | 明确某项运行时能力由哪个阶段负责实现与复审 |
| predecessor | 前置阶段 | 当前阶段进入前必须已完成并绑定证据的上游阶段 |
| queue | 队列 | 保存已准入控制任务及其调度顺序的逻辑结构 |
| recovery | 恢复 | 异常或暂停后在重新校验资源与所有权后继续处理的受控过程 |
| reference-only | 仅供引用 | 只允许作为合同或证据输入，不代表已激活对应运行时 |
| reopen | 重新打开 | 将已关闭或终态对象恢复为可执行状态；终态对象禁止此动作 |
| retry | 重试 | 满足版本化策略、资源门与预算后再次安排同一受控操作 |
| reviewed | 已复审 | 已完成独立阶段复审并保留本地证据，但不等于生产启用 |
| shutdown | 关闭 | 停止新准入、收敛已接收工作并释放所有权的有序过程 |
| terminal | 终态 | 任务生命周期不可原地重新打开的最终状态 |
| worker | 工作进程 | 领取并执行已准入队列任务的受控执行单元 |
| writer | 写入方 | 可能正在写入候选产物的受控主体；身份或静默证据未知时禁止清理 |
| changed | 已变更 | 同一请求标识下规范载荷与首次请求不一致的冲突状态 |
| extraction | 归档提取 | 从受控归档中提取成员的任务族；与同源处理共享排他域 |
| identity | 身份标识 | 候选对象及其写入方、文件和租约证据的规范身份 |
| index | 索引构建 | 为受控来源构建索引的任务族；与同源处理共享排他域 |
| manifest | 产物清单 | 记录产物身份、归属和完整性的受保护事实来源 |
| processing | 文件处理 | 处理受控来源文件的任务族；与同源其他操作共享排他域 |
| report | 报告生成 | 从受控来源生成报告的任务族；报告快照属于受保护资料 |
| source-pipeline | 同源处理管线 | 对同一来源的处理、提取、索引和报告任务共同使用的排他锁域 |
| worktree | 工作树 | 与主树隔离并绑定独立分支的 Git 开发目录 |
| audit | 审计记录 | 用于追溯候选决策身份、证据与原因的追加式引用 |
| checkpoint | 检查点 | 可验证的持久进度引用；本阶段只判断续作候选，不执行续作 |
| error | 错误引用 | 只记录安全错误代码或引用，不回显原始异常载荷 |
| grace | 宽限期 | 租约到期后仍禁止接管的附加安全等待边界 |
| input | 输入引用 | 控制请求所依据的 Git 跟踪证据引用，不包含原始业务正文 |
| output | 输出引用 | 候选决策指向的结果标识，不代表已写入运行产物 |
| payload | 载荷 | 请求或异常携带的正文内容；安全结果禁止直接回显 |
| quarantine | 隔离引用 | 局部输出的受保护引用状态，不授权读取、清理或删除 |
| request | 请求 | 绑定任务、尝试、工作进程代际、状态版本与崩溃事件的规范控制对象 |
| delete_allowed | 允许删除标志 | 候选决策中的删除授权标志；Stage 44 步骤二固定为 false，不开放删除路径 |
| lstat | 不跟随链接的文件身份检查 | 未来执行前用于读取链接本身身份且不跟随符号链接的系统调用语义；本阶段不调用 |
| checker | 检查器 | 读取批准合同与证据并输出结构化失败关闭结论的只读验证程序 |
| repair | 修复 | 针对已确认复审发现实施并由反例重新验证的有界更正 |
| replay | 重放验证 | 使用既有受控证据重新执行确定性判断，不代表重做真实运行时动作 |
| review | 复审 | 在四个步骤完成后独立核验来源、合同、证据、禁止项与下一停止门 |
| FAILED | 失败终态 | 任务明确失败且不可继续原执行链的终态；仍需满足全部清理门才可成为候选 |
| DEAD_LETTERED | 死信终态 | 重试预算耗尽后进入人工处理的终态；不自动授权清理 |
| CANCELLED | 已取消终态 | 任务经受控取消后形成的终态；不自动授权清理 |
| PAUSED | 已暂停非终态 | 任务因资源或控制条件暂停且仍可能恢复，禁止成为失败任务清理候选 |
| RETRY_WAIT | 等待重试非终态 | 任务等待重试调度且仍可能恢复，禁止成为失败任务清理候选 |
| PASS_REVIEWED_LOCAL_DELETE_DISABLED | 本地复审通过且删除禁用 | Stage 44 复审结果码；只证明本地证据通过，不开放真实清理或删除能力 |
| POSIX | 可移植操作系统接口规范 | 路径词法与未来文件系统调用语义遵循的接口规范；本轮不执行文件系统调用 |
| creator | 创建任务身份 | 创建候选产物的任务身份，必须与受治理任务标识精确一致 |
| evidence | 证据引用 | 支持候选判断且可独立核验的受控事实引用 |
| job | 任务实例 | 受状态图、尝试、所有权和审计约束的单个工作实例 |
| path | 路径 | 候选对象在批准根目录内的规范位置标识 |
| refs | 引用集合 | 请求绑定的批准输入、证据或受保护对象引用集合 |
| resource | 资源条件 | 外接存储、磁盘空间或接口预算等进入受控动作前必须满足的条件 |
| root | 批准根目录 | 治理合同允许候选路径归属的唯一规范根身份 |
| root-relative | 根目录相对路径 | 不含绝对前缀且必须保持唯一规范词法表示的候选路径 |
| durable | 持久可追溯 | 证据跨进程或后续执行仍可定位和独立核验的性质 |
| provenance | 来源链 | 候选对象从任务、输入、批准根、清单到证据的可验证归属关系 |
| HEAD | 当前提交指针 | Git 工作树当前检出的提交位置；祖先校验以该位置为终点 |
| IDS | 工业数据系统 | Industrial Data System 的项目简称 |
| MIME | 媒体类型元数据 | 文件类型检测的辅助信号，可信度低于结构有效的文件签名 |
| OOXML | Office 开放 XML 容器格式 | DOCX 与 XLSX 使用的 ZIP 容器格式；必须验证规范成员标记 |
| PASS_REVIEWED_LOCAL_PARSER_AND_FALLBACK_DISABLED | 本地复审通过且解析器与回退禁用 | Stage 45 最终复审结果码；不代表解析器、回退或生产能力已启用 |
| UTC | 协调世界时 | 请求时间戳必须满足真实日历与严格协调世界时语义 |
| completed_reviewed_local | 本地整阶段复审完成 | 本地来源、阶段、发现与治理证据已通过，但不授权上传或生产激活 |
| instructions | 使用说明来源 | Stage 45 复审精确绑定并重新计算哈希的批准使用说明文件 |
| task-pack | 任务包 | 包含阶段任务定义且由精确归档与成员哈希绑定的批准来源 |
| tests | 测试集合 | 验证合同、反例、治理与兼容性的机器可执行测试文件 |
| snapshot | 不可变快照 | 从指定已提交 Git 对象读取并重新计算哈希的上游证据版本，不引用可变工作树内容 |
| rehash | 重新计算哈希 | 直接从当前批准来源重新计算摘要并与既有绑定比较 |
| NFC | 规范组合形式 | 统一码文本的规范组合形式，用于唯一匹配归档成员名称 |
| PASS | 通过 | 机器验证全部必需条件均为真时使用的通过标记 |
| REVIEW-LOCAL- | 本地复审运行前缀 | 本地整阶段复审机器记录标识的受治理前缀 |
| RUN-IDS-STAGE | 阶段运行标识前缀 | 工业数据系统阶段机器运行记录的受治理标识前缀 |
| ancestry | 提交祖先关系 | 指定提交必须位于当前提交历史链上的可验证关系 |
| echo | 原样回显 | 把请求输入未经净化地返回到结果；无效请求中禁止此行为 |
| invalid | 无效输入 | 未满足受治理形状或枚举约束且必须失败关闭的输入状态 |
| parent | 父提交 | Git 提交直接引用的上一提交，用于不可变阶段绑定 |
