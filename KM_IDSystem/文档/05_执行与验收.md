<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE056 · IDS-V0_1-STAGE056-P4 · IDS-V0_1-STAGE056-P4

**负责：** Codex 专属 KMOS worktree，Stage056 Phase 4

## 二、怎么算做完

当前阶段显示 16 项；另有 242 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE056-P4-01 | Stage056 P4 只以冻结任务包、P1/P2/P3 合同和 Stage055 已复审工件为唯一合同上下文；五个交付样例只保留固定非业务 control 元数据，不包含业务资料、授权 fixture、真实缓存、路径、页面、图片、OCR 文本、失败内容、磁盘信息或第二权威事实源。 | 已通过 |
| ACC-STAGE056-P4-02 | 交付包含五个 metadata-only 样例、HIGH=2/MEDIUM=1/LOW=1/UNKNOWN=1 控制置信度汇总、一条显式失败清单和三条候选但未排队的 Stage054 复核路由证明；不提升高可信证据，也不声明识别准确率、质量门、实际容量或实际人工复核。 | 已通过 |
| ACC-STAGE056-P4-03 | 质量限制和三条人工确认提示均为中文且不自动确认；物理缓存条目为零，三条临时候选仍需未来 owner 与容量批准，失败产物禁止自动清理，因此重跑不扫描、删除或移动目录，回滚只回到 P3 质量 control 场景。 | 已通过 |
| ACC-STAGE056-P4-04 | 未打开真实资料、PDF、图片、缓存或磁盘信息，未检测语言、调用 OCR、执行图像处理、表格提取、准确率评估、实际人工复核、缓存清理、持久队列/缓存/复核或持久化；未启动 Agent 或模型调用、消耗模型 Token、部署 OVH、激活生产、进入整阶段复审、上传或推送。 | 已遵守 |
| ACC-STAGE056-P3-01 | Stage056 P3 只以冻结任务包、P1/P2 合同和 Stage055 已复审工件为唯一合同上下文；扫描 PDF、模糊图片、表格图片、中英文混合和低质量均为固定非业务控制类别，不包含业务资料、授权 fixture、真实缓存、路径、页面、图片、OCR 文本、失败内容或第二权威事实源。 | 已通过 |
| ACC-STAGE056-P3-02 | 纯内存专项重放 P2 四条固定策略候选，覆盖五类控制类别并保留 source page 引用；五类均有显式候选、降级或失败处置，静默丢弃为零，类别不是实际 OCR、样本或表格提取结果。 | 已通过 |
| ACC-STAGE056-P3-03 | 低置信和中英文混合控制仅形成降级证据与未排队复核声明，低质量失败控制禁止自动清理和证据提升，全部不能直接进入高可信证据层；物理缓存条目为零，三条临时资格仍需未来 owner 与容量批准。 | 已通过 |
| ACC-STAGE056-P3-04 | 未读取真实资料、fixture、缓存或磁盘信息，未执行 OCR、实际人工复核、缓存创建/写入/清理、磁盘扫描或容量评估，未执行 Agent、模型调用或模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P4。 | 已遵守 |
| ACC-STAGE056-P2-01 | Stage056 P2 只以冻结任务包、P1 静态合同和 Stage055 已复审工件为唯一合同上下文；四条输入均为固定非业务 reference-only 标量记录，不包含业务资料、授权 fixture、真实缓存、路径、页面、图片、OCR 文本、失败内容或第二权威事实源。 | 已通过 |
| ACC-STAGE056-P2-02 | 纯内存切片严格接受 11 字段缓存引用输入，为四条 control 输出 P1 定义的 10 字段策略候选并保留 source page 引用；候选不是实际缓存决策、物理路径、缓存内容、页面输出或持久记录。 | 已通过 |
| ACC-STAGE056-P2-03 | 低置信、中英文混合和失败候选分别进入可解释、未排队的复核或禁止自动清理状态，均不能直接进入高可信证据层；临时候选仍需未来 owner 保留批准与容量批准，失败产物不得自动清理。 | 已通过 |
| ACC-STAGE056-P2-04 | 未读取真实资料、授权 fixture 或缓存内容，未调用 OCR、创建真实队列/按页输出/复核/质量门/持久状态，未扫描磁盘、评估容量或写入/清理缓存，未执行 Agent、模型调用或模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P3。 | 已遵守 |
| ACC-STAGE056-P1-01 | Stage056 P1 只以冻结任务包和 Stage055 已复审工件为唯一合同上下文；临时图片、中间文本和失败产物仅作为非内容类别及引用字段，不包含业务资料、授权 fixture、真实缓存、路径、页面、图片、OCR 文本、失败内容或第二权威事实源。 | 已通过 |
| ACC-STAGE056-P1-02 | 合同固定 11 字段未来缓存输入、10 字段未来策略输出、三类产物、中文简体与英文默认语言；LOW、UNKNOWN、中英文混合和失败页不能因缓存策略直接进入高可信证据层，仍由 Stage054 受控复核所有权约束。 | 已通过 |
| ACC-STAGE056-P1-03 | 未来可清理范围只可能包括明确标识的可重建临时图片和中间文本；失败产物、原始资料、manifest、evidence ledger、audit log 和已交付报告均不在自动清理范围。当前不设置路径、保留窗口、容量阈值或清理目标，不扫描磁盘、不评估容量、不创建、写入或清理缓存。 | 已通过 |
| ACC-STAGE056-P1-04 | 未读取真实资料、授权 fixture 或缓存内容，未调用 OCR、创建队列/按页输出/复核/质量门/持久状态，未执行 Agent、模型调用或模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage056 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE056-P1-LOCAL-20260813-001` | 定义 OCR 临时图片、中间文本和失败产物的引用式缓存保留与清理静态合同 | `PASS_PHASE1_OCR_CACHE_RETENTION_POLICY_BOUNDARY_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE056-P2-LOCAL-20260813-001` | 以四条固定非业务 reference-only control 投影 OCR 缓存保留策略内存候选、来源页引用和低置信/混合/失败可解释状态 | `PASS_PHASE2_OCR_CACHE_RETENTION_POLICY_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE056-P3-LOCAL-20260813-001` | 重放四条固定非业务 reference-only OCR 缓存策略候选，以五类控制元数据验证显式候选、降级或失败处置和非物理缓存边界 | `PASS_PHASE3_OCR_CACHE_RETENTION_POLICY_CONTROLLED_QUALITY_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE056-P4-LOCAL-20260813-001` | 从 P3 五类固定非业务 OCR 缓存策略 control 场景派生 metadata-only 交付样例、控制置信度汇总、失败清单、候选复核路由证明、中文确认提示和零物理缓存重跑说明 | `PASS_PHASE4_OCR_CACHE_RETENTION_POLICY_DELIVERY_RUNTIME_DISABLED` |
