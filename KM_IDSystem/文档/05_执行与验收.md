<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE074 · IDS-V0_1-STAGE074-P3 · IDS-V0_1-STAGE074-P3

**负责：** 既有唯一 KMOS 开发 worktree，Stage074 P3

## 二、怎么算做完

当前阶段显示 12 项；另有 608 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE074-P3-01 | 冻结 Stage074 任务包、Stage074 P1/P2 合同、Stage073 Review、Stage073 P1--P4 合同及 P2/P3/P4 纯内存控制报告、Stage069--072 前序控制合同与 Batch061-070 历史上传锁保持为唯一合同上下文；P3 只重放五条固定、非业务、:control: 控制投影，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE074-P3-02 | 五条专项场景机械验证 denied 无外发、summary_only 仅摘要引用、document 收紧不得升级为文本块引用、full_text_allowed 仅保留未来文本块引用和预算不足时队列/缓存/失败重试同步暂停；五条均有显式处置，静默丢弃为零。 | 已通过 |
| ACC-STAGE074-P3-03 | 每条控制场景均保留完整十八字段审计控制投影，共完成九十次字段检查；三个未来外部 API 调用候选均保留调用前审计、token_count=0、不透明控制引用与业务线白箱人工复核前置；控制投影不等于真实审计记录、来源文档或业务事实。 | 已通过 |
| ACC-STAGE074-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、本地 provider/模型选择或下载、本地 Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage074 P4。 | 已遵守 |
| ACC-STAGE074-P2-01 | 冻结 Stage074 任务包、Stage074 P1 静态合同、Stage073 Review、Stage073 P1--P4 合同及 P2/P3/P4 纯内存控制报告、Stage069--072 前序控制合同与 Batch061-070 历史上传锁保持为唯一合同上下文；P2 只接受五条固定、非业务、:control: 输入，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE074-P2-02 | 默认 denied、三档策略、data source/document 到 chunk 自动继承、document 只能收紧、owner 不逐条标记 chunk 均保持；未授权 chunk 被阻断，summary_only 与 full_text_allowed 仅保留未来授权引用，预算不足时控制候选暂停。 | 已通过 |
| ACC-STAGE074-P2-03 | 五条固定控制请求均只在内存中投影 12/10/7 队列/缓存/失败重试、16/8 成本、6 字段模型版本和 18 字段审计；provider、model、token_count=0、chunk_id 与 policy reason 均为不透明控制标签，不代表真实模型、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE074-P2-04 | 未执行真实资料访问、解析、切块、摘要生成、本地 provider/模型选择或下载、本地 Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage074 P3。 | 已遵守 |
| ACC-STAGE074-P1-01 | 冻结 Stage074 任务包、Stage073 Review、Stage073 P1--P4 合同及 P2/P3/P4 纯内存控制报告、Stage069--072 前序控制合同与 Batch061-070 历史上传锁保持为唯一合同上下文；P1 只定义静态兜底合同，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE074-P1-02 | 未来本地 Embedding 兜底路线明确为仅合同引用、未选择 provider 或模型、未执行本地 Embedding 或索引写入；默认 denied、三档策略、data source/document 到 chunk 自动继承、document 只能收紧、owner 不逐条标记 chunk 均保持。 | 已通过 |
| ACC-STAGE074-P1-03 | 未来 12/10/7 队列/缓存/失败重试、16/8 成本、6 字段模型版本和 18 字段审计形状，以及 denied、summary_only、full_text_allowed 的操作边界、预算关闭、审计前置和失败关闭均已固定；所有字段只表示控制标签，不代表真实资料、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE074-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、本地 provider/模型选择或下载、本地 Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage074 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE074-P1-LOCAL-20260821-001` |  | `PASS_LOCAL_EMBEDDING_FALLBACK_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE074-P2-LOCAL-20260821-001` |  | `PASS_PHASE2_LOCAL_EMBEDDING_FALLBACK_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE074-P3-LOCAL-20260821-001` |  | `PASS_PHASE3_LOCAL_EMBEDDING_FALLBACK_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
