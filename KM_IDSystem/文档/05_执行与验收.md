<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE087 · IDS-STAGE087-P4 · IDS-V0_1-STAGE087-P4

**负责：** 仅使用既有唯一 KMOS 开发 worktree `/Users/linzezhang/Documents/Codex/GithubProject/_scratch/KMOS-kmids-stage071-p1`；不创建额外 worktree、分支或合并请求，且不进入 Review、OVH、生产或上传。

## 二、怎么算做完

当前阶段显示 16 项；另有 881 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE087-P1-01 | 冻结 Stage087 任务包与 Stage086 Review/P1--P4 已审核控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE087-P1-02 | query、文档类型／年份／项目／设备／资料状态／证据等级六类 filter、candidate chunks、selected chunks、score、active_index_version 与 retrieval trace 只复用不透明控制字段；关键词检索基线与向量检索基线都必须存在，向量相似度不得单独决定未来结果。 | 已通过 |
| ACC-STAGE087-P1-03 | query kind、关键词／向量基线、六类过滤与 filter state、活动索引版本、candidate／selected 引用链、score explanation、证据账本引用或 retrieval trace 任一缺失时必须失败关闭；未来 PostgreSQL full-text／BM25、pgvector、embedding、过滤、混合排序和轨迹记录仍须业务线白箱在后续授权 Phase 中受控实施。 | 已通过 |
| ACC-STAGE087-P1-04 | P1 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage087 P2。 | 已遵守 |
| ACC-STAGE087-P2-01 | 冻结 Stage087 任务包、Stage086 Review/P1--P4 已审核控制工件与 Stage087 P1 静态合同保持为唯一合同上下文；P2 只执行固定、非业务、reference-only 纯内存控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE087-P2-02 | 六条固定控制请求各含 18 个不透明字段，并覆盖文档类型、年份、项目、设备、资料状态和证据等级；每条只投影 query 9、filter 8、active_index_version 7、candidate chunks 10、score 7、selected chunks 10、retrieval trace 14 与 future integration 6 个字段，共 426 个控制检查点。candidate、selected 和 trace 的活动索引版本、元数据过滤、关键词／向量基线、评分、排序策略、评分解释与证据账本引用链必须一致。 | 已通过 |
| ACC-STAGE087-P2-03 | 关键词与向量基线必须同时声明，vector-only、非固定输入、请求 Top-K、六类过滤维度、资料状态、活动索引版本、过滤合同、candidate／selected 引用、评分、排序策略、评分解释、证据账本引用或检索轨迹任一缺失时必须失败关闭；PostgreSQL full-text／BM25、pgvector、过滤、混合排序和轨迹仅保留 future-only 路由。 | 已通过 |
| ACC-STAGE087-P2-04 | P2 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage087 P3。 | 已遵守 |
| ACC-STAGE087-P3-01 | 冻结 Stage087 任务包、Stage086 Review/P1--P4 已审核控制工件与 Stage087 P1/P2 保持为唯一合同上下文；P3 只重放 P2 的六条固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE087-P3-02 | 八类场景覆盖关键词、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引版本轨迹；材料牌号、设备型号、标准号和语义相似只表示场景类别，P2 的六条控制投影及其 426 个字段检查点只提供不透明引用；每场景固定 31 个字段，共 248 个检查点。 | 已通过 |
| ACC-STAGE087-P3-03 | 关键词与向量基线、vector-only 拒绝、六维过滤、活动索引版本、candidate／selected／score／trace／evidence ledger 引用链、Top-K／排序解释／有效性与旧索引版本轨迹必须完整；P2 输出偏离、关键引用缺失、非固定输入、控制引用泄漏或任一运行信号均失败关闭。八场景都明确要求业务线白箱人工处理，未记录自动审批、自动业务建议、自动写入或自动索引切换。 | 已通过 |
| ACC-STAGE087-P3-04 | P3 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮本地验收完成后才可在新 run 进入 Stage087 P4。 | 已遵守 |
| ACC-STAGE087-P4-01 | 冻结 Stage087 任务包、Stage086 Review/P1--P4 已审核控制工件与 Stage087 P1/P2/P3 保持为唯一合同上下文；P4 只从 P3 的八条固定、非业务、reference-only 控制场景派生 metadata-only 交付记录，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE087-P4-02 | P4 只派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口记录、4 条参数回滚说明和 4 条中文反馈；各组固定为 14/14/10/15/14/9 字段，共 572 个检查点。所有 query、filter、candidate、selected、score、活动索引版本、trace 与证据账本均只复用 P3 的不透明控制引用，未写入任何实际记录。 | 已通过 |
| ACC-STAGE087-P4-03 | P3 的关键词／向量基线、vector-only 拒绝、六维过滤、活动索引版本、candidate／selected／score／trace／evidence ledger 引用链、Top-K／排序解释／有效性与旧索引版本轨迹必须完整；P3 输出偏离、关键引用缺失、交付形状不完整、自动补全／业务建议／参数回退／索引切换或任一运行信号均失败关闭。八场景和四条参数说明都明确要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE087-P4-04 | P4 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索样例／trace／过滤／有效性报告／证据缺口读写、检索参数写入或回滚、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮本地验收完成后才可在新 run 进入 Stage087 Review。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE087-P1-2026-08-23-local` | Stage087 P1 静态检索轨迹合同（本地零运行时） | `PASS_RETRIEVAL_TRACE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE087-P2-2026-08-23-local` | Stage087 P2 检索轨迹纯内存受控切片（本地零运行时） | `PASS_RETRIEVAL_TRACE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE087-P3-2026-08-23-local` | Stage087 P3 检索轨迹受控场景（本地零运行时） | `PASS_RETRIEVAL_TRACE_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE087-P4-2026-08-23-local` | Stage087 P4 检索轨迹交付证据、回滚与中文反馈（本地零运行时） | `PASS_RETRIEVAL_TRACE_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
