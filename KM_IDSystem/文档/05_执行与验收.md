<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE069 · IDS-V0_1-STAGE069-P4 · IDS-V0_1-STAGE069-P4

**负责：** Codex 专属 KMOS worktree，Stage069 Phase 4

## 二、怎么算做完

当前阶段显示 16 项；另有 506 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE069-P4-01 | 冻结 Stage069 任务包、P1/P2/P3 合同、Stage068 本地整阶段机械复审、根策略锁、操作说明与 Batch061-070 上传锁保持为唯一合同上下文；P4 五条样例均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE069-P4-02 | P4 仅在内存中从 P3 五条固定控制场景派生五条策略样例、五条十八字段审计日志投影样例、五条零 Token/零成本估算、五条显式失败处理、五条未外发控制引用记录、四键查询说明、三条中文确认和十二类失败关闭；一个 denied、两个 summary_only、一个 full_text_allowed、一个预算不足暂停、三个未来调用候选和九十次审计字段检查只表示固定控制形状。 | 已通过 |
| ACC-STAGE069-P4-03 | denied 固定阻断，摘要/全文控制引用须业务线白箱人工复核且本 phase 运行时关闭，预算不足固定暂停；控制样例、审计日志投影、成本、失败处理、未外发原因、查询和回滚说明不能替代来源文档、形成真实审计/成本/外发事实或自动业务建议，回滚仅可回到 P3 纯内存控制场景。 | 已通过 |
| ACC-STAGE069-P4-04 | 未执行真实资料访问、解析、切块、摘要、文本外发、Embedding 队列、缓存、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算或记录、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage069 Review。 | 已遵守 |
| ACC-STAGE069-P3-01 | 冻结 Stage069 任务包、P1/P2 合同、Stage068 本地整阶段机械复审、根策略锁、操作说明与 Batch061-070 上传锁保持为唯一合同上下文；P3 只重放五条固定、非业务、reference-only 控制记录，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE069-P3-02 | 纯内存场景分别核验 denied 不形成外发载荷、summary_only 仅保留摘要引用类别、document 将 full_text_allowed 收紧为 summary_only、full_text_allowed 仅保留文本块引用类别与预算不足暂停；五条二十三字段场景结果、五条十八字段审计投影和九十次审计字段检查都只表示固定控制形状，不代表真实资料、摘要、文本、队列、provider、模型、Token、成本或审计记录。 | 已通过 |
| ACC-STAGE069-P3-03 | 每个控制场景均有审计投影，三个未来调用候选均须先具审计投影；任一 P2 形状、策略边界、预算暂停、审计字段或审计引用异常均失败关闭，四个非 denied 情形保持业务线白箱人工处理；控制结果不能替代来源文档、形成业务事实或自动业务建议。 | 已通过 |
| ACC-STAGE069-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、Embedding 队列、缓存、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算、审计写入、索引、数据库、持久化、Agent、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P4。 | 已遵守 |
| ACC-STAGE069-P2-01 | 冻结 Stage069 任务包、P1 静态合同、Stage068 本地整阶段机械复审、根策略锁、操作说明与 Batch061-070 上传锁保持为唯一合同上下文；P2 只接受五条固定、非业务、十五字段、:control: 引用请求，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、摘要、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE069-P2-02 | 纯内存切片严格核验五条固定输入并投影五条二十三字段策略解析、五条十二字段 Embedding 队列意图、五条八字段零成本/模型版本字段和五条十八字段审计投影，覆盖默认 denied、摘要继承、document 将全文收紧为摘要、全文控制与预算不足暂停；所有标签不代表真实 data source、document、chunk、摘要、文本载荷、队列、缓存、provider、模型、Token、成本或审计记录。 | 已通过 |
| ACC-STAGE069-P2-03 | 未知策略与 document 放宽均失败关闭，document 只能收紧且 chunk 自动继承有效策略、不得逐条标记；denied 固定阻断队列意图、预算不足固定暂停，任何策略解析、队列意图、成本/模型版本或审计投影均不能替代来源文档、形成业务事实或自动业务建议，策略例外须经业务线白箱人工处理。 | 已通过 |
| ACC-STAGE069-P2-04 | 未执行真实资料访问、解析、切块、摘要、文本外发、Embedding 队列、缓存、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算、审计、索引、数据库、持久化、Agent、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P3。 | 已遵守 |
| ACC-STAGE069-P1-01 | 冻结 Stage069 任务包、Stage068 本地整阶段机械复审、根策略锁、操作说明与 Batch061-070 上传锁保持为唯一合同上下文；P1 未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、摘要、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE069-P1-02 | P1 固定默认 denied、denied/summary_only/full_text_allowed 三档、data source/document→chunk 自动继承、15 字段仅引用输入、23 字段未来策略输出、12 字段未来 Embedding 队列、8 字段成本与模型版本、18 字段未来审计和 13 类失败关闭；所有字段只代表未来 schema，不填入实际资料、摘要、chunk、成本、模型、Token 或审计结论。 | 已通过 |
| ACC-STAGE069-P1-03 | 未知或放宽策略均失败关闭，document 只能请求更严格策略，chunk 只能自动继承有效 document 策略且不得逐条标记；任何策略解析、外部模型输出、成本或审计字段均不能替代来源文档、形成业务事实或自动业务建议，策略例外须经业务线白箱人工处理。 | 已通过 |
| ACC-STAGE069-P1-04 | 未执行真实资料访问、解析、切块、摘要、文本外发、Embedding 队列、缓存、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算、审计、索引、数据库、持久化、Agent、OVH、生产、P2、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage069 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE069-P1-LOCAL-20260814-001` | 本地静态外部 API 策略继承合同、治理投影与零运行时边界验证 | `PASS_LOCAL_STAGE069_PHASE1_EXTERNAL_API_POLICY_INHERITANCE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE069-P2-LOCAL-20260814-001` | 只在内存中重放五条固定、非业务、reference-only 外部 API 策略控制请求，投影 data source/document→chunk 的有效策略、Embedding 队列意图、缓存关闭、零成本/模型版本字段和审计字段，机械确认默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停；不读取、打开、解析、总结、外发、创建或写入真实业务资料、摘要、chunk、队列、缓存、审计、成本、模型或业务结论。 | `PASS_LOCAL_PHASE2_EXTERNAL_API_POLICY_INHERITANCE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE069-P3-LOCAL-20260814-001` | 只在内存中重放 Stage069 P2 的五条固定、非业务、reference-only 外部 API 策略控制记录，机械核验 denied 不形成外发载荷、summary_only 只保留摘要引用类别、document 收紧保持摘要边界、full_text_allowed 只保留文本块引用类别、预算不足暂停，以及每个未来调用候选均已有十八字段审计投影；不读取、打开、解析、总结、外发、创建或写入真实业务资料、摘要正文、文本块、chunk、队列、缓存、审计、成本、模型或业务结论。 | `PASS_LOCAL_PHASE3_EXTERNAL_API_POLICY_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE069-P4-LOCAL-20260815-001` | 只在内存中从 Stage069 P3 的五条固定、非业务、reference-only 外部 API 策略控制场景派生策略样例、审计日志投影样例、零成本估算、失败处理、未外发控制引用记录、查询说明和回滚说明；不读取、打开、解析、总结、外发、创建、写入、查询或回退真实业务资料、摘要正文、文本块、chunk、队列、缓存、审计、成本、模型或业务结论。 | `PASS_LOCAL_PHASE4_EXTERNAL_API_POLICY_DELIVERY_RUNTIME_DISABLED` |
