<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE057 · IDS-V0_1-STAGE057-P4 · IDS-V0_1-STAGE057-P4

**负责：** Codex 专属 KMOS worktree，Stage057 P4

## 二、怎么算做完

当前阶段显示 16 项；另有 262 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE057-P4-01 | Stage057 P4 只以冻结任务包、P1/P2/P3 合同和 Stage056 已复审工件为唯一合同上下文；六个交付样例只保留固定非业务 control 元数据、显式处置与来源定位引用，不包含业务资料、真实表格、工作表、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P4-02 | 交付包含 6 个 metadata-only 非真实表格事实样例、5 个字段引用标签、6 条控制质量处置、零静默丢弃和 6 条人工处理建议；字段引用报告不构成真实 schema 推断、字段识别、事实抽取、来源追溯或数值统计。 | 已通过 |
| ACC-STAGE057-P4-03 | 质量限制与 3 条人工确认提示均为中文且不自动确认；重解析与事实回滚说明只允许回到 P3 control 状态，当前不存在真实文件、事实库、typed value、数据库 migration、事实删除、覆写或回滚动作。 | 已通过 |
| ACC-STAGE057-P4-04 | 未读取、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/质量验证、事实/证据写入、数值统计、真实重解析或事实回滚、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 Review。 | 已遵守 |
| ACC-STAGE057-P3-01 | Stage057 P3 只以冻结任务包、P1/P2 合同和 Stage056 已复审工件为唯一合同上下文；六类异常仅为固定非业务控制元数据，不包含业务资料、真实表格、工作表、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P3-02 | 纯内存专项重放 P2 两条控制记录及 10 个空值事实候选，覆盖空表、合并单元格、单位混乱、日期格式不一、异常值和重复行；六类均有显式人工处置且静默丢弃为零，控制来源文档、工作表、行列范围和证据引用形状保持可追溯。 | 已通过 |
| ACC-STAGE057-P3-03 | 控制来源引用形状不等于真实源文件、真实行列或真实证据记录已验证；不解合并、不规范化单位或日期、不去重、不评估实际异常值、不生成 typed value，未验证数值必须阻断统计和模型确定性数值结论。 | 已通过 |
| ACC-STAGE057-P3-04 | 未读取、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实质量验证、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P4。 | 已遵守 |
| ACC-STAGE057-P2-01 | Stage057 P2 只以冻结任务包、P1 合同和 Stage056 已复审工件为唯一合同上下文；两条输入均为固定、非业务、reference-only 控制记录，严格使用 P1 的 12 字段且不包含业务资料、真实表格、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P2-02 | 纯内存切片形成 2 个控制 schema profile、10 个字段/19 字段事实候选和 2 个 metadata-only RAG 摘要候选；typed value 与业务字段值保持空值，每个候选保留来源文档、工作表、行列范围和证据引用但未创建事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE057-P2-03 | RAG 摘要候选只能引用事实候选，不能替代结构化事实或成为数值统计依据；数值候选不含数值，模型不能根据文本猜测数值，未验证数值不能形成确定事实或统计结论；无效控制输入必须 REJECTED。 | 已通过 |
| ACC-STAGE057-P2-04 | 未读取、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema 推断、真实字段识别、真实事实抽取、数值统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P3。 | 已遵守 |
| ACC-STAGE057-P1-01 | Stage057 P1 只以冻结任务包和 Stage056 已复审工件为唯一合同上下文；XLSX/CSV、生产记录和质检记录只登记引用字段、接口名称与静态边界，不包含业务资料、表格内容、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P1-02 | 合同固定 12 字段引用输入、19 字段未来事实输出、七类字段语义和五类来源位置；来源文档保持权威，未来事实必须关联来源文档、工作表、行列范围和证据引用，当前没有真实输入、事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE057-P1-03 | 数值统计未来只能使用带来源位置与证据绑定的结构化事实；模型不能根据文本猜测数值，未验证数值不能形成确定事实或统计结论，RAG 摘要不能替代事实或成为数值权威；无法识别的字段、单位、日期、质量结果或表格结构必须等待人工处理。 | 已通过 |
| ACC-STAGE057-P1-04 | 未读取或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行文件检测、parser、schema 推断、字段识别、事实抽取、摘要、统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE057-P1-LOCAL-20260813-001` | 定义 XLSX/CSV、生产记录和质检记录的结构化接入、字段语义、来源定位、事实与 RAG 摘要分离及数值事实静态合同 | `PASS_PHASE1_XLSX_CSV_INGESTION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE057-P2-LOCAL-20260813-001` | 以两条固定非业务 reference-only 控制记录在内存中投影 XLSX/CSV schema、字段候选、19 字段空值事实候选、来源定位与 metadata-only RAG 摘要候选 | `PASS_PHASE2_XLSX_CSV_INGESTION_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE057-P3-LOCAL-20260813-001` | 重放 P2 两条固定非业务 reference-only 控制记录的候选，验证空表、合并单元格、单位混乱、日期格式不一、异常值和重复行六类显式处置、控制来源引用形状与未验证数值阻断 | `PASS_PHASE3_XLSX_CSV_INGESTION_CONTROLLED_QUALITY_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE057-P4-LOCAL-20260813-001` | 从 P3 六类固定非业务 reference-only control 场景派生 XLSX/CSV metadata-only 交付样例、字段引用报告、质量结果、人工处理建议和重解析/事实回滚说明 | `PASS_PHASE4_XLSX_CSV_INGESTION_DELIVERY_RUNTIME_DISABLED` |
