<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE079 · IDS-V0_1-STAGE079-P2 · IDS-V0_1-STAGE079-P2

**负责：** 既有唯一 KMOS 开发 worktree，Stage079 P2

## 二、怎么算做完

当前阶段显示 8 项；另有 713 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE079-P1-01 | 冻结 Stage079 任务包、Stage078 Review 与 Stage078 已审核 P1--P4 索引冒烟测试控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE079-P1-02 | 未来索引版本、活动指针、构建中版本、影子索引和冒烟测试只复用既有控制字段形状；每次未来批量导入建立隔离候选且不得覆盖活动版本，旧活动索引在构建、冒烟和切换前继续服务；所有字段只表示静态工程边界。 | 已通过 |
| ACC-STAGE079-P1-03 | 未来原子切换必须满足候选构建完成、候选隔离、旧活动服务连续、冒烟测试通过和上一活动版本保留；冒烟失败、缺失、切换失败或回退目标未保留时活动指针保持不变，回退只可指向保留的上一活动版本，未来例外仍须业务线白箱人工处理。 | 已通过 |
| ACC-STAGE079-P1-04 | P1 期间不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、原子切换、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage079 P2。 | 已遵守 |
| ACC-STAGE079-P2-01 | 冻结 Stage079 任务包、Stage079 P1 和 Stage078 Review/P1--P4 已审核索引冒烟测试控制工件保持为唯一合同上下文；P2 只接收五条固定、非业务、reference-only 控制请求，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE079-P2-02 | 每条控制请求记录 index_version、document_scope_ref、chunk_count=0 与 embedding_model_ref，并投影候选/活动/上一活动版本和影子索引引用；候选与影子保持隔离，候选不得覆盖活动版本，旧活动版本在控制投影期间继续服务。 | 已通过 |
| ACC-STAGE079-P2-03 | 候选构建未完成、影子不隔离、冒烟未执行或失败时未来切换候选关闭且活动指针投影保持不变；冒烟完整通过只产生未应用的未来切换候选，切换失败同样保持活动版本，回退候选只指向保留的上一活动版本。 | 已通过 |
| ACC-STAGE079-P2-04 | P2 不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、原子切换、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage079 P3。 | 已遵守 |

## 三、已经做了什么（当前阶段 2 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE079-P1-LOCAL-20260821-001` |  | `PASS_ATOMIC_INDEX_SWITCH_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE079-P2-LOCAL-20260821-001` |  | `PASS_ATOMIC_INDEX_SWITCH_CONTROL_SLICE_RUNTIME_DISABLED` |
