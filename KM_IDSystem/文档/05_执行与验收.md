<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE093 · IDS-STAGE093-P4 · IDS-V0_1-STAGE093-P4

**负责：** KMOS 开发负责人；本阶段使用唯一既有开发工作树、单一分支与本地验证节奏。

## 二、怎么算做完

当前阶段显示 16 项；另有 1007 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE093-P1-01 | 冻结 Stage093 任务包与 Stage092 Review 已复审证据风险评分控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，来源文档、真实证据账本与业务线白箱人工复核继续承担业务事实权威。 | 已通过 |
| ACC-STAGE093-P1-02 | 可信等级引用固定为 14 个未来字段和 10 个未来关联字段，A/B/C/D/E 完整；关键结论未来必须关联 evidence_id 或 evidence_gap，可信等级不能替代该关联。 | 已通过 |
| ACC-STAGE093-P1-03 | 可信等级分配规则、阈值和业务判定保持业务线白箱 owner 的后续前置；低等级伪装高可信、撤回或投毒疑似自动采纳、关键引用缺失、未经授权的等级规则或阈值均失败关闭。 | 已通过 |
| ACC-STAGE093-P1-04 | P1 期间不执行真实资料或证据账本访问、风险计算、可信等级变更、撤回、投毒处理、报告状态更新、数据库、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮本地验收完成后才可在新 run 进入 Stage093 P2。 | 已遵守 |
| ACC-STAGE093-P2-01 | 冻结 Stage093 任务包、Stage093 P1 静态合同与已复审 Stage092 证据风险评分控制工件保持为唯一合同上下文；P2 只处理六条固定、非业务、reference-only 的纯内存控制输入，只绑定前序控制引用，不建立第二权威事实源，来源文档、真实证据账本与业务线白箱人工复核继续承担业务事实权威。 | 已通过 |
| ACC-STAGE093-P2-02 | 六条固定控制请求各含 27 个不透明字段，并投影可信等级 schema binding 8、可信等级关联 10、检索证据捕获 binding 11、五类风险引用 6、可信等级分配 14、撤回 8、投毒防护 8、关键结论 binding 7、降级 12、报告状态影响 8 与 future integration 8 个字段，共 600 个控制检查点；可信等级与 document、chunk、fact、query、answer、report 的 10 字段引用链保持一致。 | 已通过 |
| ACC-STAGE093-P2-03 | 资料不足场景只以 evidence_gap 控制引用绑定关键结论，其他场景保持 evidence_id/evidence_gap 控制引用；低可信、冲突、过期和撤回保持降级候选，疑似投毒保持隔离候选，报告状态影响只保留未来引用，可信等级分配规则、阈值和业务判定继续等待业务线白箱 owner。固定输入之外的内容、前序合同或引用链缺失、低等级伪装为高等级或自动业务采纳均保持失败关闭。 | 已通过 |
| ACC-STAGE093-P2-04 | P2 期间不执行真实资料、检索、回答、报告或证据账本访问、数据库 schema 或连接、实际来源、OCR、版本、复核或冲突评估、实际风险计算、可信等级分配或变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage093 P3。 | 已遵守 |
| ACC-STAGE093-P3-01 | 冻结 Stage093 任务包、Stage093 P1/P2 控制工件与已复审 Stage092 证据风险评分控制工件保持为唯一合同上下文；P3 只重放六条固定、非业务、reference-only 控制投影，不建立第二权威事实源，来源文档、真实证据账本与业务线白箱人工复核继续承担业务事实权威。 | 已通过 |
| ACC-STAGE093-P3-02 | 七个纯内存场景覆盖无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料与低等级伪装高可信结论；每场景固定 32 个控制字段，共 224 个场景字段检查点，P2 的六条、十一组投影、600 个字段检查点与 evidence／document／chunk／fact／query／answer／report 关联保持一致。 | 已通过 |
| ACC-STAGE093-P3-03 | 无内部证据保持 evidence_gap 且不伪装 evidence_id；低 OCR、旧版本、冲突与撤回维持降级候选，疑似恶意资料维持隔离候选；撤回只声明未来报告状态影响引用，不更新真实报告；D 等级证据不得伪装支撑高可信结论。P2 输出偏离、关键引用缺失、控制引用透明化、等级规则越权或运行信号出现时失败关闭，全部场景要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE093-P3-04 | P3 期间不执行真实资料、检索、回答、报告或证据账本访问、数据库 schema 或连接、实际来源、OCR、版本、复核或冲突评估、实际风险计算、可信等级分配或变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage093 P4。 | 已遵守 |
| ACC-STAGE093-P4-01 | 冻结 Stage093 任务包、Stage093 P1/P2/P3 控制工件与已复审 Stage092 证据风险评分控制工件保持为唯一合同上下文；P4 只派生 control-only 交付记录，不建立第二权威事实源，来源文档、真实证据账本与业务线白箱人工复核继续承担业务事实权威。 | 已通过 |
| ACC-STAGE093-P4-02 | P4 固定派生 evidence ledger 样例、证据等级报告、撤回影响、回归、不可作为结论依据类型、降级说明和撤回／恢复说明的 7/7/7/7/7/4/2 条控制记录，共 517 个字段检查；所有引用保持 P2／P4 不透明控制前缀。 | 已通过 |
| ACC-STAGE093-P4-03 | 无内部证据保持 evidence_gap；低 OCR、旧版本、冲突与撤回保持降级说明，疑似恶意资料保持隔离说明，低等级不得伪装支撑高可信结论；撤回影响只声明未来报告状态复核，不更新真实报告，降级、撤回与恢复继续要求业务线白箱人工审批。 | 已通过 |
| ACC-STAGE093-P4-04 | P4 期间不写入真实 evidence ledger、报告或审计日志，不执行真实资料、检索、数据库、风险计算、可信等级变更、撤回、恢复、投毒处置、模型 Token、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage093 Review。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE093-P1-2026-08-24-local` | Stage093 证据可信等级 A/B/C/D/E Phase 1 静态合同（本地零运行时） | `PASS_EVIDENCE_GRADE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE093-P2-2026-08-24-local` | Stage093 证据可信等级 A/B/C/D/E Phase 2 纯内存受控最小切片（本地零运行时） | `PASS_EVIDENCE_GRADE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE093-P3-2026-08-24-local` | Stage093 证据可信等级 A/B/C/D/E Phase 3 异常场景验证（本地零运行时） | `PASS_EVIDENCE_GRADE_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE093-P4-2026-08-24-local` | Stage093 证据可信等级 A/B/C/D/E Phase 4 交付控制验证（本地零运行时） | `PASS_EVIDENCE_GRADE_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
