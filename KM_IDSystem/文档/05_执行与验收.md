<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE063 · IDS-V0_1-STAGE063-P4 · IDS-V0_1-STAGE063-P4

**负责：** Codex 专属 KMOS worktree，Stage063 Phase 4

## 二、怎么算做完

当前阶段显示 16 项；另有 386 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE063-P4-01 | 冻结 Stage063 任务包、P1/P2/P3 控制工件、Stage062 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；P4 的 JSONL 样例、覆盖率、低质量清单、回归结果和回退说明均为固定、非业务、reference-only 控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或来源片段。 | 已通过 |
| ACC-STAGE063-P4-02 | P4 从 P3 六类控制场景派生 6 条 metadata-only chunk JSONL 样例、控制覆盖率报告、6 条低质量待人工清单、控制回归结果、策略适用边界、回到 P3 的重生成/版本回退说明和 3 条中文确认；所有引用只保持 :control: 形状，不创建真实 chunk、覆盖率、质量结果、来源绑定、版本、索引或持久化状态。 | 已通过 |
| ACC-STAGE063-P4-03 | 长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk 均明确保留业务线人工白箱处理；控制覆盖率不等于真实文档覆盖率，低质量清单不等于真实质量测量，回归结果不等于真实质量回归，重生成/版本回退说明只回到 P3 control 状态。 | 已通过 |
| ACC-STAGE063-P4-04 | 未读取、打开、检测、解析、切分、写入、重生成或回退真实文档、页面、章节、表格、来源片段、parser 输出、chunk 或 fixture，未执行真实身份/版本、重复检测或去重、分类、覆盖率、质量、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage063 Review。 | 已遵守 |
| ACC-STAGE063-P3-01 | 冻结 Stage063 任务包、P1/P2 控制工件、Stage062 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；六类场景均为固定、非业务、reference-only 控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或来源片段。 | 已通过 |
| ACC-STAGE063-P3-02 | P3 纯内存重放 P2 的三条十四字段控制候选，覆盖长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk 写入边界；六类均有显式处置、静默丢弃为零且全部要求业务线人工白箱处理。 | 已通过 |
| ACC-STAGE063-P3-03 | 每个场景保留 document、page、section、parser output、表格上下文与来源片段六维控制引用形状；该形状不等于真实来源或页码已经验证。重复场景仅确认本控制模块没有发起 embedding 或索引写入，不检测真实重复项、不计算身份/版本/哈希，也不形成真实去重结论。 | 已通过 |
| ACC-STAGE063-P3-04 | 未读取、打开、检测、解析或切分真实文档、页面、章节、表格、来源片段、parser 输出或 fixture，未执行真实章节检测、chunk 身份/版本/哈希、重复检测或去重、分类、覆盖率、质量、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage063 P4。 | 已遵守 |
| ACC-STAGE063-P2-01 | 冻结 Stage063 任务包、P1 静态合同、Stage062 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；三条输入均为固定、非业务、reference-only 控制请求，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或来源片段。 | 已通过 |
| ACC-STAGE063-P2-02 | 纯内存切片严格核验 P1 八字段控制输入，并投影三条严格十四字段章节感知切块候选；候选一对一覆盖工程步骤、验收条款和参数表三类保护语义表面，并保留 document、page、section、parser output、表格上下文和来源片段六维控制引用。 | 已通过 |
| ACC-STAGE063-P2-03 | 三条候选均保持原子、必须人工复核且不成为来源文档、业务事实或决策权威；未知、重排或篡改控制输入必须拒绝，无法确认真实章节边界、追溯或跨页参数表时必须转人工处理。chunk 身份/版本/哈希、真实语义分类、覆盖率、质量回归与质量降级继续由后续阶段负责。 | 已通过 |
| ACC-STAGE063-P2-04 | 未读取、打开、检测、解析或切分真实文档、页面、章节、表格、来源片段、parser 输出或 fixture，未执行真实章节检测、chunk 身份/版本/哈希、分类、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage063 P3。 | 已遵守 |
| ACC-STAGE063-P1-01 | 冻结 Stage063 任务包、Stage062 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；八字段引用输入和十四字段未来输出只定义接口，不建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE063-P1-02 | 未来切块明确保留 document、page、section、parser output、表格上下文和来源片段六类引用；工程步骤、验收条款和参数表不得按固定字符数任意切断，当前未创建任何真实 chunk、追溯绑定、身份、版本、分类、覆盖率、索引或 embedding。 | 已通过 |
| ACC-STAGE063-P1-03 | 来源文档和业务线人工复核保持权威；chunk、模型文本和静态合同不能替代来源文档、成为业务事实权威或形成决策结论。缺少引用、无法确认安全章节边界或遇到跨页参数表时必须关闭并转人工处理。 | 已通过 |
| ACC-STAGE063-P1-04 | 未读取、打开、检测、解析或切分真实资料或 fixture，未执行 parser、章节检测、chunk_hash、分类、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage063 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE063-P1-LOCAL-20260814-001` | 只定义冻结 Stage063 章节感知切块的引用式静态合同，覆盖章节边界、工程步骤/验收条款/参数表保护面、document/page/section/parser output 追溯、失败关闭、中文反馈和回滚边界；不读取、解析或切分真实业务资料。 | `PASS_LOCAL_PHASE1_CHAPTER_AWARE_CHUNKING_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE063-P2-LOCAL-20260814-001` | 只以三条固定、非业务、reference-only 章节感知切块控制请求在内存中投影三条待人工复核候选，机械验证 P1 的八字段输入、十四字段输出、三类工程语义保护面、六维追溯引用和后续阶段关闭边界；不读取、解析、检测或切分真实业务资料。 | `PASS_LOCAL_PHASE2_CHAPTER_AWARE_CHUNKING_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE063-P3-LOCAL-20260814-001` | 只重放三条固定、非业务、reference-only 章节感知切块控制候选，对长文档、跨页参数表、施工步骤、参数表、引用页码和重复写入边界输出六类显式人工处置；不读取、解析、检测、切分、去重、embedding、索引或写入真实业务资料。 | `PASS_LOCAL_PHASE3_CHAPTER_AWARE_CHUNKING_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE063-P4-LOCAL-20260814-001` | 只从 P3 六类固定、非业务、reference-only 章节感知切块控制场景派生内存 JSONL 样例、控制覆盖率报告、低质量待人工清单、控制回归结果、策略边界、重生成/版本回退说明和中文确认；不读取、解析、检测、切分、写入或回退真实业务资料。 | `PASS_LOCAL_PHASE4_CHAPTER_AWARE_CHUNKING_DELIVERY_RUNTIME_DISABLED` |
