<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE071 · IDS-V0_1-STAGE071-P3 · IDS-V0_1-STAGE071-P3

**负责：** Codex 专属 KMOS worktree，Stage071 P3

## 二、怎么算做完

当前阶段显示 12 项；另有 546 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE071-P3-01 | 冻结 Stage071 任务包、P1/P2 合同、Stage070 本地整阶段机械复审、Stage070 P1 队列/缓存合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P3 只重放七条固定、非业务、reference-only 控制记录，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE071-P3-02 | 纯内存场景严格重放 P2 的七条 10/18/14/10/7/18 控制投影并输出七条 35 字段专项结果，验证 denied 不外发、summary_only 只保留摘要引用、document 将全文收紧为摘要、full_text_allowed 只保留文本块引用、本批次/自然月/单任务三重预算关闭均暂停；所有标签、引用和零值只表示控制形状，不代表真实资料、金额、预算、Token、成本、队列、缓存、重试、审计或业务结论。 | 已通过 |
| ACC-STAGE071-P3-03 | 七条控制场景均有完整 18 字段审计投影，三个未来调用候选均先具审计投影，并核验策略原因、provider/model/model_version、零 Token/成本、chunk、队列与预算状态关联；任一 P2 形状、策略边界、三类预算暂停、审计字段或审计引用异常均失败关闭，六个非 denied 情形保持业务线白箱人工处理，控制结果不能替代来源文档、形成业务事实或自动建议。 | 已通过 |
| ACC-STAGE071-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、成本估算、预算查找、单任务上限判断、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage071 P4。 | 已遵守 |
| ACC-STAGE071-P2-01 | 冻结 Stage071 任务包、P1 静态合同、Stage070 本地整阶段机械复审、Stage070 P1 队列/缓存合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P2 未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE071-P2-02 | 纯内存切片严格核验七条固定、非业务、16 字段、:control: 引用请求，复用默认 denied 与 data source/document→chunk 自动继承，投影 16 字段成本治理、12/10/7 队列/缓存/重试和 18 字段审计形状；provider、model、model_version、token、chunk 与 policy reason 只作为控制标签或零值投影。 | 已通过 |
| ACC-STAGE071-P2-03 | denied 阻止未授权 chunk 外发、成本治理、队列、缓存和重试；summary_only 只保留未来已授权摘要引用；full_text_allowed 仍需未来授权、审计与业务线白箱人工复核；本批次、自然月或单任务三重预算门任一未通过均暂停控制候选，不形成持久化、调度或外部调用。 | 已通过 |
| ACC-STAGE071-P2-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、成本估算、预算查找、单任务上限判断、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage071 P3。 | 已遵守 |
| ACC-STAGE071-P1-01 | 冻结 Stage071 任务包、Stage070 本地整阶段机械复审、Stage070 P1 队列/缓存合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P1 未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE071-P1-02 | 复用默认 denied、denied/summary_only/full_text_allowed 三档策略和 data source/document→chunk 自动继承；静态固定 16 个仅引用成本治理输入、16 个未来成本治理字段、本批次/自然月/单任务三重预算范围、8 个成本/模型版本字段、18 个审计字段和 14 类失败关闭，且 P1 不写入具体金额、Token、预算或实际成本。 | 已通过 |
| ACC-STAGE071-P1-03 | denied 不形成外发载荷、队列、缓存、重试或成本治理；summary_only 仅允许未来已授权摘要引用，full_text_allowed 仅可在未来授权、三重预算、审计和业务线白箱人工复核后处理文本块；任一预算范围未知、不足或超限均失败关闭，任何成本/预算/审计字段都不能替代来源文档、形成业务事实或自动建议。 | 已通过 |
| ACC-STAGE071-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、成本估算、预算查找、单任务上限判断、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P2/P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage071 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE071-P1-LOCAL-20260815-001` |  | `PASS_LOCAL_EMBEDDING_COST_GOVERNOR_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE071-P2-LOCAL-20260815-001` |  | `PASS_LOCAL_EMBEDDING_COST_GOVERNOR_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE071-P3-LOCAL-20260815-001` |  | `PASS_LOCAL_EMBEDDING_COST_GOVERNOR_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
