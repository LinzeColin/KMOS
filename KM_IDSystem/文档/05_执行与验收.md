<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE083 · IDS-STAGE083-P4 · IDS-V0_1-STAGE083-P4

**负责：** 既有唯一 KMOS 开发 worktree 已完成 Stage083 P4 纯内存交付证据与治理兼容本地验收；本 run 停在 IDS-STAGE083-REVIEW-GATE 之前，不进入 Review

## 二、怎么算做完

当前阶段显示 16 项；另有 797 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE083-P3-01 | 冻结 Stage083 任务包、Stage083 P1/P2 与 Stage082 Review/P1--P4 已审核旧索引保留策略控制工件保持为唯一合同上下文；P3 只重放 P2 的五条固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE083-P3-02 | 八类场景覆盖关键词、材料牌号、设备型号、标准号、语义相似、五维过滤组合、Top-K／排序解释／结果有效性和旧索引版本轨迹；材料牌号、设备型号、标准号和语义相似只表示场景类别，所有引用均为不透明控制标签，未读取、匹配或计算真实业务值。 | 已通过 |
| ACC-STAGE083-P3-03 | P2 结构、关键词基线、vector-only 拒绝、五类过滤维度、候选／选择／评分解释／证据账本引用链与活动索引版本轨迹必须完整；P2 输出偏离、字段缺失、非固定控制标签或任一运行信号均失败关闭，全部场景仍需业务线白箱人工处理。 | 已通过 |
| ACC-STAGE083-P3-04 | P3 不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage083 P4。 | 已遵守 |
| ACC-STAGE083-P4-01 | 冻结 Stage083 任务包、Stage083 P1/P2/P3 与 Stage082 Review/P1--P4 已审核旧索引保留策略控制工件保持为唯一合同上下文；P4 只从八类固定、非业务、reference-only P3 场景派生 metadata-only 交付证据，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库、物理索引或检索参数。 | 已通过 |
| ACC-STAGE083-P4-02 | P4 交付八条检索样例、八条 trace 日志、八条过滤结果、八条有效性测试报告、八条检索不足或证据缺口记录、三条参数回滚说明和四条中文反馈；全部引用保持不透明 P2 控制标签，P3 的五条控制请求、250 次字段检查、八个场景、26 个场景字段和208次场景字段检查完整，未写入实际样例、trace、过滤、报告、缺口记录或参数。 | 已通过 |
| ACC-STAGE083-P4-03 | 检索不足或证据缺口逐场景显式处置，自动补全、自动业务推荐和自动参数回滚均关闭；未来参数变更必须具备版本化参数、业务线白箱批准和可验证回退目标。中文反馈清楚说明控制边界，不作检索、生产或自动化承诺。 | 已通过 |
| ACC-STAGE083-P4-04 | P4 不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索样例／trace／证据账本读写、检索参数写入或回滚、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage083 Review。 | 已遵守 |
| ACC-STAGE083-P2-01 | 冻结 Stage083 任务包、Stage083 P1 与 Stage082 Review/P1--P4 已审核旧索引保留策略控制工件保持为唯一合同上下文；P2 只接收五条固定、非业务、reference-only 控制请求，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE083-P2-02 | 五条控制请求分别覆盖文档类型、年份、项目、设备与证据等级过滤；每条投影 query、metadata filter、candidate、keyword/vector/hybrid score、selected、retrieval trace 与 future FTS／pgvector 路由，候选与轨迹关联活动索引版本，选择与轨迹关联证据账本引用。所有值仅为不透明控制标签。 | 已通过 |
| ACC-STAGE083-P2-03 | 关键词基线对全部控制请求均为必需，vector-only 输入被拒绝；输入偏离、Top-K 声明、过滤维度、活动索引版本、候选关联、排序解释、证据账本引用或轨迹引用缺失时失败关闭，不生成任何控制投影。未来真实检索参数、资料范围、证据等级映射与结果采用仍须业务线白箱人工复核。 | 已通过 |
| ACC-STAGE083-P2-04 | P2 不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage083 P3。 | 已遵守 |
| ACC-STAGE083-P1-01 | 冻结 Stage083 任务包、Stage082 Review 与 Stage082 已审核 P1--P4 旧索引保留策略控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE083-P1-02 | query、文档类型／年份／项目／设备／证据等级 filter、candidate、selected、keyword/vector/hybrid score、active_index_version 与 retrieval trace 只复用不透明控制字段；关键词检索基线必须存在，向量相似度不得单独决定结果，候选、选择和轨迹不得跨活动索引版本混用。 | 已通过 |
| ACC-STAGE083-P1-03 | 请求 Top-K、五类过滤维度、活动索引版本、排序解释、证据账本引用或检索轨迹任一缺失时必须失败关闭；未来 PostgreSQL full-text／BM25 风格关键词评分、pgvector 兼容接入、过滤、混合排序和轨迹记录仍须业务线白箱在后续授权 Phase 中受控实施。 | 已通过 |
| ACC-STAGE083-P1-04 | P1 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage083 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 2 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE083-P1-LOCAL-20260822-001` | 静态合同、治理兼容与零运行时边界本地复验 | `PASS_KEYWORD_RETRIEVAL_BASELINE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE083-P2-LOCAL-20260822-001` | 纯内存关键词检索基线控制切片、精确历史白箱兼容与零运行时边界本地复验 | `PASS_KEYWORD_RETRIEVAL_BASELINE_CONTROL_SLICE_RUNTIME_DISABLED` |
