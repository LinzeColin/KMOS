<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE066 · IDS-V0_1-STAGE066-REVIEW · IDS-V0_1-STAGE066-REVIEW

**负责：** Codex 专属 KMOS worktree，Stage066 Review

## 二、怎么算做完

当前阶段显示 20 项；另有 446 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE066-REVIEW-01 | 冻结 Stage066 任务包、P1--P4 控制合同、P3/P4 纯内存控制报告与 Batch061-070 上传锁保持为唯一合同上下文；复审未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE066-REVIEW-02 | 复审机械核验 P1 的 12/17/3/6/14、P2 的 4/4/24/1/4、P3 的六类控制场景与 36 次追溯检查、P4 的 6/6/3/11 交付形状，并保留 P4 到 P3 的纯内存控制回退；所有计数只表示控制形状。 | 已通过 |
| ACC-STAGE066-REVIEW-03 | 长文档、跨页表格、施工步骤、参数表、引用页码和重复 chunk embedding/index 边界均保持显式处置、零静默丢弃与业务线白箱人工处理；来源文档保持权威，控制标签、控制样例与模型文本均不能成为业务事实。 | 已通过 |
| ACC-STAGE066-REVIEW-04 | 复审未执行真实资料访问、解析、切块、身份/版本、去重、覆盖率、质量、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；本次不启动 Stage067，下一步仅可在新的独立 run 进入 Stage067 P1。 | 已遵守 |
| ACC-STAGE066-P4-01 | 冻结 Stage066 任务包、P1/P2/P3 控制工件、Stage065 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P4 的 JSONL 样例、控制覆盖率、低质量清单、回归结果和回退说明均为固定、非业务、reference-only 控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE066-P4-02 | P4 从 P3 六类控制场景派生 6 条 metadata-only Chunk 覆盖率指标 JSONL 样例、控制覆盖率报告、6 条低质量待人工清单、控制回归结果、策略适用边界、回到 P3 的重生成/版本回退说明和 3 条中文确认；所有引用只保持 :control: 形状，不创建真实 chunk、覆盖率、质量结果、来源绑定、版本、索引或持久化状态。 | 已通过 |
| ACC-STAGE066-P4-03 | 长文档、跨页表格、施工步骤、参数表、引用页码和重复 chunk 均明确保留业务线白箱人工处理；控制覆盖率不等于真实文档或 Chunk 覆盖率，低质量清单不等于真实质量测量，回归结果不等于真实质量回归，重生成/版本回退说明只回到 P3 control 状态。 | 已通过 |
| ACC-STAGE066-P4-04 | 未读取、打开、检测、解析、切分、写入、重生成或回退真实文档、页面、章节、表格、来源片段、parser 输出、chunk 或 fixture，未执行真实身份/版本、重复检测或去重、覆盖率、质量、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage066 Review。 | 已遵守 |
| ACC-STAGE066-P3-01 | 冻结 Stage066 任务包、P1 静态合同、P2 固定控制切片、Stage065 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；六类场景均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE066-P3-02 | P3 纯内存重放 P2 的四条十七字段控制记录，覆盖长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界；六类均有显式处置、静默丢弃为零且全部要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE066-P3-03 | 每个场景保留 document、page、section、parser output、表格上下文与来源片段六维控制引用形状；该形状不等于真实来源或页码已经验证。重复场景仅确认本控制模块没有发起 embedding 或索引写入，不检测真实重复项、不计算真实身份、版本或 hash，也不形成真实去重结论。 | 已通过 |
| ACC-STAGE066-P3-04 | 未读取、打开、检测、解析、切分、计算、分类、去重或创建真实资料、页面、chunk、身份、hash、版本、覆盖率、未覆盖页、质量或来源绑定，未执行 parser、章节检测、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage066 P4。 | 已遵守 |
| ACC-STAGE066-P2-01 | 冻结 Stage066 任务包、P1 静态合同、Stage065 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；四条输入均为固定、非业务、仅引用控制请求，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出、页面集合或 chunk。 | 已通过 |
| ACC-STAGE066-P2-02 | 纯内存切片严格核验 P1 十二字段控制输入，并投影四条严格十七字段待人工复核控制记录；解析覆盖率、Chunk 覆盖率和未覆盖页均为 :control: 标签，不生成真实 chunk、hash、分母、比率、页码、未覆盖页或覆盖率记录。 | 已通过 |
| ACC-STAGE066-P2-03 | 四条控制记录均保留 document、page、section、parser output、表格上下文和来源片段六维控制引用，工程步骤、验收条款和参数表三类受保护语义面保持原子；未知分母必须关闭，全部低可信并要求业务线白箱人工复核。控制标记不等于真实覆盖率、质量检测、质量回归或质量降级。 | 已通过 |
| ACC-STAGE066-P2-04 | 未读取、打开、检测、解析、切分、计算或创建真实资料、页面、chunk、hash、覆盖率、未覆盖页、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage066 P3。 | 已遵守 |
| ACC-STAGE066-P1-01 | 冻结 Stage066 任务包、Stage065 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P1 未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE066-P1-02 | P1 固定十二字段仅引用输入、十七字段未来覆盖率输出、解析覆盖率与 Chunk 覆盖率公式标签、未覆盖页受控引用、三类保护语义面、六维追溯和十四类失败关闭；所有字段只代表未来 schema，不填入实际数值、页码、来源、Chunk 或质量结论。 | 已通过 |
| ACC-STAGE066-P1-03 | 工程步骤、验收条款和参数表保持受保护语义面；分母未知、页面集合或来源追溯无法确认时必须关闭并交由业务线白箱人工处理，覆盖率字段、控制标签和模型输出均不能替代来源文档或形成业务事实。 | 已通过 |
| ACC-STAGE066-P1-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、解析覆盖率、Chunk 覆盖率、未覆盖页识别、质量、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P2、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage066 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE066-P1-LOCAL-20260814-001` | 只定义冻结 Stage066 Chunk 覆盖率指标的引用式静态合同，覆盖未来解析覆盖率、Chunk 覆盖率、未覆盖页面、十二字段输入、十七字段未来输出、工程步骤/验收条款/参数表保护面、document/page/section/parser output 追溯、失败关闭、中文反馈和回滚边界；不读取、打开、解析、切分、计算或创建真实业务资料、页面、chunk、覆盖率、未覆盖页面或业务结论。 | `PASS_LOCAL_PHASE1_CHUNK_COVERAGE_METRICS_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE066-P2-LOCAL-20260814-001` | 只以四条固定、非业务、reference-only Chunk 覆盖率控制请求在内存中投影四条待人工复核记录，机械验证 Stage066 P1 的十二字段输入、十七字段输出、解析覆盖率/Chunk 覆盖率/未覆盖页指标标签、工程步骤/验收条款/参数表三类受保护语义面、六维追溯、未知分母关闭和低可信人工处理标记；不读取、解析、检测、切分、计算或创建真实业务资料。 | `PASS_LOCAL_PHASE2_CHUNK_COVERAGE_METRICS_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE066-P3-LOCAL-20260814-001` | 只重放四条固定、非业务、reference-only Chunk 覆盖率控制记录，并以六类固定场景机械验证长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界的显式人工处置、六维控制追溯和零静默丢弃；不读取、解析、检测、切分、计算或创建真实业务资料。 | `PASS_LOCAL_PHASE3_CHUNK_COVERAGE_METRICS_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE066-P4-LOCAL-20260814-001` | 只从 Stage066 P3 六类固定、非业务、reference-only Chunk 覆盖率控制场景派生内存 metadata-only JSONL 样例、控制覆盖率报告、低质量待人工清单、控制回归、策略适用边界、中文确认和 P4 到 P3 的重生成/版本回退说明；不读取、解析、切分、计算、创建、写入、重生成或回退真实业务资料。 | `PASS_LOCAL_PHASE4_CHUNK_COVERAGE_METRICS_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE066-REVIEW-LOCAL-20260814-001` | 只机械复审冻结 Stage066 P1--P4 合同与 P3/P4 纯内存 Chunk 覆盖率控制报告，核验固定指标形状、受保护语义面、六类白箱人工场景、metadata-only 交付、单一权威、零运行时和 P4 到 P3 控制回退；不读取、解析、检测、切分、计算、写入、重生成或回退真实业务资料。 | `PASS_LOCAL_REVIEWED_CHUNK_COVERAGE_METRICS_RUNTIME_DISABLED` |
