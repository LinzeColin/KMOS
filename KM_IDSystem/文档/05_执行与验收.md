<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE070 · IDS-V0_1-STAGE070-REVIEW · IDS-V0_1-STAGE070-REVIEW

**负责：** Codex 专属 KMOS worktree，Stage070 Review

## 二、怎么算做完

当前阶段显示 20 项；另有 526 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE070-REVIEW-01 | 冻结 Stage070 任务包、P1--P4 合同、P2/P3/P4 纯内存控制报告、Stage069 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；复审只读重放控制工件，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE070-REVIEW-02 | 机械核验 P1 17/12/10/7/8/18/12、P2 五条 10/14/10/7/8/18 字段控制投影与 denied/预算暂停、P3 五条二十九字段显式处置/四条人工处理/九十次审计字段检查，以及 P4 五条策略样例、五条十八字段审计投影、五条零成本、五条失败处理、五条未外发记录、六键查询、三条中文确认和十二类失败关闭，发现数为零；所有计数只表示固定控制形状。 | 已通过 |
| ACC-STAGE070-REVIEW-03 | 默认 denied、data source/document→chunk 自动继承、document 只能收紧、队列/缓存/重试同闭合、先审计、业务线白箱人工处理和 metadata-only 交付边界保持一致；控制合同、审计投影、成本、失败、未外发、查询或回退均不能替代来源文档、形成真实外发/审计/成本事实或自动业务建议，P4 仅可纯内存回退到 P3。 | 已通过 |
| ACC-STAGE070-REVIEW-04 | 复审未执行真实资料访问、解析、切块、摘要生成、文本外发、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算或记录、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、批次复审、上传或推送；本次不启动 Stage071，下一步仅可在新的独立 run 进入 Stage071 P1。 | 已遵守 |
| ACC-STAGE070-P4-01 | 冻结 Stage070 任务包、P1/P2/P3 合同、Stage069 外部 API 策略继承合同与 Batch061-070 上传锁保持为唯一合同上下文；P4 只从五条固定、非业务、:control: 引用场景派生控制交付，未建立第二权威事实源，未读取、打开、保留、创建或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE070-P4-02 | P4 仅在内存中从 P3 五条固定控制场景派生五条策略样例、五条十八字段审计日志投影样例、五条零 Token/零成本估算、五条显式失败处理、五条未外发控制引用记录、六键查询说明、三条中文确认和十二类失败关闭；一个 denied、两个 summary_only、一个 full_text_allowed、一个预算不足暂停、三个未来调用候选和九十次审计字段检查只表示固定控制形状。 | 已通过 |
| ACC-STAGE070-P4-03 | denied 固定阻断队列、缓存、重试和外发；摘要/文本块控制引用须业务线白箱人工复核且本 phase 运行时关闭，预算不足固定暂停；控制样例、审计日志投影、成本、失败处理、未外发原因、查询和回滚说明不能替代来源文档、形成真实审计/成本/外发/队列/缓存事实或自动业务建议，回滚仅可回到 P3 纯内存控制场景。 | 已通过 |
| ACC-STAGE070-P4-04 | 未执行真实资料访问、解析、切块、摘要、文本外发、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算或记录、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage070 Review。 | 已遵守 |
| ACC-STAGE070-P3-01 | 冻结 Stage070 任务包、P1 静态合同、P2 纯内存控制切片、Stage069 外部 API 策略继承合同与 Batch061-070 上传锁保持为唯一合同上下文；P3 只重放五条固定、非业务、:control: 引用，未建立第二权威事实源，未读取、打开、保留、创建或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE070-P3-02 | 纯内存专项验证输出五条二十九字段场景结果，逐条重放 P2 的策略、队列、缓存、失败重试、成本/模型版本和十八字段审计投影；覆盖一个 denied、两个 summary_only、一个 full_text_allowed 与一个预算不足暂停，固定完成九十次审计字段检查与三个未来外部调用候选。所有结果、引用和计数只表示固定控制形状，不代表真实资料、摘要、文本、外发、队列、缓存、重试、审计、成本、provider、模型或业务结论。 | 已通过 |
| ACC-STAGE070-P3-03 | denied 固定无外发载荷、无队列、无缓存与无重试；summary_only 只保留摘要范围，full_text_allowed 只保留文本块控制范围，预算不足固定暂停；每个未来外部调用候选均先具控制审计投影，任一 P2 形状、策略边界、预算暂停、审计字段或关联异常均失败关闭，四个非 denied 情形保持业务线白箱人工处理。 | 已通过 |
| ACC-STAGE070-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P4。 | 已遵守 |
| ACC-STAGE070-P2-01 | 冻结 Stage070 任务包、P1 静态合同、Stage069 外部 API 策略继承合同与 Batch061-070 上传锁保持为唯一合同上下文；P2 只接受五条固定、非业务、17 字段、:control: 引用请求，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE070-P2-02 | 纯内存切片严格核验五条固定输入并投影五条策略继承、五条十二字段 Embedding 队列、五条十字段缓存、五条七字段失败重试、五条八字段零成本/模型版本和五条十八字段审计记录，覆盖默认 denied、摘要继承、document 收紧、全文控制与预算不足暂停；所有标签不代表真实资料、摘要、文本载荷、队列、缓存、重试、provider、模型、Token、成本或审计记录。 | 已通过 |
| ACC-STAGE070-P2-03 | 未知策略与 document 放宽均失败关闭，document 只能收紧且 chunk 自动继承有效策略、不得逐条标记；denied 固定阻断队列、缓存与重试，预算不足固定暂停，任何控制投影均不能替代来源文档、形成业务事实或自动业务建议，策略例外须经业务线白箱人工处理。 | 已通过 |
| ACC-STAGE070-P2-04 | 未执行真实资料访问、解析、切块、摘要、文本外发、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算、审计、索引、数据库、持久化、Agent、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P3。 | 已遵守 |
| ACC-STAGE070-P1-01 | 冻结 Stage070 任务包、Stage069 本地整阶段机械复审、Stage069 外部 API 策略继承合同与 Batch061-070 上传锁保持为唯一合同上下文；本 P1 未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE070-P1-02 | 复用默认 denied、denied/summary_only/full_text_allowed 三档策略和 data source/document→chunk 自动继承；owner 不需逐条标记 chunk，document 不得放宽来源策略；静态固定 17 个仅引用输入、12 个未来队列字段、10 个未来缓存字段、7 个未来失败重试字段、8 个成本/模型版本字段、18 个审计字段和 12 类失败关闭。 | 已通过 |
| ACC-STAGE070-P1-03 | denied 不形成外发载荷、队列、缓存或重试；summary_only 仅允许未来已授权摘要引用；full_text_allowed 仅可在未来授权、预算、审计和业务线白箱人工复核后处理文本块；任何队列、缓存、重试、审计或模型输出都不能替代来源文档、形成业务事实或自动建议。 | 已通过 |
| ACC-STAGE070-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、成本计算或记录、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P2/P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage070 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE070-P1-LOCAL-20260815-001` |  | `PASS_LOCAL_STATIC_EMBEDDING_QUEUE_CACHE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE070-P2-LOCAL-20260815-001` |  | `PASS_LOCAL_EMBEDDING_QUEUE_CACHE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE070-P3-LOCAL-20260815-001` |  | `PASS_LOCAL_EMBEDDING_QUEUE_CACHE_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE070-P4-LOCAL-20260815-001` | 只在内存中从 Stage070 P3 的五条固定、非业务、reference-only Embedding 队列、缓存与重试控制场景派生策略样例、审计日志投影样例、零成本估算、失败处理、未外发控制引用记录、查询说明和回滚说明；不读取、打开、解析、总结、外发、创建、写入、查询或回退真实业务资料、摘要正文、文本块、chunk、队列、缓存、重试、审计、成本、模型或业务结论。 | `PASS_LOCAL_PHASE4_EMBEDDING_QUEUE_CACHE_DELIVERY_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE070-REVIEW-LOCAL-20260815-001` | 只机械复审冻结 Stage070 P1--P4 合同与 P2/P3/P4 纯内存 Embedding 队列、缓存与失败重试控制报告，核验默认 denied、自动继承、队列/缓存/重试闭合、审计先决条件、业务线白箱人工处理、metadata-only 交付、单一权威、零运行时和 P4 到 P3 控制回退；不读取、解析、生成、外发、查询、写入或回退真实业务资料。 | `PASS_LOCAL_REVIEWED_EMBEDDING_QUEUE_CACHE_RUNTIME_DISABLED` |
