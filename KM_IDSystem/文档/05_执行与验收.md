<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE058 · IDS-V0_1-STAGE058-P4 · IDS-V0_1-STAGE058-P4

**负责：** Codex 专属 KMOS worktree，Stage058 Phase 4

## 二、怎么算做完

当前阶段显示 16 项；另有 282 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE058-P4-01 | Stage058 P4 只以冻结任务包、P1/P2/P3 合同和 Stage057 已复审工件为唯一合同上下文；六个交付样例只保留固定非业务 control 元数据、显式处置与来源定位引用，不包含业务资料、真实表格、工作表、表头、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P4-02 | 交付包含 6 个 metadata-only 非真实表格事实样例、6 个字段引用标签、6 条控制质量处置、零静默丢弃和 6 条人工处理建议；字段推断引用报告不构成真实 schema 推断、字段识别、事实抽取、来源追溯或数值统计。 | 已通过 |
| ACC-STAGE058-P4-03 | 质量限制与 3 条人工确认提示均为中文且不自动确认；重解析与事实回滚说明只允许回到 P3 control 状态，当前不存在真实文件、事实库、typed value、数据库 migration、事实删除、覆写或回滚动作。 | 已通过 |
| ACC-STAGE058-P4-04 | 未读取、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/质量验证、事实/证据写入、数值统计、真实重解析或事实回滚、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 Review。 | 已遵守 |
| ACC-STAGE058-P3-01 | Stage058 P3 只以冻结任务包、P1/P2 合同和 Stage057 已复审工件为唯一合同上下文；六类异常仅为固定、非业务、reference-only 控制元数据，不包含业务资料、真实表格、工作表、表头、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P3-02 | 专项重放 P2 两条控制记录和 11 条 Schema profile 候选，覆盖空表、合并单元格、单位混乱、日期格式不一、异常值和重复行；六类均有显式处置、静默丢弃为零、均要求人工处理，控制来源文档、工作表、表头行、行列范围和证据引用形状保持可追溯。 | 已通过 |
| ACC-STAGE058-P3-03 | 控制来源引用形状不等于真实源文件、真实行列或真实证据记录已验证；不解合并、不规范化单位或日期、不去重、不评估实际异常值、不生成事实，未验证数值不得形成统计结论或模型确定性数值结论。 | 已通过 |
| ACC-STAGE058-P3-04 | 未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实质量验证、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 P4。 | 已遵守 |
| ACC-STAGE058-P2-01 | Stage058 P2 只以冻结任务包、P1 静态合同和 Stage057 已复审工件为唯一合同上下文；两条输入均为固定、非业务、reference-only 十字段控制记录，不包含业务资料、真实表头、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P2-02 | 纯内存切片形成 2 个 Schema profile 组、11 条 18 字段候选、9 类字段语义、6 类候选字段类型与 11 条来源位置引用；候选列名只使用 control handle，不构成真实列名、真实 schema 或真实字段映射。 | 已通过 |
| ACC-STAGE058-P2-03 | 来源文档继续保持权威；事实抽取和 RAG 摘要分别仍归 Stage059/060，本 P2 不创建事实、摘要、数值统计、证据、数据库或持久状态；无效控制输入必须 REJECTED。 | 已通过 |
| ACC-STAGE058-P2-04 | 未读取、打开、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 P3。 | 已遵守 |
| ACC-STAGE058-P1-01 | Stage058 P1 只以冻结任务包和 Stage057 已复审工件为唯一合同上下文；XLSX/CSV、生产记录和质检记录只登记引用字段、接口名称与静态边界，不包含业务资料、表格内容、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P1-02 | 合同固定十字段 reference-only 输入、十八字段未来 Schema profile、九类字段候选、六类候选字段类型、六类来源位置和八类失败关闭；当前没有真实输入、Schema profile、字段映射、事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE058-P1-03 | 来源文档保持权威；未来数值统计只能使用带来源位置和证据绑定的结构化事实，模型不能根据文本猜测数值或统计结论，RAG 摘要不能替代事实或成为数值权威；无法识别的表头、列名、字段类型、单位、日期、设备、材料、工序或质量结果必须等待人工处理。 | 已通过 |
| ACC-STAGE058-P1-04 | 未读取、打开、检测、解析或推断真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行事实抽取、摘要、统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE058-P1-LOCAL-20260813-001` | 定义 XLSX/CSV、生产记录和质检记录的表格 Schema 推断引用输入、未来 Schema profile、字段候选、数值与 RAG 权威边界及回滚静态合同 | `PASS_PHASE1_TABLE_SCHEMA_INFERENCE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE058-P2-LOCAL-20260813-001` | 以两条固定非业务 reference-only 表格控制记录在内存中投影 Schema profile 与字段候选，验证字段类型和来源位置引用形状；不读取或解析真实表格 | `PASS_PHASE2_TABLE_SCHEMA_INFERENCE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE058-P3-LOCAL-20260813-001` | 重放 P2 两条固定非业务 reference-only 表格控制记录与 Schema profile 候选，验证六类异常的显式人工处置、控制来源位置引用形状及未验证数值的统计结论关闭；不读取、解析或评估真实表格 | `PASS_PHASE3_TABLE_SCHEMA_INFERENCE_CONTROLLED_QUALITY_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE058-P4-LOCAL-20260813-001` | 从 P3 六类固定非业务 reference-only control 场景派生表格 Schema metadata-only 交付样例、字段推断引用报告、质量结果、人工处理建议和重解析/事实回滚说明 | `PASS_PHASE4_TABLE_SCHEMA_INFERENCE_DELIVERY_RUNTIME_DISABLED` |
