<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE078 · IDS-V0_1-STAGE078-P1 · IDS-V0_1-STAGE078-P1

**负责：** 既有唯一 KMOS 开发 worktree，Stage078 P1

## 二、怎么算做完

当前阶段显示 4 项；另有 692 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE078-P1-01 | 冻结 Stage078 任务包、Stage077 Review 与 Stage077 已复审后台索引构建合同保持为唯一合同上下文；P1 只定义静态工程合同，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE078-P1-02 | 未来索引版本、活动指针、构建中版本与影子索引的控制字段均已固定；每次未来批量导入建立隔离候选且不得覆盖活动版本，旧活动索引在构建与冒烟测试期间继续服务；所有字段只表示静态工程边界，不代表真实资料、索引、检索、模型、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE078-P1-03 | 未来冒烟测试必须完整通过后才能原子切换；失败、缺失或未记录结果时活动指针保持不变，回退只可指向保留的上一活动版本，未来例外仍须业务线白箱人工处理；P1 不形成真实构建、冒烟、切换、查询或回退记录。 | 已通过 |
| ACC-STAGE078-P1-04 | P1 验证期间不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；其下一门为 Stage078 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 1 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE078-P1-LOCAL-20260821-001` |  | `PASS_INDEX_SMOKE_TEST_CONTRACT_RUNTIME_DISABLED` |
