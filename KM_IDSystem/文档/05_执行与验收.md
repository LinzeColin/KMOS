<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE075 · IDS-V0_1-STAGE075-P4 · IDS-V0_1-STAGE075-P4

**负责：** 既有唯一 KMOS 开发 worktree，Stage075 P4

## 二、怎么算做完

当前阶段显示 16 项；另有 629 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE075-P4-01 | 冻结 Stage075 任务包、Stage075 P1/P2/P3、Stage074 Review 和前序控制合同保持为唯一合同上下文；P4 只从五条固定、非业务、reference-only 控制场景和 P2 控制投影派生 metadata-only 交付证据，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE075-P4-02 | 已派生五条策略样例、五条完整十九字段审计投影与九十五次字段检查、五条零值成本估算、五条失败处理和五条未外发原因；denied、summary_only、document 收紧、full_text_allowed 与预算暂停均保持 P3 控制边界，owner 强制允许外发仍只有一条 actor/reason/old_value/new_value 四字段前置样例。 | 已通过 |
| ACC-STAGE075-P4-03 | 八键查询说明只覆盖当前 Python 进程中的控制交付报告，不存在持久审计日志、真实外发历史或真实查询；回滚只回到 Stage075 P3 控制报告，四条中文反馈保持克制，未来外发仍以前置审计和业务线白箱人工复核为准。 | 已通过 |
| ACC-STAGE075-P4-04 | 未执行真实资料访问、解析、切块、摘要生成、provider/模型选择或下载、Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 Review。 | 已遵守 |
| ACC-STAGE075-P3-01 | 冻结 Stage075 任务包、Stage075 P1/P2、Stage074 Review 和前序控制合同保持为唯一合同上下文；P3 只重放五条固定、非业务、:control: 控制投影，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE075-P3-02 | 五条专项场景机械验证 denied 阻断外发、summary_only 仅保留摘要引用、document 收紧不得升级为文本块引用、full_text_allowed 仅保留未来文本块引用和预算不足时队列/缓存/失败重试同步暂停；五条均有显式处置，静默丢弃为零。 | 已通过 |
| ACC-STAGE075-P3-03 | 每条控制场景均保留完整十九字段外部 API 覆盖审计控制投影，共完成九十五次字段检查；三个未来外部 API 调用候选均保留调用前审计、token_count=0、不透明控制引用与业务线白箱人工复核前置；owner 强制允许外发仍须一条 actor/reason/old_value/new_value 四字段前置；控制投影不等于真实审计记录、来源文档或业务事实。 | 已通过 |
| ACC-STAGE075-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、provider/模型选择或下载、Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 P4。 | 已遵守 |
| ACC-STAGE075-P2-01 | 冻结 Stage075 任务包、Stage075 P1、Stage074 Review 和前序控制合同保持为唯一合同上下文；P2 只接受五条固定、非业务、:control: 输入，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE075-P2-02 | 默认 denied、三档策略、data source/document 到 chunk 自动继承、document 只能收紧、owner 不逐条标记 chunk 均保持；未授权 chunk 被阻断，summary_only 与 full_text_allowed 只保留未来授权引用，预算不足时控制候选暂停。 | 已通过 |
| ACC-STAGE075-P2-03 | 五条固定控制请求均只在内存中投影 12/10/7 队列/缓存/失败重试、16/8 成本、6 字段模型版本、19 字段覆盖审计及一条 actor/reason/old_value/new_value owner 强制允许外发前置；provider、model、token_count=0、chunk_id 与 policy reason 均为不透明控制标签，不代表真实模型、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE075-P2-04 | 未执行真实资料访问、解析、切块、摘要生成、provider/模型选择或下载、Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 P3。 | 已遵守 |
| ACC-STAGE075-P1-01 | 冻结 Stage075 任务包、Stage074 Review 和 Stage074 P1 前序控制合同保持为唯一合同上下文；P1 只定义静态工程合同，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE075-P1-02 | 默认 denied、三档策略、data source/document 到 chunk 自动继承、document 只能收紧、owner 不逐条标记 chunk、12/10/7 队列/缓存/失败重试、16/8 成本、6 字段模型版本和 19 字段未来外部 API 覆盖审计形状均已固定；所有字段只表示控制标签，不代表真实资料、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE075-P1-03 | 未来 owner 强制允许外发必须先记录 actor、reason、old_value 与 new_value，并完成业务线白箱人工复核；P1 没有实际策略变更、外发、审计记录、审计查询或模型调用。 | 已通过 |
| ACC-STAGE075-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、provider/模型选择或下载、Embedding、索引写入、模型版本记录、成本估算、预算查找、队列、缓存、失败重试、外部 API、模型 Token、审计写入或查询、数据库、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage075 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE075-P1-LOCAL-20260821-001` |  | `PASS_EXTERNAL_API_COVERAGE_AUDIT_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE075-P2-LOCAL-20260821-001` |  | `PASS_PHASE2_EXTERNAL_API_COVERAGE_AUDIT_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE075-P3-LOCAL-20260821-001` |  | `PASS_PHASE3_EXTERNAL_API_COVERAGE_AUDIT_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE075-P4-LOCAL-20260821-001` |  | `PASS_PHASE4_EXTERNAL_API_COVERAGE_AUDIT_DELIVERY_RUNTIME_DISABLED` |
