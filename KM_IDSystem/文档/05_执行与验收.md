<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE057 · IDS-V0_1-STAGE057-P2 · IDS-V0_1-STAGE057-P2

**负责：** Codex 专属 KMOS worktree，Stage057 P2

## 二、怎么算做完

当前阶段显示 8 项；另有 262 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE057-P2-01 | Stage057 P2 只以冻结任务包、P1 合同和 Stage056 已复审工件为唯一合同上下文；两条输入均为固定、非业务、reference-only 控制记录，严格使用 P1 的 12 字段且不包含业务资料、真实表格、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P2-02 | 纯内存切片形成 2 个控制 schema profile、10 个字段/19 字段事实候选和 2 个 metadata-only RAG 摘要候选；typed value 与业务字段值保持空值，每个候选保留来源文档、工作表、行列范围和证据引用但未创建事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE057-P2-03 | RAG 摘要候选只能引用事实候选，不能替代结构化事实或成为数值统计依据；数值候选不含数值，模型不能根据文本猜测数值，未验证数值不能形成确定事实或统计结论；无效控制输入必须 REJECTED。 | 已通过 |
| ACC-STAGE057-P2-04 | 未读取、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema 推断、真实字段识别、真实事实抽取、数值统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P3。 | 已遵守 |
| ACC-STAGE057-P1-01 | Stage057 P1 只以冻结任务包和 Stage056 已复审工件为唯一合同上下文；XLSX/CSV、生产记录和质检记录只登记引用字段、接口名称与静态边界，不包含业务资料、表格内容、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE057-P1-02 | 合同固定 12 字段引用输入、19 字段未来事实输出、七类字段语义和五类来源位置；来源文档保持权威，未来事实必须关联来源文档、工作表、行列范围和证据引用，当前没有真实输入、事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE057-P1-03 | 数值统计未来只能使用带来源位置与证据绑定的结构化事实；模型不能根据文本猜测数值，未验证数值不能形成确定事实或统计结论，RAG 摘要不能替代事实或成为数值权威；无法识别的字段、单位、日期、质量结果或表格结构必须等待人工处理。 | 已通过 |
| ACC-STAGE057-P1-04 | 未读取或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行文件检测、parser、schema 推断、字段识别、事实抽取、摘要、统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage057 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 2 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE057-P1-LOCAL-20260813-001` | 定义 XLSX/CSV、生产记录和质检记录的结构化接入、字段语义、来源定位、事实与 RAG 摘要分离及数值事实静态合同 | `PASS_PHASE1_XLSX_CSV_INGESTION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE057-P2-LOCAL-20260813-001` | 以两条固定非业务 reference-only 控制记录在内存中投影 XLSX/CSV schema、字段候选、19 字段空值事实候选、来源定位与 metadata-only RAG 摘要候选 | `PASS_PHASE2_XLSX_CSV_INGESTION_CONTROL_SLICE_RUNTIME_DISABLED` |
