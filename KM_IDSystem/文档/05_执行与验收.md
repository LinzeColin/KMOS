<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE062 · IDS-V0_1-STAGE062-P3 · IDS-V0_1-STAGE062-P3

**负责：** Codex 专属 KMOS worktree，Stage062 Phase 3

## 二、怎么算做完

当前阶段显示 12 项；另有 366 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE062-P3-01 | 冻结 Stage062 任务包、P1/P2 控制工件、Stage061 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；六类异常均为固定、非业务、reference-only 控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、实际 URI、工作表、单元格或证据记录。 | 已通过 |
| ACC-STAGE062-P3-02 | P3 纯内存重放 P2 两条未绑定控制候选，覆盖空表、合并单元格、单位混乱、日期格式不一、异常值和重复行；六类均有显式处置、静默丢弃为零且全部要求业务线人工白箱处理。 | 已通过 |
| ACC-STAGE062-P3-03 | 每个场景保留 evidence_id、document_id、sheet、row、column 和 source_uri 的控制引用形状；该形状不等于真实来源位置或证据已经验证，未验证数值不得形成统计或模型确定性数值结论。 | 已通过 |
| ACC-STAGE062-P3-04 | 未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实、typed value、统计、质量验证、来源/证据绑定、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage062 P4。 | 已遵守 |
| ACC-STAGE062-P2-01 | 冻结 Stage062 任务包、P1 静态合同、Stage061 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；两条输入均为固定、非业务、reference-only 控制请求，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、实际 URI、工作表、单元格或证据记录。 | 已通过 |
| ACC-STAGE062-P2-02 | 纯内存切片严格核验 P1 十九字段控制输入，并投影两条严格十七字段 UNBOUND_REFERENCE_ONLY 表格证据绑定候选；候选覆盖 XLSX/CSV、生产/质检类别，并保留 evidence_id、document_id、sheet、row、column 和 source_uri 六维控制引用。 | 已通过 |
| ACC-STAGE062-P2-03 | 全部候选均未绑定、必须人工确认且数值权威关闭；未验证来源和数值不得形成统计或确定性结论，模型文本与 RAG 摘要不得替代结构化事实或成为数值权威，未知、重排或篡改控制输入必须拒绝。 | 已通过 |
| ACC-STAGE062-P2-04 | 未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实、typed value、统计、来源/证据绑定、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage062 P3。 | 已遵守 |
| ACC-STAGE062-P1-01 | 冻结 Stage062 任务包、Stage061 本地复审与 Batch061-070 上传锁保持为唯一合同上下文；十九字段引用式绑定输入和十七字段未来绑定输出只定义接口，不建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、实际 URI、工作表、单元格或证据记录。 | 已通过 |
| ACC-STAGE062-P1-02 | 未来表格事实绑定明确覆盖 evidence_id、document_id、sheet、row、column 和 source_uri 六个维度，并声明字段类型、单位、日期、设备、材料、质量结果、事实类型和主键引用；source_uri 只可为不透明引用标识，当前未创建绑定、来源定位、字段映射、事实或数据库 schema。 | 已通过 |
| ACC-STAGE062-P1-03 | 来源文档保持权威；数值统计未来只能使用具有六维绑定的已验证结构化事实，模型文本与 RAG 摘要不得猜测或形成确定统计结论；缺少事实、来源、字段类型、单位、日期、质量结果、事实类型或数值时必须关闭并转人工处理。 | 已通过 |
| ACC-STAGE062-P1-04 | 未读取、打开、检测、解析或评估真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实、typed value、统计、来源/证据绑定、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage062 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE062-P1-LOCAL-20260814-001` | 只定义冻结 Stage062 表格证据绑定的引用式静态合同，覆盖 evidence_id、document_id、sheet、row、column、source_uri、字段语义、数值权威、失败关闭、中文反馈与回滚边界；不读取、解析、绑定或写入真实业务资料。 | `PASS_LOCAL_PHASE1_TABLE_EVIDENCE_BINDING_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE062-P2-LOCAL-20260814-001` | 只以两条固定、非业务、reference-only 表格证据绑定控制请求在内存中投影两条未绑定候选，机械验证 P1 的输入/输出字段、XLSX/CSV 标签、生产/质检记录类别、六维引用、人工确认与数值关闭边界；不读取或运行真实业务资料。 | `PASS_LOCAL_PHASE2_TABLE_EVIDENCE_BINDING_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE062-P3-LOCAL-20260814-001` |  | `PASS_LOCAL_PHASE3_TABLE_EVIDENCE_BINDING_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
