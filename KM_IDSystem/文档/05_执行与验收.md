<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE085 · IDS-STAGE085-REVIEW · IDS-V0_1-STAGE085-REVIEW

**负责：** 既有唯一 KMOS 开发 worktree 已完成 Stage085 Review 本地治理验收；不创建额外 worktree、分支或合并请求，且不进入 Stage086 P1、OVH、生产或上传。

## 二、怎么算做完

当前阶段显示 20 项；另有 839 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE085-P1-01 | 冻结 Stage085 任务包、Stage084 Review 与 Stage084 已审核 P1--P4 向量检索基线工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE085-P1-02 | query、文档类型／年份／项目／设备／资料状态／证据等级六类 filter、candidate、selected、keyword/vector/hybrid score、active_index_version 与 retrieval trace 只复用不透明控制字段；模型／版本／维度／相似度度量必须声明，关键词检索基线与向量检索基线都必须存在，向量相似度不得单独决定结果，候选、选择和轨迹不得跨活动索引版本、元数据过滤合同、模型版本或相似度度量混用。 | 已通过 |
| ACC-STAGE085-P1-03 | 请求 Top-K、模型／版本／维度／相似度度量、六类过滤维度、资料状态、活动索引版本、过滤合同、排序解释、证据账本引用或检索轨迹任一缺失时必须失败关闭；未来 PostgreSQL full-text／BM25 风格关键词评分、pgvector 兼容接入、embedding、过滤、混合排序和轨迹记录仍须业务线白箱在后续授权 Phase 中受控实施。 | 已通过 |
| ACC-STAGE085-P1-04 | P1 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、资料状态读取、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage085 P2。 | 已遵守 |
| ACC-STAGE085-P2-01 | 冻结 Stage085 任务包、Stage085 P1 与 Stage084 Review 的已审核向量检索基线工件保持为唯一合同上下文；P2 只执行固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE085-P2-02 | 六条固定控制请求各含 20 个不透明字段，并覆盖文档类型、年份、项目、设备、资料状态和证据等级；每条只投影 query、filter、candidate、hybrid score、selected、retrieval trace 与 future integration 控制形状，candidate、selected 和 trace 的活动索引版本、元数据过滤、模型版本、相似度度量、评分解释与证据账本引用链必须一致。 | 已通过 |
| ACC-STAGE085-P2-03 | 关键词与向量基线必须同时声明，vector-only、非固定输入、请求 Top-K、模型／版本／维度／相似度度量、六类过滤维度、资料状态、活动索引版本、过滤合同、排序解释、证据账本引用或检索轨迹任一缺失时必须失败关闭；PostgreSQL full-text／BM25、pgvector、过滤、混合排序和轨迹仅保留 future-only 路由。 | 已通过 |
| ACC-STAGE085-P2-04 | P2 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、资料状态读取、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage085 P3。 | 已遵守 |
| ACC-STAGE085-P3-01 | 冻结 Stage085 任务包、Stage085 P1/P2 与 Stage084 Review 的已审核向量检索基线工件保持为唯一合同上下文；P3 只重放 P2 的六条固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE085-P3-02 | 八类场景覆盖关键词、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引版本轨迹；材料牌号、设备型号、标准号和语义相似只表示场景类别，模型／版本／维度／相似度度量与所有引用均为不透明控制标签，未读取、匹配或计算真实业务值。 | 已通过 |
| ACC-STAGE085-P3-03 | P2 结构、关键词与向量基线、vector-only 拒绝、六类过滤维度、资料状态、候选／选择／评分解释／证据账本引用链、模型／版本／维度／相似度度量与活动索引版本轨迹必须完整；P2 输出偏离、字段缺失、非固定控制标签或任一运行信号均失败关闭，全部场景仍需业务线白箱人工处理。 | 已通过 |
| ACC-STAGE085-P3-04 | P3 不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索轨迹或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage085 P4。 | 已遵守 |
| ACC-STAGE085-P4-01 | 冻结 Stage085 任务包、Stage085 P1/P2/P3 与 Stage084 Review 的已审核向量检索基线工件保持为唯一合同上下文；P4 只从 P3 的八个固定、非业务、reference-only 场景派生 metadata-only 交付证据，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库、物理索引或检索参数。 | 已通过 |
| ACC-STAGE085-P4-02 | P4 已派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口记录、4 条参数回滚说明和 4 条中文反馈；每条只保留 P3 场景、关键词与向量基线、六类过滤、模型／版本／维度／相似度度量、活动索引版本、排序解释、证据账本与轨迹的不透明控制引用。 | 已通过 |
| ACC-STAGE085-P4-03 | P3 输出、关键控制引用、vector-only 拒绝、六类过滤、资料状态、候选／选择／评分解释／证据账本引用链、模型／版本／维度／相似度度量与活动索引版本任一偏离均失败关闭；检索不足／证据缺口显式记录，全部交付仍需业务线白箱人工处理，自动补全、自动业务建议和自动参数回滚关闭。 | 已通过 |
| ACC-STAGE085-P4-04 | P4 不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索样例／trace／过滤／有效性报告／证据缺口读写、检索参数写入或回滚、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage085 Review。 | 已遵守 |
| ACC-STAGE085-REVIEW-01 | 冻结 Stage085 任务包、P1--P4 合同、P2/P3/P4 控制报告与 Stage084 Review/P1--P4 已审核工件保持为唯一合同上下文；Review 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库、物理索引或检索参数。 | 已通过 |
| ACC-STAGE085-REVIEW-02 | 机械复审 P1 的 11/7/11/8/7/10 字段与 21 类失败关闭、P2 的六条固定控制请求与七组投影／366 次字段检查、P3 的八类 31 字段场景／248 次字段检查／八条人工处理，以及 P4 的 8/8/8/8/8/4/4 交付形状、572 次交付字段检查与 18 类失败关闭；任一合同或报告形状漂移均失败关闭。 | 已通过 |
| ACC-STAGE085-REVIEW-03 | 关键词与向量基线、vector-only 拒绝、六类过滤、资料状态、模型／版本／维度／相似度度量、候选／选择／评分解释／活动索引版本／证据账本／trace 引用链、显式证据缺口、业务线白箱人工处理、单一权威与 P4→P3 控制回退链保持；通过后只打开 Stage086 P1 gate，不启动 Stage086。 | 已通过 |
| ACC-STAGE085-REVIEW-04 | Review 期间不执行真实资料访问、检索样例／trace／过滤／有效性报告／证据缺口写入、参数写入或回滚、PostgreSQL、FTS、BM25、pgvector、embedding、查询、匹配、语义计算、过滤、排序、Top-K、provider／模型、模型 Token、Agent、OVH、生产、上传或推送；后续 Stage086 仍须在新的独立 run 进入。 | 已遵守 |

## 三、已经做了什么（当前阶段 0 条）

> 还没有运行记录。每完成一步会自动追加一条，这里就有了。
