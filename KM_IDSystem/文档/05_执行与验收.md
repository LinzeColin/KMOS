<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE090 · IDS-STAGE090-P3 · IDS-V0_1-STAGE090-P3

**负责：** KMOS 开发负责人；本阶段使用唯一既有开发工作树、单一分支与本地验证节奏。

## 二、怎么算做完

当前阶段显示 12 项；另有 944 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE090-P2-01 | 冻结 Stage090 任务包、Stage090 P1 静态合同与 Stage089 Review/前序 evidence schema 合同保持为唯一合同上下文；P2 只处理六条固定、非业务、reference-only 的纯内存控制输入，只绑定前序 schema，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、检索结果、证据账本、审计日志、回答、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE090-P2-02 | 六条固定控制请求各含 26 个不透明字段，并投影 schema binding 6、检索捕获 10、账本捕获 9、关联 7、风险 8、撤回 7、投毒防护 8、关键结论绑定 7、降级 8 与 future integration 7 个字段，共 462 个控制检查点；evidence 与 document、chunk、fact、query、answer、report 的引用链必须一致。 | 已通过 |
| ACC-STAGE090-P2-03 | 关键结论必须携带 evidence_id 或 evidence_gap 控制引用；低可信、冲突、过期和撤回证据必须降级，疑似投毒必须保持隔离候选，A 等级仍待业务线白箱人工复核。任何非固定输入、前序 schema 或引用链缺失、低等级伪装为高等级或自动业务采纳均失败关闭。 | 已通过 |
| ACC-STAGE090-P2-04 | P2 期间不执行真实资料、检索、回答、报告或证据账本访问、数据库 schema 或连接、实际证据捕获、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage090 P3。 | 已遵守 |
| ACC-STAGE090-P3-01 | 冻结 Stage090 任务包、Stage090 P1/P2 控制工件与 Stage089 Review/前序 evidence schema 合同保持为唯一合同上下文；P3 重放六条固定、非业务、reference-only 控制投影，未读取、打开、保留或写入业务资料、原始元数据、manifest、检索结果、证据账本、审计日志、回答、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE090-P3-02 | 七个纯内存场景覆盖无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料与低等级伪装高可信结论；每场景固定 32 个控制字段，共 224 个场景字段检查点，P2 的六条、十组投影、462 个字段检查点与 evidence、document、chunk、fact、query、answer、report 关联保持一致。 | 已通过 |
| ACC-STAGE090-P3-03 | 无内部证据保留 evidence_gap 引用；低 OCR、旧版本、冲突与撤回维持降级候选，疑似恶意资料维持隔离候选；撤回输出未来报告状态影响引用，D 等级证据进入高可信结论拒绝状态。P2 输出偏离、关键引用缺失、控制引用透明化或运行信号出现时进入失败关闭，全部场景要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE090-P3-04 | P3 期间真实资料、检索、回答、报告、证据账本、数据库、OCR、版本评估、冲突裁决、风险计算、可信等级变更、撤回、投毒处置、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传与推送保持在后续授权范围；本轮全量本地验收完成后在新 run 进入 Stage090 P4。 | 已遵守 |
| ACC-STAGE090-P1-01 | 冻结 Stage090 任务包与 Stage089 Review/P1--P4 已复审证据账本控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、检索结果、证据账本、审计日志、回答、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE090-P1-02 | 检索 trace、query、answer、report、document、chunk、fact、source type、source version、evidence capture、evidence_id、evidence_gap、可信等级、风险、撤回、投毒防护与关键结论只复用不透明控制字段；A/B/C/D/E 可信等级完整，关键结论未来必须关联 evidence_id 或 evidence_gap。 | 已通过 |
| ACC-STAGE090-P1-03 | 检索、回答、document/chunk/fact/report、evidence capture、evidence gap 或投毒防护引用缺失，以及关键结论同时缺 evidence_id 和 evidence_gap、低等级伪装为高等级、低可信／冲突／过期／撤回证据未降级时必须失败关闭；未来业务使用仍须业务线白箱人工复核。 | 已通过 |
| ACC-STAGE090-P1-04 | P1 期间不执行真实资料、检索、回答、报告或证据账本访问、数据库 schema 或连接、实际证据捕获、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage090 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE090-P1-2026-08-23-local` | Stage090 P1 静态检索证据捕获合同（本地零运行时） | `PASS_RETRIEVAL_EVIDENCE_CAPTURE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE090-P2-2026-08-23-local` | Stage090 P2 从检索捕获证据受控最小切片（本地零运行时） | `PASS_RETRIEVAL_EVIDENCE_CAPTURE_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE090-P3-2026-08-24-local` | Stage090 P3 从检索捕获证据异常场景验证（本地零运行时） | `PASS_RETRIEVAL_EVIDENCE_CAPTURE_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
