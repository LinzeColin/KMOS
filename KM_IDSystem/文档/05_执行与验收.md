<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE067 · IDS-V0_1-STAGE067-REVIEW · IDS-V0_1-STAGE067-REVIEW

**负责：** Codex 专属 KMOS worktree，Stage067 Review

## 二、怎么算做完

当前阶段显示 20 项；另有 466 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE067-REVIEW-01 | 冻结 Stage067 任务包、P1--P4 控制工件、Stage066 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；复审只读重放合同和纯内存控制报告，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE067-REVIEW-02 | 机械核验 P1 12/17/3/6/15、P2 4/4/24、P3 六类显式人工处置与 36 次控制追溯检查、P4 6/6/4/6/36/6/3/11 metadata-only 交付形状，发现数为零；所有计数都只表示控制形状，不表示真实质量、重复检测、去重或来源验证。 | 已通过 |
| ACC-STAGE067-REVIEW-03 | 六类固定场景均维持业务线白箱人工处理，控制引用、JSONL 样例、低质量待人工清单、回归结果和策略边界不能替代来源文档或业务事实；P4 仅可纯内存回退至 P3，撤销 Review 仅回到 P4 并保留 P1--P4 证据。 | 已通过 |
| ACC-STAGE067-REVIEW-04 | 复审未执行真实资料访问、解析、切块、身份/版本、质量测量、质量回归、质量降级、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；本次不启动 Stage068，下一步仅可在新的独立 run 进入 Stage068 P1。 | 已遵守 |
| ACC-STAGE067-P4-01 | 冻结 Stage067 任务包、P1/P2/P3 控制工件、Stage066 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P4 六条样例均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE067-P4-02 | P4 仅在内存中从 P3 六类固定控制场景派生六条 JSONL 样例、控制交付覆盖报告、六条低质量待人工清单、控制回归结果、切块策略适用边界、三条中文确认和十一类失败关闭；四条唯一控制质量回归记录和三十六次控制追溯检查只表示固定控制形状。 | 已通过 |
| ACC-STAGE067-P4-03 | 长文档、跨页表格、施工步骤、参数表、引用页码与重复 chunk 均保持业务线白箱人工处理；控制样例、覆盖报告、低质量清单、回归结果和回滚说明不等于真实质量、重复检测、去重、来源验证、重生成或版本回退，不能替代来源文档或形成业务事实。 | 已通过 |
| ACC-STAGE067-P4-04 | 未读取、打开、检测、解析、切分、计算、分类、去重、创建、写入、重生成或回退真实资料、页面、chunk、身份、hash、版本、质量、质量降级、重复项或来源绑定，未执行 parser、章节检测、质量回归、质量降级、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage067 Review。 | 已遵守 |
| ACC-STAGE067-P3-01 | 冻结 Stage067 任务包、P1/P2 控制工件、Stage066 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；六类场景均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE067-P3-02 | P3 纯内存重放 P2 的四条十七字段低可信控制记录，覆盖长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界；六类均有显式处置、静默丢弃为零且全部要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE067-P3-03 | 每个场景保留 document、page、section、parser output、表格上下文与来源片段六维控制引用形状；该形状不等于真实来源或页码已经验证。工程步骤、验收条款和参数表保持受保护语义面；重复场景仅确认本控制模块没有发起 embedding 或索引写入，不检测真实重复项、不计算真实身份、版本或 hash，也不形成真实去重结论。 | 已通过 |
| ACC-STAGE067-P3-04 | 未读取、打开、检测、解析、切分、计算、分类、去重或创建真实资料、页面、chunk、身份、hash、版本、质量、质量降级、重复项或来源绑定，未执行 parser、章节检测、质量回归、质量降级、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P4。 | 已遵守 |
| ACC-STAGE067-P2-01 | 冻结 Stage067 任务包、P1 静态合同、Stage066 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P2 只接受四条固定、非业务、十二字段、:control: 引用请求，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE067-P2-02 | 纯内存切片严格核验四条固定输入并投影四条十七字段低可信待人工复核控制记录，保留工程步骤/验收条款/参数表三类保护语义面、一个重复 embedding/index 写入边界和 document/page/section/parser output/表格上下文/来源片段六维控制追溯；所有标签不代表真实 chunk、hash、页码、质量、重复项、去重或来源验证。 | 已通过 |
| ACC-STAGE067-P2-03 | 四条控制记录均为低可信并要求业务线白箱人工复核；工程步骤、验收条款和参数表不得被质量标签覆盖，重复 chunk 场景只标记不得重复 embedding/index 写入，质量降级仅保留未来 Stage068 交接未启动状态，不形成实际质量、质量降级或业务结论。 | 已通过 |
| ACC-STAGE067-P2-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、覆盖率、质量回归、质量降级、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P3。 | 已遵守 |
| ACC-STAGE067-P1-01 | 冻结 Stage067 任务包、Stage066 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P1 未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE067-P1-02 | P1 固定十二字段仅引用输入、十七字段未来质量回归输出、工程步骤/验收条款/参数表三类保护语义面、六维追溯、重复 embedding/index 写入边界和十五类失败关闭；所有字段只代表未来 schema，不填入实际质量、阈值、页码、来源、Chunk 或重复检测结论。 | 已通过 |
| ACC-STAGE067-P1-03 | 工程步骤、验收条款和参数表保持受保护语义面；跨页参数表、重复写入边界或来源追溯无法确认时必须关闭并交由业务线白箱人工处理，质量字段、控制标签和模型输出均不能替代来源文档或形成业务事实；质量降级仅交给未来 Stage068。 | 已通过 |
| ACC-STAGE067-P1-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、覆盖率、质量回归、质量降级、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P2、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage067 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE067-P1-LOCAL-20260814-001` | 只定义冻结 Stage067 切块质量回归的引用式静态合同，覆盖未来工程步骤、验收条款、参数表保护面、重复 embedding/index 写入边界、质量状态、document/page/section/parser output 追溯、失败关闭、中文反馈和回滚边界；不读取、打开、解析、切分、检测、计算或创建真实业务资料、页面、chunk、质量结果、重复项或业务结论。 | `PASS_LOCAL_PHASE1_CHUNK_QUALITY_REGRESSION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE067-P2-LOCAL-20260814-001` | 只在内存中重放四条固定、非业务、reference-only 切块质量回归控制请求，投影四条低可信待人工复核控制记录，机械确认工程步骤、验收条款、参数表保护面、重复 embedding/index 写入边界和六维追溯引用；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量结果、重复项或业务结论。 | `PASS_LOCAL_PHASE2_CHUNK_QUALITY_REGRESSION_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE067-P3-LOCAL-20260814-001` | 只在内存中重放四条固定、非业务、reference-only 切块质量回归 Phase2 控制记录，并把长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界投影为六类显式低可信业务线白箱人工处置；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量结果、重复项或业务结论。 | `PASS_LOCAL_PHASE3_CHUNK_QUALITY_REGRESSION_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE067-P4-LOCAL-20260814-001` | 只在内存中把 Stage067 P3 的六类固定、非业务、reference-only 切块质量回归控制场景投影为六条 metadata-only JSONL 样例、控制交付覆盖报告、低质量待人工复核清单、控制回归结果、切块策略适用边界和 P4 到 P3 的回退说明；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量结果、重复项、索引、数据库、OVH、生产或上传。 | `PASS_LOCAL_PHASE4_CHUNK_QUALITY_REGRESSION_METADATA_ONLY_DELIVERY_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE067-REVIEW-LOCAL-20260814-001` | 只机械复审冻结 Stage067 P1--P4 合同与 P3/P4 纯内存切块质量回归控制报告，核验固定控制形状、受保护语义面、六类白箱人工场景、metadata-only 交付、单一权威、零运行时和 P4 到 P3 控制回退；不读取、解析、检测、切分、计算、写入、重生成或回退真实业务资料。 | `PASS_LOCAL_REVIEWED_CHUNK_QUALITY_REGRESSION_RUNTIME_DISABLED` |
