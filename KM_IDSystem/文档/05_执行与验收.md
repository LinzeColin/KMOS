<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE081 · IDS-STAGE081-REVIEW · IDS-V0_1-STAGE081-REVIEW

**负责：** 既有唯一 KMOS 开发 worktree，Stage081 Review 本地验收完成

## 二、怎么算做完

当前阶段显示 20 项；另有 755 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE081-P1-01 | 冻结 Stage081 任务包、Stage080 Review 与 Stage080 已审核 P1--P4 索引回滚控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE081-P1-02 | 未来索引版本、活动指针、构建中版本、影子索引和冒烟测试只复用既有控制字段形状；每次未来批量导入建立隔离候选和影子且不得覆盖活动版本，旧活动索引在构建、冒烟、切换与回滚准备期间继续服务；所有字段只表示静态工程边界。 | 已通过 |
| ACC-STAGE081-P1-03 | 新索引未通过、缺失或未记录冒烟测试不得切换，构建或冒烟失败时活动指针保持不变；回滚只可指向保留且仍在回滚窗口内的上一活动版本，未来例外仍须业务线白箱人工处理。 | 已通过 |
| ACC-STAGE081-P1-04 | P1 期间不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、原子切换、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage081 P2。 | 已遵守 |
| ACC-STAGE081-P2-01 | 冻结 Stage081 任务包、Stage081 P1 与 Stage080 Review/P2 已审核索引回滚控制工件保持为唯一合同上下文；P2 只接收五条固定、非业务、reference-only 控制请求，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE081-P2-02 | 每条控制请求记录 index_version、document_scope_ref、chunk_count=0 与 embedding_model_ref，并投影索引版本记录、候选构建与影子、活动指针、冒烟输入输出、未应用切换和未应用回滚的 7/5/5/6/5/9/8 字段；候选、活动、上一活动和影子保持隔离，候选不得覆盖活动版本，旧活动版本在控制投影期间继续服务。 | 已通过 |
| ACC-STAGE081-P2-03 | 候选构建未完成、影子不隔离、冒烟未执行、缺失或失败时未来切换候选关闭且活动指针投影保持不变；冒烟完整通过只产生未应用的未来切换候选，计划切换失败同样保持活动版本，回滚候选只指向保留且仍在控制窗口内的上一活动版本，业务线白箱审批只表达控制标签。 | 已通过 |
| ACC-STAGE081-P2-04 | P2 不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、原子切换、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage081 P3。 | 已遵守 |
| ACC-STAGE081-P3-01 | 冻结 Stage081 任务包、Stage081 P1/P2 和 Stage080 Review/P2 已审核索引回滚控制工件保持为唯一合同上下文；P3 只重放五条固定、非业务、reference-only 的 P2 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、实际 Operations、报告或物理索引。 | 已通过 |
| ACC-STAGE081-P3-02 | 六条专项场景显式覆盖候选构建未完成的失败关闭、影子冒烟测试失败、切换失败、回滚候选、旧活动版本连续服务与后台构建期间并发检索控制隔离；P2 没有真实或模拟的物理索引构建失败，P3 只观察 CONTROL_BACKGROUND_BUILD_INCOMPLETE_REFERENCE_ONLY 的未完成关闭状态，不将其改写为真实构建失败；六条均有显式处置，静默丢弃为零。 | 已通过 |
| ACC-STAGE081-P3-03 | 失败、未完成或切换失败的候选均不改变活动版本，回滚候选只引用保留且仍在控制窗口内的上一活动版本；五条 Operations 与五条报告快照控制视图仅显示不透明控制版本引用，未写入真实界面或报告；并发检索不受影响只以活动指针未变、旧版本连续服务、候选／影子隔离和零实际检索动作表示。 | 已通过 |
| ACC-STAGE081-P3-04 | P3 验证期间不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、实际冒烟测试、活动指针读写、检索、并发检索、回退、Operations 写入、报告快照写入、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage081 P4。 | 已遵守 |
| ACC-STAGE081-P4-01 | 冻结 Stage081 任务包、Stage081 P1/P2/P3 与 Stage080 Review/P2 已审核索引回滚控制工件保持为唯一合同上下文；P4 只在内存中派生 metadata-only 索引清单、冒烟测试日志、切换记录和回滚证明，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE081-P4-02 | 五条索引清单、六条冒烟测试日志、五条切换记录、五条回滚证明、一条旧活动版本保留／未测量空间影响投影、三条操作说明和四条中文反馈均只引用不透明 control 标识；上一活动版本保留只表达回滚前置，空间影响未测量、未删除且须业务线白箱人工处理。 | 已通过 |
| ACC-STAGE081-P4-03 | 索引重建、暂停和恢复只表达未来操作前置、旧活动版本连续服务和保留上一活动版本回滚目标；不发起任何实际操作，不改变活动指针、不写入 Operations 或报告快照，中文反馈清楚说明控制边界与人工复核前置。 | 已通过 |
| ACC-STAGE081-P4-04 | P4 交付期间不执行真实资料访问、实际 manifest／日志／切换／回滚证明写入、空间测量或旧索引删除、索引重建／暂停／恢复、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、实际冒烟测试、活动指针读写、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage081 Review。 | 已遵守 |
| ACC-STAGE081-REVIEW-01 | 冻结 Stage081 任务包、P1--P4 合同、P2/P3/P4 纯内存控制报告与 Stage080 Review/P2 已审核索引回滚控制工件保持为唯一合同上下文；Review 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、实际 Operations、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE081-REVIEW-02 | 机械复审 P1 的 7/5/5/6/5/8/8/5/10、P2 的五条固定控制请求及七组各五条投影／225 次字段检查、P3 的六条场景／28 字段／168 次字段检查／五条 Operations 与五条报告快照控制视图／六条人工处理，以及 P4 的 5/6/5/5/1/3/4/13 交付形状；候选隔离、失败关闭、旧活动版本连续服务、回滚目标、保留和未测量空间影响保持一致。 | 已通过 |
| ACC-STAGE081-REVIEW-03 | P4→P3 控制回退只保留 P1--P4 工件，复审输出不得替代来源文档或业务线白箱人工复核，不自动作出业务决策；Stage082 只开放后续门禁，未启动任何 Stage082 工作。 | 已通过 |
| ACC-STAGE081-REVIEW-04 | Review 不执行真实资料访问、实际 manifest／日志／切换／回滚证明写入、空间测量或旧索引删除、索引重建／暂停／恢复、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、实际冒烟测试、活动指针读写、检索、并发检索、回退、Operations 写入、报告快照写入、provider/模型选择或调用、模型 Token、持久化、Agent、OVH、生产、上传或推送；只有本地全量验收完成后才可在新 run 进入 Stage082 P1。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE081-P1-LOCAL-20260822-001` | 静态合同、治理兼容与零运行时边界本地复验 | `PASS_SHADOW_INDEX_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE081-P2-LOCAL-20260822-001` | 固定影子索引控制切片、治理兼容与零运行时边界本地复验 | `PASS_SHADOW_INDEX_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE081-P3-LOCAL-20260822-001` | 影子索引受控场景、治理兼容与零运行时边界本地复验 | `PASS_SHADOW_INDEX_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE081-P4-LOCAL-20260822-001` | 影子索引 metadata-only 交付证据、治理兼容与零运行时边界本地复验 | `PASS_SHADOW_INDEX_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE081-REVIEW-LOCAL-20260822-001` |  | `PASS_REVIEWED_SHADOW_INDEX_RUNTIME_DISABLED` |
