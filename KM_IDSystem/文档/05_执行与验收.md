<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE051 · IDS-V0_1-STAGE051-P3 · IDS-V0_1-STAGE051-P3

**负责：** Codex 专属 KMOS worktree，Stage051 P3

## 二、怎么算做完

当前阶段显示 12 项；另有 142 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE051-P3-01 | P3 只以扫描 PDF、模糊图片、表格图片、中英文混合和低质量五类固定非业务标量 control 重放 P2 队列；类别不是文件、页面、图像、表格或 OCR 结果，未建立第二权威事实源。 | 已通过 |
| ACC-STAGE051-P3-02 | 五类 control 均有明确候选、降级或失败处置，静默丢弃为零；低置信和中英混合不允许直接进入高可信证据层，当前未创建实际人工复核队列。 | 已通过 |
| ACC-STAGE051-P3-03 | P2 缓存继续为仅内存可重建且没有缓存路径或临时产物，因此没有内置盘占用或本轮实际缓存清理；缓存保留与清理细则仍归 Stage056。 | 已通过 |
| ACC-STAGE051-P3-04 | 未读取真实资料或打开 PDF/图片，未调用 OCR 引擎、执行图像处理、表格提取、准确率评估、质量门、持久化、智能体、模型调用、OVH、生产、上传或推送。 | 已遵守 |
| ACC-STAGE051-P2-01 | P2 仅使用 P1 七字段引用输入和四个固定非业务控制页形成纯内存队列记录；未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、页面、图像或路径。 | 已通过 |
| ACC-STAGE051-P2-02 | 四个控制页均形成八字段逐页结构和内存派生的来源页引用；结果只保留候选状态，未选择或调用 OCR 引擎。 | 已通过 |
| ACC-STAGE051-P2-03 | 低置信、失败与中英混合控制页均有明确中文状态，不能直接进入高可信证据层；当前未创建实际人工复核队列。 | 已通过 |
| ACC-STAGE051-P2-04 | 缓存策略为仅内存可重建且未持久化；未执行真实资料读取、OCR 引擎调用、持久队列或页面输出写入、缓存写入、质量门、证据提升、持久化、智能体、模型调用、OVH、生产、上传或推送。 | 已遵守 |
| ACC-STAGE051-P1-01 | 冻结 Stage051 任务包与 BATCH041-050 已复审工件保持为唯一合同上下文；未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、页面、图像或路径。 | 已通过 |
| ACC-STAGE051-P1-02 | OCR 输入固定为七字段仅引用元数据和三类输入提示；未来按页输出固定为八字段，默认语言为中文简体与英文，未选择或配置 OCR 引擎。 | 已通过 |
| ACC-STAGE051-P1-03 | 低置信度页面不能直接进入高可信证据层；复核路由归 Stage054，缓存仅声明为后续可重建派生产物，当前未创建缓存或复核记录。 | 已通过 |
| ACC-STAGE051-P1-04 | 未执行真实资料读取、OCR 引擎选择或调用、队列创建、按页输出、缓存写入、质量门、证据提升、持久化、Agent、模型调用、OVH、生产、上传或推送。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE051-P1-LOCAL-20260812-001` | 定义 OCR 队列的引用输入、按页输出、默认中文简体与英文、低置信度隔离、缓存边界、中文反馈与回滚范围 | `PASS_PHASE1_OCR_QUEUE_BOUNDARY_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE051-P2-LOCAL-20260812-001` | 执行固定非业务控制页的纯内存 OCR 队列切片，生成逐页结构、来源页引用、置信度与可解释状态 | `PASS_PHASE2_CONTROLLED_OCR_QUEUE_SLICE_ENGINE_DISABLED` |
| `RUN-IDS-STAGE051-P3-LOCAL-20260813-001` | 重放四页固定非业务 control 队列，以五类标量质量类别验证候选、降级、失败与无临时缓存产物处置 | `PASS_PHASE3_CONTROLLED_OCR_QUALITY_SCENARIOS_RUNTIME_DISABLED` |
