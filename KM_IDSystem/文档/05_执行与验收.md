<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE068 · IDS-V0_1-STAGE068-P4 · IDS-V0_1-STAGE068-P4

**负责：** Codex 专属 KMOS worktree，Stage068 Phase 4

## 二、怎么算做完

当前阶段显示 16 项；另有 486 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE068-P4-01 | 冻结 Stage068 任务包、Stage068 P1/P2/P3 控制工件、Stage067 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P4 六条样例均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE068-P4-02 | P4 仅在内存中从 P3 六类固定控制场景派生六条 JSONL 样例、控制交付覆盖报告、六条低质量待人工清单、控制回归结果、切块策略适用边界、三条中文确认和十二类失败关闭；四条唯一控制质量降级记录与三十六次控制追溯检查只表示固定控制形状。 | 已通过 |
| ACC-STAGE068-P4-03 | 长文档、跨页表格、施工步骤、参数表、引用页码与重复 chunk 均保持业务线白箱人工处理；控制样例、覆盖报告、低质量清单、回归结果和回滚说明不等于真实质量、质量降级、低可信证据、重复检测、去重、来源验证、重生成或版本回退，不能替代来源文档或形成业务事实。 | 已通过 |
| ACC-STAGE068-P4-04 | 未读取、打开、检测、解析、切分、计算、分类、去重、创建、写入、重生成或回退真实资料、页面、chunk、身份、hash、版本、质量、质量降级、低可信证据、重复项或来源绑定，未执行 parser、章节检测、质量回归、质量降级、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage068 Review。 | 已遵守 |
| ACC-STAGE068-P3-01 | 冻结 Stage068 任务包、Stage068 P1/P2 控制工件、Stage067 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P3 只重放四条固定、非业务、十九字段、:control: 引用控制记录，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE068-P3-02 | 纯内存场景固定覆盖长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界六类处置，保留四条唯一 P2 控制记录、每条 document/page/section/parser output/表格上下文/来源片段六维 :control: 追溯、六条显式人工处置和零静默丢弃；所有标签不代表真实长文档、页码、chunk、质量、质量降级、低可信证据、重复项、去重或来源验证。 | 已通过 |
| ACC-STAGE068-P3-03 | 低质量不等于自动完全失败：六类控制场景均固定转人工处理，工程步骤、验收条款和参数表保持受保护语义面，重复场景只确认控制模块没有发起 embedding/index 写入，不检测真实重复项、不形成实际质量降级、低可信证据、去重或业务结论。 | 已通过 |
| ACC-STAGE068-P3-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、覆盖率、质量回归、质量降级、低可信证据创建、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P4。 | 已遵守 |
| ACC-STAGE068-P2-01 | 冻结 Stage068 任务包、Stage068 P1 静态合同、Stage067 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P2 只接受四条固定、非业务、十三字段、:control: 引用请求，未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE068-P2-02 | 纯内存切片严格核验四条固定输入并投影四条十九字段低可信待人工复核控制记录，保留工程步骤/验收条款/参数表三类保护语义面、一个重复 embedding/index 写入边界和 document/page/section/parser output/表格上下文/来源片段六维控制追溯；所有标签不代表真实 chunk、hash、页码、质量、质量降级、低可信证据、重复项、去重或来源验证。 | 已通过 |
| ACC-STAGE068-P2-03 | 低质量不等于自动完全失败：三条受保护面控制记录固定转业务线白箱人工复核，一条重复写入边界控制记录固定转低可信证据人工复核；工程步骤、验收条款和参数表不得被质量降级状态覆盖，重复场景只标记不得重复 embedding/index 写入，不形成实际质量降级或业务结论。 | 已通过 |
| ACC-STAGE068-P2-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、覆盖率、质量回归、质量降级、低可信证据创建、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P3。 | 已遵守 |
| ACC-STAGE068-P1-01 | 冻结 Stage068 任务包、Stage067 本地整阶段机械复审与 Batch061-070 上传锁保持为唯一合同上下文；P1 未建立第二权威事实源，未读取、打开或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE068-P1-02 | P1 固定 13 字段仅引用输入、19 字段未来质量降级输出、需业务线白箱人工复核/低可信证据需人工复核两个未来分流、工程步骤/验收条款/参数表三类保护语义面、六维追溯、重复 embedding/index 写入边界和 17 类失败关闭；所有字段只代表未来 schema，不填入实际质量、阈值、页码、来源、chunk 或降级结论。 | 已通过 |
| ACC-STAGE068-P1-03 | 低质量不等于自动完全失败，但未来只能在业务线白箱人工复核可接管时标记为需复核或低可信证据需人工复核；工程步骤、验收条款和参数表不得被质量降级状态覆盖，任何控制标签、低可信字段或模型输出均不能替代来源文档或形成业务事实。 | 已通过 |
| ACC-STAGE068-P1-04 | 未执行真实资料访问、解析、切块、身份/版本、分类、覆盖率、质量回归、质量降级、低可信证据创建、重复检测或去重、来源追溯、embedding、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P2、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage068 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE068-P1-LOCAL-20260814-001` | 本地静态质量降级合同、治理投影与零运行时边界验证 | `PASS_LOCAL_STAGE068_PHASE1_QUALITY_DEGRADATION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE068-P2-LOCAL-20260814-001` | 只在内存中重放四条固定、非业务、reference-only 质量降级控制请求，投影四条低可信待人工复核控制记录，机械确认工程步骤、验收条款、参数表保护面、重复 embedding/index 写入边界、六维追溯和低质量不等于自动完全失败；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量、降级、低可信证据、重复项或业务结论。 | `PASS_LOCAL_PHASE2_QUALITY_DEGRADATION_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE068-P3-LOCAL-20260814-001` | 只在内存中重放四条固定、非业务、reference-only Phase2 质量降级控制记录，将长文档、跨页表格、施工步骤、参数表、引用页码与来源反查、重复 chunk embedding/index 写入边界映射为六类显式人工处置；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量、质量降级、低可信证据、重复项或业务结论。 | `PASS_LOCAL_PHASE3_QUALITY_DEGRADATION_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE068-P4-LOCAL-20260814-001` | 只在内存中把 Stage068 P3 的六类固定、非业务、reference-only 质量降级控制场景投影为六条 metadata-only JSONL 样例、控制交付覆盖报告、低质量待人工复核清单、控制回归结果、切块策略适用边界和 P4 到 P3 的回退说明；不读取、打开、解析、切分、检测、计算、创建或写入真实业务资料、页面、chunk、质量、质量降级、低可信证据、重复项、索引、数据库、OVH、生产或上传。 | `PASS_LOCAL_PHASE4_QUALITY_DEGRADATION_METADATA_ONLY_DELIVERY_RUNTIME_DISABLED` |
