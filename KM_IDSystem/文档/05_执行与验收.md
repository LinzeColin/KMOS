<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE089 · IDS-STAGE089-REVIEW · IDS-V0_1-STAGE089-REVIEW

**负责：** 仅使用既有唯一 KMOS 开发 worktree `/Users/linzezhang/Documents/Codex/GithubProject/_scratch/KMOS-kmids-stage071-p1`；不创建额外 worktree、分支或合并请求，且不进入 Stage090、OVH、生产或上传。

## 二、怎么算做完

当前阶段显示 20 项；另有 923 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE089-P1-01 | 冻结 Stage089 任务包与 Stage088 Review/P1--P4 已审核检索结果有效性门禁控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE089-P1-02 | evidence、document、chunk、fact、query、answer、report、source type、evidence grade、evidence gap、risk score、revocation、poisoning defense 与 critical conclusion binding 只复用不透明控制字段；A/B/C/D/E 可信等级完整，关键结论必须关联 evidence_id 或 evidence_gap。 | 已通过 |
| ACC-STAGE089-P1-03 | evidence_id、document_id、chunk_id、fact_id、report_id、source_type、evidence_grade、evidence_gap、风险、撤回或投毒防护引用缺失，以及关键结论同时缺 evidence_id 和 evidence_gap、低等级伪装为高等级、低可信／冲突／过期／撤回证据未降级时必须失败关闭；未来业务使用仍须业务线白箱人工复核。 | 已通过 |
| ACC-STAGE089-P1-04 | P1 期间不执行真实资料或证据账本访问、数据库 schema 或连接、实际证据捕获、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage089 P2。 | 已遵守 |
| ACC-STAGE089-P2-01 | 冻结 Stage089 任务包、Stage089 P1 静态合同与 Stage088 Review/P1--P4 已审核检索结果有效性门禁控制工件保持为唯一合同上下文；P2 只处理六条固定、非业务、reference-only 的纯内存控制输入，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE089-P2-02 | 六条固定控制请求各含 24 个不透明字段，并投影 evidence schema 10、relation 8、evidence gap 5、evidence capture 6、risk score 8、revocation 7、poisoning defense 8、critical conclusion binding 7、degradation 8 与 future integration 7 个字段，共 444 个控制检查点；evidence 与 document、chunk、fact、query、answer、report 的引用链必须一致。 | 已通过 |
| ACC-STAGE089-P2-03 | 关键结论必须携带 evidence_id 或 evidence_gap 控制引用；低可信、冲突、过期和撤回证据必须降级，疑似投毒必须保持隔离候选，A 等级仍待业务线白箱人工复核。任何非固定输入、引用链缺失、低等级伪装为高等级或自动业务采纳均失败关闭。 | 已通过 |
| ACC-STAGE089-P2-04 | P2 期间不执行真实资料或证据账本访问、数据库 schema 或连接、实际证据捕获、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage089 P3。 | 已遵守 |
| ACC-STAGE089-P3-01 | 冻结 Stage089 任务包、Stage089 P1/P2 控制工件与 Stage088 Review/P1--P4 已审核检索结果有效性门禁控制工件保持为唯一合同上下文；P3 只重放六条固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE089-P3-02 | 七个纯内存场景覆盖无内部证据、低 OCR、旧版本、冲突、撤回、恶意资料与低等级伪装高可信结论；每场景固定 32 个控制字段，共 224 个场景字段检查点，P2 的六条／10 组投影／444 个字段检查点、evidence 与 document、chunk、fact、query、answer、report 关联和关键结论绑定必须保持一致。 | 已通过 |
| ACC-STAGE089-P3-03 | 无内部证据必须保留证据缺口引用；低 OCR、旧版本、冲突与撤回必须维持降级候选，疑似恶意资料必须维持隔离候选；撤回只声明未来报告状态影响引用，不更新真实报告；D 等级证据不得伪装支撑高可信结论。P2 输出偏离、关键引用缺失、控制引用不透明性丢失或任一运行信号均失败关闭，所有场景均要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE089-P3-04 | P3 期间不执行真实资料或证据账本访问、数据库 schema 或连接、实际 OCR 或版本评估、冲突裁决、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage089 P4。 | 已遵守 |
| ACC-STAGE089-P4-01 | 冻结 Stage089 任务包、Stage089 P1/P2/P3 控制工件与 Stage088 Review/P1--P4 已审核检索结果有效性门禁控制工件保持为唯一合同上下文；P4 只从七个固定、非业务、reference-only 场景派生交付控制记录，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、报告、审计日志、数据库或物理索引。 | 已通过 |
| ACC-STAGE089-P4-02 | P4 固定交付 7 条证据账本样例、7 条证据等级报告、7 条撤回影响、7 条回归、7 条不可作为结论依据类型、4 条降级和 2 条撤回／恢复说明，共 517 个控制字段检查点；P2 的六条／444 个检查点和 P3 的七场景／224 个检查点必须保持一致。 | 已通过 |
| ACC-STAGE089-P4-03 | 撤回影响清单只声明 control-only 报告影响且不更新报告；无内部证据、低 OCR、旧版本、冲突、撤回、恶意与低等级伪装均明确不可自动作为结论依据。降级、撤回与恢复只保留未来授权和业务线白箱人工处理前置，引用丢失、P3 偏离、形状偏离或运行信号均失败关闭。 | 已通过 |
| ACC-STAGE089-P4-04 | P4 期间不执行真实资料或证据账本访问、数据库 schema 或连接、实际 OCR 或版本评估、冲突裁决、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage089 Review。 | 已遵守 |
| ACC-STAGE089-REVIEW-01 | 冻结 Stage089 任务包、P1--P4 合同、P2/P3/P4 控制输出与 Stage088 Review/P1--P4 已审核检索结果有效性门禁控制工件保持为唯一合同上下文；Review 只机械复审控制工件，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、报告、审计日志、数据库或物理索引。 | 已通过 |
| ACC-STAGE089-REVIEW-02 | Review 固定复审 P1 的 10/8/5/8/7/8/7 字段／23 类失败关闭、P2 的 6 条／24 字段／10 组／444 检查／23 类失败关闭、P3 的 7 场景／32 字段／224 检查／7 条人工处理／15 类失败关闭，以及 P4 的 7/7/7/7/7/4/2 交付形状／4 条中文反馈／517 检查／18 类失败关闭。 | 已通过 |
| ACC-STAGE089-REVIEW-03 | 无内部证据、低 OCR、旧版本、冲突、撤回、恶意与低等级伪装保持控制引用；撤回影响只声明不更新报告，不可作为结论依据类型、未来降级／撤回／恢复前置与业务线白箱人工处理保持；任一合同、控制输出、形状、撤回语义、回退路径或运行时信号偏离均失败关闭。 | 已通过 |
| ACC-STAGE089-REVIEW-04 | Review 期间不执行真实资料或证据账本访问、数据库 schema 或连接、实际 OCR 或版本评估、冲突裁决、风险计算、可信等级变更、撤回、投毒检测、隔离、恢复、报告状态更新、外部 API、模型 Token、持久化、Agent、OVH、生产、Stage090、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage090 P1。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE089-P1-2026-08-23-local` | Stage089 P1 静态证据账本 Schema 合同（本地零运行时） | `PASS_EVIDENCE_LEDGER_SCHEMA_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE089-P2-2026-08-23-local` | Stage089 P2 纯内存证据账本受控最小切片 | `PASS_EVIDENCE_LEDGER_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE089-P3-2026-08-23-local` | Stage089 P3 纯内存证据账本异常场景验证 | `PASS_EVIDENCE_LEDGER_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE089-P4-2026-08-23-local` | Stage089 P4 纯内存证据账本交付控制证据验证 | `PASS_EVIDENCE_LEDGER_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE089-REVIEW-2026-08-23-local` | Stage089 证据账本 Schema 整阶段机械复审（本地零运行时） | `PASS_REVIEWED_EVIDENCE_LEDGER_RUNTIME_DISABLED` |
