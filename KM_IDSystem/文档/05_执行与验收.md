<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE088 · IDS-STAGE088-P4 · IDS-V0_1-STAGE088-P4

**负责：** 仅使用既有唯一 KMOS 开发 worktree `/Users/linzezhang/Documents/Codex/GithubProject/_scratch/KMOS-kmids-stage071-p1`；不创建额外 worktree、分支或合并请求，且不进入 Stage088 Review、Stage089、OVH、生产或上传。

## 二、怎么算做完

当前阶段显示 16 项；另有 902 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE088-P1-01 | 冻结 Stage088 任务包与 Stage087 Review/P1--P4 已审核检索轨迹控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE088-P1-02 | query、文档类型／年份／项目／设备／资料状态／证据等级六类 filter、candidate、selected、score、active_index_version、retrieval trace 与结果有效性门禁只复用不透明控制字段；关键词检索基线与向量检索基线都必须存在，向量相似度或系统输出不得单独决定未来结果有效性。 | 已通过 |
| ACC-STAGE088-P1-03 | query、filter、关键词／向量基线、活动索引版本、candidate／selected 引用链、请求 Top-K、混合 score、排序策略、score explanation、证据账本引用、retrieval trace、观测有效性状态或门禁状态任一缺失时必须失败关闭；未来实际检索与有效性测试仍须业务线白箱在后续授权 Phase 中受控实施。 | 已通过 |
| ACC-STAGE088-P1-04 | P1 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹、结果有效性判定或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage088 P2。 | 已遵守 |
| ACC-STAGE088-P2-01 | 冻结 Stage088 任务包、Stage088 P1 静态合同与 Stage087 Review/P1--P4 已审核检索轨迹控制工件保持为唯一合同上下文；P2 只执行六条固定、非业务、reference-only 的纯内存控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE088-P2-02 | 六条固定控制请求各含 18 个不透明字段，并覆盖文档类型、年份、项目、设备、资料状态和证据等级；每条只投影 query 9、filter 8、active_index_version 7、candidate 10、score 7、selected 10、retrieval trace 14、结果有效性门禁 16 与 future integration 7 个字段，共 528 个控制检查点。candidate、selected、trace 与结果有效性门禁的活动索引版本、元数据过滤、关键词／向量基线、评分、排序策略、评分解释与证据账本引用链必须一致。 | 已通过 |
| ACC-STAGE088-P2-03 | 关键词与向量基线必须同时声明，vector-only、非固定输入、请求 Top-K、六类过滤维度、活动索引版本、过滤合同、candidate／selected 引用、评分、排序策略、评分解释、证据账本引用、检索轨迹或结果有效性门禁引用任一缺失时必须失败关闭；仅凭系统输出不得接受结果，结果有效性保持未评估并等待业务线白箱人工复核；PostgreSQL full-text／BM25、pgvector、过滤、混合排序、轨迹与结果有效性门禁仅保留 future-only 路由。 | 已通过 |
| ACC-STAGE088-P2-04 | P2 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹、结果有效性判定或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage088 P3。 | 已遵守 |
| ACC-STAGE088-P3-01 | 冻结 Stage088 任务包、Stage088 P1/P2 合同与 Stage087 Review/P1--P4 已审核检索轨迹控制工件保持为唯一合同上下文；P3 只重放六条固定、非业务、reference-only 控制投影，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE088-P3-02 | 八类场景覆盖关键词、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K／排序解释／结果有效性和旧索引服务版本轨迹；领域项只表示场景类别，P2 六条控制投影及其 528 个字段检查点只提供不透明引用；每场景固定 33 个字段，共 264 个检查点。 | 已通过 |
| ACC-STAGE088-P3-03 | 关键词与向量基线、vector-only 拒绝、六维过滤、活动索引版本、candidate／selected／score／trace／evidence ledger／结果有效性门禁引用链必须完整；P2 输出偏离、关键引用缺失、控制引用不透明性丢失、门禁不再待人工白箱复核或任一运行信号均失败关闭。八场景都明确要求业务线白箱人工处理，结果有效性保持未评估，未记录自动审批、自动业务建议、自动写入或自动索引切换。 | 已通过 |
| ACC-STAGE088-P3-04 | P3 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、材料牌号／设备型号／标准号匹配、语义计算、元数据过滤、混合排序、Top-K 选择、检索轨迹、结果有效性判定、旧索引服务或证据账本读写、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage088 P4。 | 已遵守 |
| ACC-STAGE088-P4-01 | 冻结 Stage088 任务包、Stage088 P1/P2/P3 合同与 Stage087 Review/P1--P4 已审核检索轨迹控制工件保持为唯一合同上下文；P4 只从 P3 的八个固定、非业务、reference-only 场景派生内存控制记录，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE088-P4-02 | P4 固定派生 8 条检索样例、8 条 trace 日志、8 条过滤结果、8 条有效性测试报告、8 条检索不足／证据缺口、4 条参数回滚说明和 4 条中文反馈，共 572 个交付字段检查点；P2 的六条／528 个检查点与 P3 的八场景／264 个检查点、活动索引版本和结果有效性门禁引用链必须保持一致。 | 已通过 |
| ACC-STAGE088-P4-03 | P3 前置形状、控制引用不透明性、关键词与向量基线、vector-only 拒绝、六维过滤、candidate／selected／score／trace／证据账本／结果有效性门禁引用、P4 交付形状、显式证据缺口、业务线白箱人工复核或零运行时边界任一失配均失败关闭；结果有效性保持未评估，不记录自动缺口处理、自动业务建议、自动参数回滚、自动写入或自动索引切换。 | 已通过 |
| ACC-STAGE088-P4-04 | P4 期间不执行真实资料访问、PostgreSQL schema 或连接、FTS/BM25/pgvector 索引构建、embedding、关键词或向量检索、元数据过滤、混合排序、Top-K 选择、检索轨迹、结果有效性判定、旧索引服务或证据账本读写、参数写入或回滚、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；Stage088 Review、Stage089 与后续动作未启动，下一步只能在新 run 进入 Stage088 Review。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE088-P1-2026-08-23-local` | Stage088 P1 静态检索结果有效性门禁合同（本地零运行时） | `PASS_RETRIEVAL_RESULT_VALIDITY_GATE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE088-P2-2026-08-23-local` | Stage088 P2 纯内存检索结果有效性门禁控制切片（本地零运行时） | `PASS_RETRIEVAL_RESULT_VALIDITY_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE088-P3-2026-08-23-local` | Stage088 P3 纯内存检索结果有效性门禁受控场景（本地零运行时） | `PASS_RETRIEVAL_RESULT_VALIDITY_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE088-P4-2026-08-23-local` | Stage088 P4 检索结果有效性门禁交付证据、回滚与中文反馈（本地零运行时） | `PASS_RETRIEVAL_RESULT_VALIDITY_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
