<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE058 · IDS-V0_1-STAGE058-P2 · IDS-V0_1-STAGE058-P2

**负责：** Codex 专属 KMOS worktree，Stage058 Phase 2

## 二、怎么算做完

当前阶段显示 8 项；另有 282 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE058-P2-01 | Stage058 P2 只以冻结任务包、P1 静态合同和 Stage057 已复审工件为唯一合同上下文；两条输入均为固定、非业务、reference-only 十字段控制记录，不包含业务资料、真实表头、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P2-02 | 纯内存切片形成 2 个 Schema profile 组、11 条 18 字段候选、9 类字段语义、6 类候选字段类型与 11 条来源位置引用；候选列名只使用 control handle，不构成真实列名、真实 schema 或真实字段映射。 | 已通过 |
| ACC-STAGE058-P2-03 | 来源文档继续保持权威；事实抽取和 RAG 摘要分别仍归 Stage059/060，本 P2 不创建事实、摘要、数值统计、证据、数据库或持久状态；无效控制输入必须 REJECTED。 | 已通过 |
| ACC-STAGE058-P2-04 | 未读取、打开、检测或解析真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行真实 schema/字段/事实、统计、数据库、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 P3。 | 已遵守 |
| ACC-STAGE058-P1-01 | Stage058 P1 只以冻结任务包和 Stage057 已复审工件为唯一合同上下文；XLSX/CSV、生产记录和质检记录只登记引用字段、接口名称与静态边界，不包含业务资料、表格内容、单元格、公式、来源正文、物理路径或第二权威事实源。 | 已通过 |
| ACC-STAGE058-P1-02 | 合同固定十字段 reference-only 输入、十八字段未来 Schema profile、九类字段候选、六类候选字段类型、六类来源位置和八类失败关闭；当前没有真实输入、Schema profile、字段映射、事实、绑定或证据记录。 | 已通过 |
| ACC-STAGE058-P1-03 | 来源文档保持权威；未来数值统计只能使用带来源位置和证据绑定的结构化事实，模型不能根据文本猜测数值或统计结论，RAG 摘要不能替代事实或成为数值权威；无法识别的表头、列名、字段类型、单位、日期、设备、材料、工序或质量结果必须等待人工处理。 | 已通过 |
| ACC-STAGE058-P1-04 | 未读取、打开、检测、解析或推断真实 XLSX/CSV、生产记录、质检记录或 fixture，未执行事实抽取、摘要、统计、数据库、持久化、Agent、模型 Token、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage058 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 2 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE058-P1-LOCAL-20260813-001` | 定义 XLSX/CSV、生产记录和质检记录的表格 Schema 推断引用输入、未来 Schema profile、字段候选、数值与 RAG 权威边界及回滚静态合同 | `PASS_PHASE1_TABLE_SCHEMA_INFERENCE_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE058-P2-LOCAL-20260813-001` | 以两条固定非业务 reference-only 表格控制记录在内存中投影 Schema profile 与字段候选，验证字段类型和来源位置引用形状；不读取或解析真实表格 | `PASS_PHASE2_TABLE_SCHEMA_INFERENCE_CONTROL_SLICE_RUNTIME_DISABLED` |
