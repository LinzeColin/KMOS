<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE076 · IDS-V0_1-STAGE076-P4 · IDS-V0_1-STAGE076-P4

**负责：** 既有唯一 KMOS 开发 worktree，Stage076 P4

## 二、怎么算做完

当前阶段显示 16 项；另有 650 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE076-P1-01 | 冻结 Stage076 任务包、Stage075 Review 和 Stage075 P1 静态合同保持为唯一合同上下文；P1 只定义静态工程合同，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告或物理索引。 | 已通过 |
| ACC-STAGE076-P1-02 | 三类索引版本、八字段未来版本记录、五字段活动指针、五字段构建中版本、影子候选隔离、每次未来批量导入建立新版本和构建期间旧活动索引继续服务均已固定；所有字段只表示静态工程边界，不代表真实资料、索引、检索、模型、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE076-P1-03 | 切换前验证必须完整通过，失败或缺失结果时不得切换活动指针；未来回退只可指向保留的上一活动版本，并须业务线白箱人工处理例外情形；P1 不形成真实构建、切换、查询或回退记录。 | 已通过 |
| ACC-STAGE076-P1-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、索引构建、影子索引、切换前验证、活动指针读写、检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P2。 | 已遵守 |
| ACC-STAGE076-P2-01 | 冻结 Stage076 任务包、Stage076 P1 合同、Stage075 Review 和 Stage075 P1 静态合同保持为唯一合同上下文；P2 只接受五条固定、非业务、reference-only 控制输入，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告或物理索引。 | 已通过 |
| ACC-STAGE076-P2-02 | 五条控制输入复用三类索引版本、八字段版本记录、五字段构建中版本和五字段活动指针形状；document_scope_ref、chunk_count=0 与 embedding_model_ref 均为不透明控制标签，构建中候选与活动版本不同，旧活动版本继续服务。 | 已通过 |
| ACC-STAGE076-P2-03 | 影子候选保持隔离；六项切换前验证未完成或失败时阻断切换；已通过候选只形成未来全有或全无切换控制投影，切换失败保持活动版本，回退候选只指向保留的上一活动版本；业务线白箱人工复核仍为未来例外前置。 | 已通过 |
| ACC-STAGE076-P2-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、索引构建、影子索引、切换前验证、活动指针读写、检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P3。 | 已遵守 |
| ACC-STAGE076-P3-01 | 冻结 Stage076 任务包、Stage076 P1/P2 合同、Stage075 Review 和 Stage075 P1 静态合同保持为唯一合同上下文；P3 只重放五条固定、非业务、reference-only 控制投影，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、实际 Operations 或已交付报告。 | 已通过 |
| ACC-STAGE076-P3-02 | 六条专项观察分别覆盖构建失败、冒烟验证失败、切换失败、回退、构建期间检索隔离及 Operations／报告快照版本可见性；构建失败与验证失败只分别观察同一条既有 P2 失败投影的构建与验证状态，不新增第二条业务或控制事实；六条均有显式处置，静默丢弃为零。 | 已通过 |
| ACC-STAGE076-P3-03 | P2 的五组版本、构建中版本、活动指针、验证、切换和回退控制记录形状均保持；失败候选不改变活动版本，回退候选只引用保留的上一活动版本，构建中候选只以活动指针未变、旧版本连续服务和零实际检索动作表示检索隔离；五条 Operations 与五条报告快照投影仅显示不透明控制版本引用，未写入真实界面或报告。 | 已通过 |
| ACC-STAGE076-P3-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、Operations 写入、报告快照写入、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 P4。 | 已遵守 |
| ACC-STAGE076-P4-01 | 冻结 Stage076 任务包、Stage076 P1/P2/P3 合同、Stage075 Review 和 Stage075 P1 静态合同保持为唯一合同上下文；P4 只从既有固定、非业务、reference-only 控制投影派生交付证据，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、实际 manifest、证据账本、审计日志、Operations 或已交付报告。 | 已通过 |
| ACC-STAGE076-P4-02 | 已派生五条十字段索引清单控制样例、六条九字段冒烟记录控制样例、五条八字段切换记录控制样例及五条八字段回退证明控制样例；所有引用保持 :control: 命名空间，均未写入实际清单、日志、指针、索引或报告。 | 已通过 |
| ACC-STAGE076-P4-03 | 已投影一条九字段旧活动版本保留与空间影响说明：上一活动版本要求保留、空间影响未测量、未删除旧索引；已提供重建、暂停、恢复三条八字段操作说明与四条克制中文反馈，均要求业务线白箱人工处理且不执行实际操作。 | 已通过 |
| ACC-STAGE076-P4-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、索引清单／冒烟日志／切换记录／回退证明写入、索引构建、空间测量、旧索引删除、索引重建、暂停、恢复、检索、Operations 写入、报告快照写入、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage076 Review。 | 已遵守 |

## 三、已经做了什么（当前阶段 4 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE076-P1-LOCAL-20260821-001` |  | `PASS_INDEX_VERSION_SCHEMA_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE076-P2-LOCAL-20260821-001` |  | `PASS_INDEX_VERSION_SCHEMA_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE076-P3-LOCAL-20260821-001` |  | `PASS_INDEX_VERSION_SCHEMA_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE076-P4-LOCAL-20260821-001` |  | `PASS_INDEX_VERSION_SCHEMA_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
