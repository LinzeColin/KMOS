<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE077 · IDS-V0_1-STAGE077-REVIEW · IDS-V0_1-STAGE077-REVIEW

**负责：** 既有唯一 KMOS 开发 worktree，Stage077 Review

## 二、怎么算做完

当前阶段显示 20 项；另有 671 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE077-P1-01 | 冻结 Stage077 任务包、Stage076 Review 与 Stage076 已复审索引版本合同保持为唯一合同上下文；P1 只定义静态工程合同，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE077-P1-02 | 未来后台构建的来源导入、资料范围、切块计数、模型引用、索引类型与候选版本六字段控制输入输出均已固定；每次未来批量导入建立隔离候选且不得覆盖活动版本，旧活动索引在构建期间继续服务；所有字段只表示静态工程边界，不代表真实资料、索引、检索、模型、Token、成本、审计或业务结论。 | 已通过 |
| ACC-STAGE077-P1-03 | 未来冒烟测试必须完整通过后才能原子切换；失败、缺失或未记录结果时活动指针保持不变，回退只可指向保留的上一活动版本，未来例外仍须业务线白箱人工处理；P1 不形成真实构建、冒烟、切换、查询或回退记录。 | 已通过 |
| ACC-STAGE077-P1-04 | P1 收尾时未执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；其下一门为 Stage077 P2。 | 已遵守 |
| ACC-STAGE077-P2-01 | 冻结 Stage077 任务包、Stage077 P1、Stage076 Review 与 Stage076 已复审索引版本合同保持为唯一合同上下文；P2 只接受五条固定、非业务、reference-only 控制输入，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告或物理索引。 | 已通过 |
| ACC-STAGE077-P2-02 | 五条控制输入复用三类索引版本和 Stage076 已复审字段形状；每条只记录不透明的 index_version、document_scope_ref、chunk_count=0、embedding_model_ref 与来源导入引用，候选版本与活动版本不同，影子候选与活动服务隔离，旧活动版本继续服务。 | 已通过 |
| ACC-STAGE077-P2-03 | 冒烟门未完成或失败时阻断切换；通过候选只形成未来全有或全无切换控制投影，切换失败保持活动版本，回退候选只指向保留的上一活动版本；业务线白箱人工复核仍为未来例外前置。 | 已通过 |
| ACC-STAGE077-P2-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 P3。 | 已遵守 |
| ACC-STAGE077-P3-01 | 冻结 Stage077 任务包、Stage077 P1/P2、Stage076 Review 与 Stage076 已复审索引版本合同保持为唯一合同上下文；P3 只重放五条固定、非业务、reference-only P2 控制投影，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、实际 Operations、报告或物理索引。 | 已通过 |
| ACC-STAGE077-P3-02 | 六条专项场景显式覆盖构建未完成的失败关闭、冒烟测试失败、切换失败、回退候选、旧活动版本连续服务与后台构建期间并发检索隔离；P2 没有物理索引构建失败结果，P3 只观察 CONTROL_BACKGROUND_BUILD_NOT_STARTED 的未完成关闭状态，不将其改写为真实构建失败；六条均有显式处置，静默丢弃为零。 | 已通过 |
| ACC-STAGE077-P3-03 | 失败、未完成或切换失败的候选均不改变活动版本，回退候选只引用保留的上一活动版本；五条 Operations 与五条报告快照控制视图仅显示不透明控制版本引用，未写入真实界面或报告；并发检索不受影响只以活动指针未变、旧版本连续服务和零实际检索动作表示。 | 已通过 |
| ACC-STAGE077-P3-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、Operations 写入、报告快照写入、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 P4。 | 已遵守 |
| ACC-STAGE077-P4-01 | 冻结 Stage077 任务包、Stage077 P1/P2/P3、Stage076 Review 与 Stage076 已复审索引版本合同保持为唯一合同上下文；P4 只从既有固定、非业务、reference-only 控制投影派生交付证据，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、实际 Operations、报告或物理索引。 | 已通过 |
| ACC-STAGE077-P4-02 | 五条索引清单、六条冒烟测试日志、五条切换记录、五条回退证明、一条旧活动版本保留／空间影响投影、三条索引重建／暂停／恢复说明及四条中文反馈均只含不透明控制引用；实际清单、日志、切换、回退证明或操作均未写入。 | 已通过 |
| ACC-STAGE077-P4-03 | 旧活动版本保留只表达回退前置，空间影响明确为未测量、未删除且须业务线白箱人工处理；重建、暂停和恢复只描述未来操作前置，失败禁止切换、活动版本连续服务和回退只指向保留上一活动版本的控制边界保持。 | 已通过 |
| ACC-STAGE077-P4-04 | 未执行真实资料访问、实际索引清单／冒烟测试日志／切换记录／回退证明写入、空间测量、旧索引删除、索引重建、暂停、恢复、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、Operations 写入、报告快照写入、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；下一步仅可在新的独立 run 进入 Stage077 Review。 | 已遵守 |
| ACC-STAGE077-REVIEW-01 | 冻结 Stage077 任务包、P1--P4 合同、P2/P3/P4 纯内存控制报告、Stage076 Review 与 Stage076 已复审索引版本合同保持为唯一合同上下文；Review 只机械重放既有控制工件，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、实际清单、实际日志、物理索引、证据账本、审计日志、Operations 或报告。 | 已通过 |
| ACC-STAGE077-REVIEW-02 | 复审固定 P1 的 6/6/7/6/8，P2 的五条控制请求与七组各五条投影，P3 的六条场景／26 字段／156 次字段检查／五条 Operations 与报告快照控制视图／六条业务线白箱人工处理，以及 P4 的 5/6/5/5/1/3/4/13 交付形状；所有字段、标签、引用和零值只表示控制形状，不代表真实资料、索引、模型、Token、审计或业务结论。 | 已通过 |
| ACC-STAGE077-REVIEW-03 | 失败禁止切换、切换失败保持活动版本、旧活动版本连续服务、回退只指向保留的上一活动版本、未测量空间影响、重建／暂停／恢复的业务线白箱人工处理、纯内存交付、单一权威与 P4→P3 控制回退链保持；Review 不形成真实构建、检索、切换、回退、清单、日志、Operations 或报告记录。 | 已通过 |
| ACC-STAGE077-REVIEW-04 | 未执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、检索、并发检索、回退、实际清单／日志／记录写入、空间测量、模型选择或调用、模型 Token、持久化、Agent、OVH、生产、Stage078 P1、上传或推送；下一步仅可在新的独立 run 进入 Stage078 P1。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE077-P1-LOCAL-20260821-001` |  | `PASS_BACKGROUND_INDEX_BUILD_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE077-P2-LOCAL-20260821-001` |  | `PASS_BACKGROUND_INDEX_BUILD_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE077-P3-LOCAL-20260821-001` |  | `PASS_BACKGROUND_INDEX_BUILD_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE077-P4-LOCAL-20260821-001` |  | `PASS_BACKGROUND_INDEX_BUILD_DELIVERY_EVIDENCE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE077-REVIEW-LOCAL-20260821-001` |  | `PASS_REVIEWED_BACKGROUND_INDEX_BUILD_RUNTIME_DISABLED` |
