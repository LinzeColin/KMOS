<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE081 · IDS-STAGE081-P1 · IDS-V0_1-STAGE081-P1

**负责：** 既有唯一 KMOS 开发 worktree，Stage081 P1 本地验收完成

## 二、怎么算做完

当前阶段显示 4 项；另有 755 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE081-P1-01 | 冻结 Stage081 任务包、Stage080 Review 与 Stage080 已审核 P1--P4 索引回滚控制工件保持为唯一合同上下文；P1 只定义静态工程合同，不建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、manifest、证据账本、审计日志、报告、数据库或物理索引。 | 已通过 |
| ACC-STAGE081-P1-02 | 未来索引版本、活动指针、构建中版本、影子索引和冒烟测试只复用既有控制字段形状；每次未来批量导入建立隔离候选和影子且不得覆盖活动版本，旧活动索引在构建、冒烟、切换与回滚准备期间继续服务；所有字段只表示静态工程边界。 | 已通过 |
| ACC-STAGE081-P1-03 | 新索引未通过、缺失或未记录冒烟测试不得切换，构建或冒烟失败时活动指针保持不变；回滚只可指向保留且仍在回滚窗口内的上一活动版本，未来例外仍须业务线白箱人工处理。 | 已通过 |
| ACC-STAGE081-P1-04 | P1 期间不执行真实资料访问、批量导入、数据库 schema 或连接、后台构建、索引构建、影子索引、冒烟测试、活动指针读写、原子切换、检索、并发检索、回退、provider/模型选择或下载、外部 API、模型 Token、持久化、Agent、OVH、生产、上传或推送；本轮全量本地验收完成后才可在新 run 进入 Stage081 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 1 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE081-P1-LOCAL-20260822-001` | 静态合同、治理兼容与零运行时边界本地复验 | `PASS_SHADOW_INDEX_CONTRACT_RUNTIME_DISABLED` |
