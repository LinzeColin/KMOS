<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE072 · IDS-V0_1-STAGE072-REVIEW · IDS-V0_1-STAGE072-REVIEW

**负责：** Codex 专属 KMOS worktree，Stage072 Review

## 二、怎么算做完

当前阶段显示 20 项；另有 566 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE072-REVIEW-01 | 冻结 Stage072 任务包、P1--P4 合同、P2/P3/P4 纯内存控制报告、Stage071 本地整阶段机械复审、Stage071 P1 成本治理合同与 Batch061-070 历史上传锁保持为唯一合同上下文；Review 只读重放控制工件，未建立第二权威事实源，未读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE072-REVIEW-02 | 机械核验 P1 6/2/12/10/7/8/18/9 静态形状、P2 五条 10/14/10/7/6/8/18 字段控制投影、P3 五条三十五字段显式处置/四条人工处理/90 次审计字段检查，以及 P4 五条策略样例、五条十八字段审计投影、五条零值成本、五条失败处理、五条未外发记录、七键查询、四条中文反馈和 12 类失败关闭，发现数为零；所有计数只表示固定控制形状。 | 已通过 |
| ACC-STAGE072-REVIEW-03 | 默认 denied、data source/document→chunk 自动继承、document 只能收紧、预算不足暂停、队列/缓存/失败重试/模型版本/成本/审计同闭合、先审计、业务线白箱人工处理和 metadata-only 交付边界保持一致；三个未来调用候选继续须人工复核与审计前置，P4 仅可纯内存回退到 P3，不形成真实外发、审计、成本、查询或回退记录。 | 已通过 |
| ACC-STAGE072-REVIEW-04 | 复审未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、Stage073、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage073 P1。 | 已遵守 |
| ACC-STAGE072-P4-01 | 冻结 Stage072 任务包、Stage072 P1/P2/P3 合同、Stage071 本地整阶段机械复审、Stage071 P1 成本治理合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P4 只从五条固定、非业务、:control: P3 场景派生控制交付证据，不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE072-P4-02 | 纯内存交付严格派生五条策略样例、五条十八字段审计投影样例、九十次审计字段检查、五条零值成本估算、五条失败处理、五条未外发控制引用、七键查询说明和四条中文反馈；所有标签、引用和零值只表示控制形状，不代表真实资料、金额、预算、Token、成本、队列、缓存、重试、审计、provider、模型或业务结论。 | 已通过 |
| ACC-STAGE072-P4-03 | denied、summary_only、document 收紧、full_text_allowed 与预算不足暂停均保留 P3/P2 策略边界；三个未来调用候选仍须业务线白箱人工复核与审计前置，四个非 denied 情形保持人工处理；查询仅针对当前进程内控制样例，回滚只撤回 P4 交付证据并恢复到 P3，不形成真实外发、审计、成本、查询或回退记录。 | 已通过 |
| ACC-STAGE072-P4-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage072 Review。 | 已遵守 |
| ACC-STAGE072-P3-01 | 冻结 Stage072 任务包、Stage072 P1/P2 合同、Stage071 本地整阶段机械复审、Stage071 P1 成本治理合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P3 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE072-P3-02 | P3 只重放五条固定、非业务、:control: P2 内存控制记录，验证 denied 阻断、summary_only 摘要引用、document 收紧、full_text_allowed 文本块引用和预算不足暂停；所有字段只代表控制标签，不代表真实资料、队列、缓存、重试、模型版本、成本、审计、provider 或模型。 | 已通过 |
| ACC-STAGE072-P3-03 | 五条控制场景均有完整 18 字段审计投影，共完成 90 次字段检查；三个未来外部 API 调用候选均先具备 policy reason、provider/model 引用、token_count=0、chunk_id、队列引用、预算状态和业务线白箱人工复核前置，未形成实际外发或审计记录。 | 已通过 |
| ACC-STAGE072-P3-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage072 P4。 | 已遵守 |
| ACC-STAGE072-P2-01 | 冻结 Stage072 任务包、Stage072 P1 静态合同、Stage071 本地整阶段机械复审、Stage071 P1 成本治理合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P2 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE072-P2-02 | 纯内存切片严格核验五条固定、非业务、20 字段、:control: 引用请求，投影 data source/document→chunk 策略继承、12/10/7 队列/缓存/失败重试、六字段模型版本、8 字段零值成本和 18 字段审计形状；所有字段只代表控制标签，不代表真实资料、队列、缓存、重试、模型版本、成本、审计、provider 或模型。 | 已通过 |
| ACC-STAGE072-P2-03 | 默认 denied 阻断未授权 chunk 外发、运行时队列/缓存和模型版本记录；summary_only 只保留未来授权摘要引用，full_text_allowed 只保留未来授权文本块引用；审计控制投影保留 provider_ref、model_ref、token_count=0、chunk_id 与 policy_inheritance_reason，任何例外仍须未来授权、预算、审计与业务线白箱人工复核。 | 已通过 |
| ACC-STAGE072-P2-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage072 P3。 | 已遵守 |
| ACC-STAGE072-P1-01 | 冻结 Stage072 任务包、Stage071 本地整阶段机械复审、Stage071 P1 成本治理合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P1 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE072-P1-02 | 静态固定 provider_ref、model_ref、model_version、dimension、created_at、sent_to_external_api 六个未来字段，复用默认 denied、三档策略、data source/document 到 chunk 自动继承、12/10/7 队列/缓存/失败重试、8 个成本与模型字段、18 个审计字段和三重预算关闭；P1 不记录任何真实字段值。 | 已通过 |
| ACC-STAGE072-P1-03 | denied 不形成外发、队列、模型版本记录或 provider 调用；summary_only 仅允许未来已授权摘要引用，full_text_allowed 仅可在未来授权、预算、审计和业务线白箱人工复核后处理文本块；模型版本记录、审计或模型输出不能替代来源文档、形成业务事实或自动建议。 | 已通过 |
| ACC-STAGE072-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P2/P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage072 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 5 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE072-P1-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE072_P1_EMBEDDING_MODEL_VERSION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE072-P2-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE072_P2_EMBEDDING_MODEL_VERSION_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE072-P3-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE072_P3_EMBEDDING_MODEL_VERSION_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE072-P4-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE072_P4_EMBEDDING_MODEL_VERSION_METADATA_ONLY_DELIVERY_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE072-REVIEW-LOCAL-20260820-001` |  | `PASS_REVIEWED_LOCAL_EMBEDDING_MODEL_VERSION_RUNTIME_DISABLED` |
