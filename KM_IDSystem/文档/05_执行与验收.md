<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE073 · IDS-V0_1-STAGE073-P2 · IDS-V0_1-STAGE073-P2

**负责：** Codex 专属 KMOS worktree，Stage073 P2

## 二、怎么算做完

当前阶段显示 8 项；另有 586 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE073-P2-01 | 冻结 Stage073 任务包、Stage073 P1 静态合同、Stage072 本地整阶段机械复审、Stage072 P1 模型版本合同、Stage071 P1 审计字段合同与 Batch061-070 历史上传锁保持为唯一合同上下文；P2 只接受五条固定、非业务、:control: 控制标签，不建立第二权威事实源，也不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径或真实 URI。 | 已通过 |
| ACC-STAGE073-P2-02 | 纯内存切片机械投影默认 denied、三档策略、data source/document 到 chunk 自动继承、document 不能放宽、owner 不逐条标记 chunk、12/10/7 队列/缓存/失败重试、8 字段零值成本和 6 字段模型版本；篡改、重排、无效或放宽策略均失败关闭。 | 已通过 |
| ACC-STAGE073-P2-03 | 每条控制审计投影保留完整 18 字段、provider、model、token_count=0、不透明 chunk_id 与 policy_inheritance_reason；denied 不形成外发载荷或运行时队列，summary_only/full_text_allowed 仅保留未来授权引用，预算不足暂停；控制投影不等于真实审计记录、来源文档或业务事实。 | 已通过 |
| ACC-STAGE073-P2-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage073 P3。 | 已遵守 |
| ACC-STAGE073-P1-01 | 冻结 Stage073 任务包、Stage072 本地整阶段机械复审、Stage072 P1 模型版本合同、Stage071 P1 审计字段合同与 Batch061-070 历史上传锁保持为唯一合同上下文；本 P1 不建立第二权威事实源，不读取、打开、保留或写入业务资料、原始元数据、来源正文、摘要正文、文本块、物理路径、provider、模型、维度、时间、外发状态、金额、Token、预算、页面、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE073-P1-02 | 静态定义默认 denied、denied/summary_only/full_text_allowed 三档策略、data source/document 到 chunk 自动继承、owner 不逐条标记 chunk、12 字段未来 Embedding 队列、8 字段成本/模型控制、6 字段模型版本和完整 18 字段外部 API 审计合同；P1 不记录任何真实字段值。 | 已通过 |
| ACC-STAGE073-P1-03 | denied 不形成外发载荷、队列或 provider 调用；summary_only 仅允许未来已授权摘要引用，full_text_allowed 仅可在未来授权、预算、完整审计字段及业务线白箱人工复核后处理文本块；P3 行为验证、审计记录创建和查询均延后，审计合同或模型输出不能替代来源文档、形成业务事实或自动建议。 | 已通过 |
| ACC-STAGE073-P1-04 | 未执行真实资料访问、解析、切块、摘要生成、文本外发、模型版本记录、成本估算、预算查找、Embedding 队列、缓存、失败重试、provider 凭据读取、provider/模型选择、外部 API、模型 Token、审计测试、审计写入或查询、索引、数据库、持久化、Agent、OVH、生产、P2/P3/P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage073 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 2 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE073-P1-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE073_P1_EMBEDDING_AUDIT_TEST_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE073-P2-LOCAL-20260820-001` |  | `PASS_LOCAL_STAGE073_P2_EMBEDDING_AUDIT_TEST_CONTROL_SLICE_RUNTIME_DISABLED` |
