<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/plan.json、acceptance.json、runs/*.json -->
<!-- 上限 100 行 -->

# 执行与验收

这一轮在做什么、怎么算做完、实际做到哪了。

## 一、这一轮在做什么

**在做：** IDS-STAGE065 · IDS-V0_1-STAGE065-P3 · IDS-V0_1-STAGE065-P3

**负责：** Codex 专属 KMOS worktree，Stage065 P3

## 二、怎么算做完

当前阶段显示 12 项；另有 426 项历史验收事实仍以 `machine/facts/acceptance.json` 为准。

| 编号 | 达成标准 | 状态 |
|---|---|---|
| ACC-STAGE065-P3-01 | 冻结 Stage065 任务包、P1/P2 控制合同、Stage064 本地整阶段复审与 Batch061-070 上传锁保持为唯一合同上下文；六类场景均为固定、非业务、仅引用控制元数据，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE065-P3-02 | P3 纯内存重放 P2 的七条十六字段低可信控制记录，覆盖长文档、跨页参数表、施工步骤、参数表、引用页码和重复 chunk embedding/index 写入边界；六类均有显式处置、静默丢弃为零且全部要求业务线白箱人工处理。 | 已通过 |
| ACC-STAGE065-P3-03 | 每个场景保留 document、page、section、parser output、表格上下文与来源片段六维控制引用形状；该形状不等于真实来源或页码已经验证。重复场景仅确认本控制模块没有发起 embedding 或索引写入，不检测真实重复项、不计算真实身份/版本/Hash，也不形成真实去重结论。 | 已通过 |
| ACC-STAGE065-P3-04 | 未读取、打开、检测、解析、切分、计算、分类、去重或创建真实资料、chunk、身份、Hash、版本或分类记录，未执行 parser、章节检测、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P4、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P4。 | 已遵守 |
| ACC-STAGE065-P2-01 | 冻结 Stage065 任务包、P1 静态合同、Stage064 本地整阶段复审与 Batch061-070 上传锁保持为唯一合同上下文；七条输入均为固定、非业务、仅引用控制请求，未建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE065-P2-02 | 纯内存切片严格核验 P1 十二字段控制输入，并投影七条严格十六字段低可信待人工复核控制记录；记录覆盖 procedure、risk、acceptance、material、equipment、case、bid_response 七类标签，chunk_id、chunk_hash 和 version 都只是控制标签，不生成真实身份、Hash、版本或分类。 | 已通过 |
| ACC-STAGE065-P2-03 | 七条控制记录均保留 document、page、section、parser output、表格上下文和来源片段六维控制引用，工程步骤、验收条款和参数表三类受保护语义面保持原子；全部低可信并要求业务线白箱人工复核。低可信控制标记不等于真实低质量检测，覆盖率、质量回归和质量降级仍由后续阶段负责。 | 已通过 |
| ACC-STAGE065-P2-04 | 未读取、打开、检测、解析、切分、计算、分类或创建真实资料、chunk、身份、Hash、版本或分类记录，未执行 parser、章节检测、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、P3、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P3。 | 已遵守 |
| ACC-STAGE065-P1-01 | 冻结 Stage065 任务包、Stage064 本地整阶段复审与 Batch061-070 上传锁保持为唯一合同上下文；十二字段仅引用输入、十六字段未来分类输出和七类资产标签只定义接口，不建立第二权威事实源，未读取或保留业务资料、原始元数据、来源正文、物理路径、页面、章节、表格、parser 输出或 chunk。 | 已通过 |
| ACC-STAGE065-P1-02 | 未来工程语义资产分类 schema 明确包含 procedure、risk、acceptance、material、equipment、case、bid_response 七类标签，并保留 chunk 身份与版本、document、page、section、parser output、表格上下文和来源片段六维受控引用；当前未创建真实分类、分类记录、chunk、来源绑定、覆盖率、索引或 embedding。 | 已通过 |
| ACC-STAGE065-P1-03 | 工程步骤、验收条款和参数表不得因资产类型规则被任意切断或覆盖；来源文档和业务线人工复核保持权威，缺少引用、无法确认分类依据或遇到跨页参数表时必须关闭并转人工处理。 | 已通过 |
| ACC-STAGE065-P1-04 | 未读取、打开、检测、解析、切分、计算、分类或创建真实资料、chunk、身份、Hash、版本或分类记录，未执行 parser、章节检测、覆盖率、质量、来源追溯、索引、数据库、持久化、Agent、模型 Token、OVH、生产、整阶段复审、批次复审、上传或推送；下一步仅可在新的独立 run 进入 Stage065 P2。 | 已遵守 |

## 三、已经做了什么（当前阶段 3 条）

| 记录 | 做了什么 | 结果 |
|---|---|---|
| `RUN-IDS-STAGE065-P1-LOCAL-20260814-001` | 只定义冻结 Stage065 工程语义资产分类的引用式静态合同，覆盖 procedure/risk/acceptance/material/equipment/case/bid_response 七类资产标签、十二字段输入、十六字段未来输出、工程步骤/验收条款/参数表保护面、document/page/section/parser output 追溯、失败关闭、中文反馈和回滚边界；不读取、分类或创建真实业务资料、chunk、分类记录或业务结论。 | `PASS_LOCAL_PHASE1_ENGINEERING_SEMANTIC_ASSET_CLASSIFICATION_CONTRACT_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE065-P2-LOCAL-20260814-001` | 只以七条固定、非业务、reference-only 工程语义资产分类控制请求在内存中投影七条待人工复核记录，机械验证 Stage065 P1 的十二字段输入、十六字段输出、procedure/risk/acceptance/material/equipment/case/bid_response 七类标签、工程步骤/验收条款/参数表三类受保护语义面、六维追溯和低可信人工处理标记；不读取、解析、检测、切分、计算或创建真实业务资料。 | `PASS_LOCAL_PHASE2_ENGINEERING_SEMANTIC_ASSET_CLASSIFICATION_CONTROL_SLICE_RUNTIME_DISABLED` |
| `RUN-IDS-STAGE065-P3-LOCAL-20260814-001` | 只重放七条固定、非业务、reference-only Stage065 P2 工程语义资产分类控制记录，在内存中覆盖长文档、跨页参数表、施工步骤、参数表、引用页码与重复 chunk embedding/index 写入边界六类专项场景，输出显式人工处置和六维控制追溯；不读取、解析、检测、切分、计算、分类、去重或创建真实业务资料。 | `PASS_LOCAL_PHASE3_ENGINEERING_SEMANTIC_ASSET_CLASSIFICATION_CONTROLLED_SCENARIOS_RUNTIME_DISABLED` |
