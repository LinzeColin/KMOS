governance_spec_version: "1.0.0"
project_id: "whkmSalary"
formulas:
  - formula_id: "FORM-001"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "resolve_weights uses explicit weights first; otherwise maps province through ALIASES and projects; missing province/weights raises ValueError."
    variables:
      - name: "province"
        data_type: "string"
        unit: "province label"
        input_domain: "新疆, 山东, 青海, 湖北, 华中区域, alias 华中地区"
    constraints: ["Province weights must sum to 1.0 per active weight group."]
    missing_policy: "Missing weights and unrecognized province raise ValueError."
    output_range: "dict of metric weights."
    code_refs: ["whkmSalary/salary_logic.py:30"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/salary_logic.py:15"]
  - formula_id: "FORM-002"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_performance: if rate<0.6 return min(0, -(0.6-rate)*300); if 0.6<=rate<=0.8 return 20+(rate-0.6)*200; if 0.8<rate<=1.5 return 60+(rate-0.8)*200; else return max(200,200+(rate-1.5)*400)."
    variables:
      - name: "rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "quarter_actual/(year_target/4); year_target and quarter_actual must be >0"
    constraints: ["No explicit upper bound."]
    missing_policy: "Missing input is not handled by salary_logic; UI supplies defaults or Python raises."
    output_range: "float score; can be negative or above 200."
    code_refs: ["whkmSalary/salary_logic.py:52"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/salary_logic.py:154"]
  - formula_id: "FORM-003"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_margin: if rate<0.1 return min(0,-(0.1-rate)*1000); if 0.1<=rate<=0.25 return (rate-0.1)*200; if 0.25<rate<=0.5 return 30+(rate-0.25)*200; else return max(80,80+(rate-0.5)*500)."
    variables:
      - name: "rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "numeric margin"
    constraints: ["No UI min/max for margin in code."]
    missing_policy: "Missing input is not handled by salary_logic; UI supplies default."
    output_range: "float score."
    code_refs: ["whkmSalary/salary_logic.py:63"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:37"]
  - formula_id: "FORM-004"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_settlement: days>60 -> -150; 20<=days<=60 -> min(100,100-(days-20)*5); 1<days<20 -> min(200,200-days*5); days==1 -> 200."
    variables:
      - name: "days"
        data_type: "integer"
        unit: "working days"
        input_domain: "UI min 0; salary_logic has no return for days=0"
    constraints: ["days=0 returns None in salary_logic and can break weighted calculation."]
    missing_policy: "No fallback for days=0; governance task TASK-WHKM-B-001 tracks boundary repair decision."
    output_range: "float score or None for uncovered day value."
    open_task_ids: ["TASK-WHKM-B-001"]
    code_refs: ["whkmSalary/salary_logic.py:74"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:39"]
  - formula_id: "FORM-005"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_invoice: days>60 -> -200; 20<days<=60 -> min(0,-(days-20)*5); 5<days<=20 -> min(125,125-(days-5)*8); 1<days<=5 -> min(150,150-days*5); days==1 -> 150."
    variables:
      - name: "days"
        data_type: "integer"
        unit: "working days"
        input_domain: "UI min 0; salary_logic has no return for days=0"
    constraints: ["days=0 returns None in salary_logic and can break weighted calculation."]
    missing_policy: "No fallback for days=0; governance task TASK-WHKM-B-001 tracks boundary repair decision."
    output_range: "float score or None for uncovered day value."
    open_task_ids: ["TASK-WHKM-B-001"]
    code_refs: ["whkmSalary/salary_logic.py:85"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:43"]
  - formula_id: "FORM-006"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_payback: days>60 -> min(-10,-10-(days-60)*2); 20<days<=60 -> min(150,150-(days-20)*4); 1<days<=20 -> min(200,200-days*2.5); days==1 -> 200."
    variables:
      - name: "days"
        data_type: "integer"
        unit: "working days"
        input_domain: "UI min 0; salary_logic has no return for days=0"
    constraints: ["days=0 returns None in salary_logic and can break weighted calculation."]
    missing_policy: "No fallback for days=0; governance task TASK-WHKM-B-001 tracks boundary repair decision."
    output_range: "float score or None for uncovered day value."
    open_task_ids: ["TASK-WHKM-B-001"]
    code_refs: ["whkmSalary/salary_logic.py:98"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:45"]
  - formula_id: "FORM-007"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_audit_bias: if rate<=0.02 return min(120,120-rate*3000); if 0.02<=rate<=0.08 return min(60,60-(rate-0.02)*8000); if rate>0.08 return -420."
    variables:
      - name: "rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "UI min 0.0"
    constraints: ["Boundary rate=0.02 matches first branch due if order."]
    missing_policy: "Missing input is not handled by salary_logic; UI supplies default."
    output_range: "float score."
    code_refs: ["whkmSalary/salary_logic.py:109"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:49"]
  - formula_id: "FORM-008"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "score_customer_cost: if rate>0.03 return min(45,45-(rate-0.03)*4000); if 0<=rate<=0.03 return min(120,120-rate*2500)."
    variables:
      - name: "rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "UI min 0.0"
    constraints: ["Negative rates are not accepted by UI but salary_logic would return None for negative direct calls."]
    missing_policy: "No salary_logic fallback for negative direct-call rates; UI min is 0.0."
    output_range: "float score or None for negative direct-call rates."
    code_refs: ["whkmSalary/salary_logic.py:117"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/streamlit_app.py:51"]
  - formula_id: "FORM-009"
    model_id: "MOD-001"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "performance_rate=quarter_actual/(year_target/4); weighted_total=sum(score_k*weight_k); perf_money=36000*(weighted_total/100); total_salary=6000*3+perf_money; after_tax_salary=total_salary*tax_keep_rate."
    variables:
      - name: "weighted_total"
        data_type: "float"
        unit: "points"
        input_domain: "sum of weighted score functions"
      - name: "tax_keep_rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "UI 0.0..1.0"
    constraints: ["year_target > 0 and quarter_actual > 0.", "Policy source for tax_keep_rate is UNKNOWN and task-linked."]
    missing_policy: "Missing or invalid year_target and quarter_actual raise ValueError; other direct missing inputs are Python errors."
    output_range: "CalcResult with salary floats; no explicit cap."
    open_task_ids: ["TASK-WHKM-B-001"]
    code_refs: ["whkmSalary/salary_logic.py:134"]
    test_refs: ["FOCUSED: salary_logic.calculate fixture"]
    evidence_refs: ["whkmSalary/salary_logic.py:180"]
  - formula_id: "FORM-010"
    model_id: "MOD-002"
    assumption_ids: ["ASM-001", "ASM-002"]
    status: "active"
    expression: "Streamlit constrains some inputs via number_input min/max/defaults and passes selected province weights into calculate."
    variables:
      - name: "tax_keep_rate"
        data_type: "float"
        unit: "ratio"
        input_domain: "0.0..1.0"
    constraints: ["UI constraints are not a substitute for salary_logic direct-call validation."]
    missing_policy: "Streamlit supplies defaults; direct code callers may bypass UI constraints."
    output_range: "UI input values or displayed exception."
    code_refs: ["whkmSalary/streamlit_app.py:32", "whkmSalary/streamlit_app.py:57"]
    test_refs: ["FOCUSED: python -m compileall salary_logic.py streamlit_app.py"]
    evidence_refs: ["whkmSalary/streamlit_app.py:69"]
