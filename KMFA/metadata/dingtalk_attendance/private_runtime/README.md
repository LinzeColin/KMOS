# Private Runtime Placeholder

This directory is reserved for local KMFA 钉钉考勤 skill runtime files.

Only this README and `.gitkeep` may be tracked. `.env.local` is allowed only as an ignored local runtime file. Runtime databases, reports, raw responses, locks, temporary files, and local configuration files must stay ignored.
