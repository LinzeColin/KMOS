# KMFA 钉钉考勤 skill 功能清单

current_status: AUTO_CLOSURE_IMPLEMENTED_AWAITING_5_NATURAL_WORKDAYS

roadmap_progress: R6

## 当前功能

| 功能 | 当前状态 | 事实边界 |
|---|---|---|
| 晨间结果 | TEMPORARY_REMINDER | 只表示当时读取到的官方报表快照，不是当日最终结论 |
| 晚间结果 | TEMPORARY_REMINDER | 只表示当时读取到的官方报表快照，不是当日最终结论 |
| 官方最终核对 | R4_DATA_RECONSTRUCTION_PASS | `部门`为可选展示字段；其余 48 个必需列两天共 4,224 格，缺人/多人/必需缺失/真实差异均为 0 |
| 正式逐日对账凭证 | IMPLEMENTED_OFFLINE_VERIFIED | 两天各生成 44/44/44 人、48 列、2,112 格的统一凭证，final runner 可直接读取 |
| 真实无发送运行 | PASS | 2026-07-10 evening 按实际 42 人考勤组完成 42/42 official coverage；凭证绑定 final 通过 |
| 月累计来源隔离 | PASS | 只读取最新有效凭证绑定 final；旧 final、legacy、morning、evening 均不进入新累计 |
| 新月累计 | IMPLEMENTED_NOT_PRODUCTION_ACCEPTED | 只读取 canonical `final` archive；legacy、morning、evening archive 均不参与 |
| 钉钉发送 | DISABLED | owner policy 固定返回 `NOT_SENT_OWNER_DISABLED`，不解析目标、不调用 sender |
| 独立官方证据门禁 | IMPLEMENTED | DWS 内部检查不能单独产生官方 PASS；无独立原件时为 `EVIDENCE_MISSING`，差异未清零时为 `BLOCKED` |
| scheduler/time/timezone | UNCHANGED | R4 不修改现有 scheduler、时间、timezone、automation ID、cwd 或目标配置 |
| 自动官方报告闭环 | IMPLEMENTED_AWAITING_NATURAL_EVIDENCE | latest pending 工作日自动查找/只读导出官方 XLS/XLSX、冻结指纹、48 列还原并把正式凭证直接交给 final runner；报告未生成时等待并由下次自然运行续接 |
| 工作日与 slot 防重复 | FOCUSED_REGRESSION_PASS | 同一工作日期与 morning/evening slot 只运行一次，完整 artifact 可恢复状态而不重写 |
| 中断安全续跑 | FOCUSED_REGRESSION_PASS | 私有原子状态记录 RUNNING/INTERRUPTED，重入时先探测已完成 artifact，再决定恢复或重跑 |
| 连续自然验收 | IN_PROGRESS_0_OF_5 | 仅 automation 自然触发计数；人工和合成运行不计入 |
| 通知模板 | FROZEN | 通知预览取消为验收项；现有群通知模板及相关模板代码不允许修改 |

## 完成标准

- 一个真实已完成工作日具备晨间证据、晚间证据和官方最终报表核对的一页结果。
- 最终核对必须来自独立钉钉官方导出原件与同日 DWS raw 的完整逐格对账；DWS 内部查询和合成 fixture 均不能作为完成依据。
- 未经 owner 验收前，`production_acceptance=NOT_EVALUATED`、`owner_usability_status=NOT_ACCEPTED`、整体状态保持 `UNAVAILABLE`。

## R4 证据门禁

- 必须同时冻结独立钉钉官方导出原件、对应 DWS raw、预期结果和逐员工逐字段对账标准。
- 来源映射必须覆盖企业实际导出的全部 49 列，不得用预设九列替代；正式验收只比较 48 个必需列，`部门`单独记录可选展示状态。
- 必须逐员工逐字段比较，汇总人数相等不能作为通过依据。
- 缺少独立官方导出原件时状态固定为 `EVIDENCE_MISSING`；存在任何人员差异、必需列/单元格缺失或必需值差异时状态固定为 `BLOCKED`。
- R3.2“真实工作日验收已完成”声明已撤回。

## 2026-07-09 / 2026-07-10 反向还原结果

- 先前缺失的 22 个统计/审批列均已找到真实 DWS 来源；日期、毫秒日界、打卡时间、数字显示、空白与数字 0、UserId 前导零均按独立官方原件复核。
- 2026-07-09 使用审批结算后的最终打卡结果，不使用旧运行时点状态。
- Owner 已裁决“部门”为可选展示字段，不参与人员匹配、异常、累计、通知或验收；无可靠主部门时记录未验证。
- 48 个必需字段最终复核：缺人 0、多出 0、必需字段缺失 0、真实值差异 0，共比较 4,224 格。
- R4 官方数据还原与后续真实无发送运行均通过；当前状态为 `NO_SEND_RUNTIME_VERIFIED`，发送仍由 owner policy 硬关闭。
- 完整 49 列来源与逐列计数见 `docs/R4_OFFICIAL_RECONCILIATION_RESULT.md`；员工明细和原件不进入 GitHub。
