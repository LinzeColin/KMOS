# KMFA 钉钉考勤 skill 运行清单

current_status: UNAVAILABLE

roadmap_progress: R3 / R3.2

## 固定运行序列

1. 晚间 automation 运行 official collector，落盘 `TEMPORARY_REMINDER`，发送保持关闭。
2. 后续晨间 automation 先查找最新 canonical evening reminder 且尚无 final 的已完成工作日。
3. 如存在待核对日期，运行 `final_reconciliation.py --latest-pending`，重新读取 DingTalk official report。
4. exact columns、人员范围、工作日或覆盖不完整时 fail closed，不生成最终结论。
5. parity PASS 后写 canonical `final` raw/manifest 和 aggregate-only `one_page.md`。
6. 新月累计只读取 canonical `final` archive；所有 legacy 和暂时提醒 archive 仅保留审计。
7. 晨间 automation 再生成当天 `TEMPORARY_REMINDER`，发送仍关闭。

## 每次运行检查

- branch=`main` 且 `HEAD == origin/main`。
- DWS 授权和 browser policy READY；未授权则 fail closed。
- `notification_status=NOT_SENT_OWNER_DISABLED`。
- 不执行 target probe、resend 或 sender。
- 不把 synthetic fixture 作为真实工作日证据。
- 私有 raw、报告正文、一页结果和 manifest 只留在私有归档，不提交 GitHub。

## R3.2 真实运行证据

- 真实工作日：2026-07-10。
- 晨间/晚间历史提醒：证据存在，但均为 legacy/official parity UNKNOWN，页面保持 `UNVERIFIED_REMINDER`。
- 官方最终核对：fresh live official report exact parity PASS。
- 发送：`NOT_SENT_OWNER_DISABLED`，messages=0，target results=0。
- 一页结果：已写入私有月度归档；GitHub 只记录脱敏状态，不提交私有路径或正文。
