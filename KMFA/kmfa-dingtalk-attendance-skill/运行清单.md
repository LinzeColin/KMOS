# KMFA 钉钉考勤 skill 运行清单

current_status: UNAVAILABLE

roadmap_progress: R4 / R4.3

## 固定运行序列

1. 晚间 automation 运行 official collector，落盘 `TEMPORARY_REMINDER`，发送保持关闭。
2. 后续晨间 automation 先查找最新 canonical evening reminder 且尚无 final 的已完成工作日。
3. 如存在待核对日期，必须先取得独立官方导出原件，并按 48 个必需列完成逐员工逐格对账；`部门`只作可选展示。
4. 无独立原件时固定 `OFFICIAL_EVIDENCE_MISSING`；任何缺人、多人、必需列/单元格缺失或必需值差异均固定 `OFFICIAL_FINAL_RECONCILIATION_BLOCKED`。
5. 只有统一正式凭证 PASS 且后续 DWS 内部门禁也通过，才可写 canonical `final` raw/manifest 和 aggregate-only `one_page.md`。
6. 新月累计只读取 canonical `final` archive；所有 legacy 和暂时提醒 archive 仅保留审计。
7. 晨间 automation 再生成当天 `TEMPORARY_REMINDER`，发送仍关闭。

## 每次运行检查

- branch=`main` 且 `HEAD == origin/main`。
- DWS 授权和 browser policy READY；未授权则 fail closed。
- `notification_status=NOT_SENT_OWNER_DISABLED`。
- 不执行 target probe、resend 或 sender。
- 不把 synthetic fixture 作为真实工作日证据。
- 私有 raw、报告正文、一页结果和 manifest 只留在私有归档，不提交 GitHub。
- GitHub 只保存脱敏聚合一页结果，不保存员工明细、官方原件、私有路径或逐格差异正文。

## R3.2 运行证据（验收声明已撤回）

- 真实工作日：2026-07-10。
- 晨间/晚间历史提醒：证据存在，但均为 legacy/official parity UNKNOWN，页面保持 `UNVERIFIED_REMINDER`。
- DWS 最终读取：内部 parity PASS，但不是独立官方导出对账。
- 发送：`NOT_SENT_OWNER_DISABLED`，messages=0，target results=0。
- 一页结果：已写入私有月度归档；GitHub 只记录脱敏状态，不提交私有路径或正文。

R3.2 的“真实工作日验收已完成”声明无效。该页只能作为 DWS 运行产物，不能作为 official-export-vs-DWS 验收证书。

## R3.3 初始只读对账

- 独立官方导出原件：`EVIDENCE_MISSING`。
- DWS raw：2 份，员工集合一致。
- DWS 字段范围：员工唯一键、工作日、考勤结果和 8 个官方数值字段。
- DWS 内部比较：42 名员工 × 9 个官方字段 = 378 个单元格，差异 0。
- official export vs DWS：未运行；缺少独立原件，不能生成预期结果或差异矩阵。
- 代码修正：未运行。
- live DWS、发送、schedule/time/timezone 修改：均未发生。

## R4.1–R4.3 官方原件逐格对账

- 独立官方导出原件：2026-07-09 与 2026-07-10 两份原件及指纹均已冻结且未修改。
- 企业实际官方列仍为 49；Owner 将“部门”裁决为可选展示字段，正式验收使用其余 48 列。
- 每天官方/DWS/UserId 匹配均为 44/44/44；每天比较 2,112 个必需单元格，两天合计 4,224。
- 最终复核：缺人 0、多出 0、必需列缺失 0、必需单元格缺失 0、真实值差异 0。
- 每天均生成带官方文件指纹、工作日期和聚合计数的统一正式凭证，`final_reconciliation.py` 原样读回为 `PASS`。
- “部门”在两个工作日均记录为可选未验证字段，不猜值、不进入业务判断，也不阻止通过。
- R4 官方考勤数据还原状态为 `PASS`；整个 skill 仍为 `UNAVAILABLE`，下一阶段需完成真实无发送运行。
- 钉钉发送、automation、schedule、time、timezone、通知文案均未修改。
- 公开一页结果：`docs/R4_OFFICIAL_RECONCILIATION_RESULT.md`。
