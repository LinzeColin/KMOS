fastapi>=0.115
uvicorn[standard]>=0.30
pyyaml>=6.0
reportlab>=4.0
PyJWT[crypto]>=2.10,<3
# 3.3.4 is not present on the public index used by the production Docker
# build.  Keep the latest available binary-compatible 3.2 release so a cold
# rebuild remains reproducible.
psycopg[binary]==3.2.13
# 1.43.51 is not published by the configured package index, so a clean
# production image rebuild fails before KMFA starts.  Pin the compatible
# public release used by the worker's S3-compatible API calls.
boto3==1.42.65
# 项目成本下载要出 .xlsx——Owner 要求「你的下载产品和我的格式要保持一致」，
# 而对照格式是 8 份竣工报表那种多页签工作簿，CSV 出不了。
openpyxl>=3.1
