# KMFA Skills 登记册

> 建立：2026-07-17 ｜ 依据：Owner 指令「全部都要在 `KMFA/skills` 下做登记或迁移」
> **这里是 KMFA 全部 skill 与其定时任务的唯一登记入口。**
> 移植说明：本文件最初随技能抢救工作提交在 `CodexProject` 搁浅分支（`62c70791`），当时未随登记册 PR 进入 KMOS。2026-07-17 由打通线程移植至此，并按同目录 `registry.yaml`（更新口径）对齐了引用计数与排程归属；除此之外内容保持原样。

## 为什么要有这个

2026-07-17 清点发现两个问题：

1. **有 3 个 skill 从来不在任何版本控制下** —— 它们只存在于本机 `~/.codex/skills/`，
   而那个目录**不是 git 仓库**。丢了就没了。KMFA v1.5 就是这么没的（分支没 push 就被删）。
2. **skill 和它的定时任务是分家的** —— skill 在仓库里，排程配在 Codex 应用里，
   两边没有任何交叉引用。结果：2026-07-17 清理删掉 `main_worktree/` 后，
   6 个 ACTIVE 排程集体空跑失败，**而没有任何地方能查到"谁依赖那个目录"**。

**本登记册就是那个"能查到"的地方。**

截至 2026-07-19，仓内共有 **9 个** KMFA Skill，全部位于 `KMFA/skills/`。

## 一、Skill 清单

### 已迁入本目录（原先无版本控制，2026-07-17 抢救）

| 目录 | 机器 id | 干什么 | 外部依赖 |
|---|---|---|---|
| `工资发放标准/` | `gongzi-fafang-biaozhun` | 按上月模板生成当月工资发放表、AB Excel 校验、差异报告、金额分校验、B公司/报销路由 | 无 |
| `红圈主合同/` | `hongquan-main-contract-dws` | 从红圈 `cloud.hecom.cn` 导出/下载/归档主合同 DWS/Excel 原始数据 | 红圈网页 |
| `信息费更新/` | `info-fee-update` | 填写更新信息费申请表与 2023-2025 信息费明细表；据甲方去红圈查合同号/合同额 | 红圈网页、WPS |

> ⚠️ 迁入时核验：三者均**不含**密钥/手机号/身份证/真实金额/真实姓名 —— 是「怎么做」的说明，不是数据。
> 但 `红圈主合同` 与 `信息费更新` 各有 **1 处写死了本机绝对路径**，
> `信息费更新` 还引用了 `Downloads/KMFA_MetaData`。**这些应改成配置项**（见下方待办）。

### 2026-07-17 晚已全部物理移入（Owner 指令：目录全部改成能看懂的中文名）

| 现路径 | 机器 id（保持稳定） | 干什么 | 原路径（历史） |
|---|---|---|---|
| `KMFA/skills/钉钉考勤/` | `kmfa-dingtalk-attendance-skill` | 抓取钉钉 DWS 考勤数据做早晚两窗口检查与推送；含考勤档案归档与工资基线影子共识 | `KMFA/kmfa-dingtalk-attendance-skill/` |
| `KMFA/skills/资金周报/` | `fund-weekly-analysis-skill` | 从付款申请群财务截图提取资金数据，生成资金周报/月报与公司-银行账户矩阵 Excel 看板 | `KMFA/fund-weekly-analysis-skill/` |
| `KMFA/skills/每日工作检查/` | `daily_routine_check_skill` | 每日例行工作检查：付款、生产管理、例行交付物与现金 OCR 监控汇总推送 | `KMFA/daily_routine_check_skill/` |
| `KMFA/skills/经营月报/` | `mgmt-monthly-report-skill` | 把七个受治理输入槽位转换为 v6 口径经营月报 Excel 与董事会 PDF | `KMFA/mgmt-monthly-report-skill/` |
| `KMFA/skills/上游归档/` | `dingtalk-dws-archive-skill` | 每日全量归档上游钉钉 DWS 工作区文件到私有运行时，带产出校验 | `KMFA/dingtalk-dws-archive-skill/` |

### 2026-07-19 最后归位

| 现路径 | 机器 id（保持稳定） | 干什么 | 原路径（历史） |
|---|---|---|---|
| `KMFA/skills/项目成本表/` | `project-cost-table-skill` | 输入充分性预检、双成本口径、工资分配、fail-closed 校验与项目成本工作簿生成 | `KMFA/project-cost-table-skill/` |

至此，仓库内所有 9 个 `SKILL.md` 均已集中在 `KMFA/skills/`；`_protected/` 和私有运行时不属于公开仓库迁移范围。

**命名铁律**：目录名给人看（中文），id 给机器看（`SKILL.md` 的 `name`、`$技能名` 调用、
`skill_id`、排程映射一律继续用 id，永不改）。

**本次移动实际做了什么**：51+ 处活引用改写（含每天在跑的考勤生产代码
`KMFA/tools/dingtalk_attendance/*.py`）、路径深度差一修复 8 处、钉钉考勤
`source_checksums.sha256` 全量重算、五个验证器复跑全绿。
**历史不改写**：`stage_artifacts/`、`CHANGELOG`、append-only 账本（`events.jsonl` 等）、
三大中文台账中的旧路径按不可篡改原则保留原文，考据时对照上表 rename map。
`KMFA/metadata/mgmt-monthly-report-skill/` 是历史验收证据目录，目录名一并保留。

## 二、定时任务（排程）与 skill 的对应关系

**排程配在 Codex 应用内，不在本仓库、也不在 `~/.codex/automations/`。** 本仓库只有它们的契约记录：
`KMFA/metadata/automation/codex_app_schedules.contract.toml`。

| 排程（Owner 口中的名字） | 契约里的 id/name | 对应 skill | 状态 |
|---|---|---|---|
| 考勤早 | `KMFA｜每日钉钉考勤检查｜晨报` | `kmfa-dingtalk-attendance-skill` | 🔴 cwd 失效 |
| 考勤晚 | `KMFA｜每日钉钉考勤检查｜晚报` | 同上 | 🔴 cwd 失效 |
| 工作检查 | `KMFA｜钉钉工作检查`（13:35/19:05） | 同上 | 🔴 cwd 失效 |
| 付款申请群资金周报 | `KMFA｜资金周报自动化`（周一/六 11:00） | `fund-weekly-analysis-skill` | 🔴 cwd 失效 |
| 上游归档 | `KMFA｜上游每日钉钉DWS归档`（11:00） | `dingtalk-dws-archive-skill` | 🔴 cwd 失效 |
| dws 保活 | `DWS Auth Keepalive`（每 4 小时） | （认证底座，全部 DWS 类共用） | 🔴 cwd 失效 |

🔴 **6 条的工作目录全部失效**（Owner 已拍板路线 B：直接停用、云端接管，停用后本表更新为 DISABLED）：4 条指向已删除的 `~/Documents/Codex/main_worktree/CodexProject/kmfa`，
2 条 DWS 类指向已随清理消失的临时目录。
KMFA 已迁至 KMOS：**新目录应指向 KMOS 本地 clone（当前为 `~/Documents/Codex/GithubProject/KMOS`）**。
**只能 Owner 在 Codex 应用界面里改，agent 够不到。**
（详情原载 `_protected/BROKEN_AUTOMATIONS_20260717.md`，属历史取证文档；`_protected` 清理后以本登记册与 `registry.yaml` 为准。）

另有一条配在本机 `~/.codex/automations/dws/`：`红圈主合同DWS 每周六自动归档` —— 对应
`hongquan-main-contract-dws`，其 cwd 指向 `~/Documents/Codex`（仍存在，未失效）。

## 三、待办（本次未做）

1. **`dws` 命令本机缺失**：考勤 skill 通过 `subprocess` 调 `dws`（DingTalk Workspace CLI，
   默认取 `DWS_BIN` 环境变量，回落到 `dws`）。**本机 `command -v dws` 找不到，`~/.dws/` 也不存在** ——
   所以考勤 skill 目前在本机就跑不起来，与 cwd 失效是**两个独立故障**。
   `dws` 是 DingTalk Workspace，GitHub 上有开源实现，不绑 Codex。需确定采用哪个实现并固化版本。
2. **把 3 个迁入 skill 里写死的本机绝对路径改成配置项**（否则换机/上云即失效）。
3. ~~物理移动那 5 个在仓 skill 到本目录~~ —— **已完成（2026-07-17 晚，含中文重命名，见上表）**。
4. **排程契约与实际配置对账**：`codex_app_schedules.contract.toml` 声明 ACTIVE 的 6 条，
   在 `~/.codex/automations/` 里一条都没有 —— 契约与实际存放位置脱节，需确认哪个是权威。
