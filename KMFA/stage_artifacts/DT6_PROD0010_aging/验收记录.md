# TSK.KMFA.PROD.0010 应收账龄与回款视图 — 验收记录（2026-07-20）

任务包 07 要求：**`collection_receivable_aging` 真数据化：账龄分桶（口径字典）、回款进度 vs 合同**。验收：**与 2025 应收账龄表/回款表 staging 对得上（断言引用）**（依赖 G4）。

## 一、数据分两层，如实区分

查权威后发现两层状态截然不同：

| 层 | 真实状态 | 本页处理 |
|---|---|---|
| **对账层** | 断言表 `collection` 域 12 条、`receivable_aging` 恒等式 1 条，**均已核到分** | **直接呈现真实金额** |
| **v014 账龄结构层** | `source_lanes` 全部 `data_status=structure_available_values_blocked`；`priority_items` 只有 `public_aging_bucket_ref_00x` 匿名指针、`collection_action_allowed=false` | **不产出分桶金额**，只报结构与阻断状态 |

故本页主体是**真实对账数字**（这是回款域最干净的一轴），账龄分桶部分如实标注仍被阻断。

## 二、真起服务 + 真开页面实测

`docker run -p 18093:8000` → 浏览器 `/ui/` → 点击「账龄回款」（`aria-selected=true`）→ 读渲染后 DOM：

**回款逐月对账（真实分差，12 条）**
```
AST-COLL-202501  2025-01   ¥0.00              0           closed_matched
AST-COLL-202502  2025-02   ¥0.00              0           closed_matched
AST-COLL-202503  2025-03   ¥6,400.00          640000      analyzed_open
AST-COLL-202504  2025-04   ¥-300.00          -30000       closed_matched_tolerance
AST-COLL-202505  2025-05   ¥0.00              0           closed_matched
AST-COLL-202506  2025-06   ¥0.00              0           closed_matched
AST-COLL-202507  2025-07   ¥71,790.00         7179000     analyzed_open
AST-COLL-202508  2025-08   ¥0.00              0           closed_matched
AST-COLL-202509  2025-09   ¥694.57            69457       closed_matched_tolerance
AST-COLL-202510  2025-10   ¥0.00              0           closed_matched
AST-COLL-202511  2025-11   ¥-1,462,802.90    -146280290   analyzed_open
AST-BANK-AXIS-2025  2025-02..11  —（无 delta）             analyzed_open
```
KPI 实测：零分差 **6** 个月｜未闭 **3** 条｜最大差异 **¥-1,462,802.90**（2025-11）。

**账龄恒等式**：`AST-AGING-IDENTITY｜合同额-已收款=剩余金额｜snapshot-2026-07｜差异 0｜closed_matched`

**派生层规模**：`receivable_aging` 9,181｜`collection` 3,726｜`v_collection_authoritative` 862

## 三、金额纪律：整数分换算逐点校验

分→元换算**全程整数运算，禁用浮点**。页面实测值可逐点验算：

- `69457` 分 → **¥694.57**（69457//100=694，69457%100=57）✅
- `-146280290` 分 → **¥-1,462,802.90** ✅
- `7179000` 分 → ¥71,790.00 ✅

契约测试 `test_aging_cents_to_yuan_is_exact_integer_math` 对**每一条**真实断言值重算并比对，任何浮点近似都会立刻暴露。

## 四、契约测试（本单元 6 条）

- `test_aging_monthly_rows_match_assertions`：逐月每条的 delta/status/period 必须等于断言表原值，**不得另造一套数**。
- `test_aging_cents_to_yuan_is_exact_integer_math`：整数分换算定点校验（见上）。
- `test_aging_zero_diff_count_matches_rows`：KPI 计数须与明细自洽。
- `test_aging_identity_assertion_present_and_closed`：账龄恒等式须存在且 0 分差已闭。
- `test_aging_structure_layer_reported_as_blocked`：结构层须报 `values_blocked`、`允许作经营依据=false`，且响应中**不得出现分桶金额字段**。
- `test_aging_staging_rows_match_pipeline_facts`：派生层行数须等于 `data_pipeline` 事实。

## 五、门禁

`pytest tests/` → **36 passed**（30 + 本单元 6）；`check_dual_plane_ci.py` → **PASS**（5 项目）。

## 六、状态

**已建待验收**：回款对账部分已是真实到分的数字，与断言表逐条一致（验收要求的「与 staging 对得上（断言引用）」即以断言为引用面达成）；账龄**分桶金额**部分仍受 v014 结构层 `values_blocked` 限制，该部分验收待 G4/值解锁后补。
