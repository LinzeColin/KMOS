# PROD.0013 端到端回归确认（2026-07-25，改 facts 后复验）

## 我实际做了什么、看到了什么
本会话 #181/#182 改了 `machine/facts`(status.json 的 real_progress/evidence_status)。
0013 全流里「登录/今天」步骤断言页眉三元组与首页 facts,故必须确认没被改坏。

**在当前 main 的 app-e2e CI（run 30157112093，PR #182 的 e2e 作业）实测**：
`kmfa_html_human_flow_audit.py --app-url http://localhost:18098` 用 Playwright 真开 `/ops/app`,
逐步真点、真写决策、真跑四层链、真下载三格式报告——**11/11 PASS,退出码 0**（见 machine 件）。

关键几步（真实数据、非"页面能打开"）：
- **差异拍板**：真写一条决策并留痕（事件已写入）
- **重算**：下游影响面 `goods_movement 17,764` 行由血缘算出；四层链命中 `4/4`
- **报告导出**：HTML 3735B / CSV 763B / PDF 4875B,魔数校验 + 水印在
- **成本页**：A0 未就位时**无编造毛利**,阻塞如实呈现

## 结论
**完成判据②（Playwright 端到端全流 PASS 且入 CI 作回归基线）= 已达成**,且我的 facts 改动**未造成回归**。
0013 本身早于 2026-07-20 建成入 CI;本轮为诚实回归确认,不重复造脚本。

## 铁律
真开页面/真数据/真下载为证,不拿 pytest 绿冒充;证据零金额零公司名。
