# 6301 轴闭合复核记录（湖北开明 2025H1）

日期：2026-07-17　执行：打通线（DT5 数据线）

## 结论

**6301 轴闭合至 0 分差。** 前序记录（对账第 50 号合并单）中「6301 为双套编码映射差」的假设**不成立**，真实成因为以下两条，均有凭证级证据：

1. **方向错**：`6301` 是**营业外收入**（收入类科目），费用表把营业外收支一并收录。收入类科目的发生额在金蝶**贷方**，此前对借方，故 4 张凭证全部错配。
2. **月结凭证混入明细**：费用表第 5 行「记-101 结转本期损益」¥1,264.44 是期间损益月末结转凭证（在金蝶 `6301` 上是借方结转），被误作发生额明细录入，恰好等于此前的轴面差额。

## 凭证级对照（修正方向后）

| 凭证 | 日期 | 费用表（分） | 金蝶贷方（分） | 差 |
|---|---|---:|---:|---:|
| 记-65 | 2025-02-28 | 246510 | 246510 | 0 |
| 记-76 | 2025-03-04 | 185407 | 185407 | 0 |
| 记-103 | 2025-03-31 | 290654 | 290654 | 0 |
| 记-98 | 2025-04-30 | 126444 | 126444 | 0 |
| 记-101（结转本期损益，剔除） | 2025-04-30 | 126444 | —（借方结转，非发生额） | — |
| **合计（剔除结转后）** | | **849015** | **849015** | **0** |

金蝶侧旁证：`6301` 全期贷方合计 = 借方合计 = 849015 分（每月结转清零，闭环自洽）。

## 双套编码映射（顺带定案）

映射规律**平凡**，无需映射表：报表点分式子码两位、金蝶紧凑式子码三位，同码异写。
`6301.02` ↔ `6301002`、`6301.09` ↔ `6301009`、`6602.37` ↔ `6602037`。
已实现为 `KMFA/tools/recon_common.py` 的 `normalize_subject_code`（根码四位 + 子码左补零到三位，点分输出），带自检用例。

## 甄别边界（防误杀）

摘要含「结转」不等于应剔除：`6601`/`6602` 的「结转折旧费用」「结转研发费用」是**费用归集借方发生额**（已在 6602 六十三张凭证全齐中逐分对上）。应剔除的仅「结转**本期损益**」类期间结转凭证。

## 费用轴残差总账（本次复核后）

- `6602`：63/63 张凭证全齐，0 分差。
- `6301`：本次闭合，0 分差。
- `6711` ¥1,900.00、`6601` ¥1,343.00：各定位到唯一凭证，等下批数据源侧复核。

复现路径：`_staging.expense_lines`（`book_subject_code` 前缀 6301）对 `_staging.kingdee_ledger`（`subject_sheet` 前缀 6301、取 `credit_cents`），凭证号经 `normalize_voucher_no` 归一后联接。
