# S18-P3 后续只读连接器方案

- `redcircle`：红圈协作数据；只读方案；未授权、未连接、未调用；写回与源修改均关闭。
- `kingdee`：金蝶财务数据；只读方案；未授权、未连接、未调用；写回与源修改均关闭。
- `wps`：WPS 文档数据；只读方案；未授权、未连接、未调用；写回与源修改均关闭。

共同门禁：owner 授权、私有运行时、Schema 合同、哈希留存、幂等键和回滚要求缺一不可；任一条件不满足即安全阻断。
