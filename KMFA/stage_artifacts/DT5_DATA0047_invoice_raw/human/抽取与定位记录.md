# 项目开票原始导出抽取与定位（2026-07-18）

覆盖地图（DT5_DATA0046）把两件「项目开票导出」标为高价值候选；本单抽取并定位，**线索两处修正**。

## 抽取（`_staging.invoice_raw`，第 17 张派生表）

- 两件导出 → **8,692 行、0 拒绝**（逐票：发票号/状态/日期/收票单位/合同名/含税金额/税率/回填首值）。
- **新雷入册**：导出件 `dimension` 标签坏（声称 `A1:A1`），`read_only` 模式只读出一列——`reset_dimensions()` 后才见 22 列真身。
- 不含税/税额**不预计算**：除法舍入歧义留待按票裁定（票面为准）。

## 定位（两处修正）

1. **两件为同一导出的重复副本**：已开票 4,339 张、期间 2017-11..2026-07、2025 合计逐分相同——查询侧取单源或按发票号去重。
2. **覆盖为红圈项目合同切片**：2025 已开票含税 20,030,475.20 元，仅为 `invoice_lines`（64,193,898.47 元）的 ~31%——**不能替代开票轴既有源**；其价值在逐票粒度（税率构成/发票号级/回填状态）与 2026 期数据。
3. 材料轴线索修正：本源是**销项**票；62 组待解需要**进项**票价税分列——`下批数据需求单` 第 6 项措辞已改「进项发票明细」。

## 断言

不新增轴断言（子集源不构成新轴）；发票号级勾稽（对 `invoice_lines` 红圈子集）挂需求触发。
