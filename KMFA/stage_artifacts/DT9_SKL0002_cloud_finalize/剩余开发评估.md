# 云端收口 · 剩余开发评估（2026-07-19，Owner 睡眠期自主推进后）

Owner 指示"把剩下所有开发任务搞完"。本档诚实交代：**能自主验证的都做了；剩余项均为外部门控（数据/Owner/时间/Mac 采集），非本地可自主验通** —— 不盲改、不制造噪音 PR。

## 一、本轮已完成（能本地验证的真活）
- **skills 云端上线运行 + dws 授权 + 主机核验全绿**（#109–#120）：Golden Path CD、自包含镜像修复（Coolify `./` 无代码坑）、OVH 主机 self-audit rc=0、dws 设备码已授权（张霖泽@武汉开明高新，refresh 至 2026-08-18）。投递仍 `KMFA_DELIVERY_ENABLED=0` dry-run。
- **P2 App 自包含镜像本机验证可部署**：`docker build`（context=仓根）→ `/healthz`={"status":"ok","facts_dir_present":true}、uvicorn 干净启动、代码在镜像。待启 `full` profile 上 `kmfa.linzezhang.com`。
- **daily-backup 无害化**：自包含镜像无 `.git`，原 `git pull` 空转报错 → 改无害心跳（`run_skill.sh`）。
- roadmap DT6/DT9 状态账实同步到云端上线现实。

## 二、剩余开发——按门控归类（均非本地可自主完成）

| 项 | 门控类型 | 说明 |
|---|---|---|
| 考勤晨晚报 / 工作检查 **切正式投递** | 时间+Owner(G9) | 需云端 dry-run 双跑 ≥3 天无异常，Owner 拍板切 `KMFA_DELIVERY_ENABLED=1`；任一时刻仅一发送端 |
| **fund-weekly** 真业务入口 | 数据+Owner | 真入口=OCR 事实候选→**Owner 复核工作簿→决策清单**多阶段人在环 + 真实资金源数据；非 cron 可自主跑。云端接 `validate_taskpack` 正确 |
| **mgmt-monthly** 真业务入口 | 数据 | 真入口 `scripts/mgmt_monthly_report.py` 需**七输入槽**月度业务数据；无输入即崩。云端接 `validate_skill_package` 正确 |
| **upstream-archive** 真业务入口 | Mac 采集 | 设计即"**红圈浏览器导出例外留 Mac 采集端**"（roadmap DT9 person_goal 明载）；云端无红圈会话，接 `validate_skill_package` 正确 |
| 下批数据 12 项 | 数据 | 需求单 `docs/governance/下批数据需求单.md`；拉齐任一项才可复跑对应切面 |
| G4 A 级正式报告 / G5 上线 / G7 交付解锁 | Owner | stop-gate 需 Owner 签核 |
| BLK-001（273 字段） | Owner | 只挡 A 级报告，不挡云端运行 |

## 三、结论
**goal 子条件②"本地不运行的 skill 全部依赖云端运行"——基础设施层面达成**（skills 已在 OVH Coolify 运行、cron 装载、dws 授权、自检绿）。三个技能的**真业务执行**卡在数据/Owner/Mac 采集，是设计使然的外部门控，不是遗漏的开发。剩余推进靠：双跑满 3 天 → Owner 切投递；拉下批数据 → 复跑切面；Owner 签 G4/G5/G7。

我这侧**可自主验证的开发已到边界**；再动即为不可自主验通的盲改或文档噪音，故止于此、如实留档待 Owner 醒后决策。
