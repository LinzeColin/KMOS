# 断言证据链完整性自检（2026-07-18）

无会议对账制的可追溯承诺 =「每条断言的 `evidence_ref` 都能落到仓内真实证据」。本单做全量自检并把检查器固化为仓内工具。

## 结果

- **30/30 条断言证据链全部可追溯**：零悬空引用、零空目录、零缺失字段。
- `stage_artifacts` 结构口径定案：记录类单元 = 平铺 md 或仅 `human/` 均可；带机器产物的单元必须有 `machine/`。硬线只有一条——**引用不悬空**（由工具把守）。

## 工具

`KMFA/tools/evidence_check.py`：全量校验 + `--selftest`（悬空/空目录/缺字段三路负例）。退出码可直接进 CI；本轮先随手动流程跑，是否入 GitHub Actions 工作流挂后续（当前工作流只跑双平面门，扩流水线属 OPS 变更单独议）。
