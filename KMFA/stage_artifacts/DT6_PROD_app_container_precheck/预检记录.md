# App 栈容器预检记录（2026-07-18）

skills 栈已经容器实证（`DT9_SKL0002_deploy_precheck`）；「双栈 `docker compose up`」的另一半——**App 栈**此前只用 venv 起过、镜像从未构建。本单在本机 `arm64`（=Oracle A1 同架构）补齐同待遇预检。

## 结果：全绿，零缺陷

| 项 | 结果 |
|---|---|
| `docker build`（`python:3.12-slim` + fastapi/uvicorn） | **BUILD-OK**（依赖仅两行，秒级） |
| 容器起 + 仓库只读挂载（compose 同款 `-v repo:/opt/kmfa/KMOS:ro`） | 起动即 healthy |
| `/healthz` | `{"status":"ok","facts_dir_present":true}` ✅ |
| `/api/状态` `/api/技能` | 页眉三元组与 8 技能富化字段正常 ✅ |
| `/ui/` | **200**（React dist 随仓，容器内直接可用） ✅ |

与 skills 栈预检不同：**本次零缺陷**——Dockerfile 极简（镜像不含业务数据、仓库卷挂载）的设计本身消除了雷面。

## 结论

双栈（skills-runtime + App）均经目标架构容器实证。实例日 `docker compose up -d --build` 两个目录各跑一次即可，无已知构建/运行风险。预检镜像与容器已本机清理。
