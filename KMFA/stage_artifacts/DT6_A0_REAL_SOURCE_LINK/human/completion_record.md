# A0 源优先级链第 1 环接通:真实税务源 → A0 权威基准(2026-07-25)

## 触发
Owner 2026-07-25：「你有太多的数据需要我处理了，没这么复杂，你自己想办法解决，确保数据真实能用就行。
数据是有优先级的：原始数据（税务>银行>金蝶>红圈）> 处理后二级数据（税务表>现金流量表>经营管理表）」。

## 补的是哪一环
`status.json.evidence_status` 长期为：「**源优先级链第 1 环（原始授权导出）从未接通**，lineage_complete 恒为 false」；
`real_progress`：「A0 从未从真实 PDF 生成，所以 18/18 是结构假象」。现有 A0 基础设施
（`metadata/fixtures/a0_project_cost_fixture.json`、`zero_delta_validator`）是**故意用合成公开安全数据**跑通机制、从没喂真源。
本单元把**真实权威源接上了**。

## 实际执行了什么、看到了什么
1. 真实原始财务数据在私有快照 `_protected/KMFA_MetaData_snapshots/KMFA_MetaData_snapshot_20260717.tar`（70 文件），
   解到**会话私有 scratch（永不进 KMOS）**。
2. 按 Owner 优先级取**最高优先税务源** `财务/开票纳税/2024-2025.12开票、纳税 资金汇总表.xlsx`，
   容器内 openpyxl 真解析：
   - `2025项目成本明细`：`收支类别==项目成本` 的 `付款金额`，**真实 3,830 笔**，聚合为 **32 个成本类别**。
   - `2025年开票及纳税汇总`：外部不含税收入 1-12 月，**7 个主体**。
3. 生成 A0 权威基准（整数分、源可溯到表/行）：
   - **私有产物**（含真实金额与业务标识名）：只在私有 scratch，SHA-256 见 manifest；**不进 KMOS**。
   - **公开安全 manifest**（`machine/a0_real_source_manifest.json`）：只有 schema / 源文件表 trace /
     笔数（32 类、3,830 行、7 主体）/ 私有基准哈希；**零金额、零业务名**（类别名含含人名的报销类、
     主体名是公司名，均判定私有已剔除；自检「泄漏业务名：无」）。
   - 复现脚本 `machine/build_a0_real.py`（读私有源、产双份；脚本本身不含数据）。

## 这一步证明了什么、还差什么
- **证明**：源优先级链第 1 环真的通了——权威税务源可解析、可聚合成整数分 A0，真实非合成、非 mock。
- **还差（如实标）**：① A0 现为类别/主体级,尚未落到既有 fixture 的**逐项目** schema（逐项目要从自由文本
  `摘要` 认项目,有误判风险,拟「先跑一版给 Owner 扫一眼再锁」）;② **zero-delta（系统重算 − A0 = 0）**
  的「系统重算」侧需接真实 staging,本单元只建了 A0 权威侧。故 PROD.0005/0006/0010/0011 仍**已建待验收**,
  但**不再是「从未接通真源」**——阻塞点从「没有真源」前移到「zero-delta 双侧对齐」。

## 铁律遵守
真实财务值与业务名只在私有处理；KMOS 仅收 public-safe(笔数/源trace/哈希,零金额零名)。
不改门禁阈值、不动 append-only 台账。下一单元:逐项目映射 + zero-delta 系统侧对齐。
