# 费用表双码合规审计（母表上岗第一战，2026-07-18）

母表（`subject_code_map`，PR #84）入仓当轮即上岗：审计 `expense_lines` 全部去重（公司, 统一码, 账套码）对是否被制度定义覆盖。

## 结果

- **154 对中 153 对母表命中（99.4%）**——费用表的双码书写与「17底表/Sheet1」制度定义高度一致，双套编码现象彻底闭环（平凡映射 22 对由 `normalize_subject_code` 覆盖，非平凡对由母表覆盖）。
- **唯一违例 = 源表自身的 `#N/A`**：武汉开明 记-49（2025-03-27，¥0.86，账套码 6711.06）——源工作簿统一码列的查找公式失败，字面 `#N/A` 落入数据。

## 连环闭案

该行正是材料轴/费用轴两次挂起的「微额科目归属候选 记-49 ¥0.86」：现定案为**源表查找失败行**——统一码缺失导致其在费用表按账套码 6711.06（营业外支出）列示，而金蝶实际记于材料/采购科目。成因=源表制度映射断链，非账务错误。`AST-EXP-AXIS-WHKM-2025` 的 记-49 残差项由「待查」改「已定案」。

## 处置

- 下批费用表：修复该行统一码（顺带即消掉费用轴最后一笔 expense-more 残差）。
- 双码合规审计可复跑（SQL 见本记录生成过程），阈值=零非平凡未登记对（`#N/A` 类源错误单列）。
