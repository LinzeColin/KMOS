# KMFA skills 云端端到端上线记录（OVH，2026-07-19）

goal 子条件②「本地不运行的技能全部依赖云端运行」**运行层面达成**：skills 技能栈在 OVH Singapore VPS-1（amd64）Coolify 上运行、主机侧核验全绿（dry-run）。本档记录 CD 打通 → 真机暴露的 bug → 修复 → 主机核验的完整链路（含诚实的验证盲区与纠正）。

## 一、CD 打通（Golden Path，#117）
- 基建线（operate/「OVH Singapore VPS-1 基建线」会话）铺好可复用工作流 `LinzeColin/CodexProject/.../linze-golden-path.reusable.yml`、KMOS 预设 `COOLIFY_BASE_URL`/`COOLIFY_API_TOKEN` secrets、预建 Coolify 应用 `gz5qao2k0zrx3polpbwgcg51`。
- 我认领仓侧两步：Patch A（compose 路径改仓根相对）+ `.github/workflows/deploy.yml` caller（`git add --sparse`——`.github/` 在 KMOS 稀疏检出锥外）。
- 合并后 push main → deploy 工作流 verify→deploy→home-sync 全绿，Coolify API 回 `deployment queued`（app `kmfa-kmos-p1`）。

## 二、真机暴露的 bug（Coolify `./` ≠ 完整仓）
- 首部署容器 healthy 但 `self-audit rc=2`：`ls /opt/kmfa/KMOS` **只有 README+compose**、KMFA/KMDatabase 不在。
- 根因：**真 Coolify 对 Docker-Compose 部署，compose 里 `./` 运行时绑定挂载解析到"只含 compose 的部署工作目录"，不是完整仓 checkout**，且 shadow 掉镜像。技能无代码可跑。
- **我的验证盲区**：本机上次用 `docker compose --project-directory 仓根` 测，`./`=完整仓故未复现。基建线在主机 `ls`/`find` 抓获。

## 三、修复（自包含镜像，#119）
- 两镜像（skills-runtime / app/backend）build.context 改**仓根** + Dockerfile `COPY . /opt/kmfa/KMOS` 把代码烧进镜像；compose 删除两处 `./:/opt/kmfa/KMOS:ro` 挂载；新增 `.dockerignore` 排除 `.git`/`KMDatabase/data/objects`(120M)/`node_modules`。
- 每次 push→重 build 更新代码，**不依赖任何运行时卷挂载**。
- **纠正验证法**：本机改用**零挂载** `docker run`（不挂仓）→ `ls /opt/kmfa/KMOS`=完整仓、`self-audit rc=0`——复现 Coolify 真实情形。（教训沉淀：本机验证不可用 `--project-directory 仓根`，会用宿主码掩盖此坑。）

## 四、主机核验全绿（基建线，OVH 主机）
新容器 `skills-gz5qao2k0zrx3polpbwgcg51-121435155647`，`Up healthy`：
- `ls /opt/kmfa/KMOS` = 完整仓（AGENTS.md / KMDatabase / KMFA / KM_IDSystem / whkmSalary / …）
- `date +%z` = **+0800**
- `KMFA_DELIVERY_ENABLED` = **0**（dry-run 不投递）
- `run_skill.sh self-audit` = **rc=0**：30 条断言证据链零悬空 + 血缘 FRESH + 5 项目双平面 PASS + 台账 +08:00

skills 无入站（runtime worker，无公开 URL）；`healthy` + `self-audit` 绿即**上线成功**。

## 五、剩余（唯一）
- **Owner dws 设备码**（钉钉手机点一次）解锁真发；之后测试发张霖泽 → dry-run 双跑 ≥3 天 → 切 `KMFA_DELIVERY_ENABLED=1`。
- GitHub App 确认免除（公开仓/API）；DNS 待 P2 App 面板。

## 结论
KMOS 端到端上云 + 云端运行验证通过。技能已依赖云端运行（dry-run）；真投递仅差 Owner 一次 dws 授权。
