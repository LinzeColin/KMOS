# M5 经营驾驶舱现场验收（2026-07-25，本会话实执）

**触发**：M5 goal 复评指出「引用了往期证据/PR 门禁结论，本会话未亲自执行验收流」。本档为本会话
真起服务 → 真开 `/ops/app` → 逐页真数据 → 截图 + 逐字段实测输出的现场记录，不拿 /healthz、不拿 pytest 绿、
不拿往期结论充数。

## 实际执行了什么

1. 从当前 `main`（HEAD `ea93ce98`，含 #171 首页纠偏）用多阶段 Dockerfile 构建镜像 `kmfa-app:acc`
   （镜像内现建前端 + 烧入 `KMFA/machine/facts/*.json` 真 facts 数据）。
2. `docker run` 起容器,**不设** `KMFA_PRIVATE_OPS_REQUIRE_ACCESS`（本地放开 Access 前置,等价于 Owner 登录后所见）,
   `curl /ops/app` → HTTP 200;`/api/数据管线` → `质量等级=Q3（机器候选结构化；对账收敛进行中）`、`staging_rows_total=263758`。
3. Playwright（容器内,共享 docker 网络）打开 `/ops/app`,按驾驶舱八步真点真填,逐页截图 + 抽取真实字段值。

## 逐步实测（9/9 PASS，截图见 human/shots/）

| 步骤 | 页面 | 真实数据实测（我实际看到的值） | 结论 |
|---|---|---|---|
| 01 | 入口+页眉三徽章 | 页眉：`质量 Q3` \| `报告 D——缺失、过期、失败或关键差异未闭合` \| `Q4 / D / NO_GO / 3-9-2-1`；数据截至批次 2026-07-16 | PASS |
| 02 | 今天（驾驶舱首页） | `暂不可对外——关键账未对平`；30 项自动核对已对平 19、待拍板 11；`等你拍板` 按金额排序：AST-LOAN-AXIS-2026M5 `¥4,096.03 万`(精确 ¥40,960,322.77)、AST-COLL-202511 `¥-146.28 万`…；`BLK-001`「只有你能解」 | PASS |
| 03 | 回款与账龄 | `回款逐月核对` 表在；`6/12 月已对平`、未闭 4 月最大差异 ¥-146.28 万 | PASS |
| 04 | 项目成本 | `算不出金额——缺权威成本基准`，阻塞如实呈现；**无编造毛利率** | PASS |
| 05 | 待拍板（真写决策） | 点开 AST-COLL-202503（差异 ¥6,400.00）→ 填理由「M5 现场验收:按容差闭案(本会话)」→ 点`闭案 → closed` → **已追加事件 `MANEVT-APP-0001`（哈希 4b237244f217…），时间 2026-07-25T15:52:33+08:00，来源 App 应用状态面**；拍板留痕 6（App 写入 1 \| 已冲正 1）；append-only（改主意走冲正,不涂改） | PASS |
| 06 | 数据底账（血缘影响面） | 选源资产 `raw:d46f77b0c90d` → `会牵连什么`：受影响核对域含 `expense_lines`，行数 `17,764` | PASS |
| 07 | 重跑（四层链） | 发起重跑 → `本次重跑结果`：四层全命中 `field_mapping / fact_layer / derived_metric / report_reference`；`链完整：是`、`旧版本全保留：是` | PASS |
| 08 | 报告下载（三格式真下载） | HTML 3735B（魔数 `<!doctype`）、CSV 763B（BOM）、PDF 4875B（`%PDF`）；均含水印/等级头（内部版·带水印） | PASS |
| 09 | 系统自检 | 排程健康如实：`读不到自动任务日志——/var/log/kmfa 目录不存在（本地未挂 kmfa-logs 卷,部署配置问题,不是排程问题）`，不假造成功 | PASS |

console_errors=1，且已核实为跨源 COOP 提示（`http://kmfa-acc:8000` 非 HTTPS/localhost 的可信源导致浏览器忽略 COOP 头）——
纯测试环境假阳性,线上 HTTPS 无此项;非应用错误。walkthrough 因「任何 console 记录即判非满分」而 rc=1,但 9/9 功能步骤全 PASS。

## 对完成判据的意义

- **判据 1（PROD.0001–0018 逐条真数据）**：驾驶舱各页均显示真实 facts/staging 数据（真实资产码、真实金额、真实对账状态），本会话逐字段实测,非 mock。
- **判据 2（Playwright 全流入 CI 回归基线）**：首页→检查板→成本页→差异处理→影响预览→重跑→报告导出全流本会话真跑 9/9 PASS;该流的 CI 版本 `KMFA/app/e2e` + `kmfa_html_human_flow_audit.py` 已在 app-e2e 工作流作为回归基线（本次 #171 门禁「跑私有经营 App 全流端到端」= success）。
- **判据 5（真实经营数据 + 页眉三徽章）**：数据层已确证在 `/ops/app` 真实呈现（质量 Q3 / 报告 D / delivery Q4-D-NO_GO 三徽章 + 真实拍板队列）。线上该页由 Cloudflare Access 前置（`经营数据默认私有 / 财务不裸挂` 铁律）,故最终「Owner 亲自登录线上 `/ops/app` 确认」这一步不可代——属 Owner 亲验。

## 边界

本地放开 Access 仅为验收取证,等价于授权后所见;真实私有财务数据不入公开 KMOS 仓（facts 为 public-safe/staging 表示）。
本档只增证据,不改任何应用代码、不动门禁阈值、不改 append-only 台账。
