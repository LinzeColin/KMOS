# 附录:补齐 criterion 2 链的入口段与「检查板」段(2026-07-25 本会话)

M5 goal 复评指出:官方全流审计覆盖的是驾驶舱内八步(今天→…→报告),但完成判据 2 的链写作
「**首页→检查板→**成本页→差异处理→影响预览→重跑→报告导出」,其中「真实入口」与「检查板(源检查板 PROD.0005)」
应显式覆盖。本附录补这两段,本会话真执行,3/3 PASS。

## 实际执行

`docker` 起 `kmfa-app:sb` 容器 → `curl /api/源检查` 确认真数据 → Playwright 真走入口链:

1. **入口①(真实入口)**:打开公开首页 `/` → `title=KMFA｜经营驾驶舱`、hero「把钱、票、成本与拍板…」、
   `进入经营驾驶舱` 主按钮存在(截图 `E1-public-entry.png`)。
2. **入口②**:点「进入经营驾驶舱」→ 落到 `/ops/app` 驾驶舱,页眉三徽章
   `质量 Q3 / 报告 D / Q4·D·NO_GO·3-9-2-1` 在(截图 `E2-cockpit-arrived.png`)。
3. **检查板③(源检查板 PROD.0005,现收进「数据底账」页的源清单)**:真数据(截图 `E3-source-board.png`):
   - 一、数据从哪来:登记数据源文件 **53**、数据批次 **2026-07-16**、派生表/行 **17 / 263,758**、新鲜度 **新鲜**。
   - 覆盖矩阵(源×登记状态):WPS钉钉红圈 合计 **13**、绩效 **9**、财务 **31**。
   - 二、接入了多少:已接入原始记录 **263,758**、质量档位 **Q3(机器候选结构化;对账收敛进行中)**、未决质量卡点 2 项。
   - 各表行数(对数轴,真值):kingdee_ledger **162,979**、goods_movement 38,732、kingdee_voucher 21,334、
     receivable_aging 9,181、invoice_raw 8,692、expense_lines 8,327、…、loan_register 23。
   - 对账现状(八切面真状态):回款 7/11 月 0 分差;开票三角差 3 分+逐票层 126/134 闭案;五账套凭证 0 不平;
     费用轴两账套收口零候选;税费轴 36/39 格;借款轴流量级闭合;材料轴 125/190 假设穷尽;个人借支 27/30 在位;
     open 项收敛为下批数据四类依赖。

## 意义

- 完成判据 2 的链现已**逐段真覆盖**:真实入口(公开首页→驾驶舱)+ 检查板(源检查板真数据)+ 其余八步(官方审计 11/11,见 `machine/official_e2e_11of11.csv`)。
- PROD.0005 源检查板**页面按真实事实建成并如实呈现**(质量 Q3、覆盖矩阵、对账切面),其**更深层验收**(把 open 项四类依赖收敛、质量升级)仍待 **G4 保证门**——G4 是 Owner 保证+私有数据对账收敛的治理门,非可取值,页面不造假、如实标阻断。
- 仅增证据,不改应用代码、门禁阈值、append-only 台账。
