# 收发明细内部恒等式审计（2026-07-18）

对 `_staging.goods_movement`（38,732 行）做「期初 + 收入 − 发出 = 结存」自洽审计，两个粒度：

## 结果

| 粒度 | 口径 | 成立 | 备注 |
|---|---|---:|---|
| 行级 | 段内 本行结存 = 上行结存 + 收 − 发 | 25,054 / 25,938（96.6%） | 破例呈**同日内乱序**形态（源表滚动余额按别的排序计算） |
| 段级 | 段期初 + Σ收 − Σ发 = 段末结存 | 9,032 / 10,032（90.0%） | 段 = 每个期初行到下一期初行 |

## 失败族分类（段级 1,000 段）

| 族 | 段数 | 定性 |
|---|---:|---|
| 清零段（计算结存 = 0） | 836 | **源表显示伪象**：物料清零时末行「结存金额」列不写 0（其中 500 段恰写成当期收或发金额）。两版工作簿数值完全一致——伪象来自源表生成器，非抽取失真 |
| 其他 | 164 | 与行级同日乱序同族（滚动列排序口径） |

## 结论

1. **抽取零失真实证**：两版（cc3963bc/d46f77b0）在全部失败段上数值逐分一致——伪象随源而来。
2. 收入/发出金额列（材料轴使用的两列）**不受伪象影响**——伪象只在「结存」滚动列；此前七域勾稽结论无一依赖结存列。
3. 处置：结存列标注「仅供参考、清零段与同日序不可靠」，下游一律用收/发列自行滚动。断言 `AST-GM-IDENTITY` 落 closed_matched_tolerance（伪象已分类、无未解释残差族）。
