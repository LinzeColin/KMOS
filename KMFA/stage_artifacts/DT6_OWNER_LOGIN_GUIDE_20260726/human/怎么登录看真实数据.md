# 三步登录 /ops/app 看真实经营数据（判据⑤）

> 前提已核实（2026-07-26 实测）：服务器**活着**。`kmfa.linzezhang.com/` 返回 200（DYNAMIC 实时应答，非缓存），
> `/ops/app` 正确 302 跳到 Cloudflare 登录页。你之前看到的 "unavailable" 多半是我推备份 PR 时
> Coolify 正在重新部署那几分钟，现已恢复。

## 第 1 步：打开地址
浏览器输入：**`https://kmfa.linzezhang.com/ops/app`**
（不是根路径——根路径是对外公开首页、没有经营数据；`/ops/app` 才是你的私有驾驶舱。）

## 第 2 步：过 Cloudflare 登录（收邮箱验证码）
它会自动跳到一个 **Cloudflare Access 登录页**（网址是 `…cloudflareaccess.com`，正常）：
1. 选 **邮箱验证码 / Send me a code**。
2. 邮箱填 **`linzezhang35@gmail.com`**（只有这个邮箱被授权）。
3. 去 Gmail 收一封验证码邮件，把 **6 位码**填回去 → 进入。
   - 收不到：翻垃圾邮件箱；仍无就等 1 分钟再点一次 Send。

## 第 3 步：核对你该看到什么（这就是判据⑤的验收点）
进去后是**经营驾驶舱**。请确认这三样**都是真实的、不是占位**：
1. **页眉三徽章**：`数据质量 · …（质量 Q?）`、`报告 · …（报告 D/…）`、`对外交付 · …（GO状态）` 三个都在、都有值。
2. **今天页**：能看到"等你拍板"队列，带**真实金额**（万/精确到分）。
3. **随便点一页**（回款与账龄 / 项目成本 / 数据底账）：显示的是真实经营数字，不是 0、不是"示例"。

看到了 → 判据⑤过。你在这条对话里回我一句"**看到了，三徽章和真实数据都在**"，我就把亲验记进 roadmap，M5 这条正式闭。

## 如果还是打不开
- 显示 Cloudflare **52x / "web server is down"**：多半又赶上重新部署，等 2–3 分钟重刷。
- 显示 **Access 拒绝 / 未授权**：确认邮箱是 `linzezhang35@gmail.com`（大小写、别打错）。
- 手机上不方便收码：用电脑浏览器登一次更顺。
- 真过不去：截个图发我，我按你看到的报错对症处理（这属你能亲历、我看不到的部分）。
