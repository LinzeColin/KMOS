# 派生层登记册切私有库读取 — 记录（2026-07-20）

仓库拆分线移交：KMOS 公网上 53 个财务文件 + 含真人姓名的 `manifest.jsonl` 需清除。
**Owner 当面选定「先只做代码解耦，清除本身押后」**，本次即执行这一步。

> 清除动作（重写历史 / 切 Private）**未执行**，等 Owner 后续拍板。转述来的授权不作为执行依据。

## 一、先核实，再动手

拆分线给的结论我逐条自己验过，不是照抄：

| 主张 | 我的实测 |
|---|---|
| `facts_from_staging.py` 直读本地 manifest | 属实，第 21 行 |
| manifest 含真人姓名与客户名 | 属实，53 条中 `余永昕`、`新疆宜化`、`福建鼎信`、`山东圣川`、`贵州凯里瑞安` **全部命中** |
| 公网确有数据 | `KMDatabase/data/objects` **53 文件 / 120M** |
| 删除窗口还在 | `forkCount: 0`、`visibility: PUBLIC` |
| SDK 与路牌已就位 | `private_db_client.py` 10,818 字节、`WHERE_IS_THE_DATA.md` 均在 |

## 二、切换前先比对两边内容

不能盲切——先把私有库那份拉下来对：

```
本地 53 条 ｜ 私有仓 56 条 ｜ 字段集相同
私有仓多出 3 条（域=工资，批次 2026-07-16）：
  2026.04月发放-20260528-模版.xlsx
  2026.05月发放-20260629.xlsx
  26年6月工资-发张总-工资为初稿_Mac兼容版.zip
```

## 三、把「改代码」与「重跑事实」拆开做

切源会连带改三个事实值，其中两个有外溢：

| 事实 | 现值 | 切源重跑后 | 去向 |
|---|---|---|---|
| `raw_assets_registered` | 53 | **56** | App「数据管线」页可见 |
| `raw_domains` | `[WPS钉钉红圈, 绩效, 财务]` | **+`工资`** | 公开仓 `machine/facts/data_pipeline.json` |
| `data_as_of_batch` | 2026-07-16 | 不变 | — |

`raw_domains` 不被渲染器或页面取用，但那个 JSON 本身在公开仓——重跑就等于**在收紧隐私的当口，往公开仓加一个「工资」域标签**。

所以本次**只改读取路径，不重跑、不提交新事实**，正好落在 Owner 选的「只做代码解耦」上，零连带影响。
重跑与否是下一个决策点，留给 Owner。

## 四、实现：读私有库，且**不做本地回退**

```python
def load_kmdb_manifest() -> list[dict]:
    """从私有库读原始件登记册。

    **不做本地回退**：回退到公开仓的那份，等于把刚解耦掉的依赖又悄悄接回去，
    而且会在清除之后静默读到空/旧数据。读不到就明说、就停。
    """
```

不加回退是有意的。今天已经在「静默失败」上栽过两次（排程装错却不报错、我自己的自检被 `set -e` 打死不吭声），这里不再重蹈。

## 五、实测

**正例**（真调 SDK 走 `gh` 认证）：
```
✓ 登记册取自私有库 Private-KMDatabase/manifest.jsonl：56 条
域: ['WPS钉钉红圈', '工资', '绩效', '财务']   批次: 2026-07-16
```

**负例①**（SDK 缺失）：
```
缺少私有库 SDK：/nonexistent/private_db_client.py
```

**负例②**（私有库读不到）：
```
从私有库读 Private-KMDatabase/manifest_不存在.jsonl 失败：gh: Not Found (HTTP 404)
  · 确认 `gh auth status` 已登录且对 LinzeColin/Private-Database 有读权限
  · 本生成器**不回退读公开仓**，见函数注释
```

**无回退确认**：代码中 `KMDatabase/data/manifest.jsonl` 仅剩注释提及 1 处，无 `except → read_text` 之类的回退分支。

## 六、状态

**已改并实测**。公开仓那份 manifest 与 objects **仍在原处未动**——清除方式待 Owner 决定（重写历史 / 切 Private / 其他）。
本改动已解除派生层对它的依赖，所以将来无论选哪条路，管线都不会因此断。
