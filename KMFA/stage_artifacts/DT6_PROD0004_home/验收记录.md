# TSK.KMFA.PROD.0004 首页/「我在哪」 — 验收记录（2026-07-20）

任务包 07 要求：**复刻 v1_4 首页样板为 React 版：当前 Stage/Phase/质量等级/差异状态/hard blocks 总览（数据来自 facts）**。验收：**与 `00_我在哪.md` 渲染件同源一致；Playwright 用例过**（Playwright 属 PROD.0013，本单元先做同源一致）。

## 一、交付

- 后端 `GET /api/我在哪`：与渲染件**同源**——同吃 `machine/facts/{status,blockers,roadmap}.json`（渲染件文件头已声明其事实源即这三份）。
- 前端首页改为「我在哪」，三块结构对齐渲染件：**一、当前状态｜二、卡住的事｜三、路线图**，并保留数据面快览卡片与断言图表。

## 二、顺带修掉一处「假数据」

`/api/状态` 的页眉质量等级原为**硬编码字符串 `"Q3"`**——违反本任务「数据来自 facts」，且 facts 一旦升级（Q3→Q4）页眉会**静默说谎**。已改为取自 `data_pipeline.quality_grade_current` 并截取短码，另加契约测试钉死。

## 三、真开页面 + 与渲染件逐字段比对（验收基准）

`docker run -p 18091:8000` 起服务 → 浏览器 `/ui/` → 读取渲染后 DOM：

| 字段 | `00_我在哪.md` 渲染件 | 页面实测 | 一致 |
|---|---|---|---|
| 版本 | `0.1.4` | `0.1.4` | ✅ |
| 进行到哪 | S05　A0 权威项目成本黄金基准 · S05-P3　权威基准锁定 · S05-P3-T1　人工确认后将 Q4 字段升级为 Q5 计算基准 | 逐字相同 | ✅ |
| 进度 | 4/18 —— 结构上 18 个阶段校验器全部通过…「18/18」是假的。 | 逐字相同 | ✅ |
| 报告可信度 | D —— 缺失、过期、失败或关键差异未闭合 | 相同 | ✅ |
| 业务结论 | Q4 / D / NO_GO / 3-9-2-1 | 相同 | ✅ |
| 卡住的事 | 1 件 | 1 件 | ✅ |
| BLK-001 | 内容 273 行字段人工确认…／**只有你能解**／20 天 | 三项全同 | ✅ |
| 路线图 | S01–S18 | 18 行 | ✅ |

页眉实测：`质量 Q3`（现取自 facts）｜`报告 D —— 缺失、过期、失败或关键差异未闭合`｜`Q4 / D / NO_GO / 3-9-2-1`。
页签实测：我在哪 / 源检查板 / 差异工作台 / 数据管线 / 技能。

> 浏览器面板视口报 0×0、可访问性树不可用，故未附截图；以**读取渲染后 DOM 正文**作等效证据（同 PROD.0005）。

## 四、把验收标准做成可执行断言

新增 3 个契约测试，其中两个**直接读 `文档/00_我在哪.md` 做交叉断言**：
- `test_where_am_i_same_source_as_rendered_doc`：版本/阶段/分期/任务/进度/报告可信度/业务结论/卡住件数/BLK 编号与时长，**逐值必须出现在渲染件中**——页面写死或 facts 更新未同步，任一侧漂移都会失败。
- `test_where_am_i_roadmap_matches_doc_stage_count`：18 阶段的 id 与 name 均须在渲染件中。
- `test_header_quality_grade_comes_from_facts_not_hardcoded`：页眉等级须为 `data_pipeline.quality_grade_current` 的前缀。

## 五、门禁

`pytest tests/` → **26 passed**（23 + 本单元 3）；`check_dual_plane_ci.py` → **PASS**（5 项目）。

## 六、状态

**已完成**（本项不依赖 G4；依赖项 PROD.0002 已完成）。唯 Playwright 用例待 PROD.0013 统一建设，届时把本页纳入端到端首站。
