# TSK.KMFA.PROD.0011 开票纳税与资金贷款视图 — 验收记录（2026-07-20）

任务包要求：`invoice_tax_plan` / `fund_cash_loan_plan` 真数据化。
两条红线写死在任务包里：**资金/现金/贷款线不做付款操作**（§212）、**开票/纳税/税务政策线不做正式纳税申报，只出证据缺口和风险提示**（§213）。

## 一、数据分两层，如实区分（与账龄页同构）

| 层 | 真实状态 | 本页处理 |
|---|---|---|
| **对账层** | 断言表 `invoicing` 2 条、`tax` 1 条、`loan` 2 条，**均已核到分** | **直接呈现真实金额** |
| **v014 S14 计划层** | P1 四车道 + P2 三车道全部 `values_unproven`；九个方法全部 `blocked_no_authoritative_*_value_binding`；两段决策皆 `NO_GO`，`value_binding_proven_lane_count=0`、`public_business_amount_count=0` | **不产出计划金额、不列贷款到期提示** |

## 二、真起服务 + 真开页面实测

`docker run -p 18094:8000`（零挂载，代码烧在镜像里）→ 浏览器 `/ui/` → 真点「开票纳税」页签（`aria-selected=true`）→ 读渲染后 DOM：

**开票对账（2 条）**
```
AST-INV-AXIS-2025      报表开票金额vs开票明细合计        2025-01..11  —        —           analyzed_open
AST-INV-TRIANGLE-2025  明细=指标带总开票（集团口径）      2025         ¥0.03    3           closed_matched_tolerance
```

**税务对账（1 条）**
```
AST-TAX-AXIS-HBKM-2025 税负率明细板表=费用表6403计提（按月×税种） 2025-01..10 ¥0.01  1  closed_matched_tolerance
```
> 39 格逐月逐税种对照：36 格 0 分差；6/7 月印花税 ∓6,757 分互为对冲；9 月地方教育附加差 1 分（源表亚分舍入）。

**贷款对账（2 条，零分差 1）**
```
AST-LOAN-AXIS-2026M5        贷款一览表结存=金蝶借款科目余额  截至 2026-05           ¥40,960,322.77  4096032277  analyzed_open
AST-LOAN-ZHONGLI-ADHERENCE  仲利摊销模型=金蝶实还（本金/利息） 2025-01..2026-05(7-23期) ¥0.00           0           closed_matched
```
「结论」折叠块**真点开验证**：`AST-LOAN-AXIS-2026M5` 展开出 454 字定案全文——三账套负债类期初余额系统性为零（导出口径缺陷）、窗口内新贷三笔 0 分差、湖北中行差 ¥9,916.67 定位到单张凭证等，说明这条 4 千万的 open 项**原因已查清、不是数据错**。

**派生层规模**：`invoice_raw` 8,692｜`invoice_lines` 548｜`tax_composition` 798｜`loan_register` 23

**红线（8 项全零，且全部读自 v014 summary 事实，非硬编码）**
```
开票次数 0｜纳税申报次数 0｜付款或动账次数 0｜银行操作次数 0
付款审批次数 0｜贷款管理动作数 0｜政策申报提交次数 0｜补贴申请次数 0
```

**税务政策：证据缺口与风险提示（0/5 证据完备，不作资格判断）**
科小/高新/专精特新/小巨人/研发费用 五项，逐项给风险等级 + 风险提示 + 证据缺口；`allowed_formal_policy_qualification_conclusion` 全 false。

## 三、金额纪律：整数分换算逐点校验

分→元**全程整数运算，禁用浮点**。页面实测值可逐点验算：

- `3` 分 → **¥0.03** ✅　- `1` 分 → **¥0.01** ✅　- `0` 分 → **¥0.00** ✅
- `4096032277` 分 → **¥40,960,322.77**（4096032277//100=40960322，%100=77）✅

契约测试 `test_invoice_tax_cents_to_yuan_is_exact_integer_math` 对**每一条**真实断言值重算比对，并对上述四个已知真值做定点断言。

## 四、真开页面逮到的 2 个问题（单测抓不到）

1. **九个方法里三个的「还缺什么」显示 `—`**：`cash_summary_methods` 事实里没有 `method_note`，但**有** `visible_name`（中文名）和 `required_lanes`。页面把已有事实丢了，只甩英文 id。
   → 修：API 补 `名称`/`依赖车道`，并由 `required_lanes` 推出兜底说明句。**兜底落在 API 而非页面**——否则导出/自动化拿到的仍是 `null`（第一版就是这么写的，被契约测试打回）。
2. **依赖车道列相邻 `<code>` 只靠 `margin` 分隔**：视觉有空隙，但 `innerText`/复制粘贴粘连成 `invoice_planinvoice_tax_cash_summary`。
   → 修：插入真实空白文本节点。

## 五、契约测试（本单元 7 条）

- `test_invoice_tax_blocks_match_assertion_domains`：三域逐条的 delta/status/period/finding 必须等于断言表原值。
- `test_invoice_tax_cents_to_yuan_is_exact_integer_math`：整数分换算 + 四个已知真值定点校验。
- `test_invoice_tax_zero_diff_counts_match_rows`：KPI 计数须与明细自洽。
- `test_invoice_tax_red_lines_are_zero_and_fact_sourced`：8 项红线须全零**且逐项等于 v014 summary 原值**（防有人把红线写死成 0）。
- `test_invoice_tax_plan_layer_reported_as_blocked_without_amounts`：两段须 NO_GO、车道 `values_unproven`、九方法全 blocked、每个方法有中文名/依赖车道/非空说明；并按**字段名**扫描确认不存在「计划金额/到期提示/资金缺口金额/预计现金流入/预计税款」等字段（只扫字段名——诚实边界正文里"不产出计划金额"是否定句，扫全文会误伤）。
- `test_invoice_tax_policy_gaps_are_tips_only`：五个政策项须证据不完备、不允许出资格结论、风险提示与缺口均非空。
- `test_invoice_tax_staging_rows_match_pipeline_facts`：派生层行数须等于 `data_pipeline` 事实。

## 六、门禁

`pytest tests/` → **43 passed**（36 + 本单元 7）；`check_dual_plane_ci.py` → **PASS**（5 项目）。

## 七、状态

**已建待验收**：三域对账均为真实到分的数字，与断言表逐条一致；计划层受 v014 S14 `values_unproven` 限制，本页不产出计划金额与到期提示，待 G4/值解锁后补。红线 8 项全零已由事实与契约测试双重锁定。
