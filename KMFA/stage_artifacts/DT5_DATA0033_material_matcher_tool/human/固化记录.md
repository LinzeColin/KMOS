# 材料轴匹配引擎固化记录（2026-07-18）

`DT5_DATA0029-0032` 的三层匹配与假设探针此前为一次性脚本，违背零重建纪律。本单固化为仓内工具 **`KMFA/tools/material_matcher.py`**：

- 三层匹配（L1 全额等 / L2 子集和 / L3 双向差额定位）+ 两探针（同月异号、他科目命中）；组合穷举上限 18 行（超限如实计数）。
- 自带 `--selftest`（合成数据覆盖六种判定路径）；输出机器摘要 JSON（`machine/match_summary.json`）。
- **实跑精确复现全部已记录裁定**：125/190（L1 98 / L2 19 / L3 8），同月异号 1、他科目命中 2、未解 62（与前记录 65 的口径差正是探针两类的分层，62+1+2=65 ✓）。

## 固化即抓 bug

自检当场抓住 L3 缺陷：金蝶材料行为空时「差额 = 收发全额」，membership 检验恒真——未固化前的首版工具实跑虚高到 174/190。修复（L3 要求金蝶行非空）后自检通过、数字回到与手工裁定逐项一致。**教训入册：临时脚本的正确性只被跑过的那一次数据保证；固化+自检才是可复跑的证据。**
