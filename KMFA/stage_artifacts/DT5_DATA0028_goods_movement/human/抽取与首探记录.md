# 材料/存货域首件：收发明细表抽取与轴面首探（2026-07-18）

## 抽取（`_staging.goods_movement`，第 14 张派生表）

- 报表系列一两册「8收发明细表」（56 列宽表）→ **38,732 行、0 金额拒绝、0 亚分舍入**——目前最大的单 sheet 族。
- 落库口径：物料键（长代码/名称/规格/批次）+ 期间/日期/单据号/凭证字号 + **四个金额列入分**（期初结存/收入/发出/结存）；数量与单价**非货币，原文字符串**；单位组其余 39 列显式缓议（列数记入抽取清单 `deferred_cols`）。
- 两册期间重叠（202412 册 2023-2024、202510 册 2024-2025）：一律入库、`source_sha8` 区分版次，查询期「期间取新册」（同回款权威视图纪律）。
- 单据号形态：`WIN*`（入库）/`SOUT*`（出库），带凭证字号可联金蝶。

## 轴面首探（定性：先建映射，再收轴）

1. **两账套混编实证**：2025 年收发凭证号（月+号双键）在湖北开明 1401/1403 联接率 **130/210**、武汉开明 **93/210**——该表无公司列，实为两账套材料流水混编。
2. **月度面不与单账套对齐**：202510 册收入金额按月对湖北开明 1403 借方，十个月差额有正有负（样本见机器摘要旁的探针查询），确认不能按「整表=单账套」收轴。
3. **收轴前置两件**（进断言 `AST-MAT-AXIS-PROBE`，analyzed-open）：
   - 物料类别（`01` 焊材等长代码前缀）↔ 金蝶科目（`1403001` 主要材料/`1403002` 辅助材料/`140300401` 工具…）映射表；
   - 行级账套归属拆分（凭证号+月+金额三键联接定归属）。
   触发 = 下轮材料轴专项。

## 事实面联动

血缘重建（10 类 staging 表）；`data_pipeline.json` 派生层 **253,864 行**（+38,732）；断言表 27 条（closed 族 16 / analyzed-open 11）。

七个数据域（回款/开票/凭证/费用/税费/借款/材料）全部开张：五域凭证级闭合、借款域流量级闭合、材料域完成入仓与首探定性。
