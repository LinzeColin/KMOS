# DATA.0007·板表族首件：税负率明细带状解析与税费轴闭合（2026-07-17）

## 抽取

- 报表系列一两册的「7税费构成表」（2024）与「7.2025年税负率明细表」（2025）→ `_staging.tax_composition`：**798 行、0 金额拒绝**（长表 = 公司×年×月×税种；比率列只存原文，不引入 `float`）。
- 版式：公司带头（湖北/武汉/彤烨/岚丹/信茂，签名 = 列0公司简称 + 列1「收入」+ 列2「增值税」）→ 12 月行 + 合计行；合计行不入库，改做带内脚验。
- `v2` 修复：尾部辅助列会重复「合计」表头导致双计——同一税种多列命中只取首列。
- 源表存在**亚分精度**单元（公式所得，如 2594.0383 元）：按半进位（`ROUND_HALF_UP`）入分共 71 处，原文全数保存在 `amount_raw`，金额整数分纪律不破。
- 带内脚验（各月之和 vs 合计行）：**90/94 平**；余 4 条差 ±1~2 分，系逐月舍入积累（月格先舍入再相加 vs 合计格一次舍入），性质登记不修数。

## 税费轴勾稽（新对账轴，开张即闭合）

湖北开明 2025-01..10，税负表 vs 费用表 `6403` 计提明细（按月×税种，五税种：城建税/教育费附加/地方教育附加/印花税/房产税土地使用税）：

- **39 格中 36 格 0 分差**（含房产税土地使用税季度格 20,544.03 元 × 3 全中）。
- 6 月印花税 −6,757 分与 7 月 +6,757 分**互为对冲**：同一笔 ¥67.57 两侧跨月归属不同，两月合计相等——期间归属差，非错漏。
- 9 月地方教育附加差 1 分：源表亚分单元（192.679 元）舍入。

断言落 `AST-TAX-AXIS-HBKM-2025`（`closed_matched_tolerance`）。

## 事实面联动

- `data_pipeline.json` 重生成：派生层 **215,109 行**。本次重生成同时**自愈一处漏登**——`expense_lines`（8,327 行）此前不在 `staging_tables` 清单（事实文件生成早于费用抽取且未重跑）；对账现状口径同步至报告第 3 号。
- `changelog.json` 增 `v1.5.6`；血缘 `machine/lineage.yaml` 重建（8 张 staging 表、状态 FRESH）。

## 板表族其余处置

- 资产台账三表（14 固定资产/15 低值易耗品/16 库存商品）：两版**均为空模板**（仅表头无数据行），如实登记，不做抽取。
- 3/4 费用对比、9 采购汇总、10 领料汇总、11 库存账龄等：仍缓议，等对账需求触发。
