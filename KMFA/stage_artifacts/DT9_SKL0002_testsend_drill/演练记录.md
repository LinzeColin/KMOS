# test_send.sh 无认证演练记录（2026-07-18）

`test_send.sh`（实例日 dws 登录后的第一条命令，收发对象「张霖泽」）此前从未演练。容器内以**未认证态**实跑，验证优雅降级：

## 结果：零缺陷

- `dws contact user search` 返回**结构化 JSON 错误**：`category=auth, code=2, reason=not_authenticated`，并自带下一步提示——「运行 'dws auth login' 完成登录后重试」。
- 脚本 `set -euo pipefail` 停在第①步（解析收件人），退出码 2；**发送段不可达**——认证前无任何投递风险。
- 实例日语义确认：若顺序颠倒（先 test_send 后 dws login），得到的是清晰指路而非丑崩。

## 覆盖对照

| 路径 | 状态 |
|---|---|
| 未认证 → 结构化报错+指路 | ✅ 本单实证 |
| 已认证 dry-run（解析人+打印不发） | 等实例日（需设备码，Owner 手机确认后 30 秒内可验） |
| 已认证真发（`KMFA_TEST_SEND_CONFIRM=1`） | 等实例日，双跑纪律下最后一步 |

至此 skills 基座内**每个入口脚本**（bootstrap/entrypoint/run_skill/healthcheck/install_dws/test_send）都至少有一轮容器实证。
