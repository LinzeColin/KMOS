# A0 强锁执行·基础层(税务源核对已通过)2026-07-26

## Owner 授权
2026-07-26 经问答明确:**按源优先级强锁 A0**(税务>银行>金蝶>红圈)。本轮起执行,**不再拖到下轮**。

## 我实际执行了什么、看到了什么
在私有库 `LinzeColin/Private-Database` 自带 Actions 跑 `kmfa-a0-tax-derive`(私算、真额只在私有 run):

1. **先探结构**(不猜列号——上轮 +269% 就是猜列号翻的车):
   税务表 sheet `2025项目成本明细` 表头 = `银行日记账表号|日期|摘要|收支类别|收款金额|付款金额|余额|内容`。
2. **拦下一个列错位 bug**:首版按 `'金额'` 关键字匹配,误中 `col4 收款金额`(应是 `col5 付款金额`)——
   探到就改对了,**没出错额**。这正是 +269% 那类错,这次 inspect-first 拦住。
3. **正确聚合**(col5 付款金额、收支类别=项目成本):**3830 笔付款**;
   `内容`(col7)去重 **32 组**=干净聚合键(top 组占 44.1%);`摘要`去重 2713=自由文本,不可用。

## A0 目标结构(fact 层要求,已核对)
**4 项目 × 9 成本类别**(人工/材料/机械/外协/运输/差旅/税费/管理费/利息)**× 6 指标**
(revenue/contract/invoice/collection/cost_total/cost_category)。

## 还差什么才能锁(诚实)
① 税务 `内容`(32) 语义映射到 4×9 网格;② 从开票/回款 sheet 组装 revenue/invoice/collection;
③ 建 S09-P2 毛利 + S09-P3 差异对账(源优先级裁决 11 残差)两正式层+校验器;④ 页面接线+e2e。
**这是银行/税务要看的金额,分步做、每步私有库私算+核对,不赶工。**

## 铁律
真额只在私有库 run;本记录 public-safe(列名/行数/分组数,零金额零项目名)。
