# TSK.KMFA.PROD.0006 项目成本页 — 验收记录（2026-07-20）

任务包 07 要求：**fact layer：项目×成本归集×毛利/现金毛利；下钻到 staging 行级引用（私面数据只在本机 App 显示，不入导出默认）**。验收：**与 `project_cost_fact_layer` 输出一致；真数据用例过**（依赖 G4）。

## 一、先查权威，结论决定了本页形态

`project_cost_fact_layer` 的真实产物：

- `metadata/reports/project_cost_fact_layer_manifest.json` → `fact_layer_status = "structural_fact_layer_blocked_for_formal_calculation"`
- `metadata/lineage/project_cost_fact_records.jsonl` → **4 条记录，全部** `amount_calculation_performed=false`、`formal_calculation_allowed=false`、`calculation_status=blocked_pending_quality_resolution`
- 明文值**不在公开仓**：只有 `sha256:` 指纹与 `private_ref://` 指针，`metric_values_public_committed=false`

**即：成本结构（9 类成本 × 6 项指标）已定义，但一个金额都没算过**——因其依赖 A0 权威基准，而 A0 依赖 BLK-001（Owner 约 273 行字段确认）。

> 故本页**不产出任何毛利/现金毛利数字**。编造金额可以让页面"好看"，但那正是本 goal 明令禁止的占位交差，且会污染一条以「零差异」为纪律的对账线。

## 二、交付

- 后端 `GET /api/项目成本`：事实层状态 + 必需结构（9 成本类别 / 6 指标）+ 4 条记录槽位与哈希登记情况 + **阻塞链**（A0 → BLK-001）+ **可下钻派生层**（真实行数）+ 诚实边界声明。
- 前端「项目成本」页签：顶部醒目警示条说明金额不可计算，下接结构、记录表、阻塞链、下钻表。
- 私面纪律：页面只显示 `private_ref://` 指针与哈希，不显示、也无从显示明文值。

## 三、真起服务 + 真开页面实测

`docker run -p 18092:8000` → 浏览器 `/ui/` → 点击「项目成本」页签（`aria-selected=true`），读取渲染后 DOM：

**事实记录表（4 条，与 fact layer 逐条一致）**
```
记录号            项目实体                              计算状态                            金额已计算  槽位   哈希  明文已公开
PCF-S09P1-001    entity_ref://KMFA/S08-P2/project/001  blocked_pending_quality_resolution   否        9/6    15    否（仅私有面）
PCF-S09P1-002    …/project/002                         同上                                 否        9/6    15    否（仅私有面）
PCF-S09P1-003    …/project/003                         同上                                 否        9/6    15    否（仅私有面）
PCF-S09P1-004    …/project/004                         同上                                 否        9/6    15    否（仅私有面）
```

**阻塞链**：BLK-001｜销售绩效考核 8 份 PDF 加 1 份电子表格约 273 行字段人工确认｜**只有你能解**｜已卡 20 天

**可下钻派生层（真实行数，A0 就位后据此归集）**
```
kingdee_ledger 162,979 ｜ goods_movement 38,732 ｜ kingdee_voucher 21,334
receivable_aging 9,181 ｜ expense_lines 8,327 ｜ collection 3,726 ｜ invoice_lines 548
```

## 四、把「不准编数」做成可执行断言

新增 4 个契约测试，其中一个专为诚实性而设：

- `test_project_cost_emits_no_money_numbers_while_blocked`：断言 `已算金额记录数==0`，且响应 JSON 中**不得出现** `毛利`/`现金毛利`/`gross_margin`/`revenue_amount` 等金额字段——**一旦有人日后"补上好看的数字"，此测试立刻失败**。
- `test_project_cost_matches_fact_layer_output`：4 条记录、9/6 槽位、状态字段逐项对齐 fact layer。
- `test_project_cost_blocking_chain_points_to_owner_blocker`：阻塞链须指向 BLK-001 且标记只有 Owner 可解。
- `test_project_cost_drilldown_rows_match_pipeline_facts`：下钻行数须等于 `data_pipeline` 事实，不得另编一套数。

## 五、门禁

`pytest tests/` → **30 passed**（26 + 本单元 4）；`check_dual_plane_ci.py` → **PASS**（5 项目）。

## 六、状态

**已建待验收**：页面与事实层逐条一致、下钻真实；但任务要求的「毛利/现金毛利」在 **A0 就位前不存在**，该部分验收须待 BLK-001 解除 → A0 生成 → 事实层重算后补。
解锁路径：Owner 完成 BLK-001 的 273 行确认 → Q4 升 Q5 → A0 权威基准生成 → 事实层 `formal_calculation_allowed` 翻真 → 本页金额自动填充（结构与下钻均已就位，无需再改页面）。
