# TSK.KMFA.PROD.0005 源检查板 — 验收记录（2026-07-20）

任务包 07 要求：**四源 × 期间覆盖 × 登记状态矩阵；新批次 stale 提示**。验收：**与源检查矩阵 metadata 一致；真数据用例过**（该验收依赖 G4 → 本单元标「已建待验收」）。

## 一、先查权威，再动手

`KMFA/metadata/sources/` 下确有权威三件：`source_check_matrix.jsonl` / `_schema.json` / `_policy.yaml` + `source_registry.yaml`。读出来的真实状态是：

- 矩阵**只有一行 `protocol_header`**，`status = protocol_defined_no_source_rows_committed`（S03-P2 协议定义态）
- 必需维度 6 个：`source_system / business_segment / source_package_ref / entity_ref / account_ref / frequency`
- 允许状态 5 个：已就绪 / 部分-阻塞 / 失败-不适用 / 已过期 / 人工复核
- 源行由 `KMFA/tools/file_import_register.py` 产出，**当前零行**

**故本板如实报出「已提交源行 0」，不编造 `entity_ref`/`account_ref` 等取不到的维度值充数**（违反 goal「禁止占位/mock 交差」）。真实覆盖视图另取自血缘图与 `data_pipeline` 事实。

## 二、交付

- 后端 `GET /api/源检查`：矩阵协议态 + 源×登记状态覆盖矩阵 + 新鲜度 stale 判定 + 派生层规模。
  新鲜度由 `data_pipeline.data_as_of_batch` 与血缘节点批次**比对**得出——**全程不读 raw inbox**（沿用 PROD.0002 的 `FORBIDDEN_READ_ROOT` 纪律）。
- 前端新增「源检查板」页签，渲染上述三块。

## 三、真起服务 + 真打开页面（非 mock、非健康检查）

`docker run -p 18090:8000` 起 App → 浏览器打开 `/ui/` → **驱动真实点击**「源检查板」页签（`aria-selected` 由 false 变 true）→ 页面渲染实测：

```
登记资产 53 ｜ 数据批次 2026-07-16 ｜ 派生表/行 17 / 263,758 ｜ 新鲜度 FRESH
无更新批次，覆盖面与事实批次一致｜血缘批次：2026-07-16

源 × 登记状态 矩阵
源            deferred_all_sheets  not_yet_extracted  已抽取  合计
WPS钉钉红圈             0                  6            7     13
绩效                    0                  9            0      9
财务                    2                 23            6     31

正式源检查矩阵（协议态）
schema      kmfa.source_check_matrix.v1
阶段        S03-P2
状态        protocol_defined_no_source_rows_committed
已提交源行  0
必需维度    source_system、business_segment、source_package_ref、entity_ref、account_ref、frequency
允许状态    已就绪、部分/阻塞、失败/不适用、已过期、人工复核
```

**交叉核对**：13 + 9 + 31 = **53**，与「登记资产 53」「血缘 raw_assets 53」三方一致。

> 浏览器面板视口报 0×0、可访问性树取不到，故未附截图；改以**JS 驱动真实点击 + 读取渲染后 DOM 正文**作为等效证据（如上）。此为面板环境限制，非页面缺陷——`get_page_text` 可取到完整正文。

## 四、真打开页面才发现的缺陷（已修）

页签原为 `<span onClick>`：**既不可键盘操作、也不进可访问性树**，自动化点不到（我第一次点击落在 (0,0) 无效）。已改为语义化 `<button type="button" role="tab" aria-selected>`。

这不仅是可访问性问题——**PROD.0013 的 Playwright 端到端本来会卡在这里**。只跑 API/单测不会暴露。

## 五、门禁

- `pytest tests/` → **23 passed**（20 + 本单元 3 个源检查契约测试：协议如实报出、覆盖矩阵合计=血缘总数且逐行状态自洽、新鲜度只由生成面得出）
- `check_dual_plane_ci.py` → **PASS**（5 项目）

## 六、状态

**已建待验收**：功能与真数据渲染已完成并实测；任务包该项验收标注依赖 G4，故正式验收章待 G4 后补。
正式源检查矩阵的源行需 `file_import_register` 产出后方能填充——那属数据登记流程，非本页面缺失。
