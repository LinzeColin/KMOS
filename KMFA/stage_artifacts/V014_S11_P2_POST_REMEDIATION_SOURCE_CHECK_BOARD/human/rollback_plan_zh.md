# S11-P2 回滚方案

1. 回退本 phase 的本地 commit 和 `KMFA/stage_artifacts/V014_S11_P2_POST_REMEDIATION_SOURCE_CHECK_BOARD` 公开证据。
2. 删除本 phase ignored private browser/raw 证据，不触碰原始目录。
3. 恢复到 S11-P1 的 `Q4 / D / NO_GO` 状态。
4. 不回退、不移动、不删除、不覆盖任何原始文件。
