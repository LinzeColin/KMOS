# TSK.KMFA.PROD.0017 文档渲染件同步 — 验收记录（2026-07-20）

权威任务包（`_protected/…/07_任务包_M5_M6_产品与交付解锁.md` 第 26 行）：

> facts 更新（App 现实）→ `04_操作流程`/`06_运维手册` 渲染件自然更新；运维手册含**服务启停/备份/断链自检**
> **验收：渲染门保持真绿；手册走查**

## 一、动机：基建线撤了，操作知识只在本轮记录里

2026-07-20 Owner 告知基建线已撤。那套「怎么重部署、令牌怎么换、投递开关为什么必须是 1」的操作知识原本只在对方脑子里，人一走就断。本单元把它固化进 facts，让手册自动带出来——**下次不依赖任何人的记忆**。

## 二、走 facts → 渲染，不手改渲染件

`06_运维手册.md` 由 `render_human.py` 从 `machine/config.yaml` + `machine/facts/ops.json` + `changelog.json` 生成。所以做法是：

1. `ops.json` 新增 `runbook` 键（服务启停 5 条 / 备份 4 条 / 断链自检 6 条）
2. `render_human.py` 增三节，从该键渲染
3. **一行渲染件都没手改**

`troubleshooting` 同时从 14 条扩到 **20 条**，新增 6 条**全部来自本轮真踩过的坑**（排程装错位置、cron 不继承环境、缺 `HOME` 导致认证失败、派发静默 `FAILED`、部署令牌 `401`、归档根写死 `Mac` 路径），不是想象出来的。

## 三、手册走查（不只看门绿）

真读渲染出来的七节：

```
一、怎么跑          二、参数在哪调      三、报错了怎么办
四、服务启停        五、备份去哪        六、断链自检
七、都改过什么（最近 10 条）
```

**走查逮到一个真问题**：新三节最初编成「五六七」，插在原有的「四、都改过什么」**前面**，序号乱成 一二三五六七四。手册是给人读的，序号错位是硬伤。改成四五六、原「四」顺延为七后复渲，顺序正确。

> 这条只有真把渲染件读一遍才会发现——三道门只管体积、中文、纯净，不管章节序号是否连贯。

**服务启停**（节选）：
```
云端重部署      | 推 main 自动触发；令牌失效时需 Owner 在 Coolify 手动重部署
云端换接口令牌  | Coolify 生成新令牌 → 更新仓库 COOLIFY_API_TOKEN → 重部署
开启真实投递    | 部署时设 KMFA_DELIVERY_ENABLED 为 1，否则排程按空跑
```

**断链自检**（本单元最有价值的一节，开头写着「一条链断了往往不报错，只是安静地不干活」）：
```
排程真装上了吗 | 容器内 grep -c run_skill /etc/cron.d/kmfa-skills，应为 10
环境行齐吗     | 同文件头部应有 HOME、TZ、KMFA_DELIVERY_ENABLED
排程真跑过吗   | 看 /var/log/kmfa/ledger.jsonl 有没有排程来源的记录
认证还活着吗   | dws-keepalive 每四小时一次，看日志有无刷新失败
页面还能用吗   | 跑端到端回归 app-e2e，八步全绿才算通
部署链通吗     | 看仓库 Actions 里 deploy 是不是绿的；红且报 401 即令牌失效
```
这六条对应的正是本轮逐个踩中的静默失效点。

## 四、「facts 更新 → 渲染件自然更新」实证

不是嘴上说走 facts，实测了一遍：

```
改 facts 前 df8f0f0faf7fa05d → 后 bdfd3f553c8397c6（变了：True）
新条目出现在渲染件：True
撤回后回到原样：True
```
往 `ops.json` 临时插一条探针 → 渲染 → 哈希变、内容出现；撤回 → 渲染 → **哈希回到改动前**。绑定关系成立，且无残留。

## 五、中文门：用反引号豁免，未调阈值

首渲染 `check_doc_budget.py` 报「未登记的英文术语 `['Mac']`」。按工作纪律**用反引号豁免**（`Mac` 路径），**没有调阈值、没有改词表**。复渲后三道门全过。

## 六、门禁

- `render_human.py` → 7 个文件全部由机器平面生成
- `check_doc_budget.py` → **PASS**（人类平面三道门全过）；手册 **105 / 200 行**
- `check_blocker_stop.py` → **PASS**（1 个阻塞，无重复重审）
- `check_dual_plane_ci.py` → **PASS**（5 个项目全部合规）

## 七、状态

**已建**。运维手册三节齐（服务启停/备份/断链自检），渲染门保持真绿，手册走查过并修掉了序号错位。
