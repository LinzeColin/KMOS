# Python test/runtime caches are reproducible local artifacts, never source.
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
# Runtime/operator credentials are never source. Public templates use the
# explicit .env.example suffix and remain trackable.
**/.env
**/.env.*
**/*.env
!**/.env.example
