# 镜像 build context 精简（Coolify build context = 仓根）。
# 自包含镜像只复制 KMFA 与其共享契约层 KMDatabase；以下不进构建上下文。
.git
.github
# 其余产品线不属于 KMFA 运行镜像。排除它们既避免无关代码进入生产镜像，
# 也控制 Coolify 小磁盘主机的无缓存构建占用。
KM_IDSystem/
whkmSalary/
**/node_modules
**/__pycache__
**/*.pyc
**/.pytest_cache
**/.ruff_cache
# Operator credentials are injected by the runtime; local env files must never
# be copied into the self-contained application image.
**/.env
**/.env.*
**/*.env
# 内容寻址大对象（~120M）——技能与 self-audit 均不需要（self-audit 的 tar 影子亦排除之）
KMDatabase/data/objects
# 私有运行时（gitignored；本地若存在也不入镜像）
**/.codex_private_runtime
