# KMOS Agent Contract

KM 系列商用线仓库，中文优先；代码、API、库名、模型名和错误可保留英文。

## 永久规则

- 本仓库是**商用线**。代码与数据默认专有，不得以任何开源许可对外发布。
- 治理框架来自共享仓库 LinzeColin/Governance。
  **禁止**在本仓库内复制、分叉或重建治理框架。
  **禁止**用 git submodule 引入它 —— 通过 CI checkout 或 pip 安装消费。
- 跨 KM 项目的数据交换必须经过 KMDatabase 声明的 schema，
  **禁止**直接引用其他项目的内部路径。
- **数据落地铁律（长期有效 · 自运行分仓治理）**：长期/业务/运行时数据一律写私有仓
  `LinzeColin/Private-Database` 的 `Private-KMDatabase/`，用 `KMDatabase/machine/tools/private_db_client.py`
  免 clone 读写（`ingest/get/list/verify`）；**禁止把数据提交进本代码仓**，派生/临时物走 `.gitignore`。
  Private-Database 禁止 `git clone`。目的：分仓治理长期自运行，不需 Owner 反复人工迁移。

## 迁移状态

本仓库从 `LinzeColin/CodexProject` 拆分而来。

- `KMFA/` 已在 2026-07-17 迁入并成为 KMFA 的唯一 GitHub 源码归属。
- `KM_IDSystem/` 是 KMIDS 在 `LinzeColin/KMOS` 中的 canonical 目录；业务名 KMIDS 不对应另一个仓库或目录。
- 旧 `CodexProject` checkout 仅是历史/私有运行态来源，不得再作为 KMFA 或 KMIDS 的提交、推送或新开发入口。

处理 KMFA 前先读 `KMFA/HANDOFF.md`；处理 KMIDS 前先读 `KM_IDSystem/docs/HANDOFF.md`。真实员工、财务、群聊、考勤、SQLite、压缩包、凭据和 IDS 原始元数据不得进入本公开仓库。

---

## 云成本红线：对象存储必须零付费（Owner 硬指令 · 长期有效）

**云端账单必须恒为 $0.00。不允许任何 agent 触发收费行为。**

1. **禁止 `InfrequentAccess` 存储类** —— 建桶、写对象、生命周期转换，一律不许。
   R2 的免费额度（10GB 存储 / 100 万 Class A / 1000 万 Class B）**只覆盖 Standard**；
   IA 从第 1 次操作起计费，且**按整计费单位向上取整**。
   2026-08-07 实账单：**51 次 IA 操作 = $9.00**，同期 **301 万次 Standard 操作 = $0.00**。
   根因是建桶时默认存储类选了 IA，写入端不指定存储类就全部继承 —— 一次手滑，之后静默自动计费。
2. **禁止"整包下载来判断存在 / 做校验"的高频轮询。** 判断对象存在用 `HeadObject`
   （写入时把 sha256 放进对象 `Metadata`，Head 就读得到）；真要逐字节复核，
   **按天或按周跑，不许按分钟跑**。
   反例：memory-atlas reconcile 每 15 分钟把 2466 个对象整包拉一遍核 sha256，
   折合 71 万次 Class B/天、21.3M/月，直接打穿 10M/月免费额度。
3. **新增或改动任何周期性任务，先算月操作量**：
   `每轮操作数 × 每天轮数 × 31 < 免费额度 × 50%`。**算不出来就不上线。**
4. **存储优先级**：**GitHub Release 资产 > R2 > OVH 本地**。
   Release 资产不计仓库体积、没有操作计费，永远优先。

完整事故记录、账单逐行归因、免费额度速查表 → **`Private-Database` 仓 `OPS/AGENT_ONBOARDING.md` §9.7**。
机器守卫 → OVH `/usr/local/bin/linze-r2-free-tier-guard.py`（每 6 小时，非 Standard 桶自动熔断改回；
判定 `/srv/linze/apps/status/data/r2_free_tier_guard.json`）。

### R2 周期任务清单与预算（改动前必读）

云端账单恒为 $0.00，靠的是下面这份预算不被打破。**改这些任务的频率、范围或参数之前，先算月操作量。**
数字为 2026-08-07 实测（Cloudflare GraphQL `r2OperationsAdaptiveGroups`，7 个完整日日均外推）。

| 任务 | 频率 | 桶 | 作用 | 月 Class A | 月 Class B | **一碰就变收费的地方** |
|---|---|---|---|---|---|---|
| `weread-port-r2-oci-backup` | 每日 04:23 | weread-port-private | 加密用户对象镜像到 OCI 异地冷备 | 465 | 0 | **`rclone sync` 必须带 `--fast-list`**。删掉它 → 按前缀逐个列举，实测 15 次 → **9,300 次**（Class A 额度的 28.8%），且随对象数线性增长 |
| `memory-atlas-reconcile` | **每日** | weread-port-private | 核对 R2 是否仍持有 manifest 里的字节 | 434 | **229,338 (2.3%)** | **频率**。原为每 15 分钟 = 21.3M/月，直接打穿 10M 额度。因为 `exists_with_hash()` 对每个对象**整包下载**（2 Head + 1 Get × 2466 对象 = 7,398/轮） |
| `linze-status-r2-mirror.sh` | 每 5 分钟 | primary-objects | status 站数据镜像 | 31,872 (3.2%) | ~200 | **镜像的文件个数**。每多镜像 1 个文件 = +8,928 次/月 |
| weread-port 平台写入（常驻） | 持续 ~56 次/小时 | weread-port-private | 加密笔记 / 跨设备同步的对象写入 | 41,664 (4.2%) | 0 | 随用户活跃度增长。**写入方未逐一归因**，但已确认不是 reconcile（降频后仍在） |
| `social-archive-replication` | 每 15 分钟 | social-archive-e2n-v0004 | 对象复制到多存储 | 3,224 | 19,468 | **`--limit 200` 这个上限**，别放大 |
| `weread-port-private-database-backup` | 每日 04:01 | backups | Private-Database git bundle 冷备 | 190 | ~30 | 有 `UNCHANGED` 短路，**别去掉** |
| `linze-offsite-backup.sh` | 每日 03:40 | backups | 全量加密备份（单对象） | ~60 | ~30 | 别改成分片小块上传 |
| `cyberboss-backup` | 每日 03:35 | cyberboss-cold | CyberBoss 冷备 | 35 | ~150 | — |
| `memory-atlas-action-worker` | 每分钟 | weread-port-private | 有界 owner 动作队列 | ~0 | ~0 | 队列空时不发任何 R2 请求；**队列一旦长期非空，就会变成每分钟打 R2** |
| 其余（adp / sl-* / kmfa / status-evidence） | 每日 | 各自 | 各项目产物 | <900 | <100 | — |
| **合计** | | | | **≈ 8.0% 的 100 万/月** | **≈ 2.5% 的 1000 万/月** | |

**余量**：Class B 有 **40 倍**余量；Class A 有 **12 倍**余量。两者都健康，但 **Class A 历来是先见底的那个**
（修 `--fast-list` 之前它已经到 37%，而 Class B 只有 2.5%）—— 盯额度先盯 Class A。

**改动这些任务时的三条硬规则**

1. **别删这三类参数** —— 它们是额度的直接开关，不是性能调优：
   `--fast-list`（rclone 列举方式）、`--limit`（单轮上限）、`UNCHANGED` / `--skip-if-unchanged`（无变化短路）。
2. **别把日级任务改成分钟级。** 先算：`每轮操作数 × 每天轮数 × 31 < 免费额度 × 50%`。**算不出来就不上线。**
3. **别用"整包下载"判断对象存在或做校验。** 判断存在用 `HeadObject` 读 `Metadata.sha256`；
   逐字节复核按天/周跑，不许按分钟跑。（`exists_with_hash()` 就是反例，它是这次事故的第二个根因。）

**改完自己核**（不要交给 owner 去发现）：

```bash
ssh ovh 'sudo /usr/local/bin/linze-r2-free-tier-guard.py'
```

它会打印本计费周期 Class A / Class B / 存储对免费额度的投影占比，≥70% 报 WARN、≥90% 报 CRIT，
并把判定写进每日复审清单。完整事故记录见 `Private-Database` 仓 `OPS/AGENT_ONBOARDING.md` §9.7。

**存储维度（唯一跨月累积的）**：操作次数每计费周期清零，**存储不清零**。2026-08-10 实测 **4.55 GB / 10 GB = 44.4%**。

| 桶 | 当前 | 状态 |
|---|---|---|
| `weread-port-private` | 3.22 GB | 冻结（memory-atlas 迁出后不再增长） |
| `backups` | 0.96 GB | 冻结（`linze-offsite-backup.sh` 的 R2 写入已停用：`R2_CODE=disabled_zero_charge_policy`） |
| `social-archive-e2n-v0004` | 0.31 GB | **3 天保留封顶**（见下） |
| 其余 7 个桶 | 合计 <0.06 GB | 冻结 |

**social-archive 的 3 天保留（Owner 2026-08-10 定）**

`backups/runtime-db/` 每 15 分钟写一份 1.03 MB 加密快照，而 `prune_runtime_db_snapshots.py`
**只清本地**——它的文件头明确写着「不碰远端副本(R2/OCI/GitHub)，保留期是另一个决定」，
那个决定一直没给，于是 R2 上累积了 **512 份 / 521 MB、+99 MB/天**，是当时账号里唯一还在长的东西。

现由 `social-archive/scripts/prune_r2_backup_replicas.py --apply` 承接（挂在
`social-archive-backup.service`，每日 03:20），保留 **72 小时**，稳态约 290 MB。首次执行删了 258 个 / 234 MB。

> **改动禁区**：① 别删那条 `ExecStart`，② 别把 `--apply` 拿掉，③ 别放宽 `--hours`。
> 脚本的安全底线也别削：**删 R2 对象前先 `HeadObject` 核对 OCI 上同 key 同大小，核不上就跳过不删**；
> 最新一批永远保留；只碰 `backups/<组>/<时间戳>/`，**不碰 `primary-objects/`（那是制品字节，删了就是毁档）**。
> 每份快照有 `r2`/`oci`/`github` 三个 verified 副本，删掉 R2 那份仍剩两份 —— 这是「卸载」不是「删除」。

- **结论**：阶段推进时，历史白箱断言必须把 `(phase, task, next_gate)` 作为完整合法状态追加 P2 后继，不能直接替换 P1 文本。
  **为什么**：P1→P2 会同时改变 phase、task 与 next gate；逐字替换会破坏嵌套状态元组的语义，遗漏 gate 候选又会让真实治理状态被历史回归误判。
  **代价**：两轮本地定位与回归；未产生运行时动作。

- **结论**：P3 场景验证必须重放 P2 的固定控制输入，并把“未来调用候选”与“实际调用”分开断言。
  **为什么**：策略允许、审计投影存在和预算可用只能证明候选前置已齐，不等于可外发、更不等于产生真实审计或业务事实。
  **代价**：一次 P3 本地实现与全链路回归；未产生运行时动作。

- **结论**：P4 交付证据必须把“控制样例可查询”与“真实外发记录可查询”分开断言。
  **为什么**：七键查询、回退说明和零值成本只存在于当前纯内存控制报告；没有持久审计日志或外发历史，不能升级为生产可观察性结论。
  **代价**：一次 P4 合同、历史合法后继与双平面回归；未产生运行时动作。

- **结论**：Stage Review 只能机械复核 P1--P4 的冻结合同、控制报告与回退边界，不能升格为来源文档或业务事实的第二权威。
  **为什么**：复审通过只证明固定控制形状、合法状态后继和零运行时边界一致；真实资料、索引、检索与业务线白箱人工处理仍处于该复审之外。
  **代价**：一次 Review 本地实施、历史合法后继更新与全链白箱回归；未产生模型 Token、Agent、OVH、生产或上传动作。

- **结论**：后台索引构建 P1 只应固定候选隔离、旧活动索引连续服务、冒烟门、原子切换与回退的静态合同，不能把未来“构建”误做成当前运行。
  **为什么**：候选版本、影子索引、活动指针和回退只有在后续受控阶段才可被业务线白箱验证；P1 的正确产物是可审计的失败关闭边界，而不是索引、日志或生产能力。
  **代价**：一次静态合同、历史合法后继、中文投影与全链白箱回归；未产生真实资料访问、模型 Token、Agent、OVH、生产或上传动作。

- **结论**：后台索引构建 P2 只能使用固定、非业务、reference-only 输入，在内存中投影候选构建、影子隔离、冒烟门、活动指针、未来切换与回退候选；所有真实构建、检索、切换和回退仍须留给后续独立阶段。
  **为什么**：把候选版本或未来原子切换直接连到索引、数据库或活动指针，会把控制形状误升级为生产事实，绕开单一权威与业务线白箱人工处理。
  **代价**：一次 P2 控制切片、历史合法后继、中文词典补齐与 812 项白箱回归；未产生真实资料访问、模型 Token、Agent、OVH、生产或上传动作。

- **结论**：后台索引构建 P3 只能从 P2 的固定控制投影重放构建未完成、冒烟失败、切换失败、回退、旧活动版本连续服务、零实际并发检索隔离和控制版版本可见性；P2 没有物理构建失败时，只能保留未完成关闭状态，不能伪造真实失败。
  **为什么**：把控制状态、候选引用或版本可见性升级为实际后台任务、索引、检索、Operations／报告写入或业务事实，会破坏单一权威与业务线白箱人工处理边界。
  **代价**：一次 P3 纯内存控制场景、合法后继兼容、中文词典补齐与 823 项白箱回归；未产生真实资料访问、模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：后台索引构建 P4 只能从 P2/P3 的固定控制投影派生 5 份索引清单、6 份冒烟日志、5 份切换记录、5 份回退证明、1 份旧活动版本保留／空间影响投影及 3 条人工操作说明；这些均须保持 control-only，不可冒充持久交付或生产可观察性。
  **为什么**：控制清单、日志、保留和恢复说明能说明未来操作的可核对形状，却不能证明真实索引、实际空间、活动指针、回退、业务事实或外部服务已发生；只有来源文档与业务线白箱人工处理保有权威。
  **代价**：一次 P4 纯内存交付合同、79 份历史合法后继兼容更新、中文视图与 831 项白箱回归；未产生真实资料访问、模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage085 P3 的元数据过滤验证只能重放 Stage085 P2 已冻结的六条非业务控制投影，八类场景只能证明控制形状与失败关闭边界，不能证明真实检索能力或生产可用。
  **为什么**：材料牌号、设备型号、标准号、语义相似、六维过滤、Top-K、排序解释和旧索引轨迹均只为控制类别；实际资料、数据库、索引、检索与业务线白箱处理仍属于后续受控阶段，且历史状态须只新增 P3→P4 的精确合法后继。
  **代价**：一次 P3 纯内存实现、机器事实与历史白箱兼容更新；P1/P2/P3 聚焦 `22/22`、Stage060--085 白箱 `1168/1168`，未创建额外 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage085 P4 只能从 P3 的八个固定、非业务、`reference-only` 场景派生 metadata-only 交付证据，并把历史白箱的唯一合法当前态精确延长为 `Stage085 P3 → Stage085 P4 → Stage085 Review gate`。
  **为什么**：检索样例、trace、过滤结果、有效性报告、证据缺口、参数回退说明和中文反馈都只是控制形状；将其升格为真实检索记录、参数回滚、业务结论或生产可观察性会突破单一权威、零运行时和业务线白箱边界。P4→Review 必须是独立后继元组，不能把 Review gate 附加到 P3 元组。
  **代价**：一次 P4 纯内存交付、历史白箱兼容与双平面回归；P1/P2/P3/P4 聚焦 `33/33`、Stage060--085 白箱 `1179/1179`，未创建额外 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage085 Review 只能机械复审冻结 P1--P4 纯内存控制工件，并将历史白箱的唯一合法当前态精确延长为 Stage085 P4 → Stage085 Review → Stage086 P1 gate。
  **为什么**：Review 只证明合同和控制报告的内存重放保持一致；把它或下一门误作实际检索、数据库、业务事实或 Stage086 已启动，将突破单一权威、零运行时与业务线白箱边界。P4→Review→Stage086 P1 必须是独立合法后继，不能把 Stage086 入口附加到 P4。
  **代价**：一次 Review 纯内存复审、精确历史后继兼容与双平面回归；P1--Review 聚焦 44/44、Stage060--085 白箱 1190/1190，未创建额外 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage086 P1 必须把 active_index_version 固定为独立静态记录形状，并让 candidate、selected 与 retrieval trace 直接保持同一版本引用；只在既有唯一开发 worktree 交付这一合同。
  **为什么**：任务包明确要求定义该记录，若只把版本放在 query 或候选引用中，selected 的可追溯性会留下缺口；静态字段和精确后继断言只能证明控制边界，不能升级为检索、排序或业务事实。
  **代价**：一次 P1 静态合同、历史合法后继和双平面回归；P1 聚焦 7/7、Stage060--086 受控白箱 1197/1197，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage086 P2 只能以六条固定、非业务、reference-only 输入完成纯内存混合排序控制投影，并将历史白箱的合法当前态精确延长为 P2 → P3 gate。
  **为什么**：query、六类过滤、活动索引版本、candidate、hybrid score、selected、trace 与 future route 都是不透明控制形状；vector-only 或任何非固定输入必须失败关闭，不能把控制投影升格为真实检索、排序或业务事实。
  **代价**：对历史允许集逐形状补齐并完成 P1/P2 聚焦 15/15、Stage060--086 受控白箱 1205/1205；未创建额外 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage086 P3 只能精确重放 P2 六条固定、非业务、`reference-only` 控制投影为八类混合排序受控场景，历史白箱只追加 P3 → P4 gate 的合法后继。
  **为什么**：材料牌号、设备型号、标准号、语义相似、六维过滤、Top-K、排序解释、结果有效性和旧索引版本均仅是控制类别或不透明引用；宽松匹配会掩盖越过 P4 的非法推进，实际检索或排序则会突破单一权威与零运行时边界。
  **代价**：P1/P2/P3 聚焦 22/22、Stage060--086 受控白箱 1212/1212；全程只使用既有唯一开发 worktree，未读取真实资料、未消耗模型 Token、未执行 Agent、OVH、生产、上传或推送。

- **结论**：Stage086 P4 只能从 P3 的八个固定、非业务、`reference-only` 场景派生 metadata-only 交付证据，并将历史白箱的唯一合法当前态精确延长为 `Stage086 P3 → Stage086 P4 → Stage086 Review gate`。
  **为什么**：检索样例、trace、过滤结果、有效性测试报告、证据缺口、参数回滚说明和中文反馈只描述未来人工可核对的控制形状；把它们升格为真实检索记录、参数回滚、业务事实或生产可观察性，会突破单一权威、零运行时和业务线白箱边界。P4→Review 必须作为独立后继元组、映射和计划门禁存在，不能改写 P3 历史状态。
  **代价**：一次 P4 纯内存交付、98 个历史合法后继兼容更新与双平面回归；P1/P2/P3/P4 聚焦 33/33、Stage060--086 受控白箱 1223/1223，全程只使用既有唯一开发 worktree，未读取真实资料、未消耗模型 Token、未执行 Agent、OVH、生产、上传或推送。

- **结论**：Stage086 Review 必须把 P1--P4 合同、P2/P3/P4 控制报告、固定形状、失败关闭、业务线白箱人工处理、P4→P3 回退与零运行时边界聚合为同一失败关闭结论，并只精确延长历史合法当前态为 `Stage086 Review → Stage087 P1 gate`。
  **为什么**：Review 通过只证明冻结控制工件一致，不能把候选、分数、选择、轨迹或参数回退投影升级为真实检索、排序、业务事实或 Stage087 已启动；遗漏完整四元组会使历史白箱误报，宽松匹配则会掩盖非法跨阶段推进。
  **代价**：一次纯内存复审与 98 个历史合法后继精确兼容更新；Review `11/11`、P1--Review `44/44`、Stage060--086 受控白箱 `1234/1234`，全程只使用既有唯一开发 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage087 P1 只能固定关键词／向量检索基线、六类过滤、candidate/selected chunks、score、活动索引版本和检索轨迹的静态控制合同，并将历史白箱精确延长为 `Stage087 P1 → Stage087 P2 gate`。
  **为什么**：这些字段与历史后继只证明可追溯的工程边界；把它们升格为 PostgreSQL、索引、embedding、检索、排序、业务事实或 P2 已启动会突破单一权威、零运行时与业务线白箱边界。
  **代价**：一次受限历史后继兼容、机器术语表补齐与双平面回归；P1 `7/7`、Stage060--087 `1241/1241`，全程只使用既有唯一开发 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage087 P2 只能以六条固定、非业务、`reference-only` 输入完成纯内存检索轨迹控制切片，并将历史白箱的合法当前态精确延长为 `Stage087 P2 → Stage087 P3 gate`。
  **为什么**：query、六类过滤、active index、candidate/selected chunks、score、trace 与 future integration 都只是不可持久化的控制形状；`vector-only`、非固定输入或任一运行信号必须失败关闭，不能把它们升格为 PostgreSQL、检索、排序、证据账本或业务事实。
  **代价**：P1/P2 聚焦 `14/14`、Stage005 直接治理与两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage087 P3 只能把 P2 的六条固定、非业务、`reference-only` 控制投影重放为八个纯内存场景，并将唯一合法当前态精确延长为 `Stage087 P3 → Stage087 P4 gate`。
  **为什么**：材料牌号、设备型号、标准号、语义相似、六维过滤、Top-K、排序解释、结果有效性和旧索引版本都只是场景类别或不透明控制引用；把它们升格为真实检索、业务事实、自动写入或自动索引切换会突破单一权威、零运行时与业务线白箱边界。
  **代价**：P1/P2/P3 聚焦 `21/21`、Stage005 直接治理和两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage087 P4 只能从 P3 的八个固定、非业务、`reference-only` 场景派生 `8/8/8/8/8/4/4` 条纯内存交付控制证据，并把唯一合法当前态精确延长为 `Stage087 P4 → Review gate`。
  **为什么**：检索样例、trace、过滤结果、有效性报告、证据缺口、回滚说明与中文反馈都只是不可持久化的控制引用；将其写为实际检索、实际交付、自动回滚或生产状态会突破单一权威、零运行时与业务线白箱边界。
  **代价**：P1/P2/P3/P4 聚焦 `32/32`、Stage005 直接治理和两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。


- **结论**：Stage087 Review 只能机械复审冻结 P1--P4 合同、P2/P3/P4 控制报告、固定形状、失败关闭、单一权威、业务线白箱人工处理与 P4→P3 回退，并将唯一合法当前态精确延长为 `Stage087 Review → Stage088 P1 gate`。
  **为什么**：Review 通过只证明不透明控制工件与零运行时边界一致；把它、样例、评分、选择、轨迹或回退说明升级为真实检索、业务事实、参数回滚或 Stage088 已启动，会突破单一权威和业务线白箱边界。
  **代价**：一次纯内存复审、精确历史后继兼容与双平面回归；Review `11/11`、P1--Review `43/43`，全程只使用既有唯一开发 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage088 P1 只能固定检索结果有效性门禁的静态控制形状，并将唯一合法当前态精确延长为 `Stage088 P1 → Stage088 P2 gate`。
  **为什么**：query、六类 filter、candidate、selected、score、活动索引版本、检索轨迹与结果有效性门禁都只是不可持久化的不透明控制引用；关键词和向量基线必须同时存在，`vector-only` 或仅凭系统输出均不得作出实际有效性、业务建议或决策，业务使用前仍须白箱人工复核。
  **代价**：P1 聚焦 `7/7`、与 Stage087 Review 关联聚焦 `18/18`、Stage005 与两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage088 P2 只能以六条固定、非业务、`reference-only` 输入完成纯内存结果有效性门禁控制切片，并将唯一合法当前态精确延长为 `Stage088 P2 → Stage088 P3 gate`。
  **为什么**：query、六类过滤、active index、candidate、score、selected、trace、结果有效性门禁与 future integration 都只是不可持久化的控制形状；关键词与向量基线必须同时声明，`vector-only`、非固定输入、仅凭系统输出或任一控制链缺失必须失败关闭，不能把控制投影升格为真实检索、真实有效性、证据账本或业务事实。
  **代价**：P2 聚焦 `7/7`、与 Stage087 Review/P1 关联聚焦 `25/25`、Stage005 与两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage088 P3 只能精确重放 P2 六条固定、非业务、reference-only 控制投影为八个纯内存结果有效性门禁受控场景，并将唯一合法当前态精确延长为 Stage088 P3 → Stage088 P4 gate。
  **为什么**：关键词、材料牌号、设备型号、标准号、语义相似、六维过滤组合、Top-K、排序解释、结果有效性和旧索引服务版本轨迹均只表示场景类别或不透明控制引用；结果有效性必须保持未评估、门禁必须待业务线白箱人工复核，任何运行时信号、非不透明引用或控制链缺失均必须失败关闭。
  **代价**：P3 聚焦 8/8、与 Stage087 Review/P1/P2 关联聚焦 33/33、Stage005 与两个既有批次检查器通过，机器平面重渲染 7 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage088 P4 只能从 P3 的八个固定、非业务、reference-only 场景派生 8/8/8/8/8/4/4 的纯内存交付控制证据，并将唯一合法当前态精确延长为 Stage088 P4 → Stage088 Review gate。
  **为什么**：检索样例、trace、过滤结果、有效性测试报告、证据缺口、参数回滚说明与中文反馈都只是不可持久化的控制引用；结果有效性仍未评估且全部门禁待业务线白箱人工复核，把它们升格为真实检索、实际参数回滚、业务事实或生产状态会突破单一权威与零运行时边界。
  **代价**：P4 聚焦 11/11、与 Stage087 Review/P1/P2/P3 关联聚焦 44/44、Stage005 与两个既有批次检查器通过；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。
- **结论**：Stage088 Review 只能机械复审冻结 P1--P4 合同、P2/P3/P4 控制报告、固定形状、失败关闭、业务线白箱人工处理、P4 交付控制回退与零运行时边界，并将唯一合法当前态精确延长为 `Stage088 Review → Stage089 P1 gate`。
  **为什么**：Review 通过只证明不透明控制工件一致；把检索样例、trace、有效性报告、缺口或回退说明升级为真实检索、真实有效性、业务事实、参数回滚或 Stage089 已启动，会突破单一权威、零运行时和业务线白箱边界。
  **代价**：一次纯内存复审与精确历史后继兼容更新；Review `11/11`、Stage087 Review 与 Stage088 P1--Review `55/55`、Stage005 直接治理 `valid=true`，两个既有批次检查器均通过；全程只使用既有唯一开发 worktree，未产生模型 Token、Agent、OVH、生产、上传或推送动作。

- **结论**：Stage089 P1 只能固定证据账本 Schema 的静态控制形状，并将唯一合法当前态精确延长为 `Stage089 P1 → Stage089 P2 gate`。
  **为什么**：evidence、document、chunk、fact、query、answer、report、source type、A/B/C/D/E 可信等级、证据缺口、风险、撤回、投毒防护和关键结论绑定都只是不可持久化的控制引用；关键结论必须关联 evidence_id 或 evidence_gap，低可信、冲突、过期、撤回或可疑投毒证据不得自动升格、采纳或形成业务结论。
  **代价**：P1 聚焦 `8/8`、与 Stage088 Review 关联聚焦 `19/19`、Stage005 与两个既有批次检查器通过，机器平面重渲染 `7` 个中文文件并通过三项文档门禁；全程只使用既有唯一开发 worktree，未读取真实资料、未产生模型 Token、Agent、OVH、生产、上传或推送动作。
